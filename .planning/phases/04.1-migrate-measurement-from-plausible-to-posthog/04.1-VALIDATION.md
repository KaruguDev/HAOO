---
phase: 04.1
slug: migrate-measurement-from-plausible-to-posthog
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-03
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `04.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3.2.4 (jsdom 26.1.0, `@testing-library/react` 16.3.2) |
| **Config file** | `vitest.config.ts` — `environment: 'jsdom'`, `globals: false`, `setupFiles: ['./src/test/setup.ts']` |
| **Quick run command** | `npx vitest run src/test/measurement.test.ts src/test/haoo-report.test.ts` |
| **Full suite command** | `npm test` (= `npm run build && vitest run`) |
| **Estimated runtime** | quick ~10s · full ~60s (includes a production build) |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run src/test/measurement.test.ts src/test/haoo-report.test.ts`
- **After every plan wave:** Run `npm test`, `npm run typecheck`, and `npm run lint`
- **Before `/gsd-verify-work`:** Full suite green, plus `node scripts/verify-phase4-coverage.mjs <COVERAGE.md>` green against the rewritten coverage tables
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

> Task IDs are assigned by the planner; this table pins the requirement → command mapping
> the planner must attach to tasks. Status is updated during execution.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | 0 | MEAS-01 / MEAS-02 | — | Provider contract fixture pins PostHog's capture payload shape independently of the adapter (04-06 pattern) | contract | `npx vitest run src/test/measurement.test.ts` | ❌ W0 — `src/test/fixtures/posthog-capture-contract.ts` | ⬜ pending |
| TBD | TBD | 0 | MEAS-02 | — | D-04 gate 2: driving every journey path produces exactly the ten bare names on the wire and nothing else | integration | `npx vitest run src/test/measurement-page.test.tsx -t "network payload"` | ❌ W0 — new describe | ⬜ pending |
| TBD | TBD | 0 | MEAS-08 | — | Bundle/source boundary: replacement invariant for the withdrawn `posthog` prohibition | contract | `npm test` (requires a fresh `dist/`) | ❌ W0 — rewrite in `src/test/build-output.test.ts` | ⬜ pending |
| TBD | TBD | 0 | MEAS-01 / MEAS-08 | — | Coverage verifier re-pointed from Plausible tables/regexes to PostHog ones | contract | `node scripts/verify-phase4-coverage.mjs <COVERAGE.md>` | ❌ W0 — `scripts/verify-phase4-coverage.mjs` | ⬜ pending |
| TBD | TBD | 1+ | MEAS-02 | — | `before_send` reduces every payload to `{token, distinct_id, $process_person_profile}` and drops any event name outside the ten | unit | `npx vitest run src/test/measurement.test.ts -t "name-only provider sink"` | ✅ (extend) | ⬜ pending |
| TBD | TBD | 1+ | MEAS-02 / MEAS-03 | — | D-04 gate 1: the lockdown readback refuses when any locked key resolves wrong; no sink, no capture | unit | `npx vitest run src/test/measurement.test.ts -t "fail-closed provider initialization"` | ✅ (extend) | ⬜ pending |
| TBD | TBD | 1+ | MEAS-03 | — | No browser storage write occurs from the provider path; the `zph.haoo.ctx.v1` record is unchanged in shape | unit | `npx vitest run src/test/measurement.test.ts -t "exact stored schema"` | ✅ (extend) | ⬜ pending |
| TBD | TBD | 1+ | MEAS-04 | — | The disclosure renders a US-processing statement sourced from product data, not a component literal | component | `npx vitest run src/test/measurement-page.test.tsx -t "disclosure"` | ✅ (extend) | ⬜ pending |
| TBD | TBD | 1+ | MEAS-06 | — | `save_campaign_params: false` and `save_referrer: false` are asserted in the readback; `readCampaign` remains the only campaign path | unit | `npx vitest run src/test/measurement.test.ts -t "campaign whole-value allowlist"` | ✅ (extend) | ⬜ pending |
| TBD | TBD | 1+ | MEAS-07 | — | A refusing/blocked provider leaves every visitor action working and does not unmount the page (Phase 4 gap 1) | component | `npx vitest run src/test/measurement-page.test.tsx -t "provider failure isolation"` | ✅ (extend) | ⬜ pending |
| TBD | TBD | 1+ | MEAS-01 / MEAS-08 | — | HogQL row reader rejects unknown name, duplicate row, non-integer, negative, reordered columns | unit | `npx vitest run src/test/haoo-report.test.ts -t "parseGoalCounts"` | ✅ (extend) | ⬜ pending |
| TBD | TBD | 1+ | MEAS-08 | — | Echo validator refuses a response whose echoed `query`/`hogql` is not the submitted SQL; report states the project-id echo weakening | unit | `npx vitest run src/test/haoo-report.test.ts -t "provenance"` | ✅ (extend) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/test/fixtures/posthog-capture-contract.ts` — independent transcription of PostHog's capture payload contract, importing nothing from `src/measurement/`, replacing `src/test/fixtures/plausible-preload-contract.ts` — covers MEAS-01/MEAS-02
- [ ] New `describe('network payload regression')` in `src/test/measurement-page.test.tsx` — covers MEAS-02 (D-04 gate 2)
- [ ] Rewrite of `UNCONDITIONAL_ANALYTICS_ORIGINS_FORBIDDEN` and its two call sites, plus the replacement runtime invariant, in `src/test/build-output.test.ts` — covers the Pitfall 1 withdrawal
- [ ] `REQUIRED_TABLES` in `scripts/verify-phase4-coverage.mjs` rewritten from two Plausible tables to two PostHog tables (browser SDK capabilities; Query API capabilities), and the four `PLAUSIBLE_*` / `provider selector remains unset` boundary regexes re-pointed
- [ ] No framework install needed — vitest 3.2.4 is present.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| PostHog project setting "Discard client IP data" is enabled | MEAS-03 | Server-side GeoIP cannot be suppressed from code — `posthog-js`'s `ip` option is documented `@deprecated - THIS OPTION HAS NO EFFECT`. The only in-repo lever is a `$geoip_disable: true` event property, which contradicts the bare-name payload contract. | Owner opens the PostHog project settings, enables "Discard client IP data", and records the confirmation in the phase artifacts. Planner must gate this as a named owner checkpoint. |
| `posthog-js` install and any `@posthog/wizard` run | MEAS-01 | Both packages flagged `[SUS] too-new` (latest versions published within 48 hours — PostHog release cadence, not slopsquatting evidence). | `checkpoint:human-verify` before install; pin with `npm install --save-exact`. |
| Visitor-facing disclosure copy | MEAS-04 | Copy is owner-approved, in the style of Phase 4's C-1/C-2 checkpoints. | Owner reviews the US-processing statement, and rules on the open question of whether the per-page-load transport `distinct_id` needs its own clause. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
