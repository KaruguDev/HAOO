# Deleted provider test cases — `04.1-04` → `04.1-05` hand-off

The tracer slice (`04.1-04`) removed the Plausible adapter and every test case written
against it. The six provider describes in `src/test/measurement.test.ts` survive at
**tracer depth only** for exactly one wave: each keeps a single migrated PostHog case so
the describe still asserts something, and everything else listed below is gone until
`04.1-05` restores it at full depth.

This file — not the `04.1-04` SUMMARY prose — is the authority on that reduction.
`04.1-05` Task 3 closes it out and fails while any row still reads `pending`.

**How to read a row.** `Case` is the exact `it(...)` title as it stood before deletion.
`Restored by` names the `04.1-05` task that owns its PostHog successor. `Status` is
`pending` until that task writes the successor and flips the row to `restored`, adding
the successor's describe name.

## Removed cases

| Describe | Case | Requirement | Restored by | Status |
| --- | --- | --- | --- | --- |
| fail-closed provider resolution | resolves %s to the named script source (20 rows: scheme, credentials, query, fragment, extension-variant path, foreign origin, lookalike suffix host, non-default port, uppercase host, protocol-relative, non-URL, undefined, empty) | MEAS-02 | 04.1-05 Task 1 | pending |
| fail-closed provider resolution | fails closed when no approved-source contract is supplied | MEAS-02 | 04.1-05 Task 1 | pending |
| fail-closed provider resolution | carries the approved contract into the build for %s only when entitled (6 rows) | MEAS-02 | 04.1-05 Task 1 | pending |
| name-only provider sink | returns no sink when %s (3 rows: no-op provider, empty script source, empty site domain) | MEAS-06 | 04.1-05 Task 1 | pending |
| name-only provider sink | appends the configured site script exactly once with deferred loading | MEAS-06 | 04.1-05 Task 1 | pending |
| name-only provider sink | initializes with automatic pageview capture disabled before any forwarded event | MEAS-02 | 04.1-05 Task 1 | pending |
| name-only provider sink | forwards exactly one bare argument for every one of the ten event names | MEAS-02 | 04.1-05 Task 2 | pending |
| name-only provider sink | matches the documented preload options and event-queue contract | MEAS-02 | 04.1-05 Task 2 | pending |
| fail-closed provider initialization | returns no sink and appends no script when the existing provider has no initializer | MEAS-06 | 04.1-05 Task 1 | pending |
| fail-closed provider initialization | returns no sink and appends no script when the existing initializer throws | MEAS-06 | 04.1-05 Task 1 | pending |
| fail-closed provider initialization | returns no sink when the existing initializer records no options | MEAS-06 | 04.1-05 Task 1 | pending |
| fail-closed provider initialization | installs, initializes, and confirms the stub before appending the script | MEAS-06 | 04.1-05 Task 1 | pending |
| fail-closed provider initialization in the full journey | keeps the whole journey working when %s (3 rows: absent initializer, throwing initializer, silent initializer) | MEAS-07 | 04.1-05 Task 3 | pending |
| fail-closed provider initialization in the full journey | leaves a refused provider global at its original identity | MEAS-07 | 04.1-05 Task 3 | pending |
| provider failure isolation | leaves the journey unchanged when the provider global is absent | MEAS-07 | 04.1-05 Task 3 | pending |
| provider failure isolation | leaves the journey unchanged when the provider script load fails | MEAS-07 | 04.1-05 Task 3 | pending |
| facade contract under the widened provider seam | wires the configured provider sink when no sink adapter is injected | MEAS-06 | 04.1-05 Task 3 | pending |
| facade contract under the widened provider seam | leaves the whole journey working when the configured script source is rejected | MEAS-07 | 04.1-05 Task 3 | pending |

## Deliberately not restored

<!--
This section must stay empty. A row may only be added here with a named reason AND a
requirement id, and adding one is a coverage reduction the phase carries past its last
provider-test plan — a scope change that needs owner approval, per `04.1-04` and
`04.1-05` Task 3. The table header itself is deliberately absent so that "no rows
beneath this heading" is checkable by looking for any line at all that begins with a
table pipe.
-->

_None._
