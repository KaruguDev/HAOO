---
phase: 04.1-migrate-measurement-from-plausible-to-posthog
plan: 11
subsystem: infra
tags: [posthog, github-actions, privacy, deployment, coverage-audit, uat, vitest]

# Dependency graph
requires:
  - phase: 04.1-migrate-measurement-from-plausible-to-posthog (04.1-09)
    provides: "`boundPostHogClient()` — the value-position SDK import, plus the four renarrowed build-output invariants. Enabling delivery of an unloaded SDK would have set the provider variables on a build that captures nothing."
  - phase: 04.1-migrate-measurement-from-plausible-to-posthog (04.1-10)
    provides: "the adoption-free `resolveClient` — what makes the visitor-facing disclosure sentence true of every client the adapter can initialize, and therefore approvable"
provides:
  - "A recorded owner approval of PostHog as the analytics processor and of production collection, taken BEFORE any variable that enables delivery was set (D-06)"
  - "A deploy workflow whose Build step supplies the three public `VITE_HAOO_*` values from repository variables, with the variables-not-secrets reasoning recorded in place"
  - "An operational boundary that states the enabled reality, with all three replaced pinned assertions and two ADDED anti-overclaim pins landing in the same commit as the prose"
  - "A workflow-source credential gate one layer earlier than the bundle scan, deriving its forbidden names from `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN`"
  - "Seven UAT checkpoints moved from `blocked_by: release-build` to `[pending]`, none marked passed"
  - "A sixth USER-SETUP gate recording that the three repository variables are NOT yet created — so a green workflow is never read as a capturing deploy"
affects: [04.1-verification, 05, any future change to the deploy workflow's Build environment or COVERAGE.md's operational boundary]

actuals:
  tokens: 10844
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Take the owner's approval as one verbatim word and record it unparaphrased — infer nothing stronger from it than what was said"
    - "Amend a gate-pinned sentence and its pin in the same commit; replace the pin with a NAMED successor across the claim, never relax it to a fragment the opposite sentence would also satisfy"
    - "When a state assertion becomes false for one document but stays true for another, SPLIT it rather than deleting it — keep it where it is still the record, replace it where it is not"
    - "When enabling something, pin the caveats too: an enabled state is exactly where a document starts overclaiming, and an unpinned caveat is a sentence anyone can delete"
    - "Assert presence before absence — a prohibition over a file that failed to load, or a Build step whose env block moved, is vacuously true"

key-files:
  created: []
  modified:
    - .github/workflows/deploy.yml
    - scripts/verify-phase4-coverage.mjs
    - src/test/build-output.test.ts
    - src/test/haoo-report.test.ts
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-USER-SETUP.md
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-UAT.md
    - README.md
    - .planning/REQUIREMENTS.md

key-decisions:
  - "The owner's answer is recorded as the single word `approved` and nothing stronger is inferred from it — in particular it is NOT read as a statement that the three repository variables were created"
  - "The three PostHog values are repository VARIABLES, not secrets: the `phc_` project key is public write-only by design and is inlined into a world-readable bundle, so a secret would imply a confidentiality property the artifact cannot have"
  - "`boundaryChecks` grew 9 -> 11: three deferral-state pins replaced by named successors, two ADDED to guard the new overclaim surface (green-workflow-is-not-a-capturing-deploy, and live outcomes remain owner observations)"
  - "The README/Phase-4 deferral assertion in `haoo-report.test.ts` was SPLIT rather than dropped — kept over the Phase 4 record where it stays true, replaced for README by an enabled-state successor pinned across the claim"
  - "A sixth gate was added to `04.1-USER-SETUP.md` for the three uncreated repository variables, because nothing in this tree can observe them and their absence is silent"
  - "MEAS-01 and MEAS-08 stay unchecked and `Gaps Found`: deployment made seven checkpoints performable, not passed"

patterns-established:
  - "Recorded-answer evidence: the owner's verbatim word, what they were shown before giving it, and an explicit statement of what is NOT inferred from it"
  - "Anti-overclaim pinning: when a document moves from a deferred state to an enabled one, add pins for what remains unproven at the same time as the enabled claims"
  - "Workflow-source credential gates: assert the build environment, not only the built artifact — the artifact scan can only report a leak the environment already caused"

requirements-completed: []

coverage:
  - id: D1
    description: "The owner's approval of PostHog as the analytics processor and of production collection is recorded verbatim, before any variable enabling delivery was set"
    requirement: "MEAS-01"
    verification:
      - kind: other
        ref: "grep -cE '^- \\[x\\] \\*\\*Processor privacy approval — CLEARED [0-9]{4}-[0-9]{2}-[0-9]{2}\\.\\*\\*' 04.1-USER-SETUP.md -> 1"
        status: pass
      - kind: other
        ref: "git log --oneline -1 -- .github/workflows/deploy.yml -> d6b8878 (predates this plan) at the moment the approval was recorded"
        status: pass
    human_judgment: false
  - id: D2
    description: "COVERAGE.md's operational boundary states the enabled reality, and every pinned assertion over it was amended in the same commit as the prose"
    requirement: "MEAS-08"
    verification:
      - kind: integration
        ref: "npm run verify:coverage -> exit 0, 69 required capabilities across 3 tables"
        status: pass
      - kind: integration
        ref: "src/test/haoo-report.test.ts#passes the real capability audit rather than a hand-rolled resemblance of it"
        status: pass
      - kind: other
        ref: "five mutation probes: blanking each newly-pinned sentence in a scratch COVERAGE.md makes node scripts/verify-phase4-coverage.mjs exit 1 naming the missing statement"
        status: pass
    human_judgment: false
  - id: D3
    description: "The deploy workflow's Build step supplies exactly the three public VITE_HAOO_* values, sourced from repository variables, and no report credential can enter that environment"
    verification:
      - kind: unit
        ref: "src/test/build-output.test.ts#keeps every report credential out of the deploy workflow Build environment"
        status: pass
      - kind: other
        ref: "four mutation probes: browser-prefixed credential export, dropped public variable, renamed Build step (vacuity), and a bare POSTHOG_PROJECT_ID assignment each turn the gate red"
        status: pass
    human_judgment: false
  - id: D4
    description: "The 04.1-09 successor invariants hold against a provider-SELECTED dist, which is the artifact CI now produces"
    verification:
      - kind: integration
        ref: "npx vitest run against a dist built with the three variables set -> 675 passed, 0 failures"
        status: pass
      - kind: integration
        ref: "npx vitest run build-output against a dist built with none set -> 39 passed"
        status: pass
    human_judgment: false
  - id: D5
    description: "The seven blocked_by: release-build UAT checkpoints are performable and named, with checkpoint 10 identified as the ingestion-acceptance check"
    verification:
      - kind: other
        ref: "grep -c 'blocked_by: release-build' 04.1-UAT.md -> 0; seven entries now result: [pending] with per-checkpoint reasons"
        status: pass
    human_judgment: false
  - id: D6
    description: "Production collection actually captures: exactly one bare allowlisted event per action, no automatic companion, no person profile, and PostHog Cloud US ACCEPTS the three-property reduced payload"
    requirement: "MEAS-01"
    verification: []
    human_judgment: true
    rationale: "Only a live, credentialed project and a real browser session can observe what ingestion does with the payload. Zero events arriving is a FAILURE of this gate, not a quiet pass — a silent ingestion rejection reads as a dead funnel rather than a broken one (D-05). It is UAT checkpoint 10 and gate 3 in 04.1-USER-SETUP.md, and it additionally depends on an owner action this repository cannot perform (creating the three repository variables)."
  - id: D7
    description: "The owner report reconciles against the live project, and the provider's evaluation of the query's inclusive day bounds is confirmed"
    requirement: "MEAS-08"
    verification: []
    human_judgment: true
    rationale: "This repository asserts the exact SQL text it submits, byte for byte, but cannot assert the provider's evaluation of it. Requires a personal API key and arriving events. UAT checkpoint 8, to be performed after checkpoint 10 — reconciling zeros against zeros settles nothing."
  - id: D8
    description: "The three GitHub Actions repository variables exist and carry values, so the deployed build actually captures"
    verification: []
    human_judgment: true
    rationale: "They are created outside version control and nothing in this tree can observe them. The workflow reads them; it cannot create them. An absent variable expands to the empty string, the selector fails closed to `none`, and the deploy captures nothing while every automated gate still passes. Recorded as the sixth gate in 04.1-USER-SETUP.md and in .planning/WINDOWS.md."

# Metrics
duration: 22 min
completed: 2026-09-05
status: complete
---

# Phase 04.1 Plan 11: Turn Delivery On, In The Right Order Summary

**The privacy owner's verbatim `approved` is recorded before anything enabled delivery, the deploy workflow's Build step now supplies the three public `VITE_HAOO_*` values from repository variables behind a workflow-source credential gate, and every sentence the coverage verifier pins over the operational boundary was amended in the same commit as its pin — with two new pins added so the enabled state cannot overclaim.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-05T01:07Z
- **Completed:** 2026-09-05T01:29Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- **The approval exists, and it was taken first.** The re-opened processor gate is ticked `CLEARED 2026-09-05` with the owner's single word `approved` as its sole evidence. At the moment it was recorded, no `VITE_HAOO_*` variable was set and `.github/workflows/deploy.yml` still pointed at `d6b8878`, a commit predating this plan — the D-06 ordering the gate exists to enforce, verified rather than asserted.
- **Delivery is wired.** The Build step passes `VITE_HAOO_MEASUREMENT_PROVIDER`, `VITE_HAOO_POSTHOG_TOKEN` and `VITE_HAOO_POSTHOG_API_HOST` from GitHub Actions repository *variables*, with the reasoning recorded beside them in the file's own voice: the `phc_` project key is public write-only by design and Vite inlines it into a world-readable bundle, so storing it as a secret would imply a confidentiality property the artifact cannot have.
- **No gate-pinned sentence was left false.** Three `boundaryChecks` entries (OPT-OUT, deferred approval/project/variables, capabilities-verified-while-unset) were replaced by named successors in the same commit as the prose they pin, and paragraph 2's "that selector is still unset" was amended with them. Five mutation probes confirm each new pin fails when its sentence is blanked — including one that restored the *predecessor* sentence and correctly failed.
- **The credential boundary moved one layer earlier.** The bundle scan can only report a credential that already leaked; the new workflow-source case stops the leak being arranged, deriving its forbidden names from `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN` so the two gates cannot drift. Four mutation probes give it teeth, one of them a vacuity probe.
- **The successor invariants were confirmed by running, not by reasoning.** The full suite is green against a `dist` built *with* the three variables (675 passed) and `build-output` is green against a `dist` built with none set (39 passed). Nothing from 04.1-09 needed relaxing.
- **Seven owner checkpoints became performable.** UAT 4, 5, 6, 7, 8, 9 and 10 moved from `blocked` to `[pending]`; none is marked passed.

## Task Commits

1. **Task 1: Owner approval of PostHog as the analytics processor and of production collection** — `3d337e3` (docs)
2. **Task 2: Amend the operational boundary and its pinned assertions in one commit** — `b95841a` (docs)
3. **Task 3: Supply the three public variables to the deployment build and name the gates it unblocks** — `b3ffd4c` (feat)

## The seven previously-blocked UAT checkpoints

All seven carried `blocked_by: release-build` and now read `result: [pending]` with a per-checkpoint reason recording that the release build delivers. None is marked passed — performing them is the owner's, after the deploy lands.

| # | Title | Note |
|---|---|---|
| 4 | Pinned PostHog Packages Are Legitimate | The pinned packages are now the ones that will actually run in production |
| 5 | One Visitor Action, One Name-Only Event | Perform in the same session as checkpoint 10 |
| 6 | D-04 Gates Are Independent and Mutation-Proof | Still a by-hand re-run; nothing in the committed suite re-runs the probes |
| 7 | Processor Group Layout at 320px and 200% Zoom | The deployed page carrying the processor group is now the release build |
| 8 | Report Counts Reconcile Against Live PostHog | Perform AFTER 10 — reconciling zeros against zeros settles nothing |
| 9 | Geo-IP Enrichment Disabled on PostHog Project | Owner recorded `enabled` on 2026-09-04; live re-confirmation now performable |
| **10** | **Live One-Action-One-Event, No Person Profile** | **Also the ingestion-acceptance check** |

**Checkpoint 10 is the one that doubles as the ingestion-acceptance check.** It is the only check anywhere that confirms PostHog Cloud US *accepts* a payload reduced to three transport properties. Zero events arriving is a FAILURE of that checkpoint, never a quiet pass: a silent ingestion rejection reads as a dead funnel rather than a broken one (D-05). Its `reason` field says so, and says to confirm the three repository variables exist first, or an unconfigured build fails the gate for the wrong reason.

## Mutation probes

**COVERAGE.md boundary pins** — each newly-pinned sentence blanked in a scratch copy, then `node scripts/verify-phase4-coverage.mjs <scratch>`:

| Probe | Mutation | Result |
|---|---|---|
| P1 | Restored the predecessor `Production analytics enablement remains OPT-OUT for this phase.` | exit 1 — `must state that production analytics enablement is opt-in and enabled` |
| P2 | Blanked the approval/project/variables claim | exit 1 — `must state that the processor approval is recorded, the project exists, and the workflow supplies the three public build variables` |
| P3 | Restored the `implemented and fixture-verified while the provider selector remains unset` claim | exit 1 — `must state that the integration capabilities are reached at runtime rather than only fixture-verified` |
| P4 | Blanked the green-workflow caveat | exit 1 — `must state that a green workflow run is not evidence of a capturing deploy` |
| P5 | Blanked the owner-observations caveat | exit 1 — `must state that ingestion acceptance and the absent person profile remain owner observations` |

P1 and P3 are the strongest of the five: they did not merely delete the new sentence, they reinstated the *old, opposite* one, and the successor pins still failed. A pin that a contradicting sentence satisfies is the failure mode this repository's `boundaryChecks` convention exists to prevent.

**Workflow credential gate** — `src/test/build-output.test.ts#keeps every report credential out of the deploy workflow Build environment`, mutating the real `deploy.yml` and restoring it:

| Probe | Mutation | Result |
|---|---|---|
| M1 | Exported `VITE_POSTHOG_QUERY_API_KEY` in the Build env | red — `POSTHOG_QUERY_API_KEY must never appear under a browser prefix` |
| M2 | Dropped `VITE_HAOO_POSTHOG_API_HOST` | red — `assignments in the Build step env block: expected +0 to be 1` |
| M3 | Renamed `- name: Build` to `- name: Compile` (vacuity probe) | red — `the Build step in .github/workflows/deploy.yml: expected '' not to be ''` |
| M4 | Assigned `POSTHOG_PROJECT_ID` as a plain env key | red — `must never be assigned in any step of the deploy workflow` |

M3 is the one that matters most for longevity: it proves the case reads something. A prohibition over a Build step whose `env` block moved or was renamed would otherwise pass vacuously forever.

## Both `dist` states exercised

| Build | Command | Result |
|---|---|---|
| Provider-SELECTED | `VITE_HAOO_MEASUREMENT_PROVIDER=posthog VITE_HAOO_POSTHOG_TOKEN=phc_… VITE_HAOO_POSTHOG_API_HOST=https://us.i.posthog.com npm run build` then `npx vitest run` | 11 files, **675 passed, 0 failures** |
| Provider-UNSET | `env -u …` `npm run build` then `npx vitest run build-output` | **39 passed** |

This is the state CI now produces, and 04.1-09's `Next Phase Readiness` predicted it: its narrowed successor case builds its own provider-unset probe into a throwaway directory rather than assuming `dist` is unset. Confirmed by running — no successor invariant needed relaxing, and nothing was accommodated by weakening an assertion.

## Files Created/Modified

- `.github/workflows/deploy.yml` — three public variables added to the Build `env` block from repository variables, with the variables-not-secrets reasoning and the deliberate absence of both report credentials recorded in place; the `Verify capability coverage` comment restated (it claimed the audit checks that production analytics remains OPT-OUT, which this plan replaced)
- `scripts/verify-phase4-coverage.mjs` — `boundaryChecks` 9 → 11: three named successors, two added anti-overclaim pins, each with its predecessor and reason recorded in comment
- `src/test/build-output.test.ts` — new workflow-source credential case, forbidden names derived from `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN`
- `src/test/haoo-report.test.ts` — deferral assertion split (kept for the Phase 4 record, replaced for README by an enabled-state successor); audit docblock's assertion count corrected 9 → 11
- `.planning/phases/04.1-…/COVERAGE.md` — operational boundary states the enabled reality plus the two things this repository does not establish
- `.planning/phases/04.1-…/04.1-USER-SETUP.md` — processor gate cleared with the verbatim answer; a sixth gate added for the three uncreated repository variables; header restated, `Status: Incomplete` unchanged
- `.planning/phases/04.1-…/04.1-UAT.md` — seven checkpoints `blocked` → `[pending]`; summary counts `blocked: 0`, `pending: 7`
- `README.md` — the 04.1-09 forward reference to this plan resolved; the delivery-condition sentence and its heading kept verbatim because the build-output gate reads them
- `.planning/REQUIREMENTS.md` — dated enablement note; no box checked, no status cell changed

## Decisions Made

- **The owner's word is taken exactly as given.** `approved` answered the processor question. It is not read as a statement that the three repository variables were created, and the record says so explicitly. Recording it as anything stronger would have manufactured evidence for a step nobody performed.
- **Variables, not secrets.** Storing a world-readable, public write-only project key as a GitHub secret would teach the next reader that `VITE_*` is a place secrets may go. It is not, and the workflow comment says why.
- **Two pins were added, not just three replaced.** An enabled state is precisely where a document begins to overclaim. The green-workflow caveat and the owner-observations caveat are now gated; without a pin, either is a sentence anyone deletes on a tidy-up while the audit stays green (T-04.1-13, T-04.1-20).
- **The deferral assertion was split, not deleted.** Leaving a bare `toContain('deferred')` would have been satisfied by "no longer deferred" — the exact bare-fragment failure this plan's own prohibitions forbid.
- **MEAS-01 and MEAS-08 stay unchecked.** Deployment made seven checkpoints performable. It did not perform them.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] The approval record is unindented, unlike the client-IP gate it was to mirror**

- **Found during:** Task 1
- **Issue:** Task 1's acceptance criteria said to write the record "in the same form the cleared client-IP gate uses" — which indents the `**Recorded answer (` line by six spaces inside the list item — while Task 2's precondition asserts it with the line-anchored `awk '/^\*\*Recorded answer \(/'`. An indented line cannot satisfy a `^`-anchored pattern, so the two instructions could not both be met.
- **Fix:** The recorded-answer paragraph is written at column 0 so the mechanical assertion — the thing that actually gates Task 2 — passes. A short note in the file records why it deliberately differs from the gate below it, so the divergence reads as a decision rather than sloppiness.
- **Files modified:** `.planning/phases/04.1-…/04.1-USER-SETUP.md`
- **Verification:** `grep -cE '^- \[x\] \*\*Processor privacy approval — CLEARED [0-9]{4}-[0-9]{2}-[0-9]{2}\.\*\*'` → 1; the Task 2 precondition exits 0
- **Committed in:** `3d337e3`

**2. [Rule 3 - Blocking] `src/test/haoo-react.test.ts`'s deferral assertion pinned a README sentence this plan makes false**

- **Found during:** Task 2
- **Issue:** `src/test/haoo-report.test.ts#documents both local report inputs separately from deferred public build inputs` asserted `production collection` and `deferred` over *both* README.md and the Phase 4 USER-SETUP. Amending README's deferral sentence — required by this task — would have turned that case red. `src/test/haoo-report.test.ts` is not in Task 2's declared `<files>`, but leaving the amendment and the assertion in different commits is exactly what this plan's prohibitions forbid.
- **Fix:** The assertion was **split** in the same commit as the README change: kept over the Phase 4 document, where the deferral is still the truthful record of Phase 4 and which is amended by appending rather than rewriting; replaced for README by two enabled-state successors pinned across the claim (that collection is approved and the workflow supplies the three values, and that the report credentials still may not cross into that build environment). A bare `toContain('deferred')` was deliberately *not* left in place — "no longer deferred" would satisfy it, which is the failure mode this plan's second prohibition names. The withdrawal and its reason are recorded in the docblock.
- **Files modified:** `src/test/haoo-report.test.ts`
- **Verification:** `npx vitest run haoo-report` → 169 passed
- **Committed in:** `b95841a`

**3. [Rule 2 - Missing Critical] Two anti-overclaim pins added, and a sixth USER-SETUP gate for the uncreated repository variables**

- **Found during:** Tasks 2 and 3
- **Issue:** The plan's action required stating what remains unproven, but pinned only the three enabled claims. An unpinned caveat is deletable with no gate noticing — which is `T-04.1-13` (COVERAGE.md drifting from the code) and `T-04.1-20` (closure claimed on deployment alone) exactly. Separately, and more sharply: the owner approved the processor but did **not** state that the three GitHub Actions repository variables had been created. The workflow reads them; nothing in this tree can observe them; an absent variable expands to the empty string and fails the selector closed to `none`. Every automated gate here would stay green over a deploy that captures nothing.
- **Fix:** Two `boundaryChecks` entries added — `a green workflow run is not evidence of a capturing deploy` and `ingestion acceptance and the absent person profile remain owner observations` — with probes P4 and P5 proving them. A sixth gate was added to `04.1-USER-SETUP.md` naming the three variables as an outstanding owner action and stating the misreading it prevents; the same fact is carried in the REQUIREMENTS note, in UAT checkpoint 10's reason, and appended to `.planning/WINDOWS.md` as an `unmet-truth` entry so it survives past this phase's context. The two comments stating "nine operational-boundary assertions" (`deploy.yml`, `haoo-report.test.ts`) were corrected to eleven in the commits that changed the count.
- **Files modified:** `scripts/verify-phase4-coverage.mjs`, `.planning/phases/04.1-…/04.1-USER-SETUP.md`, `src/test/haoo-report.test.ts`, `.github/workflows/deploy.yml`, `.planning/WINDOWS.md`
- **Verification:** probes P4 and P5 exit 1 naming the missing statement; `npm run verify:coverage` exit 0 with 11 entries
- **Committed in:** `b95841a`, `b3ffd4c`

---

**Total deviations:** 3 auto-fixed (2 blocking, 1 missing critical)
**Impact on plan:** No scope creep. Deviation 1 resolved a contradiction internal to the plan; deviations 2 and 3 were both required by the plan's own must_have truth #3 and its second prohibition — a pinned sentence that goes false must be amended with its pin, and a pin must not be relaxed to a fragment the opposite sentence satisfies. The `boundaryChecks` array grew rather than shrank, and no requirement box was closed.

## Issues Encountered

**The three repository variables are not confirmed set — and this is the single most important thing to carry forward.** The owner replied `approved` to the processor decision. They did not state that they had created `VITE_HAOO_MEASUREMENT_PROVIDER`, `VITE_HAOO_POSTHOG_TOKEN` or `VITE_HAOO_POSTHOG_API_HOST` in GitHub → Settings → Secrets and variables → Actions → Variables. Nothing in this repository records them as set, because nothing in this repository can observe them.

The failure mode is silent and it looks exactly like success: an absent variable expands to the empty string, `parseMeasurementProvider` fails closed to `none`, no sink is created, the journey works perfectly, every test passes, the workflow goes green, and the deployed build captures nothing. **A green deploy workflow is not evidence of a capturing deploy.** That sentence is now pinned in `COVERAGE.md`, gated by `scripts/verify-phase4-coverage.mjs`, recorded as the sixth gate in `04.1-USER-SETUP.md`, named in the REQUIREMENTS status note, repeated in UAT checkpoint 10's `reason`, and appended to `.planning/WINDOWS.md`. Six places, because the one thing that must not happen is someone reading a report of zeros as a dead funnel when the real answer is an uncreated variable.

**One intentional single-commit window.** The `Verify capability coverage` comment in `deploy.yml` still said the audit checks "that production analytics remains OPT-OUT" for the duration of commit `b95841a`, and was corrected in `b3ffd4c`. The plan assigns that comment to Task 3 explicitly, and Task 3's precondition requires Task 2 to be committed first, so the ordering is designed rather than accidental.

## User Setup Required

**Yes — and it is not optional.** See [04.1-USER-SETUP.md](./04.1-USER-SETUP.md). Six gates; two are cleared.

| Gate | State |
|---|---|
| Processor privacy approval | **CLEARED 2026-09-05** — verbatim `approved` |
| Client IP discard confirmed | **CLEARED 2026-09-04** — verbatim `enabled` |
| The three public build variables exist as repository variables | **OPEN — not yet done.** Blocks everything below it |
| Ten dashboard goals — retired by design | Open (acknowledgement only) |
| One action, one event (live) | Open — UAT checkpoint 10, the ingestion-acceptance check |
| Live report reconciliation | Open — UAT checkpoint 8, perform after 10 |

Do the third row first. The two live gates cannot be performed against a build that was never configured, and attempting them first would fail them for the wrong reason.

## Next Phase Readiness

- **Gap G-04.1-1 is closed at the repository level.** Delivery is enabled behind a recorded approval taken first, the build supplies exactly the three public values and no credential, and every pinned sentence moved with its pin.
- **`04.1-VERIFICATION.md` can re-verify** the automated half of this plan immediately. The live half cannot be verified from this tree by construction.
- **Blocker for phase completion:** the three repository variables. Until they are created and UAT checkpoints 10 and 8 are performed, MEAS-01 and MEAS-08 remain `Gaps Found` and `04.1-UAT.md` stays `status: partial` with 7 pending.
- **Standing obligation carried from 04.1-09:** the whole-bundle credential scope rests on a measurement at posthog-js 1.425.1. `keeps the vendor chunk itself free of every report credential shape` enforces re-measurement mechanically at any version bump. The new workflow gate derives from the same group, so widening one widens both.

## Self-Check: PASSED

- `.github/workflows/deploy.yml` — FOUND, contains `VITE_HAOO_MEASUREMENT_PROVIDER`
- `scripts/verify-phase4-coverage.mjs` — FOUND, contains `boundaryChecks` (11 entries)
- `src/test/build-output.test.ts` — FOUND, reads `.github/workflows/deploy.yml`
- `.planning/phases/04.1-…/04.1-USER-SETUP.md` — FOUND, `## Verification` present, processor gate ticked
- `.planning/phases/04.1-…/COVERAGE.md` — FOUND, `## Operational boundary` amended
- `.planning/phases/04.1-…/04.1-UAT.md` — FOUND, `grep -c 'blocked_by: release-build'` → 0
- `README.md`, `.planning/REQUIREMENTS.md`, `src/test/haoo-report.test.ts` — FOUND
- Commits `3d337e3`, `b95841a`, `b3ffd4c` — all FOUND in `git log --oneline --all`
- Plan `<verification>` re-run: `npm run typecheck` 0, `npm run lint` 0, `npm run verify:coverage` 0, `npm run build` 0, `npx vitest run` 675 passed / 0 failures; MEAS-01 and MEAS-08 confirmed unchecked and `Gaps Found`
- No commit in this plan deleted a tracked file

---
*Phase: 04.1-migrate-measurement-from-plausible-to-posthog*
*Completed: 2026-09-05*
