---
phase: 04.1-migrate-measurement-from-plausible-to-posthog
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md
  - scripts/verify-phase4-coverage.mjs
autonomous: true
requirements: [MEAS-01, MEAS-08]

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The phase carries a decided API coverage matrix for both PostHog surfaces — the browser SDK and the Query API — in which every capability is INTEGRATE or a reasoned OPT-OUT."
    - "Every automatic-capture surface named in D-03 appears in the browser-SDK table as an explicit OPT-OUT row with a reason, so the lockdown is a recorded decision rather than an implementation detail."
    - "The coverage verifier validates the PostHog tables and the migrated operational-boundary sentences, and fails on a malformed row or a reasonless OPT-OUT."
    - "The FormSubmit table is carried forward unchanged, so re-pointing the verifier does not silently drop a previously-decided external surface."
    - statement: "The Query API response envelope for a HogQLQuery is `results` as positional arrays with a parallel `columns` array (researcher assumption A4); the coverage row recording it is confirmed only by the first live run."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT record an OPT-OUT without a reason — an un-reasoned opt-out is an undecided hole, which is the exact failure this matrix exists to close."
      category: transparency
      requirement_id: MEAS-01
  artifacts:
    - path: ".planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md"
      provides: "Decided API coverage matrix for the PostHog browser SDK, the PostHog Query API, and FormSubmit"
      contains: "## Operational boundary"
    - path: "scripts/verify-phase4-coverage.mjs"
      provides: "Coverage verifier re-pointed from the two Plausible tables to the two PostHog tables"
      contains: "REQUIRED_TABLES"
  key_links:
    - from: "scripts/verify-phase4-coverage.mjs"
      to: ".planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md"
      via: "REQUIRED_TABLES heading keys must equal the COVERAGE.md `## ` headings exactly"
      pattern: "REQUIRED_TABLES"
---

<objective>
Produce the decided API coverage matrix this phase's external integrations require, and
re-point the coverage verifier at it.

The API-coverage detector fires on this phase (`detected: true`, signal: the Query API).
Two external surfaces enter the project: the `posthog-js` browser SDK and the PostHog Query
API. Full coverage is the default; every subtraction is an explicit, reasoned OPT-OUT — and
because D-03's automatic-capture lockdown IS a set of subtractions, recording them here is
what makes the lockdown a decision rather than a configuration accident.

Purpose: the phase cannot seal without a decided matrix, and the existing verifier already
knows how to validate one — it just points at the wrong tables.
Output: `COVERAGE.md` for this phase, and a verifier that validates it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-CONTEXT.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-RESEARCH.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the decided PostHog API coverage matrix</name>
  <files>.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md</files>
  <read_first>
    - .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md (the whole file — its three-column table shape, its OPT-OUT reason voice, its `## Operational boundary` section, and the FormSubmit table that is carried forward verbatim)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-RESEARCH.md (Pattern 1 for the full lockdown option list and the verified defaults table; Pattern 4 and Pitfall 7 for the Query API surface; the Alternatives Considered table for the rejected options)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-CONTEXT.md (D-03 for the four automatic-capture surfaces, D-06 for the retired dashboard-goals gate, D-08 for the region)
  </read_first>
  <action>
    Create `.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md`
    following the Phase 4 file's exact structure: an `# API Coverage — …` title, a
    full-coverage-by-default note, then one `## ` heading per surface with a
    `| capability | decision | reason |` table, then `## Operational boundary`.

    Three tables, with these exact headings:

    1. `## PostHog — browser SDK (posthog-js, bundled)` — INTEGRATE rows for: bundled
       `posthog.init(token, config)` with an explicit lockdown object; merged-config
       readback via `instance.config`; `capture(name)` with a bare name and no property
       argument; `before_send` payload reduction; `person_profiles: 'never'`;
       `persistence: 'memory'` with `disable_persistence: true`; `advanced_disable_flags`
       to suppress the remote-configuration fetch; `disable_external_dependency_loading`;
       `defaults: 'unset'` pinning of the date-gated default set; PostHog Cloud US
       ingestion host. OPT-OUT rows, each with a reason, for at minimum: DOM autocapture;
       rageclick; dead clicks; automatic `$pageview`; automatic `$pageleave`; session
       recording / replay; surveys; automatic survey display; product tours;
       conversations; web experiments; heatmaps; exception autocapture; performance and
       web vitals; scroll properties; site apps; feature flags; toolbar metrics;
       `save_referrer`; `save_campaign_params`; `identify()` / `alias()` / `group()` /
       `setPersonProperties()`; `property_denylist` as a privacy boundary;
       `sanitize_properties`; the deprecated `ip` option; `cookieless_mode`;
       `$geoip_disable` as an event property. The reasons must be the real ones from
       CONTEXT and RESEARCH — for example, that `property_denylist` runs before
       `$process_person_profile` is appended and therefore cannot be the contract, and
       that `$geoip_disable` as a property would contradict the bare-name payload.
    2. `## PostHog — Query API` — INTEGRATE rows for: `POST /api/projects/:project_id/query/`;
       `HogQLQuery` body kind; the descriptive `name` parameter; bearer authentication from
       the local process environment with the single scope PostHog labels *Query Read*;
       `count()` aggregate grouped by `event`; `toTimeZone(timestamp, 'Africa/Nairobi')`
       pinned inside the SQL; explicit inclusive date bounds; an explicit `LIMIT` above the
       allowlist size; echoed `query` / `hogql` provenance; response `results` rows;
       response `columns` equality assertion. OPT-OUT rows, each with a reason, for at
       minimum: bulk or recurring event export; OFFSET pagination; the Insights API and
       dashboard actions (retired by design per D-06); person, session, and cohort queries;
       `query_log`; async/polling query execution; shared links and embedded dashboards;
       funnels and user journeys; `refresh` cache controls. Record explicitly, as an
       INTEGRATE row reason, that the API echoes `query` and `hogql` but **not** the project
       id — the loss of the `site_id` echo Phase 4 relied on.
    3. `## FormSubmit — AJAX email delivery (re-decided from a full baseline)` — carried
       forward byte-identically from the Phase 4 `COVERAGE.md`, including every reason
       string. Do not re-derive it and do not change a decision; it is unrelated to this
       migration and re-deciding it here would be an undeclared scope change.

    `## Operational boundary` must state, in sentences the verifier can pattern-match
    (Task 2 pins the exact patterns): that production analytics enablement remains OPT-OUT
    for this phase; that the deferred items are processor approval, project creation, and
    deployment variables; that the provider selector remains unset; that
    `POSTHOG_QUERY_API_KEY` and `POSTHOG_PROJECT_ID` are local report-process inputs;
    and that neither may enter a `VITE_*` variable or the published bundle. Also state that
    analytics data is processed in the United States (D-08), that the all-time range begins
    at first PostHog ingestion because no prior provider data exists, and that
    "Discard client IP data" is an owner-performed project setting the code cannot assert.
  </action>
  <verify>
    <automated>[ "$(grep -c '^## ' .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md)" -eq 4 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md` exists.
    - `[ "$(grep -c '^## ' .planning/phases/04.1-*/COVERAGE.md)" -eq 4 ]` succeeds — exactly three surface headings plus `## Operational boundary`, no more and no fewer.
    - Every row matching `| OPT-OUT |` has a non-empty third cell — checkable with `awk -F'|' '/OPT-OUT/ { if ($4 ~ /^[[:space:]]*$/) exit 1 }'` exiting 0.
    - The FormSubmit table body is byte-identical to the Phase 4 file's — `diff <(sed -n '/## FormSubmit/,/^$/p' .planning/phases/04-*/COVERAGE.md) <(sed -n '/## FormSubmit/,/^$/p' .planning/phases/04.1-*/COVERAGE.md)` exits 0.
    - The `## Operational boundary` section contains the literal strings `POSTHOG_QUERY_API_KEY`, `POSTHOG_PROJECT_ID`, and `United States`.
  </acceptance_criteria>
  <done>A three-table coverage matrix exists in which every capability carries a decision and every OPT-OUT carries a reason, including one OPT-OUT row per automatic-capture surface D-03 locks off.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point the coverage verifier at the PostHog tables</name>
  <files>scripts/verify-phase4-coverage.mjs</files>
  <read_first>
    - scripts/verify-phase4-coverage.mjs (lines 1-60 for REQUIRED_TABLES, lines 120-165 for the boundaryChecks table and the error-accumulation discipline)
    - src/test/haoo-report.test.ts (line ~1100 — the `owner command registration` describe references this script by path, so the filename must not change). This task READS that file but does not write it; 04.1-01 rewrites its `credential and provider-origin boundary` describe, which is why this plan declares `depends_on: ["04.1-01"]` and runs in wave 2 even though the two plans share no `files_modified` entry.
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md (written by Task 1 — the heading strings and capability strings must match exactly)
  </read_first>
  <action>
    Rewrite `REQUIRED_TABLES` in `scripts/verify-phase4-coverage.mjs` so its three keys are
    the three `## ` headings written in Task 1, and its `Map` entries are the exact
    capability strings and expected decisions from that file. Keep the FormSubmit map
    unchanged. Do not rename the script file — `src/test/haoo-report.test.ts` asserts the
    owner command registration by that exact path.

    Re-point the `boundaryChecks` table, preserving its established discipline: each entry
    stays `[human-readable description, pattern]`, the description is what the error message
    interpolates, patterns stay pinned to the sentence they mean to assert rather than to a
    bare word, and errors accumulate into `errors[]` and are thrown once as a bulleted list
    rather than failing fast. The four provider-specific entries become:
    - `POSTHOG_QUERY_API_KEY stays local` → a pattern matching the backticked variable name
    - `POSTHOG_PROJECT_ID stays local` → a pattern matching the backticked variable name
    - `analytics data is processed in the United States` → a pattern pinned to that sentence
    - `client IP discard is an owner-performed project setting` → a pattern pinned to the
      sentence naming the setting
    Keep `production analytics remains OPT-OUT`, `provider selector remains unset`, and
    `local report variables stay outside Vite/browser configuration` as they are; update the
    `processor approval, dashboard setup, and deployment variables remain deferred` entry so
    its pattern matches the migrated sentence (project creation replaces dashboard setup).
    Above any newly-tightened pattern, add a comment recording why it was tightened, matching
    the precedent already set above the second entry.

    Update the usage string so it names this phase's coverage file rather than a generic one.
  </action>
  <verify>
    <automated>node scripts/verify-phase4-coverage.mjs .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-phase4-coverage.mjs .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md` exits 0 and prints a passed line naming the capability and table counts.
    - `grep -c 'PostHog — browser SDK' scripts/verify-phase4-coverage.mjs` returns 1.
    - `grep -c 'PostHog — Query API' scripts/verify-phase4-coverage.mjs` returns 1.
    - `grep -c 'POSTHOG_QUERY_API_KEY' scripts/verify-phase4-coverage.mjs` returns 1.
    - `ls scripts/verify-phase4-coverage.mjs` succeeds (the file was not renamed).
    - Removing a single OPT-OUT reason cell from `COVERAGE.md` makes the command exit non-zero (mutation probe; restore the file afterwards).
    - `npx vitest run src/test/haoo-report.test.ts -t "owner command registration"` reports 0 failures.
  </acceptance_criteria>
  <done>The verifier validates this phase's matrix, fails on a malformed row or a reasonless OPT-OUT, and its boundary checks assert the migrated operational sentences.</done>
</task>

</tasks>

## Artifacts this phase produces

New files and symbols created by this plan:

- `.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md` (new file)
- `REQUIRED_TABLES` keys `PostHog — browser SDK (posthog-js, bundled)` and `PostHog — Query API` (rewritten in `scripts/verify-phase4-coverage.mjs`)
- New `boundaryChecks` entries for `POSTHOG_QUERY_API_KEY`, `POSTHOG_PROJECT_ID`, the US-processing sentence, and the client-IP-discard sentence

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner shell → PostHog Query API | A credentialed request leaves the owner machine |
| documentation → phase seal | An undecided capability would seal green as if decided |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-10 | Elevation of Privilege | PostHog personal API key scope | medium | mitigate | The Query API table records the single scope PostHog labels *Query Read* as the only INTEGRATE row, and every write-capable surface is an explicit OPT-OUT with a reason |
| T-04.1-13 | Repudiation | `COVERAGE.md` OPT-OUT rows | medium | mitigate | The verifier fails on a reasonless OPT-OUT, so an automatic-capture surface cannot be silently un-decided |
| T-04.1-04 | Information Disclosure | PostHog server-side GeoIP | medium | mitigate | Recorded in the operational boundary as an owner-performed project setting the code cannot assert; the enforcing gate lives in 04.1-08 |
| T-04.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate + blocking human checkpoint for `[ASSUMED]`/`[SUS]` — enforced in 04.1-03 before any install runs |
</threat_model>

<verification>
- `node scripts/verify-phase4-coverage.mjs .planning/phases/04.1-.../COVERAGE.md` exits 0.
- A deliberately blanked OPT-OUT reason makes it exit non-zero.
- `npm test` green (this plan touches no production source module).
</verification>

<success_criteria>
- Every capability of both PostHog surfaces carries a decision, and every subtraction carries a reason.
- The verifier validates this phase's matrix and its migrated operational-boundary sentences.
- The FormSubmit decisions are carried forward without re-derivation.
</success_criteria>

<output>
Create `.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-02-SUMMARY.md` when done
</output>
