---
status: partial
phase: 04.1-migrate-measurement-from-plausible-to-posthog
source: 04.1-01-SUMMARY.md, 04.1-02-SUMMARY.md, 04.1-03-SUMMARY.md, 04.1-04-SUMMARY.md, 04.1-05-SUMMARY.md, 04.1-06-SUMMARY.md, 04.1-07-SUMMARY.md, 04.1-08-SUMMARY.md
started: 2026-09-04T00:54:44Z
updated: 2026-09-05T00:00:00Z
---

## Current Test

[testing paused — 10 items outstanding]
## Tests

### 1. Cold Start Smoke Test
expected: From a clean tree (no dist/, no node_modules cache warmth): install, `npm run build`, and serve the built output. The build completes without errors, the app loads in a browser, and the HAOO page renders with the measurement disclosure present.
result: skipped
reason: "i can\'t verify"

### 2. Vendor Fixture Matches Real posthog-js Runtime
expected: The fixture's transcription actually matches what posthog-js 1.425.1 does at runtime — that init merges as transcribed, that a blank token does not throw, and that the three transport-required keys are the real minimum ingestion accepts.
coverage_id: 04.1-01 D7
needs_human: human_judgment
rationale: The fixture is a transcription of vendor documentation, so by construction it is not evidence about the real SDK. No SDK is installed at this plan (that is 04.1-03), so nothing here can be executed against the vendor. The live one-action-one-event confirmation is the human gate recorded in 04.1-USER-SETUP.md, and RESEARCH assumption A7 flags that the three-key minimum was never verified against a live endpoint.
result: skipped

### 3. Query API Response Envelope Shape
expected: The Query API response envelope for a HogQLQuery is `results` as positional arrays with a parallel `columns` array (researcher assumption A4); the coverage row recording it is confirmed only by the first live run.
coverage_id: 04.1-02 D5
needs_human: human_judgment
rationale: Declared a backstop truth in the plan's must_haves. The row is a recorded expectation transcribed from PostHog's documentation; nothing in this repository issues a live Query API request, and the envelope shape can only be confirmed against the real project during the MEAS-08 live reconciliation gate (D-07). The 'response `columns` equality assertion' row is precisely the guard that will turn a wrong assumption into a fail-closed error rather than a silently swapped name and count.
result: skipped

### 4. Pinned PostHog Packages Are Legitimate
expected: Both packages are legitimate and the pinned version is the one the owner intends to run.
coverage_id: 04.1-03 D8
needs_human: human_judgment
rationale: This is the Task 1 human gate and it was cleared with \"approved\" — a human read the npm registry pages for posthog-js and @posthog/wizard, weighed the [SUS] too-new verdicts, and accepted the pin. Recorded here as human_judgment because registry legitimacy is not a property any test in this repository can assert; re-confirmation belongs to a human at any future version bump.
result: [pending]
reason: "04.1-11 enabled delivery: the release build now selects the provider, so the pinned packages are the ones that will actually run in production. Re-confirming the registry pages and the pin is a live judgement now rather than a hypothetical one. Performable, not yet performed."

### 5. One Visitor Action, One Name-Only Event
expected: One real visitor action on a deployed build produces exactly one name-only event in the PostHog project, and no automatic capture appears alongside it.
coverage_id: 04.1-04 D13
needs_human: human_judgment
rationale: The vendor fixture is an independent transcription of published documentation, so it pins shape agreement and never vendor runtime behaviour. Only a live, credentialed project can show what actually arrives at ingestion. This is the blocking owner gate 04.1-08 records, and it is the same gate the previous provider carried.
result: [pending]
reason: "04.1-11 enabled delivery: the release build now sends the ten allowlisted names, so this is performable on the deployed page. Perform it in the same session as checkpoint 10, which observes the same actions from the ingestion side. Performable, not yet performed."

### 6. D-04 Gates Are Independent and Mutation-Proof
expected: Both D-04 gates are independent rather than the same assertion twice, and each is proven to fail on a weakening mutation
coverage_id: 04.1-05 D8
needs_human: human_judgment
rationale: The probes were run and recorded, but they are a transient local weakening rather than a standing assertion — nothing in the committed suite re-runs them, so a reviewer confirming the gates still have teeth must re-run them by hand.
result: [pending]
reason: "04.1-11 enabled delivery, so the tree these probes re-weaken is the tree that ships. The probes remain a by-hand re-run — nothing in the committed suite re-runs them. Performable, not yet performed."

### 7. Processor Group Layout at 320px and 200% Zoom
expected: The new processor group wraps without clipping, ellipsis, or overlap at 320px and at 200% browser zoom, introduces no body-level horizontal scroll, reads naturally in position, and is announced by its heading to a screen reader.
coverage_id: 04.1-06 D7
needs_human: human_judgment
rationale: Visual wrapping at a specific viewport and zoom level, natural reading order, and screen-reader announcement are perceptual judgments jsdom cannot make. Declared a held-out human check by the plan, harvested end-of-phase.
result: [pending]
reason: "04.1-11 enabled delivery, so the deployed page carrying the processor group is the release build. Performable at 320px and 200% zoom on that page. Performable, not yet performed."

### 8. Report Counts Reconcile Against Live PostHog
expected: The 7/30/90/all-time counts and the first recorded day reconcile against the real PostHog project, and the provider's own evaluation of the inclusive bounds is confirmed
coverage_id: 04.1-07 D7
needs_human: human_judgment
rationale: No PostHog project or personal API key exists yet (D-09). The submitted SQL is asserted in-repo, but the provider's evaluation of it — and the A4 response-envelope assumption — can only be settled by a live run. Recorded as the owner reconciliation gate in 04.1-USER-SETUP.md by plan 04.1-08.
result: [pending]
reason: "04.1-11 enabled delivery, so the report finally has arriving events to reconcile against. Perform it AFTER checkpoint 10 has confirmed that events arrive at all — reconciling zeros against zeros settles nothing, and the inclusive-bound question this checkpoint exists to answer needs data on both bounds. Performable, not yet performed."

### 9. Geo-IP Enrichment Disabled on PostHog Project
expected: Server-side geo-IP enrichment is suppressed on the PostHog project
coverage_id: 04.1-08 D6
needs_human: human_judgment
rationale: The setting lives in the provider's own UI, outside version control. No code in this tree can establish or observe it; the owner's recorded reply is the entire evidence.
result: [pending]
reason: "04.1-11 enabled delivery. The owner recorded `enabled` for the client-IP discard setting on 2026-09-04 and that record remains the whole evidence; live re-confirmation in the provider UI is now performable alongside checkpoint 10. Performable, not yet performed."

### 10. Live One-Action-One-Event, No Person Profile
expected: One visitor action produces exactly one bare-name event with no automatic companion and no person profile, confirmed live
coverage_id: 04.1-08 D7
needs_human: human_judgment
rationale: Still open. Requires a configured, deployed PostHog project and a real browser session. Recorded as an open gate in 04.1-USER-SETUP.md.
result: [pending]
reason: "04.1-11 enabled delivery: the release build delivers, so this is performable. THIS IS ALSO THE INGESTION-ACCEPTANCE CHECK — the only check anywhere that confirms PostHog Cloud US ACCEPTS a payload reduced to three transport properties. Zero events arriving is a FAILURE of this checkpoint, never a quiet pass: a silent ingestion rejection reads as a dead funnel rather than a broken one (D-05). Confirm first that the three GitHub Actions repository variables were actually created (04.1-USER-SETUP.md), or an unconfigured build fails this gate for the wrong reason. Performable, not yet performed."

### 11. The boundary suite admits a bundled analytics SDK: the vendor token is out of the unconditional competitor-origin list and no remaining bundle prohibition would be tripped by the vendor's own artifact for the reasons research verified.
expected: The boundary suite admits a bundled analytics SDK: the vendor token is out of the unconditional competitor-origin list and no remaining bundle prohibition would be tripped by the vendor's own artifact for the reasons research verified.
result: pass
source: automated
coverage_id: 04.1-01 D1

### 12. The withdrawn bundle-level provider-origin guarantee is replaced by PROVIDER_INGESTION_HOST_SOURCE_FORBIDDEN, asserted over every module under src/, and the withdrawal is recorded in-source with 04-08 named.
expected: The withdrawn bundle-level provider-origin guarantee is replaced by PROVIDER_INGESTION_HOST_SOURCE_FORBIDDEN, asserted over every module under src/, and the withdrawal is recorded in-source with 04-08 named.
result: pass
source: automated
coverage_id: 04.1-01 D2

### 13. Identity, fingerprint and ordered-queue prohibitions still hold, asserted against production source files instead of against a minified vendor bundle.
expected: Identity, fingerprint and ordered-queue prohibitions still hold, asserted against production source files instead of against a minified vendor bundle.
result: pass
source: automated
coverage_id: 04.1-01 D3

### 14. Report credential shapes remain forbidden in every browser bundle, re-pointed at POSTHOG_QUERY_API_KEY and the PostHog query path with Authorization and Bearer unchanged.
expected: Report credential shapes remain forbidden in every browser bundle, re-pointed at POSTHOG_QUERY_API_KEY and the PostHog query path with Authorization and Bearer unchanged.
result: pass
source: automated
coverage_id: 04.1-01 D4

### 15. The report suite's derived reader is re-anchored on the renamed constant, so the two suites cannot drift apart.
expected: The report suite's derived reader is re-anchored on the renamed constant, so the two suites cannot drift apart.
result: pass
source: automated
coverage_id: 04.1-01 D5

### 16. src/test/fixtures/posthog-capture-contract.ts exists, imports nothing from production code, and compiles — declaring the capture payload shape and a stub client whose defaults are the vendor's documented defaults.
expected: src/test/fixtures/posthog-capture-contract.ts exists, imports nothing from production code, and compiles — declaring the capture payload shape and a stub client whose defaults are the vendor's documented defaults.
result: pass
source: automated
coverage_id: 04.1-01 D6

### 17. The phase carries a decided API coverage matrix for both PostHog surfaces — the browser SDK and the Query API — in which every capability is INTEGRATE or a reasoned OPT-OUT.
expected: The phase carries a decided API coverage matrix for both PostHog surfaces — the browser SDK and the Query API — in which every capability is INTEGRATE or a reasoned OPT-OUT.
result: pass
source: automated
coverage_id: 04.1-02 D1

### 18. Every automatic-capture surface named in D-03 appears in the browser-SDK table as an explicit OPT-OUT row with a reason, so the lockdown is a recorded decision rather than an implementation detail.
expected: Every automatic-capture surface named in D-03 appears in the browser-SDK table as an explicit OPT-OUT row with a reason, so the lockdown is a recorded decision rather than an implementation detail.
result: pass
source: automated
coverage_id: 04.1-02 D2

### 19. The coverage verifier validates the PostHog tables and the migrated operational-boundary sentences, and fails on a malformed row or a reasonless OPT-OUT.
expected: The coverage verifier validates the PostHog tables and the migrated operational-boundary sentences, and fails on a malformed row or a reasonless OPT-OUT.
result: pass
source: automated
coverage_id: 04.1-02 D3

### 20. The FormSubmit table is carried forward unchanged, so re-pointing the verifier does not silently drop a previously-decided external surface.
expected: The FormSubmit table is carried forward unchanged, so re-pointing the verifier does not silently drop a previously-decided external surface.
result: pass
source: automated
coverage_id: 04.1-02 D4

### 21. posthog-js@1.425.1 is an exact-pinned dependency with a committed lockfile, installed only after a human confirmed both packages against their registry pages.
expected: posthog-js@1.425.1 is an exact-pinned dependency with a committed lockfile, installed only after a human confirmed both packages against their registry pages.
result: pass
source: automated
coverage_id: 04.1-03 D1

### 22. The one-shot scaffolding wizard never entered package.json or the lockfile, and no app-root instrumentation from it survives.
expected: The one-shot scaffolding wizard never entered package.json or the lockfile, and no app-root instrumentation from it survives.
result: pass
source: automated
coverage_id: 04.1-03 D2

### 23. The ingestion host literal lives in a repository-owned module outside src/ and reaches a bundle only through a provider-gated build-time constant.
expected: The ingestion host literal lives in a repository-owned module outside src/ and reaches a bundle only through a provider-gated build-time constant.
result: pass
source: automated
coverage_id: 04.1-03 D3

### 24. The approved-host module applies the same trim-and-lowercase provider normalization as the runtime provider resolver, by exact equality, so build-time and runtime cannot disagree and no near miss selects the origin.
expected: The approved-host module applies the same trim-and-lowercase provider normalization as the runtime provider resolver, by exact equality, so build-time and runtime cannot disagree and no near miss selects the origin.
result: pass
source: automated
coverage_id: 04.1-03 D4

### 25. With the provider selector unset, the ingestion host literal is absent from the bundle entirely, not merely unused.
expected: With the provider selector unset, the ingestion host literal is absent from the bundle entirely, not merely unused.
result: pass
source: automated
coverage_id: 04.1-03 D5

### 26. The plan is additive: the superseded Plausible configuration module, define, and both environment-variable declarations are still present and still read, so the repository typechecks and the suite is green at every commit.
expected: The plan is additive: the superseded Plausible configuration module, define, and both environment-variable declarations are still present and still read, so the repository typechecks and the suite is green at every commit.
result: pass
source: automated
coverage_id: 04.1-03 D6

### 27. The pinned SDK's option surface actually supports the lockdown 04.1-04 must configure, and Pitfall 3's person-processing hazard is real at this exact version.
expected: The pinned SDK's option surface actually supports the lockdown 04.1-04 must configure, and Pitfall 3's person-processing hazard is real at this exact version.
result: pass
source: automated
coverage_id: 04.1-03 D7

### 28. The lockdown object typechecks against the SDK's own config type, so a renamed or removed option is a build failure rather than a silently-ignored key.
expected: The lockdown object typechecks against the SDK's own config type, so a renamed or removed option is a build failure rather than a silently-ignored key.
result: pass
source: automated
coverage_id: 04.1-04 D1

### 29. No sink is returned and no capture is possible unless the merged configuration read back from the initialized instance matches every locked key, including advanced_disable_flags true and before_send being a function.
expected: No sink is returned and no capture is possible unless the merged configuration read back from the initialized instance matches every locked key, including advanced_disable_flags true and before_send being a function.
result: pass
source: automated
coverage_id: 04.1-04 D2

### 30. One HAOO event travels the whole path — facade to locked-down adapter to the wire — as a bare name carrying exactly token, distinct_id and $process_person_profile.
expected: One HAOO event travels the whole path — facade to locked-down adapter to the wire — as a bare name carrying exactly token, distinct_id and $process_person_profile.
result: pass
source: automated
coverage_id: 04.1-04 D3

### 31. before_send drops a null input, a name outside the ten, a name differing only by case, and a payload whose properties are empty — emitting nothing rather than a partial payload.
expected: before_send drops a null input, a name outside the ten, a name differing only by case, and a payload whose properties are empty — emitting nothing rather than a partial payload.
result: pass
source: automated
coverage_id: 04.1-04 D4

### 32. A build whose provider selector is unset, blank, whitespace, a near miss, or an absolute URL resolves to 'none', creates no sink, never calls the SDK's init, and leaves every visitor action working.
expected: A build whose provider selector is unset, blank, whitespace, a near miss, or an absolute URL resolves to 'none', creates no sink, never calls the SDK's init, and leaves every visitor action working.
result: pass
source: automated
coverage_id: 04.1-04 D5

### 33. A pre-existing ambient provider global that is not an object with a callable init is refused intact — not overwritten, not initialized — and the whole journey keeps working.
expected: A pre-existing ambient provider global that is not an object with a callable init is refused intact — not overwritten, not initialized — and the whole journey keeps working.
result: pass
source: automated
coverage_id: 04.1-04 D6

### 34. The runtime ingestion-host resolver accepts a configured value only on exact origin equality against the trust anchor, and the project key only on the exact phc_ shape; both fail closed to the empty string and to an empty approved set when the build-time constant is absent.
expected: The runtime ingestion-host resolver accepts a configured value only on exact origin equality against the trust anchor, and the project key only on the exact phc_ shape; both fail closed to the empty string and to an empty approved set when the build-time constant is absent.
result: pass
source: automated
coverage_id: 04.1-04 D7

### 35. No person-processing API is called or exposed anywhere in the provider modules, and no provider state is written to browser storage or cookies.
expected: No person-processing API is called or exposed anywhere in the provider modules, and no provider state is written to browser storage or cookies.
result: pass
source: automated
coverage_id: 04.1-04 D8

### 36. The superseded Plausible build wiring is gone, not merely unused: the adapter, preload fixture, approved-source module, provider-gated define, build-time constant declaration and both environment-variable declarations were removed in the same commit that removed their last reader.
expected: The superseded Plausible build wiring is gone, not merely unused: the adapter, preload fixture, approved-source module, provider-gated define, build-time constant declaration and both environment-variable declarations were removed in the same commit that removed their last reader.
result: pass
source: automated
coverage_id: 04.1-04 D9

### 37. The ingestion origin reaches a provider-selected bundle exactly once and a provider-unset bundle not at all — the deferred half of 04.1-03's bundle contract, now falsifiable because this plan added the reader.
expected: The ingestion origin reaches a provider-selected bundle exactly once and a provider-unset bundle not at all — the deferred half of 04.1-03's bundle contract, now falsifiable because this plan added the reader.
result: pass
source: automated
coverage_id: 04.1-04 D10

### 38. Both PostHog build variables are declared and read, keeping the bidirectional ImportMetaEnv exhaustiveness invariant green in both directions (deferred item D3).
expected: Both PostHog build variables are declared and read, keeping the bidirectional ImportMetaEnv exhaustiveness invariant green in both directions (deferred item D3).
result: pass
source: automated
coverage_id: 04.1-04 D11

### 39. Every provider test case deleted with the Plausible describes is enumerated in a tracked inventory naming the 04.1-05 task that restores it, so the one wave of reduced provider test depth is auditable from the repository.
expected: Every provider test case deleted with the Plausible describes is enumerated in a tracked inventory naming the 04.1-05 task that restores it, so the one wave of reduced provider test depth is auditable from the repository.
result: pass
source: automated
coverage_id: 04.1-04 D12

### 40. D-04 gate 1: for every locked configuration key individually, a merged config resolving that one key wrong yields no sink, no capture, and a refusal naming the lockdown gate
expected: D-04 gate 1: for every locked configuration key individually, a merged config resolving that one key wrong yields no sink, no capture, and a refusal naming the lockdown gate
result: pass
source: automated
coverage_id: 04.1-05 D1

### 41. D-04 gate 2: driving every event path of the full HAOO journey puts exactly the ten allowlisted bare names on the wire, each carrying exactly token, distinct_id and $process_person_profile
expected: D-04 gate 2: driving every event path of the full HAOO journey puts exactly the ten allowlisted bare names on the wire, each carrying exactly token, distinct_id and $process_person_profile
result: pass
source: automated
coverage_id: 04.1-05 D2

### 42. Every post-provider-check refusal emits a named reason through an owner-observable channel; pre-provider-check refusals stay silent
expected: Every post-provider-check refusal emits a named reason through an owner-observable channel; pre-provider-check refusals stay silent
result: pass
source: automated
coverage_id: 04.1-05 D3

### 43. A blocked, read-only, frozen or throwing provider slot leaves every visitor action working and does not unmount the product page (Phase 4 gap 1, re-armed)
expected: A blocked, read-only, frozen or throwing provider slot leaves every visitor action working and does not unmount the product page (Phase 4 gap 1, re-armed)
result: pass
source: automated
coverage_id: 04.1-05 D4

### 44. With the provider selector unset the sink factory returns undefined and the SDK's init is never called — the runtime successor to the withdrawn bundle-level provider-origin prohibition
expected: With the provider selector unset the sink factory returns undefined and the SDK's init is never called — the runtime successor to the withdrawn bundle-level provider-origin prohibition
result: pass
source: automated
coverage_id: 04.1-05 D5

### 45. No browser storage write occurs from the provider path, and campaign parameters never reach the wire while the address bar is cleaned before a sink exists
expected: No browser storage write occurs from the provider path, and campaign parameters never reach the wire while the address bar is cleaned before a sink exists
result: pass
source: automated
coverage_id: 04.1-05 D6

### 46. The 04.1-04 deleted-case inventory is closed: every row restored with a named successor describe, none deliberately dropped
expected: The 04.1-04 deleted-case inventory is closed: every row restored with a named successor describe, none deliberately dropped
result: pass
source: automated
coverage_id: 04.1-05 D7

### 47. A visitor reading the disclosure learns analytics data is processed by PostHog in the United States, sourced from product data.
expected: A visitor reading the disclosure learns analytics data is processed by PostHog in the United States, sourced from product data.
result: pass
source: automated
coverage_id: 04.1-06 D1

### 48. The processor statement is a required interface member: a product configuration that omits it is a typecheck failure, not a blank.
expected: The processor statement is a required interface member: a product configuration that omits it is a typecheck failure, not a blank.
result: pass
source: automated
coverage_id: 04.1-06 D2

### 49. No processor, region, or provider literal appears in MeasurementDisclosure.tsx; the component renders product data.
expected: No processor, region, or provider literal appears in MeasurementDisclosure.tsx; the component renders product data.
result: pass
source: automated
coverage_id: 04.1-06 D3

### 50. The processor group renders in full regardless of stored context, provider state, or campaign presence, and reflects no runtime value back to the visitor.
expected: The processor group renders in full regardless of stored context, provider state, or campaign presence, and reflects no runtime value back to the visitor.
result: pass
source: automated
coverage_id: 04.1-06 D4

### 51. The group is positioned after the campaign group and before the never-collected group, as a labelled section.
expected: The group is positioned after the campaign group and before the never-collected group, as a labelled section.
result: pass
source: automated
coverage_id: 04.1-06 D5

### 52. The approved wording has one source, so a change cannot land on some surfaces and not others.
expected: The approved wording has one source, so a change cannot land on some surfaces and not others.
result: pass
source: automated
coverage_id: 04.1-06 D6

### 53. The row reader parses positional HogQL rows with an asserted column pair and preserves every prior rejection rule
expected: The row reader parses positional HogQL rows with an asserted column pair and preserves every prior rejection rule
result: pass
source: automated
coverage_id: 04.1-07 D1

### 54. The echo validator proves the response answered the exact SQL this repository submitted, in either documented echo shape
expected: The echo validator proves the response answered the exact SQL this repository submitted, in either documented echo shape
result: pass
source: automated
coverage_id: 04.1-07 D2

### 55. Each range is one HogQL aggregate with the reporting timezone pinned in the query text and both day bounds inclusive
expected: Each range is one HogQL aggregate with the reporting timezone pinned in the query text and both day bounds inclusive
result: pass
source: automated
coverage_id: 04.1-07 D3

### 56. A non-ok response, an echo refusal, a row refusal, or a malformed first-day answer aborts before any write and leaves the previous report byte-identical
expected: A non-ok response, an echo refusal, a row refusal, or a malformed first-day answer aborts before any write and leaves the previous report byte-identical
result: pass
source: automated
coverage_id: 04.1-07 D4

### 57. The report states in its own rendered words that all-time counts begin at first ingestion and that the provider echoes the query but not the project
expected: The report states in its own rendered words that all-time counts begin at first ingestion and that the provider echoes the query but not the project
result: pass
source: automated
coverage_id: 04.1-07 D5

### 58. The credentialed CLI reads the two new variables, names the rename on a stale environment, and remains the only credentialed module
expected: The credentialed CLI reads the two new variables, names the rename on a stale environment, and remains the only credentialed module
result: pass
source: automated
coverage_id: 04.1-07 D6

### 59. The owner has one document covering project creation, both keys, all five environment variables, and the two project settings the code cannot assert
expected: The owner has one document covering project creation, both keys, all five environment variables, and the two project settings the code cannot assert
result: pass
source: automated
coverage_id: 04.1-08 D1

### 60. The three MEAS-01 human gates are re-scoped in writing as re-opened, retired by design, and kept-and-strengthened
expected: The three MEAS-01 human gates are re-scoped in writing as re-opened, retired by design, and kept-and-strengthened
result: pass
source: automated
coverage_id: 04.1-08 D2

### 61. No repository instruction names the removed provider
expected: No repository instruction names the removed provider
result: pass
source: automated
coverage_id: 04.1-08 D3

### 62. Every Phase 4 claim that named the removed provider is restated against PostHog by appended amendment, with no verdict changed and no requirement box checked
expected: Every Phase 4 claim that named the removed provider is restated against PostHog by appended amendment, with no verdict changed and no requirement box checked
result: pass
source: automated
coverage_id: 04.1-08 D4

### 63. The phase's final tree is green and its coverage matrix is complete
expected: The phase's final tree is green and its coverage matrix is complete
result: pass
source: automated
coverage_id: 04.1-08 D5

## Summary

total: 63
passed: 53
issues: 0
pending: 7
skipped: 3
blocked: 0

## Gaps

[none yet]
