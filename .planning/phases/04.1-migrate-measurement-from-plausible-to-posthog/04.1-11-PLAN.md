---
phase: 04.1-migrate-measurement-from-plausible-to-posthog
plan: 11
type: execute
wave: 3
# Two artifact edges, both hard:
#   04.1-09 (wave 1) — ARTIFACT. Enabling delivery of an unloaded SDK would set the provider
#     variables on a build that captures nothing, turning the four UAT gates below into
#     checks with nothing to observe. It also owns the successor invariants; this plan makes
#     `dist` a provider-SELECTED artifact in CI, which is exactly the assumption those
#     successors were written to stop depending on.
#   04.1-10 (wave 2) — ARTIFACT. This plan's owner checkpoint approves the disclosure
#     sentence as true. Until adoption is removed that sentence is not true of every client
#     the adapter can initialize, so asking for approval first would be asking the owner to
#     approve a statement the code does not yet keep.
# wave = max(1, 2) + 1 = 3.
depends_on: ["04.1-09", "04.1-10"]
files_modified:
  - .github/workflows/deploy.yml
  - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md
  - scripts/verify-phase4-coverage.mjs
  - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-USER-SETUP.md
  - README.md
  - src/test/build-output.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-UAT.md
autonomous: false
requirements: [MEAS-01, MEAS-04, MEAS-08]
gap_closure: true
gap_ids: [G-04.1-1]

user_setup:
  - service: posthog
    why: "Production ingestion of the ten allowlisted HAOO events, and the owner report"
    env_vars:
      - name: VITE_HAOO_MEASUREMENT_PROVIDER
        source: "GitHub → Settings → Secrets and variables → Actions → Variables. Repository VARIABLE, not a secret."
      - name: VITE_HAOO_POSTHOG_TOKEN
        source: "PostHog → Settings → Project → Project API key (the `phc_` value). Repository VARIABLE — it is public write-only by design and ships in the bundle."
      - name: VITE_HAOO_POSTHOG_API_HOST
        source: "The repository-approved ingestion origin. Repository VARIABLE; it may only select from the approved list and can never add to it."
    dashboard_config:
      - task: "Record processor approval for PostHog as the analytics processor, and approve production collection"
        location: "Owner decision — see the blocking gate in 04.1-USER-SETUP.md"

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The owner has recorded approval of PostHog as the analytics processor and of production collection BEFORE any variable that enables delivery is set (D-06)."
    - "The deployed build sets the three public PostHog variables, and neither report credential enters a `VITE_*` variable or the published bundle (MEAS-08's credential boundary)."
    - "COVERAGE.md's operational boundary and the pinned assertions in `scripts/verify-phase4-coverage.mjs` state the enabled reality in the SAME commit, so no gate-pinned sentence is left false."
    - "The seven `blocked_by: release-build` UAT checkpoints in 04.1-UAT.md are performable, and the plan names each one."
    - "The live one-action-one-event gate is named as the check that also confirms ingestion ACCEPTS the three-property reduced payload — the failure mode where the funnel reads as dead rather than broken (D-05)."
  prohibitions:
    - statement: "MUST NOT set any variable that enables delivery before the blocking processor-approval checkpoint is recorded as cleared — the previous provider's approval is not inherited (D-06)."
      category: transparency
      requirement_id: MEAS-01
    - statement: "MUST NOT amend a sentence the coverage verifier pins without amending the pinned assertion in the same commit, and MUST NOT relax a pinned pattern to a bare fragment a sentence saying the opposite would also satisfy."
      category: transparency
      requirement_id: MEAS-08
    - statement: "MUST NOT add `POSTHOG_QUERY_API_KEY` or `POSTHOG_PROJECT_ID` to the workflow's Build step, to any `VITE_*` name, or to any repository variable — they are local report-process inputs only."
      category: security
      requirement_id: MEAS-08
    - statement: "MUST NOT check a requirement box on the strength of deployment alone — MEAS-01 and MEAS-08 stay unchecked until the live gates are performed and recorded."
      category: transparency
      requirement_id: MEAS-01
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "The Build step that selects the provider and supplies the public ingestion configuration"
      contains: "VITE_HAOO_MEASUREMENT_PROVIDER"
    - path: "scripts/verify-phase4-coverage.mjs"
      provides: "Boundary assertions restated for the enabled state"
      contains: "boundaryChecks"
    - path: "src/test/build-output.test.ts"
      provides: "The gate proving no report credential name reaches the workflow build environment"
      contains: "deploy.yml"
    - path: ".planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-USER-SETUP.md"
      provides: "The cleared processor-approval gate with the owner's recorded answer"
      contains: "## Verification"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "vite.config.ts"
      via: "`VITE_HAOO_MEASUREMENT_PROVIDER` reaches `approvedAnalyticsHostsForProvider` through `loadEnv`, so the deployed build is the first that publishes the approved ingestion origin"
      pattern: "VITE_HAOO_MEASUREMENT_PROVIDER"
    - from: ".planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md"
      to: "scripts/verify-phase4-coverage.mjs"
      via: "every amended operational-boundary sentence has a pinned regex amended in the same commit"
      pattern: "Operational boundary"
    - from: ".github/workflows/deploy.yml"
      to: "src/test/build-output.test.ts"
      via: "a workflow-source assertion proves no report-credential name appears under a VITE_ prefix in the Build step the bundle scan cannot see"
      pattern: "POSTHOG_QUERY_API_KEY"
---

<objective>
Turn delivery on — in the right order, with the owner's approval first and no gate-pinned
statement left false.

Seven UAT checkpoints are `blocked_by: release-build` and cannot be performed until a
configured build actually captures. `04.1-VERIFICATION.md` singles one of them out: the
person-profile check is also the only check that confirms PostHog Cloud US ACCEPTS a payload
reduced to three transport properties. If ingestion silently rejects it, the funnel reads as
dead rather than broken — failure mode D-05, and the exact thing the refusal channel exists
to distinguish.

Enabling delivery is not a configuration edit. `scripts/verify-phase4-coverage.mjs` pins the
sentence "deferred processor approval, project creation, and deployment variables" as a CI
assertion over COVERAGE.md's operational boundary, alongside the OPT-OUT statement and the
selector-remains-unset claim. All three become false the moment the workflow sets the
provider, so prose and pinned assertion move together in one commit — and only after the
owner records the approval D-06 re-opened. The owner has already created the project,
supplied a `phc_` token, and answered `enabled` to the geo-IP gate (D-09); the processor
approval itself is still unrecorded, and it is the approval of the disclosure sentence
`MeasurementDisclosure.tsx` renders as true and acceptable (D-10, D-08).

Purpose: the funnel measures, and every document that describes it is true on the same commit.
Output: a recorded owner approval, a deploying build that captures, an amended and re-pinned
operational boundary, and seven UAT gates the owner can now perform.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-CONTEXT.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-UAT.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-USER-SETUP.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-09-SUMMARY.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-10-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Owner approval of PostHog as the analytics processor and of production collection</name>
  <read_first>
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-USER-SETUP.md sections `## Account and Approval` and `## Verification` (the gate as written, and the four open boxes)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-CONTEXT.md (D-06 for why the approval is re-opened rather than inherited, D-08 for the region, D-10 for the disclosure sentence being approved)
    - src/products/haoo.ts line 271 (the exact processor sentence the visitor reads — the owner is approving THIS text as true and acceptable)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-VERIFICATION.md `human_verification` (the gate's own wording, including the instruction that the provider variable must not be set before it)
  </read_first>
  <what-built>
    Plans 04.1-09 and 04.1-10 have made the measurement stack operative and bound: a
    production module loads the pinned SDK, the adapter initializes only the module this
    repository imported, and every automatic-capture surface is locked and read back before
    any sink is returned. Nothing is delivered yet — the provider selector is still unset in
    deployment, which is the single change Tasks 2 and 3 make. This checkpoint is the
    approval that must exist before that change.
  </what-built>
  <how-to-verify>
    Read the visitor-facing processor sentence at `src/products/haoo.ts:271`, rendered by
    `src/components/MeasurementDisclosure.tsx`. Quote it verbatim to the owner and ask them to
    approve PostHog as the analytics processor and to approve production collection.

    State plainly what is being turned on: from the next push to `main`, a visitor's ten
    allowlisted HAOO actions are sent as bare event names to PostHog Cloud US, carrying three
    transport properties, with no person profile, no browser persistence and no cookie.

    The previous provider's approval is NOT inherited — different processor, different region
    (United States), different retention posture (D-06, D-08). Approving this is approving
    that sentence as true and acceptable (D-10).

    `VITE_HAOO_MEASUREMENT_PROVIDER` must not be set before the answer is recorded. Do not
    proceed to Task 2 or Task 3 without it. This gate is not auto-approvable.
  </how-to-verify>
  <resume-signal>Type "approved" to record the approval and continue, or describe the concern — a decline stops the run and Tasks 2 and 3 do not execute.</resume-signal>
  <acceptance_criteria>
    - The owner's verbatim answer is recorded in `04.1-USER-SETUP.md` as the sole evidence for the processor-approval box, in the same form the cleared client-IP gate uses.
    - The processor-approval box in that file's Verification section is ticked and dated, rewritten to EXACTLY the cleared client-IP gate's literal form so the next task can assert it mechanically rather than by reading: the line becomes `- [x] **Processor privacy approval — CLEARED YYYY-MM-DD.**` and the body carries a line beginning `**Recorded answer (`. Tasks 2 and 3 grep for exactly these two literals; a differently-worded tick halts the run.
    - `grep -cE '^- \[x\] \*\*Processor privacy approval — CLEARED [0-9]{4}-[0-9]{2}-[0-9]{2}\.\*\*' .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-USER-SETUP.md` returns 1.
    - No `VITE_HAOO_*` variable was set, and `.github/workflows/deploy.yml` is unmodified, at the moment the approval is recorded — `git log --oneline -1 -- .github/workflows/deploy.yml` still points at a commit predating this plan.
    - If the owner declines, the run stops here and reports the decline; Tasks 2 and 3 do not execute.
  </acceptance_criteria>
  <done>Production collection has a recorded owner decision behind it, taken before anything enabled it, and the visitor-facing sentence it approves is quoted in the record.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the operational boundary and its pinned assertions in one commit</name>
  <files>.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md, scripts/verify-phase4-coverage.mjs, .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-USER-SETUP.md, README.md, .planning/REQUIREMENTS.md</files>
  <precondition>Task 1's approval is recorded and ticked — assert it by RUNNING this before editing any file, and halt on a non-zero exit rather than proceeding on judgement: `grep -qE '^- \[x\] \*\*Processor privacy approval — CLEARED [0-9]{4}-[0-9]{2}-[0-9]{2}\.\*\*' .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-USER-SETUP.md &amp;&amp; grep -q '\*\*Recorded answer (' .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-USER-SETUP.md`</precondition>
  <read_first>
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md lines 97-126 (the whole `## Operational boundary` section — which paragraphs 04.1-09 already amended and which three claims this task must amend)
    - scripts/verify-phase4-coverage.mjs lines 165-232 (the `boundaryChecks` array — the convention is to pin the CLAIM across a sentence, never a bare fragment that a sentence saying the opposite would satisfy; each entry's comment explains why it was tightened)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-USER-SETUP.md (the header `Status:` line and the Verification boxes — three gates remain open after Task 1 and the header rule is stated in that file)
    - README.md (the section 04.1-09 wrote — it names 04.1-11 as the plan that enables delivery, and that forward reference resolves here)
    - .planning/REQUIREMENTS.md lines 37-95 and 155-170 (the MEAS-01/MEAS-08 status notes and the traceability table — the note that code-level closure is necessary but not sufficient must stay true)
  </read_first>
  <action>
    Three claims in COVERAGE.md's operational boundary become false the moment Task 3 lands,
    and each has a pinned regex in `scripts/verify-phase4-coverage.mjs`. Amend prose and
    pinned assertion together, in this one commit, so the audit never enforces a statement the
    document no longer makes and never passes a document that says the opposite:

    - The claim that production analytics enablement remains OPT-OUT for this phase.
    - The claim that processor approval, project creation and deployment variables are
      deferred.
    - The claim that the integration capabilities are implemented and fixture-verified while
      the provider selector remains unset.

    Replace each with the enabled statement and pin the new claim in the same tightened shape
    the existing entries use — across the sentence, not on a word. State what is now true: the
    processor is approved and the approval is recorded with the owner's own words; the project
    exists; the three public build variables are supplied by the deployment workflow as
    repository variables; and the browser-SDK capabilities are reached at runtime rather than
    only fixture-verified. State what remains unproven: the live gates listed in Task 3 have
    not been performed yet, so ingestion acceptance and the absence of a person profile are
    still owner observations rather than facts this tree has established.

    Leave every other boundary paragraph exactly as it is. The report-credential separation,
    the United States region, the all-time-begins-at-first-ingestion fact and the
    owner-performed client-IP setting are unchanged and their pinned checks must keep passing
    untouched.

    In `04.1-USER-SETUP.md`, tick the processor-approval box with the owner's recorded answer,
    and leave the remaining gates open. The header stays `Status: Incomplete` while any gate is
    open — do not change it on the strength of deployment.

    In `README.md`, resolve the forward reference 04.1-09 wrote: the section that named this
    plan as the enabler now states that delivery is enabled, which build variables carry it,
    and that they are repository VARIABLES rather than secrets because the `phc_` project key
    is public write-only by design and ships in the bundle. Keep the sentence that events are
    delivered only when the provider selector is set — it is still the operative condition and
    the build-output gate reads it.

    In `.planning/REQUIREMENTS.md`, append a status note dated today recording that delivery
    is enabled and which live gates remain. Do not check MEAS-01 or MEAS-08: code-level and
    deployment-level closure is necessary and not sufficient, and the existing note says so.
  </action>
  <verify>
    <automated>npm run verify:coverage &amp;&amp; node -e "const s=require('fs').readFileSync('scripts/verify-phase4-coverage.mjs','utf8');const n=(s.match(/^\s*\[/gm)||[]).length;process.exit(n>0?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:coverage` exits 0 against the amended COVERAGE.md.
    - The `boundaryChecks` array has the same number of entries as before, or more — no check was dropped rather than replaced.
    - Blanking each newly-pinned sentence in a scratch copy of COVERAGE.md makes `node scripts/verify-phase4-coverage.mjs <scratch>` exit 1 naming the missing statement; recorded as three probe rows in the SUMMARY.
    - `git show --stat HEAD` for this commit lists both `COVERAGE.md` and `scripts/verify-phase4-coverage.mjs`, and the commit subject contains the literal `04.1-11` (the standard `type(04.1-11): …` form) — Task 3's precondition greps for it, so a subject without it halts the next task.
    - `04.1-USER-SETUP.md` header still reads `Status: Incomplete`, its processor-approval box is ticked with the owner's verbatim answer, and at least three boxes remain unticked.
    - `.planning/REQUIREMENTS.md` MEAS-01 and MEAS-08 checkboxes remain unchecked, and the traceability rows for them still read `Gaps Found`.
    - The four unchanged boundary claims still pass their original pinned checks — verified by the same `npm run verify:coverage` run.
  </acceptance_criteria>
  <done>Every gate-pinned sentence in the operational boundary matches what the repository does, on the same commit, and the requirement boxes still refuse to close on anything short of the live gates.</done>
</task>

<task type="auto">
  <name>Task 3: Supply the three public variables to the deployment build and name the gates it unblocks</name>
  <files>.github/workflows/deploy.yml, src/test/build-output.test.ts, .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-UAT.md</files>
  <precondition>Task 2 is committed, so no operational-boundary sentence is left false at the moment the workflow starts selecting the provider — assert it by RUNNING this and halt on a non-zero exit: `git log -1 --format=%s | grep -q '04\.1-11' &amp;&amp; git show --stat --format= HEAD | grep -q 'verify-phase4-coverage\.mjs' &amp;&amp; git diff --quiet -- .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md scripts/verify-phase4-coverage.mjs`</precondition>
  <read_first>
    - .github/workflows/deploy.yml (the whole file — the Build step currently passing one variable, and the comment explaining why the Test step runs `test:unit` after Build rather than `test`)
    - src/test/build-output.test.ts (the `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN` group and the case that scans the bundle for it — the new workflow-source assertion is the same claim applied one layer earlier, where the bundle scan cannot see)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-09-SUMMARY.md (the successor invariants — this task makes `dist` a provider-SELECTED artifact in CI, which is the case those successors were written to survive; confirm they do rather than assuming it)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-UAT.md (the seven checkpoints carrying `blocked_by: release-build`, by their numbers and titles)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-VERIFICATION.md `behavior_unverified_items` (the person-profile item — the reason it is also the ingestion-acceptance check)
  </read_first>
  <action>
    Add `VITE_HAOO_MEASUREMENT_PROVIDER`, `VITE_HAOO_POSTHOG_TOKEN` and
    `VITE_HAOO_POSTHOG_API_HOST` to the Build step's `env` block, sourced from repository
    variables alongside the existing form-endpoint variable. They are variables, not secrets:
    the project key is public write-only by design and is inlined into a world-readable
    bundle, so storing it as a secret would imply a confidentiality property the artifact
    cannot have. Record that reasoning in a comment beside them, in the voice the file already
    uses.

    Do not add `POSTHOG_QUERY_API_KEY` or `POSTHOG_PROJECT_ID` anywhere in the workflow. They
    are local report-process inputs; the bundle scan already forbids their shapes in the
    built artifact, but a workflow that exported one under a browser prefix would put it in
    the bundle before that scan ever ran.

    Add the missing layer as a test in `src/test/build-output.test.ts`: read
    `.github/workflows/deploy.yml` as text, assert it is non-empty (the guard this file
    already applies everywhere a missing subject would make a prohibition vacuous), assert
    each of the three public variable names is present exactly once in the Build step's
    environment, and assert that neither report-credential name appears anywhere in the file
    under a browser prefix. Derive the forbidden names from the existing credential group
    rather than restating them, so widening that group without widening this gate fails here.

    Confirm — by running, not by reasoning — that the whole gate suite stays green when `dist`
    is a provider-selected build, since that is the state CI now produces:
    build with the three variables set to representative values, then run the unit suite
    against that `dist`. If a successor invariant from 04.1-09 fails, that is a finding about
    04.1-09's narrowing and must be reported and fixed, not accommodated by relaxing the
    assertion.

    Finally, in `04.1-UAT.md`, change each of the seven `blocked_by: release-build`
    checkpoints from `blocked` to the not-yet-performed state that file uses for an open item,
    and record in each `reason` that the release build now delivers. Call out checkpoint 10
    explicitly: it is the one that also confirms PostHog Cloud US accepts a payload reduced to
    three transport properties, and a silent ingestion rejection would read as a dead funnel
    rather than a broken one. Do not mark any of them passed — performing them is the owner's,
    after the deploy lands.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run verify:coverage &amp;&amp; npm run build &amp;&amp; npx vitest run 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'VITE_HAOO_MEASUREMENT_PROVIDER\|VITE_HAOO_POSTHOG_TOKEN\|VITE_HAOO_POSTHOG_API_HOST' .github/workflows/deploy.yml` returns 3.
    - `grep -c 'VITE_HAOO_POSTHOG_QUERY\|VITE_POSTHOG\|VITE_HAOO_POSTHOG_PROJECT' .github/workflows/deploy.yml` returns 0, and the new workflow test asserts the same thing derived from the credential group rather than restated.
    - `npx vitest run build-output` passes against a `dist` built with the three variables set, and again against a `dist` built with none set — both states are exercised and recorded in the SUMMARY.
    - `npm run typecheck`, `npm run lint`, `npm run verify:coverage`, `npm run build` all exit 0 and `npx vitest run` reports 0 failures.
    - `grep -c 'blocked_by: release-build' .planning/phases/04.1-*/04.1-UAT.md` returns 0.
    - The SUMMARY names all seven previously-blocked checkpoints by number and title, and states which one doubles as the ingestion-acceptance check.
  </acceptance_criteria>
  <verify-human>
    After the deploy lands, perform each of the ten visitor actions once on the deployed page
    and open the PostHog live event view: exactly one event per action, the allowlisted bare
    name, no `$pageview` / `$pageleave` / `$autocapture` / `$rageclick` / `$web_vitals` /
    `$exception` alongside it, and no person profile created for the visit. Then run the owner
    report against the approved project and reconcile the 7/30/90/all-time counts and dates
    against the raw event view. Zero events arriving is a FAILURE of this gate, not a quiet
    pass — it is the ingestion-rejection case D-05 exists to make visible.
  </verify-human>
  <done>The deployed build captures, no report credential can reach it, the successor invariants are confirmed against a provider-selected `dist`, and the seven owner checkpoints are performable and named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions Build environment → published bundle | anything exported there is inlined into a world-readable artifact if it carries the browser prefix |
| repository variables → deployed page | the values that select the provider and address the ingestion endpoint arrive from outside version control |
| owner approval → visitor disclosure | the rendered processor sentence becomes an operative claim about live data flow rather than about intent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-25 | Information Disclosure | `.github/workflows/deploy.yml` Build environment | high | mitigate | Only the three public `VITE_*` names are added, with the variables-not-secrets reasoning recorded in place. A new build-output case reads the workflow source and asserts no report-credential name appears under a browser prefix, deriving the forbidden names from `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN` so widening that group without widening this gate fails. The existing bundle scan remains as the second layer. |
| T-04.1-06 | Information Disclosure | report credential handling | high | mitigate | Unchanged and now checked one layer earlier: the key still travels only in an `Authorization` header from a local process, and the workflow gate stops it entering the build environment at all. |
| T-04.1-04 | Information Disclosure | PostHog server-side geo-IP enrichment | high | mitigate | Owner answered `enabled` on 2026-09-04; the setting is re-confirmable only in the provider UI and is recorded as such. Live confirmation is one of the seven checkpoints this plan unblocks. |
| T-04.1-13 | Repudiation | COVERAGE.md operational boundary drifting from the code | medium | mitigate | Every amended sentence has its pinned regex amended in the same commit; three mutation probes prove each new pin fails when its sentence is blanked. The verifier runs as a workflow gate, so drift cannot pass unnoticed. |
| T-04.1-20 | Repudiation | requirement boxes closed on deployment alone | medium | mitigate | MEAS-01 and MEAS-08 stay unchecked and `Gaps Found`; the appended status note records that deployment is necessary and not sufficient, and the seven checkpoints stay unperformed rather than being marked passed. |
| T-04.1-09 | Tampering | successor invariants under a provider-selected `dist` | medium | mitigate | Both states are built and the suite run against each; a failure is treated as a finding about 04.1-09's narrowing rather than accommodated by relaxing an assertion. |
| T-04.1-11 | Spoofing | project-id echo in the report | medium | accept | Unchanged — PostHog does not echo the project a query ran against; the residual gap remains stated in the report's own caveat block. |
</threat_model>

<verification>
- Task 1's approval is recorded with the owner's verbatim answer before any other file changes.
- `npm run verify:coverage` exits 0, with the `boundaryChecks` array no smaller than before.
- Three probes confirm each newly-pinned boundary sentence fails the audit when blanked.
- `npm run typecheck`, `npm run lint`, `npm run build` clean; `npx vitest run` 0 failures.
- `npx vitest run build-output` passes against both a provider-selected and a provider-unset `dist`.
- `grep -c 'blocked_by: release-build'` over `04.1-UAT.md` returns 0.
- MEAS-01 and MEAS-08 remain unchecked in `.planning/REQUIREMENTS.md`.
</verification>

<success_criteria>
Delivery is enabled behind a recorded owner approval taken first, the deployment build
supplies exactly the three public variables and no credential, and every sentence the
coverage verifier pins over COVERAGE.md's operational boundary was amended in the same commit
as the pin. The seven `blocked_by: release-build` UAT checkpoints are performable and named,
with the person-profile check identified as the one that also confirms ingestion accepts the
three-property reduced payload. No requirement box closes on deployment alone.
</success_criteria>

## Artifacts this phase produces

Created or changed by this plan:

| Symbol | File | What it is |
|---|---|---|
| `VITE_HAOO_MEASUREMENT_PROVIDER` / `VITE_HAOO_POSTHOG_TOKEN` / `VITE_HAOO_POSTHOG_API_HOST` in the Build step | `.github/workflows/deploy.yml` | The three public repository variables that make the deployed build capture |
| workflow credential-boundary case | `src/test/build-output.test.ts` | Asserts no report-credential name reaches the Build environment under a browser prefix, derived from the existing credential group |
| enabled-state boundary checks | `scripts/verify-phase4-coverage.mjs` | Successors to the OPT-OUT, deferred-approval and selector-unset pinned assertions |
| amended `## Operational boundary` | `COVERAGE.md` | States the enabled reality and what remains owner-observed |
| cleared processor-approval gate | `04.1-USER-SETUP.md` | The owner's verbatim approval as the box's sole evidence; header stays `Status: Incomplete` |
| dated enablement status note | `.planning/REQUIREMENTS.md` | Records deployment without closing MEAS-01 or MEAS-08 |
| seven unblocked checkpoints | `04.1-UAT.md` | Previously `blocked_by: release-build`, now performable and named |

<output>
Create `.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-11-SUMMARY.md` when done.
</output>
