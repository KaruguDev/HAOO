---
phase: 04.1-migrate-measurement-from-plausible-to-posthog
plan: 09
type: execute
wave: 1
# No dependency on another gap-closure plan. This is the plan that creates the link
# everything else in this round rests on: until a production module loads the SDK there is
# no client to bind to, so 04.1-10's "refuse any occupied slot outright" has nothing to
# fall back on and 04.1-11 would enable delivery of nothing. Every executed plan
# (04.1-01..04.1-08) is already merged, so those are history rather than edges.
depends_on: []
files_modified:
  - src/measurement/posthog.ts
  - vite.config.ts
  - src/test/build-output.test.ts
  - src/test/measurement.test.ts
  - README.md
  - scripts/verify-phase4-coverage.mjs
  - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md
  - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/deferred-items.md
autonomous: false
requirements: [MEAS-01, MEAS-02, MEAS-06, MEAS-08]
gap_closure: true
gap_ids: [G-04.1-1]

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A production module imports `posthog-js` in a value position, so a provider-selected build emits the vendor chunk and the adapter resolves a real client instead of refusing at an empty ambient slot (G-04.1-1, D-02)."
    - "EVERY build-output invariant the vendor chunk contradicts is resolved in the same commit as the import — renarrowed with a NAMED successor in the shape 04.1-01 established, or its whole-bundle scope RE-JUSTIFIED by a recorded measurement against the emitted vendor chunk. Never widened, never left to fire with no sanctioned path (G-04.1-1)."
    - "The competitor-origin and report-credential bundle scan is measured against the emitted vendor chunk rather than assumed, so the doc-comment claim that `Authorization` and `Bearer ` are absent from the published SDK artifact is either re-established by this commit's own measurement or replaced by a named narrowing (G-04.1-1, MEAS-08)."
    - "A provider-unset probe build still carries no approved ingestion origin in this project's own chunks, so an unselected build cannot address the endpoint (D-08)."
    - "README.md and COVERAGE.md state the loaded-SDK reality, and the coverage verifier's pinned boundary assertion moves with the prose in the same commit — prose and gate never disagree."
    - "The vendor chunk is isolated under a name the suite asserts, so the exclusion that lets the identity scan stay truthful cannot hide this project's own code."
  prohibitions:
    - statement: "MUST NOT widen, delete, or silence either affected build-output invariant without naming its successor in the same commit — a renarrowing is a recorded decision, a deletion is a regression."
      category: transparency
      requirement_id: MEAS-02
    - statement: "MUST NOT let any test initialize the real imported SDK — no `init` call on the vendor module may occur under the test runner, because that would put the suite on the network."
      category: isolation
      requirement_id: MEAS-07
    - statement: "MUST NOT set `VITE_HAOO_MEASUREMENT_PROVIDER` anywhere outside a throwaway probe build directory — enabling delivery is 04.1-11 and is gated on an owner approval this plan does not hold (D-06)."
      category: transparency
      requirement_id: MEAS-01
  artifacts:
    - path: "src/measurement/posthog.ts"
      provides: "The value import of posthog-js and the named binding the adapter falls back to"
      contains: "boundPostHogClient"
    - path: "vite.config.ts"
      provides: "The named vendor chunk that makes the bundle scans partitionable"
      contains: "posthog-sdk"
    - path: "src/test/build-output.test.ts"
      provides: "The successor invariants, the amended exactly-once case, the fourth subject's measured or narrowed scope, and the vendor-chunk identity assertion"
      contains: "projectBundleText"
    - path: "scripts/verify-phase4-coverage.mjs"
      provides: "The pinned operational-boundary assertion for the loaded SDK"
      contains: "delivery depends on the provider selector"
  key_links:
    - from: "src/measurement/posthog.ts"
      to: "posthog-js"
      via: "a value import bound by `boundPostHogClient`, used by `resolveClient` when the ambient slot is empty — the single missing link 04.1-VERIFICATION.md recorded as NOT WIRED"
      pattern: "from 'posthog-js'"
    - from: "vite.config.ts"
      to: "src/test/build-output.test.ts"
      via: "the `posthog-sdk` manual chunk name is the partition key both the vendor-identity assertion and the project-chunk scans read"
      pattern: "posthog-sdk"
    - from: ".planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md"
      to: "scripts/verify-phase4-coverage.mjs"
      via: "the operational-boundary sentence and the regex that pins it are edited in one commit, so the audit enforces the statement the document actually makes"
      pattern: "delivery depends on the provider selector"
---

<objective>
Close the single missing link: make the pinned SDK actually load, and pay the price of that
honestly rather than by widening a proven assertion.

`04.1-VERIFICATION.md` measured it rather than inferring it — a probe build with the
provider selected, a valid `phc_` token and the approved api host produced a 240 kB bundle
with zero vendor code, and the repository's own suite asserts the consequence at
`src/test/measurement.test.ts:959`. Every privacy invariant this phase proves is, today, a
statement about unreachable code. This is deferred item D4, which named 04.1-08 as its
closer; 04.1-08 neither made the decision nor carried it forward.

The change is not free and the plan says so up front. A value import emits the vendor chunk
into `dist/assets`, and a minified SDK legitimately carries browser-storage tokens and the
vendor's own default host string. Currently-green build-output invariants therefore stop
being truthful as written. Task 2 step 3 names FOUR affected cases and gives each a
sanctioned resolution — three renarrowed with a named successor or a recorded amendment,
and a fourth resolved by MEASUREMENT against the emitted vendor chunk with both outcomes
written out in advance. Nothing is left to fire against a prohibition the executor is
forbidden to widen and has no instruction to narrow; that stall is the defect this round
exists to close. The shape throughout is the one 04.1-01 established when it withdrew the
delivery-mechanism guarantee — recorded withdrawal, never weakening by silence. Clause B of
Success Criterion 1 (D-01, full replacement) is already met in substance; this plan closes
clause A.

Purpose: the measurement stack this phase protects can measure.
Output: a loaded SDK, four resolved build-output invariants each carrying its own recorded
reason, a partitioned bundle scan whose exclusion is itself asserted, and documentation that
matches the code — with the coverage verifier's pinned sentence moving in the same commit as
the prose it pins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-CONTEXT.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-VERIFICATION.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/deferred-items.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-01-SUMMARY.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide the SDK loading strategy — this is D4, and it is one-way</name>
  <read_first>
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/deferred-items.md (section `## D4` — the two options it records, and the table of which invariants fire and why)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-VERIFICATION.md (gap 1's `missing:` list — both options are sanctioned there)
    - src/measurement/index.ts lines 303-337 (`initialize()` — the sink is assigned synchronously inside the product page's mount effect, and `track()` calls `eventSink?.(event)` on the same tick)
    - src/test/build-output.test.ts lines 220-260 (the `PROVIDER_INGESTION_HOST_SOURCE_FORBIDDEN` doc comment, which already records a narrowing on the stated premise that this project imports the SDK)
  </read_first>
  <decision>
    Which loading strategy closes D4?

    **Option A — static value import from `src/measurement/posthog.ts` (RECOMMENDED).**
    The vendor chunk ships in every build, including a provider-unset one. Cost: FOUR
    build-output cases are affected and each must be resolved in the same commit as the
    import — two renarrowed with named successors, one amended with its narrowing recorded,
    and one (the competitor-origin and report-credential bundle scan) resolved by measuring
    every pattern against the emitted vendor chunk and then either re-justifying its
    whole-bundle scope or narrowing exactly the patterns that fire. The bundle scans must
    also be partitioned so the identity scan stays a claim about this project rather than
    about the vendor's minified artifact.

    **Option B — provider-gated dynamic import.** The vendor chunk is emitted only for a
    selected build, so a provider-unset bundle stays byte-for-byte as clean as it is today.
    Cost: `createPostHogEventSink` and `initialize()` are synchronous, and the first
    `haoo_page_view` fires on the same tick as `initialize()`. A dynamic import resolves in
    a later microtask, so that event is dropped unless an emission queue exists — and
    `eventQueue`, `eventLog`, `emittedEvents`, `retryTimer`, `setTimeout` and `setInterval`
    are forbidden by name in `src/measurement/index.ts` by the `pins the local record and
    bare tracking call` case, while `eventQueue` and `clickstream` are forbidden across all
    production source by `MEASUREMENT_IDENTITY_SOURCE_FORBIDDEN`. Option B therefore costs
    an async measurement facade PLUS the widening of two privacy prohibitions, against
    Option A's two bundle-scope renarrowings.

    Recommendation: **Option A.** Beyond the cost comparison, the phase's own recorded
    reasoning already assumes it — the `PROVIDER_INGESTION_HOST_SOURCE_FORBIDDEN` doc
    comment written by 04.1-01 states "This project now imports the SDK instead, so the
    vendor's own default host string necessarily ships inside the vendor chunk."

    Why this is one-way: the choice fixes whether the event sink seam is synchronous. It is
    read by `resolveMeasurementProvider`, the `eventSink` seam, and 662 tests (D-02 already
    rates relocating that boundary `costly`). Reversing to Option B later means turning the
    facade async and re-opening the queue prohibitions this project deliberately closed.
  </decision>
  <context>
    D4 explicitly assigned this decision to plan 04.1-08, which neither made it nor carried it
    forward. Nothing downstream can be built until it is settled: 04.1-10 needs a legitimate
    client to bind to before it can refuse an occupied slot, and 04.1-11 would otherwise
    enable delivery of an SDK that never loads. It is also the decision that determines
    whether the event-sink seam stays synchronous, which is why it is gated rather than taken
    inside a diff.
  </context>
  <options>
    <option id="option-a">
      <name>Static value import from `src/measurement/posthog.ts` (recommended)</name>
      <pros>Keeps the synchronous sink seam and the 662-test surface intact; delivers the first `haoo_page_view` on the same tick as `initialize()`; matches the premise 04.1-01 already recorded in the `PROVIDER_INGESTION_HOST_SOURCE_FORBIDDEN` doc comment.</pros>
      <cons>The vendor chunk ships in every build including provider-unset; four build-output cases are affected and all four must be resolved in the same commit — two renarrowed with named successors, one amended with the narrowing recorded, and one resolved by per-pattern measurement against the emitted vendor chunk — and the bundle scans must be partitioned.</cons>
    </option>
    <option id="option-b">
      <name>Provider-gated dynamic import</name>
      <pros>A provider-unset bundle stays byte-for-byte as clean as it is today — no vendor code at all in a build that has not selected the provider.</pros>
      <cons>Forces an async measurement facade and drops the first page-view event unless an emission queue exists; `eventQueue`, `eventLog`, `emittedEvents`, `retryTimer`, `setTimeout` and `setInterval` are all forbidden by name in `src/measurement/index.ts`, so it costs two widened privacy prohibitions on top of the async rewrite. Tasks 2 and 3 are written for Option A and would need replanning.</cons>
    </option>
  </options>
  <resume-signal>Type "option-a" to proceed with Task 2 as written, or "option-b" to stop and request replanning.</resume-signal>
  <acceptance_criteria>
    - A decision is recorded verbatim in the plan SUMMARY under a heading naming D4, together with the option chosen.
    - If Option B is chosen, no source file is modified by this plan and the run stops with the replanning request recorded.
  </acceptance_criteria>
  <done>The loading strategy D4 deferred is a recorded decision with its one-way cost stated, rather than a choice an executor makes silently inside a diff.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Load the SDK for real, and renarrow every invariant it contradicts in the same commit</name>
  <files>src/measurement/posthog.ts, vite.config.ts, src/test/build-output.test.ts, src/test/measurement.test.ts, README.md, scripts/verify-phase4-coverage.mjs, .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md</files>
  <reversibility rating="one-way">Binds the event-sink seam to a synchronous client and puts vendor code in every build; reversing means an async facade and re-opened queue prohibitions.</reversibility>
  <read_first>
    - src/measurement/posthog.ts (the whole file — `PostHogScope`, `PostHogClient`, `POSTHOG_REFUSAL`, `resolveClient` at 148-170, and the ordered-gates doc comment at 172-195 that IS the privacy contract)
    - src/measurement/posthog-lockdown.ts line 1 (the existing `import type` — the specifier that must gain a value sibling) and lines 194-292 (`PostHogLockdownExpectation` and `lockdownHolds`, already pinning `before_send` by identity since commit 33deb5a — do not re-do that work)
    - src/test/build-output.test.ts lines 195-330 (`UNCONDITIONAL_ANALYTICS_ORIGINS_FORBIDDEN`, `PROVIDER_INGESTION_HOST_SOURCE_FORBIDDEN`, `MEASUREMENT_IDENTITY_SOURCE_FORBIDDEN`, `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN`, `builtBundleText` and its loud-failure guard — the guard is the pattern the new partition helpers must copy)
    - src/test/build-output.test.ts lines 253-262 (the `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN` doc comment — it asserts `Authorization` and `Bearer ` "were verified absent from the published SDK artifact", a verification taken BEFORE the vendor chunk shipped in any build; step 3's fourth subject is the one that re-establishes or withdraws it)
    - src/test/build-output.test.ts lines 738-756 (`ships the unset provider bundle without competitor analytics, property, or credential seams` — the FOURTH affected case: it scans `builtBundleText()`, the whole bundle, for the competitor origins, the four credential shapes and the event-with-property-bag pattern. `posthog-js` carries a Segment integration and issues authorized XHRs against `/api/…`, so from this commit onward every one of those patterns is being asserted over a vendor's minified artifact unless measured otherwise)
    - src/test/build-output.test.ts lines 800-816 (`keeps the production bundle free of identity and ordered-emission channels`), lines 955-1006 (the provider-selected probe build and its exactly-once origin count), lines 1008-1057 (the README delivery-claim gate), lines 1060-1070 (`ships the provider-unset bundle with no approved ingestion origin at all`)
    - src/test/measurement.test.ts lines 1050-1070 and 1155-1195 and 1335-1350 (every case that expects `POSTHOG_REFUSAL.absentClient` for an empty scope — those are the cases whose meaning this task changes)
    - vite.config.ts (the `define` block and `build.rollupOptions.input` — `manualChunks` is added under `output`, beside `input`, without touching either)
    - README.md lines 90-130 (the delivery-disclaimer section the build-output gate currently requires)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md lines 13-20 and 97-112 (the browser-SDK table heading, the `init` row, and the operational-boundary paragraph)
    - scripts/verify-phase4-coverage.mjs lines 1-10 and 165-232 (the `REQUIRED_TABLES` key for the browser-SDK table, and the `boundaryChecks` array whose pinned sentence must move with the prose)
  </read_first>
  <behavior>
    - With an empty ambient slot and no injected client, the adapter resolves the imported module rather than refusing — the case `src/test/measurement.test.ts:959` currently asserts the opposite of.
    - Importing the module leaves `window.posthog` undefined, so the bound client never occupies the slot the adapter classifies.
    - A bound module that exposes no callable `init` is refused with a named reason and no sink is returned.
    - A provider-unset probe build emits no approved ingestion origin in this project's own chunks.
    - A provider-selected probe build emits the approved origin exactly once in this project's own chunks, and emits a vendor chunk carrying the pinned SDK.
    - The project chunks of a built bundle carry `visitOrdinal` and `lastSeenDay` and none of the identity or ordered-emission patterns; the vendor chunk is excluded from that scan and is separately asserted to be the SDK.
    - Every competitor-origin and report-credential pattern is asserted over a subject the measurement in this commit shows it can hold truthfully — the whole bundle where the vendor chunk is measurably clean of it, this project's own chunks where it is not.
  </behavior>
  <action>
    All of the following land in ONE commit. D4 requires it, and splitting them would ship a
    tree in which a documented guarantee is false — the exact defect this plan closes.

    **1. Load the SDK (D-02).** In `src/measurement/posthog.ts` add a value import of the
    `posthog-js` default export. Expose it through a named exported accessor
    `boundPostHogClient(): unknown` returning that module value, and use it inside
    `resolveClient` as the client for the empty-ambient-slot branch. Keep the `adapters.client`
    test seam ahead of everything, and keep the ambient-slot classification exactly as it is
    for this plan — an occupied slot is still classified before anything is written, and an
    ambient value exposing a callable initializer is still adopted. Removing that adoption is
    04.1-10 and depends on this binding existing first.

    Guard the bound value rather than trusting it: if it does not expose a callable
    initializer, refuse. Mint a new member of `POSTHOG_REFUSAL` for that outcome and record,
    in the doc comment on the retired member, that the empty-slot refusal it named is
    WITHDRAWN because an empty slot is now the normal path, naming the new member as its
    successor and this plan as the withdrawing plan. Do not delete the retired member
    silently and do not repurpose its string value.

    Do not touch the ordered gates in `createPostHogEventSink`, the lockdown object, or
    `lockdownHolds` — D-03's automatic-capture posture and D-04's two independent gates are
    unchanged by this plan, and the readback is now reached against a real vendor merge for
    the first time, which is the point.

    **2. Isolate the vendor chunk (`vite.config.ts`).** Add `build.rollupOptions.output.manualChunks`
    in the object form, seeding a chunk named `posthog-sdk` from the `posthog-js` specifier, so
    modules reachable only from the SDK land there and this project's own code does not. The
    SDK has real runtime dependencies (preact, dompurify, fflate, core-js, web-vitals and the
    `@posthog/*` packages), so after building, MEASURE which chunk each landed in. If any
    vendor module leaks into a project chunk, extend the seed list by naming that package
    explicitly and record why — never by relaxing a prohibition in step 3.

    **3. Resolve all FOUR affected build-output cases in this commit — each by a named
    successor, a recorded amendment, or a recorded measurement. None is left to fire against
    a prohibition you may not widen.**
    In `src/test/build-output.test.ts` add two partition helpers beside `builtBundleText`,
    copying its loud-failure discipline exactly (an empty partition throws with a message
    naming the command that fixes it, so a prohibition can never pass against nothing):
    `vendorChunkFiles()` and `projectChunkFiles()`, partitioning `dist/assets/**/*.js` by the
    `posthog-sdk` chunk name, plus `projectBundleText()` joining the project side. Accept an
    explicit directory argument so the probe builds can reuse them.

    - Replace `keeps the production bundle free of identity and ordered-emission channels`
      with a successor whose name states the narrowed subject — this project's own chunks
      rather than the whole bundle — scanning `projectBundleText()` with the same three
      patterns and the same two positive `visitOrdinal` / `lastSeenDay` assertions. Add a
      doc comment recording what the predecessor proved, what the successor proves, why the
      claim moved (a minified vendor artifact carries browser-storage and identifier tokens
      of its own, so asserting them over the vendor chunk would be a claim about the vendor),
      and that this plan is the one that narrowed it. Add a companion case asserting the
      exclusion has teeth: the vendor partition is non-empty and contains a marker unique to
      the pinned SDK, and the project partition is non-empty. Without that companion the
      partition could silently swallow the whole bundle and the scan would pass on nothing.

    - Replace `ships the provider-unset bundle with no approved ingestion origin at all` with
      a successor that builds its OWN provider-unset probe into a throwaway directory rather
      than asserting over the repository's `dist`. This is the second half of the narrowing
      and it is not optional: 04.1-11 adds the provider variables to the deployment build, so
      `dist` stops being a provider-unset artifact and a case that assumes otherwise would
      start passing or failing for reasons unrelated to what it means. Mirror the existing
      provider-selected probe exactly — same `npx`/`npx.cmd` platform handling, same blanked
      provider environment, same `finally` cleanup, same timeout — and assert zero
      occurrences of every approved origin and hostname in that probe's PROJECT chunks.
      Record the predecessor by name in the doc comment.

    - Amend `publishes the approved ingestion origin exactly once in a provider-selected build`
      so the exactly-once count runs over the probe's project chunks, and add an explicitly
      acknowledged assertion that the vendor chunk carries the vendor's own default host —
      stated as a claim about the vendor's published artifact, not about this repository.
      Moving the count's subject from the whole probe bundle to the project chunks IS a
      narrowing under this repository's discipline, so it carries the same recorded note as
      the two replacements above rather than passing as a mechanical edit: a doc comment
      stating what the predecessor counted (every emitted chunk), what the successor counts
      (this project's own chunks), why the subject moved (the vendor's minified artifact
      carries the vendor's own default host string, so a whole-bundle count of the approved
      origin would be counting the vendor), and naming plan 04.1-09 as the amending plan.

    - **FOURTH SUBJECT — `ships the unset provider bundle without competitor analytics,
      property, or credential seams` (`src/test/build-output.test.ts:738-756`).** This case
      scans `builtBundleText()` — the whole bundle of the repository's `dist` — for the
      competitor origins in `UNCONDITIONAL_ANALYTICS_ORIGINS_FORBIDDEN`, the four shapes in
      `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN`, and the event-with-property-bag pattern. Under
      the chosen strategy the vendor chunk ships in EVERY build including a provider-unset
      one, so from this commit onward each of those patterns is a claim about a vendor's
      minified artifact unless this commit shows otherwise — the same reasoning 04.1-01 used
      to relocate `MEASUREMENT_IDENTITY_SOURCE_FORBIDDEN` and withdraw the bundle-level
      origin claim. The SDK carries a Segment integration and issues authorized XHRs against
      `/api/…`, and the doc comment at lines 253-262 asserts that two of the credential
      shapes "were verified absent from the published SDK artifact" — a verification taken
      before the chunk shipped in any build, which nothing has re-run.

      MEASURE FIRST, then take the branch the measurement selects. Both branches are written
      out here, so a pattern that fires has a sanctioned path and this task never stalls.

      Measurement: after the step 2 build, run each pattern this case applies against the
      VENDOR CHUNK TEXT ALONE (`dist/assets/posthog-sdk*.js`, read through
      `vendorChunkFiles()`). Record the result in the SUMMARY as one row per pattern —
      pattern source, hit or no-hit, and for a hit the matching chunk and a truncated
      excerpt. Derive the pattern list from the two exported constants rather than restating
      the literals, so a later widening of either group is measured too.

      - **Branch NO-HIT** (every pattern clean over the vendor chunk): the case KEEPS
        `builtBundleText()` as its subject. Its scope is not assumed — it is re-justified by
        this commit's own measurement, and the doc comment says so: name plan 04.1-09, state
        that the vendor chunk now ships in every build, record the measured result and the
        pinned SDK version it was taken against, and state that it must be re-measured at any
        version bump. Replace the stale sentence in the `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN`
        doc comment ("verified absent from the published SDK artifact") with what this commit
        actually measured, since the claim it makes now has a shipping artifact behind it for
        the first time. Add a companion assertion that pins the measurement rather than
        leaving it as prose: the vendor partition is non-empty and matches none of the
        credential shapes, so a version bump that introduces one goes red here.

      - **Branch HIT** (any pattern matches the vendor chunk): narrow THAT PATTERN AND ONLY
        THAT PATTERN to `projectBundleText()`, leaving every non-hitting pattern on the whole
        bundle. Never widen a pattern, never delete one, never drop the case. The doc comment
        names the predecessor case, the exact pattern whose subject moved, the measured
        evidence that forced the move, what the successor still proves, and plan 04.1-09. If
        the pattern that moved is a credential shape, record in the same comment that
        04.1-11 Task 3's `deploy.yml` credential-source gate is the compensating layer for
        the withdrawn half: it asserts one layer earlier — in the workflow's Build
        environment, where a bundle scan cannot see — that no report-credential name reaches
        the build at all.

      Either branch is a recorded decision naming this plan. Neither is a silent widening,
      and neither leaves a green suite depending on an assertion nobody re-checked.

    **4. Make the documentation true, and move the gate with it.**
    - `README.md`: replace the delivery-disclaimer section with a positive successor headed
      `### The SDK is loaded — delivery depends on the provider selector`, stating that
      `src/measurement/posthog.ts` imports the SDK in a value position, that the vendor chunk
      ships in every build, and carrying the sentence
      `**Events are delivered only when `VITE_HAOO_MEASUREMENT_PROVIDER` is set to `posthog`.**`
      verbatim. State that the provider is still unset in deployment and name 04.1-11 as the
      plan that enables it, so a reader of the shipped documentation can discover the state
      without reading `.planning/`.
    - `src/test/build-output.test.ts`: strengthen the README delivery-claim gate's
      loaders-present branch so it also requires that positive heading and that sentence,
      and requires the section to name the loading module path. The gate stays bidirectional:
      today it fails if the disclaimer is missing, and after this commit it fails if the
      disclaimer survives OR the successor statement is absent.
    - `COVERAGE.md`: change the browser-SDK table heading so it no longer says the SDK is
      unloaded; correct the `init` row's REASON so it does not claim the capability is
      unreached at runtime, leaving that row's CAPABILITY cell byte-identical (keep the
      reason cell within 200 characters and the capability cell within 80, the API-coverage
      gate's limits); and replace the operational-boundary
      paragraph about the unloaded SDK with one stating that the SDK is loaded by
      `src/measurement/posthog.ts` and that delivery depends on the provider selector. Leave
      every other boundary paragraph alone — production analytics enablement is still
      OPT-OUT and the selector is still unset, and 04.1-11 owns those sentences.
    - `scripts/verify-phase4-coverage.mjs`: exactly one key in `REQUIRED_TABLES` moves — the
      TABLE HEADING key at lines 5-6, `'PostHog — browser SDK (posthog-js, pinned; not yet
      loaded)'`, which must match the amended COVERAGE.md heading. Do NOT touch the `init`
      row's key. The verifier keys its rows on CAPABILITY text, not on reason text, and the
      capability cell `` `posthog.init(token, config)` with an explicit lockdown object ``
      is unchanged by this plan — only that row's reason changes, in COVERAGE.md, and the
      verifier never reads it. Leave every other row key in that Map byte-identical.
      Then replace the boundary check that pins the unloaded-SDK sentence with one pinning
      the successor sentence. Follow the existing convention in that array: pin the CLAIM
      across the sentence, not a bare fragment that a sentence saying the opposite would
      also satisfy.

    **5. Restate the tests whose meaning this changes.** In `src/test/measurement.test.ts`,
    every case that drove an empty scope with no injected client to assert the empty-slot
    refusal now describes a path that resolves the bound module. Restate each one rather than
    deleting it: inject `adapters.client` where the case is about downstream behaviour, and
    add one case per behaviour in the `<behavior>` block above for the binding itself. The
    binding cases assert `boundPostHogClient()` by identity against a direct
    `import posthog from 'posthog-js'` in the test file, assert it exposes a callable
    initializer, and assert that importing it leaves the ambient slot undefined. No test may
    call the real module's initializer — that would put the suite on the network.

    **6. Close D4.** In `deferred-items.md`, append a `RESOLVED` note to the D4 section
    recording the option chosen in Task 1, this plan as the closer, and the names of all four
    resolved build-output cases with the resolution each received.

    **Overflow, pre-authorized — do not compress.** This task is large on purpose: the
    paired restatement must land in ONE commit, and that constraint is not negotiable. It
    does not, however, license shedding work to fit. If step 2's measurement turns up vendor
    modules leaking into project chunks, or step 3's fourth subject takes the HIT branch and
    needs further narrowing, the extra work belongs in THIS commit — extend the seed list,
    write the extra doc comment, and carry on.

    What IS pre-authorized to move out, with no further approval needed: Task 3's probe
    programme. If this task's commit has consumed the working budget, stop after committing
    it, write the SUMMARY, and record a request for a follow-on plan `04.1-12` carrying
    Task 3's probe table in full — naming every probe subject it was to cover. That is a
    deferral of VERIFICATION of work already landed, not a reduction of the work.

    What is NOT authorized under any circumstance: splitting step 1, 3 or 4 across commits;
    landing the import without all four cases resolved; narrowing a pattern without the
    measurement that justifies it; or shortening a doc comment to save tokens. If you project
    that you cannot finish this task in one commit, STOP BEFORE COMMITTING ANYTHING and report
    that — a partial paired restatement is the exact defect this plan exists to prevent, and a
    clean stop costs a replan while a partial commit ships a false guarantee.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run verify:coverage &amp;&amp; npm run build &amp;&amp; npx vitest run 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "from 'posthog-js'" -- src` reports at least one line that is not an `import type` line.
    - `npm run build` succeeds and `ls dist/assets | grep -c '^posthog-sdk'` is at least 1.
    - `npm run typecheck`, `npm run lint`, `npm run verify:coverage` and `npm run build` all exit 0.
    - `npx vitest run` reports 0 failures, and the total count is at least the 662 recorded in `04.1-REVIEW-FIX.md`.
    - `npx vitest run build-output -t "README delivery claim"` passes with the SDK loaded — proving the tripwire at the former `src/test/build-output.test.ts:1010-1023` was rewritten rather than deleted.
    - `git show --stat HEAD` for this task's commit lists `src/measurement/posthog.ts`, `src/test/build-output.test.ts`, `README.md`, `scripts/verify-phase4-coverage.mjs` and `COVERAGE.md` together — the paired restatement and the documentation correction are in the same commit as the import.
    - `grep -c 'posthog-sdk' vite.config.ts` is at least 1 and `grep -c 'projectBundleText' src/test/build-output.test.ts` is at least 2.
    - Each of the four affected cases carries a doc comment naming plan 04.1-09 and its own resolution — the two replacements name their predecessor case, the amended exactly-once case names what its count's subject was and is, and the fourth subject records either the measurement that re-justified its whole-bundle scope or the pattern whose subject moved and the measured evidence that moved it. `grep -c '04.1-09' src/test/build-output.test.ts` is at least 4, and the count holds on either branch of the fourth subject.
    - The fourth subject's measurement is recorded in the SUMMARY as one row per pattern, derived from `UNCONDITIONAL_ANALYTICS_ORIGINS_FORBIDDEN` and `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN` rather than restated, each row naming the pattern, hit or no-hit, and for a hit the chunk and a truncated excerpt.
    - On the NO-HIT branch, the stale sentence in the `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN` doc comment claiming the shapes were verified absent from the published SDK artifact is replaced by this commit's own measurement, and a companion assertion pins it so a version bump that introduces one of those shapes goes red. On the HIT branch, exactly the hitting pattern(s) moved to `projectBundleText()`, every non-hitting pattern still runs over the whole bundle, no pattern was deleted or widened, and a moved credential shape's comment names 04.1-11 Task 3's `deploy.yml` gate as the compensating layer.
    - No test invokes the real module's initializer: the suite completes with no outbound request, and the provider-selected probe build directories are removed by their `finally` blocks (`ls -d dist-*probe* 2>/dev/null` returns nothing after the run).
  </acceptance_criteria>
  <done>A production module loads the SDK; a provider-selected build carries the vendor chunk and a real client; both affected invariants have named successors asserted over a partition whose exclusion is itself asserted; and README, COVERAGE.md and the coverage verifier state the same thing the code does.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the new gates have teeth, by mutation rather than by assertion</name>
  <files>.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-09-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-REVIEW-FIX.md (the per-finding probe records — the format and the standard of evidence this repository expects)
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-VERIFICATION.md (the `Probe Execution` table — the verifier mutation-probed two central assertions and both went red; anything added here is held to the same bar)
    - src/test/build-output.test.ts (the cases written in Task 2 — each one is a probe subject)
  </read_first>
  <action>
    A renarrowed invariant that cannot fail is worse than the one it replaced, because it
    reads as preserved rigour. Probe each new or amended gate by neutering it locally,
    re-running, confirming red, then restoring with `git checkout --`.

    Probe subjects, one row each:
    - The project-chunk identity scan: make the partition return every file including the
      vendor chunk, and confirm the identity patterns fire.
    - The partition-has-teeth companion: make the partition return nothing on the project
      side, and confirm the non-empty guard fires rather than the scan passing on nothing.
    - The provider-unset probe: point it at a provider-selected build and confirm the
      approved origin is found.
    - The exactly-once count in the provider-selected probe: count over all chunks instead
      of the project chunks and confirm it reports more than one.
    - The README delivery gate: remove the positive successor sentence and confirm the case
      fails naming what must be restored; then restore it, remove the value import instead,
      and confirm the case fails demanding the disclaimer back.
    - The coverage verifier's new boundary check: blank the successor sentence in a scratch
      copy of COVERAGE.md and confirm `node scripts/verify-phase4-coverage.mjs <scratch>`
      exits 1 naming the missing statement. Pass the scratch path explicitly — the script
      takes the COVERAGE.md path as `process.argv[2]` and exits 1 with a Usage error when it
      is omitted, so an argument-less invocation goes red for the wrong reason and would be
      recorded as a passing probe that proves nothing.
    - The fourth subject (`ships the unset provider bundle without competitor analytics,
      property, or credential seams`, whichever branch step 3 took): append a line carrying
      one literal drawn from `REPORT_CREDENTIAL_BUNDLE_FORBIDDEN` to a PROJECT chunk in
      `dist/assets`, confirm the case goes red naming the pattern, and restore by rebuilding.
      The project chunks are scanned on both branches, so this probe is branch-independent —
      it proves the case still has teeth over this repository's own code whether its subject
      stayed the whole bundle or narrowed. On the NO-HIT branch, probe the companion
      assertion too: append the same literal to the VENDOR chunk and confirm the
      vendor-partition credential check goes red rather than passing silently.

    Record every probe in the plan SUMMARY as a table of subject, mutation, observed result,
    and restore confirmation. A probe that does NOT go red is a finding: report it and fix
    the assertion rather than recording the probe as inconclusive.
  </action>
  <verify>
    <automated>test "$(grep -c '| *red\|went red\|FAIL' .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-09-SUMMARY.md)" -ge 7 &amp;&amp; git diff --quiet -- src scripts README.md</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a probe table with at least seven rows, each naming the subject, the mutation applied, and the observed failure.
    - Every mutation was restored: `git status --porcelain -- src scripts README.md` is empty after the probes. The check is scoped to the mutation surface deliberately — the SUMMARY this task is writing is legitimately uncommitted at that moment, so an unscoped `git status --porcelain` would report dirt that proves nothing about probe hygiene. `dist` is gitignored, so the rebuild that restores the chunk mutation needs no separate check beyond the suite passing.
    - `npx vitest run` passes on the restored tree with no failures.
    - Any probe that did not go red is recorded as a finding with the assertion strengthened, not recorded as passing.
  </acceptance_criteria>
  <done>Every gate this plan adds or narrows is proven falsifiable by measurement, so the renarrowing is recorded rigour rather than a claim about itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled vendor SDK → visitor browser | third-party code now executes in the page for the first time in this project's history |
| merged config → `lockdownHolds` | the vendor's real merge result reaches the readback for the first time, rather than a fixture's |
| `dist/assets` partition → bundle prohibitions | an exclusion rule now stands between this project's assertions and the artifact they scan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-SC | Tampering | `posthog-js@1.425.1` executing in the visitor's browser | high | mitigate | Pin and lockfile unchanged — no install occurs in this plan, so the cleared legitimacy checkpoint still covers the exact artifact. `disable_external_dependency_loading: true` and `advanced_disable_flags: true` keep the loaded SDK from fetching further vendor code or reading remote configuration. Re-confirmation is required at any version bump. |
| T-04.1-24 | Tampering | the `posthog-sdk` chunk partition in `src/test/build-output.test.ts` | high | mitigate | The exclusion is asserted in both directions: the vendor partition must be non-empty and carry a marker unique to the pinned SDK, the project partition must be non-empty and carry `visitOrdinal` and `lastSeenDay`. A partition that swallowed project code fails rather than passing on nothing — the same loud-failure discipline `builtBundleText` already applies. Mutation-probed in Task 3. |
| T-04.1-26 | Repudiation | competitor-origin and report-credential bundle scan under a bundled vendor chunk | high | mitigate | The whole-bundle scan at `src/test/build-output.test.ts:738-756` becomes a claim about a vendor artifact the moment the chunk ships. Every pattern is MEASURED against the emitted vendor chunk in the same commit: a clean pattern keeps whole-bundle scope with the measurement recorded in-source and pinned by a companion assertion; a firing pattern narrows to project chunks with its predecessor, evidence and successor named. The stale doc-comment claim that the credential shapes were verified absent from the published SDK artifact is re-established or withdrawn rather than inherited. A moved credential shape's compensating layer is 04.1-11 Task 3's `deploy.yml` gate, one layer earlier than any bundle scan can see. Mutation-probed in Task 3. |
| T-04.1-09 | Tampering | provider-origin guarantee under a bundled vendor chunk | medium | mitigate | `PROVIDER_INGESTION_HOST_SOURCE_FORBIDDEN` over every `src/` module is unchanged; the withdrawn bundle-level half gains a successor that builds its own provider-unset probe and asserts zero approved-origin occurrences in that probe's project chunks. |
| T-04.1-12 | Repudiation | narrowed build-output invariants reading as unnoticed regressions | medium | mitigate | Both withdrawals are recorded in-source naming the predecessor case, the successor case, and plan 04.1-09, in the shape 04.1-01 established; D4 is closed with the option chosen recorded. |
| T-04.1-01 | Tampering | the automatic-capture lockdown, now against a real vendor merge | high | mitigate | Unchanged and now actually exercised: `lockdownHolds` compares 32 keys to literals plus `before_send` by identity, and refuses before any sink is returned (D-03, D-04). |
| T-04.1-08 | Repudiation | the retired empty-slot refusal reason | medium | mitigate | The retired member is documented as withdrawn with its successor named rather than deleted or repurposed, so a reader cannot mistake a changed refusal vocabulary for a lost refusal path (D-05). |
| T-04.1-23 | Information Disclosure | build-time constant absent under the test runner | low | accept | Unchanged — the runtime resolver still fails closed to an empty approved set under vitest. |
</threat_model>

<verification>
- `npm run typecheck` — clean.
- `npm run lint` — clean.
- `npm run verify:coverage` — exits 0 against the amended COVERAGE.md.
- `npm run build` — succeeds, and `dist/assets` contains a `posthog-sdk` chunk.
- `npx vitest run` — 0 failures, count at or above 662.
- `git grep -n "from 'posthog-js'" -- src` — reports a value import.
- Task 3's probe table — at least seven subjects, each observed red and restored.
- The fourth subject's per-pattern vendor-chunk measurement is recorded, and the branch it selected is named.
- One commit carries the import, all four resolved build-output cases, README, COVERAGE.md and the coverage verifier together.
</verification>

<success_criteria>
The link `04.1-VERIFICATION.md` recorded as **NOT WIRED** is wired: `src/measurement/posthog.ts`
imports `posthog-js` as a value, a provider-selected build carries the vendor chunk, and the
adapter resolves a real client instead of refusing at an empty slot. All four build-output
cases the vendor chunk affects are resolved in that same commit — two by named successors,
one by a recorded amendment, and one by a per-pattern measurement against the emitted vendor
chunk that either re-justifies its scope or narrows exactly the patterns that fired — each
asserted over a partition whose exclusion is itself asserted and mutation-probed. README.md, COVERAGE.md and
`scripts/verify-phase4-coverage.mjs` state the loaded reality together, so no reader of the
shipped repository can be told something the code does not do.
</success_criteria>

## Artifacts this phase produces

Created by this plan:

| Symbol | File | What it is |
|---|---|---|
| `boundPostHogClient()` | `src/measurement/posthog.ts` | Named accessor returning the value-imported `posthog-js` module; the client `resolveClient` falls back to when the ambient slot is empty |
| new `POSTHOG_REFUSAL` member for an unusable bound module | `src/measurement/posthog.ts` | Successor to the retired empty-slot refusal reason |
| `vendorChunkFiles()` | `src/test/build-output.test.ts` | The `posthog-sdk` side of the `dist/assets` partition, with a loud-failure guard |
| `projectChunkFiles()` | `src/test/build-output.test.ts` | This project's own chunks, with a loud-failure guard |
| `projectBundleText()` | `src/test/build-output.test.ts` | Joined project-chunk text, the new subject of the identity scan |
| successor to `keeps the production bundle free of identity and ordered-emission channels` | `src/test/build-output.test.ts` | Same three patterns and two positive assertions, narrowed to project chunks |
| successor to `ships the provider-unset bundle with no approved ingestion origin at all` | `src/test/build-output.test.ts` | Builds its own provider-unset probe rather than assuming `dist` is unset |
| vendor-chunk identity case | `src/test/build-output.test.ts` | Proves the exclusion contains the SDK and nothing of this project's |
| recorded amendment on `publishes the approved ingestion origin exactly once in a provider-selected build` | `src/test/build-output.test.ts` | The exactly-once count moves from the whole probe bundle to the probe's project chunks, with the narrowing recorded rather than made silently |
| measured resolution of `ships the unset provider bundle without competitor analytics, property, or credential seams` | `src/test/build-output.test.ts` | Per-pattern measurement against the emitted vendor chunk, then either a re-justified whole-bundle scope with a pinning companion assertion, or a per-pattern narrowing with predecessor, evidence and compensating layer named |
| `manualChunks` entry `posthog-sdk` | `vite.config.ts` | The partition key both the test helpers and the identity case read |
| loaded-SDK boundary check | `scripts/verify-phase4-coverage.mjs` | Pinned successor to the unloaded-SDK operational-boundary assertion |
| `### The SDK is loaded — delivery depends on the provider selector` | `README.md` | The positive successor to the delivery disclaimer |

<output>
Create `.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/04.1-09-SUMMARY.md` when done.
</output>
