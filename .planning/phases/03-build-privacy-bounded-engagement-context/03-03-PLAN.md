---
phase: 03-build-privacy-bounded-engagement-context
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/components/BrochurePanel.tsx
  - src/components/OnboardingChoices.tsx
  - src/components/QualifyForm.tsx
  - src/test/measurement-page.test.tsx
autonomous: true
requirements: [MEAS-02, MEAS-03, MEAS-07]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The first successful brochure preview availability is counted once per page instance despite responsive duplicate media, while open and download are counted per deliberate activation."
    - "WhatsApp, phone, email, and self-onboarding events fire per native outbound activation at all three placements without changing href, target, or default navigation."
    - "Qualification start fires once on first real form interaction; qualification submit fires bare only after validation passes and immediately before the network attempt (D-05, D-06)."
    - "Every measurement call site sends one exact event literal and no form value, DOM value, campaign object, product contact, or event property."
    - "Provider/storage failures never change brochure availability, form validation/submission, or outbound onboarding behavior (D-02, D-11, MEAS-07)."
  artifacts:
    - path: "src/components/BrochurePanel.tsx"
      provides: "Deduplicated preview and per-activation brochure events"
    - path: "src/components/OnboardingChoices.tsx"
      provides: "Per-activation assisted and self-onboarding events"
    - path: "src/components/QualifyForm.tsx"
      provides: "One-shot start and validation-admitted bare submit events"
    - path: "src/test/measurement-page.test.tsx"
      provides: "All-ten emission-site and degradation integration contracts"
  key_links:
    - from: "BrochurePanel media/actions"
      to: "measurement.track"
      via: "one shared preview ref and named action handlers"
      pattern: "brochure_(preview|open|download)"
    - from: "QualifyForm validation-admitted branch"
      to: "measurement.track"
      via: "bare qualify_submit before fetch"
      pattern: "qualify_submit"
    - from: "OnboardingChoices anchors"
      to: "measurement.track"
      via: "named handlers without preventDefault"
      pattern: "assisted_contact|self_onboarding"
---

<objective>
Expand the proven seam across every HAOO engagement action while preserving each native journey behavior and the no-form-data boundary.

Purpose: make all ten D-05 signals observable and all five D-09 context concepts updateable without introducing analytics properties or UI dependency on measurement.
Output: brochure, qualification, assisted-contact, and self-onboarding instrumentation with StrictMode and failure-path tests.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-build-privacy-bounded-engagement-context/03-CONTEXT.md
@.planning/phases/03-build-privacy-bounded-engagement-context/03-RESEARCH.md
@.planning/phases/03-build-privacy-bounded-engagement-context/03-PATTERNS.md
@.planning/phases/03-build-privacy-bounded-engagement-context/03-02-SUMMARY.md
@src/components/BrochurePanel.tsx
@src/components/OnboardingChoices.tsx
@src/components/QualifyForm.tsx
@src/test/measurement-page.test.tsx
</context>

## Artifacts this phase produces

- `src/components/BrochurePanel.tsx`: `handlePreviewAvailable`, `handleBrochureOpen`, and `handleBrochureDownload` event seams.
- `src/components/OnboardingChoices.tsx`: named outbound measurement handlers for WhatsApp, phone, email, and self-onboarding.
- `src/components/QualifyForm.tsx`: one-shot `qualify_start` and validation-admitted `qualify_submit` seams.
- `src/test/measurement-page.test.tsx`: exact ten-signal call-site, cardinality, native-destination, and failure-isolation cases.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure brochure availability and deliberate brochure actions</name>
  <files>src/components/BrochurePanel.tsx, src/test/measurement-page.test.tsx</files>
  <read_first>
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-RESEARCH.md` Pitfall 2 and Pattern 5.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-PATTERNS.md` BrochurePanel native-control analog.
    - `src/components/BrochurePanel.tsx` current dual responsive media and independent action controls.
    - `src/test/measurement-page.test.tsx` tracer render helper and measurement spy seam.
  </read_first>
  <behavior>
    - The first successful mobile image or desktop object availability emits `brochure_preview` once; the other responsive node cannot double emit.
    - Every Open activation emits `brochure_open` and every Download activation emits `brochure_download` with no second argument.
    - Preview failure/recovery and measurement exceptions do not alter the controls or their destinations.
  </behavior>
  <action>
Write the failing component cases, then add named handlers that call only the typed measurement API. Share one per-component ref across the mobile image and desktop object availability callbacks so CSS-hidden duplicate media cannot double-count; do not redefine preview as viewport visibility. Keep Open and Download as independent native anchors and preserve their current `href`, `target`, `rel`, and `download` attributes. Catching is owned by the facade, so components neither inspect capability nor branch on results. Do not measure disclosure reading or failed preview as a signal.
  </action>
  <verify>
    <automated>npm run test:unit -- src/test/measurement-page.test.tsx -t "measures brochure"</automated>
  </verify>
  <acceptance_criteria>
    - Two responsive success callbacks yield one preview event.
    - Repeated deliberate Open/Download activations each yield one matching bare event.
    - Existing brochure recovery and native-destination tests remain green under throwing measurement.
  </acceptance_criteria>
  <reversibility rating="reversible">Local named handlers and one-shot guards can be adjusted without changing the event or storage schema.</reversibility>
  <done>Brochure observations and actions update the bounded measurement seam with correct cardinality and no behavioral coupling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Measure every assisted and self-onboarding activation</name>
  <files>src/components/OnboardingChoices.tsx, src/test/measurement-page.test.tsx</files>
  <read_first>
    - `src/components/OnboardingChoices.tsx` three-placement native link rendering.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-CONTEXT.md` D-05 and D-09.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-PATTERNS.md` per-activation intent pattern.
  </read_first>
  <behavior>
    - Each WhatsApp, phone, and email activation emits its exact assisted-contact event; self-onboarding emits its exact event.
    - All three repeated onboarding placements use the same typed handlers and count every deliberate activation.
    - No handler prevents default, rewrites hrefs, appends campaign/context, or depends on a sink result.
  </behavior>
  <action>
Add named click handlers for the four outbound destinations and invoke `track` with only the corresponding exact literal. Preserve the existing product-generic component: product contacts stay in product data and no HAOO literal or browser API enters the component. Count every deliberate activation across opening, mid-page, and closing placements; do not globally deduplicate user intent. Tests must compare hrefs before/after activation and prove a throwing sink cannot suppress navigation semantics.
  </action>
  <verify>
    <automated>npm run test:unit -- src/test/measurement-page.test.tsx -t "measures every assisted and self-onboarding activation"</automated>
  </verify>
  <acceptance_criteria>
    - Twelve assisted-link activations and three self-onboarding activations map to the correct bare names without data properties.
    - Native destinations and accessible names are byte-identical to the pre-measurement controls.
    - Measurement failure is invisible to every onboarding path.
  </acceptance_criteria>
  <reversibility rating="reversible">The handlers are local additions behind the stable typed facade.</reversibility>
  <done>Every assisted and self-service intent is counted truthfully per activation without changing the onboarding route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Measure form start and validation-admitted submit without form data</name>
  <files>src/components/QualifyForm.tsx, src/test/measurement-page.test.tsx</files>
  <read_first>
    - `src/components/QualifyForm.tsx` validation and synchronous in-flight request path.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-CONTEXT.md` D-05, D-06, D-11.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-PATTERNS.md` correct submit-event seam.
    - `src/test/measurement-page.test.tsx` existing emission and degradation cases.
  </read_first>
  <behavior>
    - First actual focus/change interaction inside the form emits `qualify_start` once; rerenders, validation corrections, and retries do not repeat it.
    - Invalid submit emits no `qualify_submit`; a valid submission emits it immediately before the fetch attempt, whether that attempt later succeeds or fails.
    - The track call receives no values/request body/role/county/timeframe/portfolio/message/contact data.
    - Measurement failure cannot change validation, retained values, in-flight concurrency, retry, or fetch outcome UI.
  </behavior>
  <action>
Write cases around the existing validation-admitted transport branch. Add a per-instance ref for the first real form interaction and call the bare start event once. Place bare `qualify_submit` after validation has returned no errors and after the synchronous in-flight guard admits the attempt, immediately before transport. Never pass `values`, the body, field config, or any selected/typed value to measurement per D-06. Preserve all Phase 2 accessibility, retention, concurrency, and retry behaviors and prove them with the existing form tests plus the new page suite.
  </action>
  <verify>
    <automated>npm run test:unit -- src/test/measurement-page.test.tsx src/test/qualify-form.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Start and submit cardinalities match observable user intent and validation admission.
    - Track calls contain exactly one event literal and zero form-derived values.
    - All Phase 2 form state, accessibility, payload, retry, and concurrency tests remain green under measurement failure.
  </acceptance_criteria>
  <reversibility rating="reversible">The two call sites can move within the existing interaction/submit seams without altering external contracts.</reversibility>
  <done>The form contributes only truthful bare start/attempt counts while its personal data and functional behavior remain isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM/form state -> measurement calls | Rich personal UI state is adjacent to a deliberately name-only analytics seam. |
| measurement handlers -> native anchors/form | Optional side effects must never replace or gate native action behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Information Disclosure | QualifyForm instrumentation | high | mitigate | Assertions inspect call arity/arguments and forbid any form-derived value or request body. |
| T-03-10 | Repudiation | signal cardinality | medium | mitigate | StrictMode, responsive duplicate, invalid-submit, retry, and repeated-placement tables pin observable meanings. |
| T-03-11 | Denial of Service | optional measurement handlers | high | mitigate | Facade contains errors; integration tests require unchanged links, form behavior, and recovery when it throws. |
| T-03-SC | Tampering | package installs | high | mitigate | No package-manager action exists. |
</threat_model>

<verification>
- `npm run test:unit -- src/test/measurement-page.test.tsx src/test/qualify-form.test.tsx src/test/haoo-page.test.tsx` passes.
- `npm run typecheck && npm run lint` passes.
- All ten event meanings have at least one exact call-site test and no call has a property argument.
</verification>

<success_criteria>
- The full HAOO journey produces the closed ten-name vocabulary with intended one-shot/per-activation semantics.
- Form values and native destinations remain outside and independent of measurement.
- Failure of every optional measurement path leaves the existing journey complete.
</success_criteria>

<output>
Create `.planning/phases/03-build-privacy-bounded-engagement-context/03-03-SUMMARY.md` when done.
</output>
