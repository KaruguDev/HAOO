---
phase: 03-build-privacy-bounded-engagement-context
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/products/types.ts
  - src/products/haoo.ts
  - src/products/copy.ts
  - src/components/MeasurementDisclosure.tsx
  - src/components/QualifyForm.tsx
  - src/pages/ProductPage.tsx
  - src/test/measurement-page.test.tsx
  - src/test/build-output.test.ts
autonomous: true
requirements: [MEAS-02, MEAS-03, MEAS-04, MEAS-06, MEAS-07]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The visible collection notice always renders the owner-approved sentence; absence of analytics, storage, campaign values, or prior context never creates a blank notice."
    - "The visible collection notice renders immediately with the form and never shows a skeleton, spinner, progressive reveal, or delayed replacement."
    - "Form submission errors never remove, replace, or shorten the visible collection notice."
    - "The normal visible-notice state is the single owner-approved sentence rendered verbatim and wired into the submit control's accessible description."
    - "The visible notice is atomic copy: it renders completely or the contract test fails; no partial clauses or conditional fragments are permitted."
    - "The visible notice wraps naturally inside the 560px form measure and never clips, truncates, ellipsizes, or creates horizontal scrolling."
    - "The visible notice and inset grow vertically for long text at 320px and 200% zoom."
    - "An empty disclosure event map is a type/contract failure; production never renders a blank collected-signals section."
    - "Analytics, storage, campaign, or history failures never remove the disclosure; its static content remains readable."
    - "The populated disclosure renders ten event lines, five bounded context facts, campaign rules, the never-collected list, the summary boundary, and the clear action in fixed order."
    - "Event and disclosure mappings are total; missing product copy fails typecheck/tests rather than silently rendering a partial list."
    - "Disclosure paragraphs and lists use block flow, visible bullets, and wrapping with no tables, chips, fixed-width labels, clipping, or horizontal scrolling at 320px or 200% zoom."
    - "The collected-signals list renders exactly ten lines one-to-one with the event union; zero or any other cardinality is a contract-test failure."
    - "Disclosure paragraphs, list items, and summary labels wrap without fixed height, line clamp, truncation, or ellipsis."
    - "Persistent clear failure clears page memory and renders the locked truthful status without throwing, disabling the page, or hiding onboarding actions."
    - "The clear control and status region grow vertically when copy exceeds the container; reserved status height is a minimum rather than a clipping boundary."
    - "Clear-status and control labels wrap while preserving the 44px minimum interactive target."
    - "Without JavaScript, the footer measurement link remains a native fragment link to the disclosure summary; scripted expansion is progressive enhancement only."
    - "The footer's flex-wrap layout moves the measurement link onto another row before it can shrink, clip, or create horizontal overflow."
    - "The footer label wraps without truncation or ellipsis while preserving its 44px minimum target."
  artifacts:
    - path: "src/components/MeasurementDisclosure.tsx"
      provides: "Native exhaustive disclosure, clear action, and dedicated mounted status"
    - path: "src/products/haoo.ts"
      provides: "Exact ten-line disclosure map and all locked plain-language copy"
    - path: "src/products/copy.ts"
      provides: "Owner-approved present-tense atomic notice"
    - path: "src/pages/ProductPage.tsx"
      provides: "Progressively enhanced footer fragment link"
    - path: "src/test/measurement-page.test.tsx"
      provides: "UI contract, failure, cardinality, order, clear, and footer evidence"
  key_links:
    - from: "closed event tuple"
      to: "HAOO disclosure map"
      via: "Record<EventName, string> total mapping"
      pattern: "Record<.*Event.*string>"
    - from: "QualifyForm visible notice"
      to: "submit control"
      via: "stable aria-describedby id"
      pattern: "aria-describedby"
    - from: "footer measurement anchor"
      to: "native details element"
      via: "fragment href plus handler setting open=true without preventDefault"
      pattern: "measurement-disclosure"
  prohibitions:
    - statement: "The disclosure must not imply that Phase 3 attaches an engagement summary, confirms provider delivery, or establishes legal compliance."
      status: unverified
      flagged: true
    - statement: "Clearing page context must not erase form answers, submission state, campaign/browser history, or data outside `zph.haoo.ctx.v1`."
      status: unverified
      flagged: true
---

<objective>
Give every visitor an exact, discoverable, actionable explanation of HAOO measurement and browser context, including a truthful clear control, without overstating Phase 3 behavior.

Purpose: implement approved D-12 through D-16 and MEAS-04 while making disclosure completeness mechanically inseparable from the event allowlist.
Output: product-configured native disclosure, approved atomic notice, clear feedback, footer discovery path, and complete UI contract tests.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-build-privacy-bounded-engagement-context/03-CONTEXT.md
@.planning/phases/03-build-privacy-bounded-engagement-context/03-UI-SPEC.md
@.planning/phases/03-build-privacy-bounded-engagement-context/03-PATTERNS.md
@.planning/phases/03-build-privacy-bounded-engagement-context/03-VALIDATION.md
@.planning/phases/03-build-privacy-bounded-engagement-context/03-03-SUMMARY.md
@src/products/copy.ts
@src/components/QualifyForm.tsx
@src/pages/ProductPage.tsx
@src/test/measurement-page.test.tsx
</context>

## Artifacts this phase produces

- `src/components/MeasurementDisclosure.tsx`: `MeasurementDisclosure` and its native disclosure/clear UI.
- `src/products/types.ts`: readonly disclosure copy/config shapes total over the event union.
- `src/products/haoo.ts`: exact ten-signal map, browser/campaign/never-collected copy, and clear strings.
- `src/products/copy.ts`: approved atomic Phase 3 collection notice.
- `src/pages/ProductPage.tsx`: `handleMeasurementDisclosureLink` progressive enhancement.
- `src/test/measurement-page.test.tsx`: all 20 resolved UI-state contracts and four reasoned non-applicable-state guards.
- `src/test/build-output.test.ts`: no-JavaScript fragment and static-copy safety contracts.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the approved exhaustive disclosure and truthful clear action</name>
  <files>src/products/types.ts, src/products/haoo.ts, src/products/copy.ts, src/components/MeasurementDisclosure.tsx, src/components/QualifyForm.tsx</files>
  <read_first>
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-UI-SPEC.md` full component, interaction, copy, layout, and 20 explicit UI considerations.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-CONTEXT.md` D-12, D-13, D-15, D-16 and deferred ideas.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-PATTERNS.md` product config and MeasurementDisclosure analogs.
    - `src/products/copy.ts` existing collection-note builder and `src/components/QualifyForm.tsx` notice/submit/status structure.
  </read_first>
  <behavior>
    - The exact approved notice renders atomically and immediately above submit in all idle/invalid/submitting/failed states and remains in its `aria-describedby` chain.
    - Native collapsed `details` has `summary` first and fixed-order sections containing exactly ten total signal lines, five context facts, campaign rules, exclusions, Phase 3 no-summary sentence, clear button, and a separately mounted status.
    - Missing event copy fails typecheck/test; no runtime branch can render a partial or empty collected list.
    - Clear success/already-empty and blocked-removal outcomes use the exact locked strings; blocked removal still clears page memory and never affects form/journey state.
    - Static copy remains present and readable when provider/storage/history fail.
  </behavior>
  <action>
Write role/text/order/cardinality/failure tests first. Replace only `qualifyCollectionNotePageContext` with the already owner-approved D-16 sentence verbatim and remove its obsolete forward-looking comment. Add readonly product-generic disclosure configuration whose event copy is a total mapping over the ten-name union; HAOO owns every literal per D-04. Build `MeasurementDisclosure` with uncontrolled native `details`/`summary`, no redundant ARIA/open state/key handler, the exact section order and copy from UI-SPEC, visible-bullet semantic lists, and the specified one-column wrapping surfaces. Place it after the visible notice and before submit. Its button calls only `clearContext`, never storage directly, and its distinct mounted `role=status` uses exact success/blocked copy. Do not expose whether a record existed, clear form values or history, render campaign runtime values, add a loading state, add green styling, or claim Phase 4 summary attachment/legal approval.
  </action>
  <verify>
    <automated>npm run test:unit -- src/test/measurement-page.test.tsx -t "renders the approved complete measurement disclosure|clears only bounded page context" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Approved notice is byte-exact, atomic, always visible in applicable form states, and accessible from submit.
    - Disclosure order/cardinality/copy matches UI-SPEC and remains static through all measurement failures.
    - Clear is synchronous, idempotent, truthful, 44px minimum, wrapping, and isolated from all form/journey state.
    - No fixed heights, line clamps, ellipses, tables, chips, custom accordion semantics, or horizontal overflow patterns are introduced.
  </acceptance_criteria>
  <reversibility rating="costly">D-15 binds public privacy copy exhaustively to the event vocabulary; changing either requires coordinated product, type, and test updates.</reversibility>
  <done>Visitors can inspect and clear the complete disclosed context through a native, accessible, failure-independent UI whose copy cannot drift from the event contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the progressive footer discovery path and seal UI/build contracts</name>
  <files>src/pages/ProductPage.tsx, src/test/measurement-page.test.tsx, src/test/build-output.test.ts</files>
  <read_first>
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-UI-SPEC.md` footer link, progressive enhancement, responsive, and dismissed-state rationale.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-CONTEXT.md` D-14.
    - `src/pages/ProductPage.tsx` current flex-wrap footer link group.
    - `src/test/build-output.test.ts` no-script/static boundary contracts.
  </read_first>
  <behavior>
    - Footer link text is exactly `How we measure this page`, appears before `Back to ZERO-PAPER HUB`, and uses the namespaced native fragment.
    - Activation sets the target details `open = true` but does not prevent default, move focus, or measure disclosure reading.
    - Without JavaScript it remains a usable fragment to the summary; flex-wrap and text wrapping prevent clipping/overflow.
    - Full tests prove all 20 explicit UI considerations; the four dismissed loading/cardinality cases remain absent for their recorded static/synchronous reasons.
  </behavior>
  <action>
Add the native anchor before the back link with the existing 44px focusable footer style and a named handler that locates the namespaced `HTMLDetailsElement` and sets `open = true`. Preserve the fragment destination and browser focus/navigation semantics: no `preventDefault`, programmatic focus, custom router, duplicate disclosure, or measurement event. Extend component/static contracts for source order, target/open behavior, no-JavaScript usefulness, wrapping classes, no truncation/fixed height, and absence of loading UI. Register `MeasurementDisclosure.tsx` under the full static capability boundary. Run the entire validation suite and record the green commands in the plan summary.
  </action>
  <verify>
    <automated>npm run test:unit -- src/test/measurement.test.ts src/test/measurement-page.test.tsx src/test/build-output.test.ts && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Footer fragment works with and without enhancement, opens the single disclosure when scripted, and never hijacks focus/navigation.
    - All 20 approved UI considerations are asserted or structurally enforced; no dismissed loading/collection state was invented.
    - MeasurementDisclosure has no storage/history/provider/form access and every existing product boundary remains intact.
    - Full production build, unit suite, typecheck, and lint pass.
  </acceptance_criteria>
  <reversibility rating="reversible">The footer enhancement is a local progressive layer over a durable native fragment link.</reversibility>
  <done>The disclosure is findable from anywhere on the product page, progressively opens without breaking native behavior, and Phase 3 passes its complete validation contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event contract -> public disclosure | Public copy must remain complete and truthful as the closed vocabulary changes. |
| clear control -> browser context | User action may remove only the namespaced bounded record and page-memory copy. |
| footer navigation -> disclosure DOM | Progressive enhancement may open the native target but cannot hijack navigation/focus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Repudiation | disclosure/event mapping | high | mitigate | A total typed map and exact ten-line cardinality/order test fail on any event/copy drift. |
| T-03-13 | Information Disclosure | public notice | high | mitigate | Exact approved copy distinguishes local context, bare aggregate names, and absent Phase 3 form summary; no runtime values render. |
| T-03-14 | Tampering | clear action | high | mitigate | The UI calls only namespaced `clearContext`; tests retain form state/history and require truthful blocked-removal feedback. |
| T-03-15 | Denial of Service | disclosure/footer enhancement | medium | mitigate | Static content and native details/anchor remain usable without JavaScript or measurement/browser APIs. |
| T-03-SC | Tampering | package installs | high | mitigate | No registry or dependency is added; native controls and existing Tailwind patterns are used. |
</threat_model>

<verification>
- Focused Phase 3 tests pass after every task; wave gate runs `npm run typecheck && npm run lint && npm run test:unit`.
- Before verification, `npm test` passes and includes a fresh production build.
- Manual inspection at 320px and 200% zoom confirms natural wrapping/no horizontal overflow; keyboard confirms native summary, clear, and footer-link behavior.
</verification>

<success_criteria>
- Visitors can find, read, understand, and act on the exact privacy-bounded measurement disclosure.
- Disclosure and clear behavior remain truthful through empty, unavailable, failed, and repeated states.
- Event code, public copy, browser storage, and qualification payload remain mutually consistent and mechanically checked.
</success_criteria>

## Multi-Source Coverage Audit

| Source | Items audited | Coverage |
|--------|---------------|----------|
| GOAL / ROADMAP | aggregate/coarse/bounded/no-identity behavior; disclosure; campaign normalization; fail-open journey | 03-01 through 03-04 |
| REQUIREMENTS | MEAS-02, MEAS-03, MEAS-04, MEAS-06, MEAS-07 | all IDs appear in plan frontmatter and task acceptance criteria |
| CONTEXT | D-01 through D-16, including approved ordinal/day derivation metadata | D-01–11 in 03-01/02; D-05/06/09/11 in 03-03; D-12–16 in 03-04 |
| RESEARCH | no-package seam, patterns 1–5, pitfalls 1–8, validation architecture, ASVS V5/V8/V9/V14 | tracer, exhaustive reducer tests, call-site integration, disclosure, and all threat models |
| UI-SPEC | exact notice/copy/order/components/interactions plus 20 explicit and 4 reasoned dismissed considerations | 03-04 truths, tasks, acceptance criteria, and verification |
| VALIDATION | Wave 0 measurement/page tests, build guard, deterministic clock, mutation probes, sampling commands | 03-01/02 create/extend tests; every task has focused automated verification; 03-04 runs full suite |
| PATTERNS | product-generic config, fail-closed data/fail-open journey, native semantics, one-shot/per-activation, static capability enforcement | implemented across all four plans |

Deferred site-wide privacy routes/nav, segmented analytics, console logs, Phase 4 reporting/email summary, Phase 5 activation, CRM, and advertising identity remain excluded.

<output>
Create `.planning/phases/03-build-privacy-bounded-engagement-context/03-04-SUMMARY.md` when done.
</output>
