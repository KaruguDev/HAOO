---
phase: 03-build-privacy-bounded-engagement-context
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/measurement/index.ts
  - src/test/measurement.test.ts
  - src/test/build-output.test.ts
autonomous: true
requirements: [MEAS-02, MEAS-03, MEAS-06, MEAS-07]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All ten exact event names are accepted as bare names, every other runtime value is rejected, and no accepted event is queued or retried (D-02, D-05, D-06)."
    - "Visits cross `first`/`returning`/`frequent` at 1/2/4, ordinal saturates at 4, date bands age deterministically, and records older than about 180 days rebuild fresh (D-09, D-10)."
    - "Only the five disclosed interaction concepts persist; flags are idempotent, the day-only and ordinal fields never enter analytics or form payloads, and clear resets persistent plus page-memory context (D-09 through D-12)."
    - "Campaign parsing accepts only unique allowlisted values whose complete canonical form is `[a-z0-9-]+`, then caps length; it never launders invalid characters into valid-looking attribution (D-07, D-08)."
  artifacts:
    - path: "src/test/measurement.test.ts"
      provides: "Exhaustive event, campaign, schema, transition, expiry, failure, and clear contracts"
    - path: "src/measurement/index.ts"
      provides: "Complete bounded measurement state machine"
  key_links:
    - from: "src/measurement/index.ts runtime guard"
      to: "no-op sink"
      via: "validated event-name-only call"
      pattern: "isMeasurementEventName"
    - from: "tracked event"
      to: "bounded context flags"
      via: "exhaustive idempotent reducer"
      pattern: "switch.*event"
    - from: "stored day value"
      to: "date band and expiry"
      via: "injected deterministic current day"
      pattern: "180"
  prohibitions:
    - statement: "Invalid stored or campaign input must not be salvaged, partially cleaned, or silently promoted into trusted context."
      status: unverified
      flagged: true
---

<objective>
Complete the finite measurement state machine so every accepted and rejected value, bounded transition, expiry, failure, and clear outcome is mechanically specified.

Purpose: satisfy the full MEAS-02/03/06/07 data contract and the corrected D-09/D-10 metadata decision before adding more UI emission sites.
Output: exhaustive pure/browser tests and the complete product-generic context reducer.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-build-privacy-bounded-engagement-context/03-CONTEXT.md
@.planning/phases/03-build-privacy-bounded-engagement-context/03-RESEARCH.md
@.planning/phases/03-build-privacy-bounded-engagement-context/03-VALIDATION.md
@.planning/phases/03-build-privacy-bounded-engagement-context/03-01-SUMMARY.md
@src/measurement/index.ts
@src/test/build-output.test.ts
</context>

## Artifacts this phase produces

- `src/test/measurement.test.ts`: exhaustive finite contracts and mutation probes.
- `src/measurement/index.ts`: complete context schema guard, reducers, campaign parser, clear result, and failure adapters.
- `src/test/build-output.test.ts`: identity/form/clickstream negative source and bundle contracts.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the bounded context, campaign, and event transition tables</name>
  <files>src/measurement/index.ts, src/test/measurement.test.ts</files>
  <read_first>
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-CONTEXT.md` D-02, D-05 through D-12.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-RESEARCH.md` parse→validate→reduce→persist, whole-value acceptance, and pitfalls 1, 3, 4, 6.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-VALIDATION.md` Wave 0 matrix and deterministic clock requirement.
    - `src/measurement/index.ts` tracer public API; preserve it while completing its finite behaviors.
  </read_first>
  <behavior>
    - Exhaustive tables accept exactly ten ASCII event literals and reject empty/null/unknown/near-Unicode values without sink or context side effects.
    - Schema tests reject malformed JSON, unknown version, missing/extra keys, invalid enums, ordinal outside 1..4, invalid day-only values, identifiers, and ordered-event fields.
    - Visit tables prove 1→first, 2/3→returning, 4+→frequent with ordinal saturation; injected days prove today/this-week/this-month/earlier and roughly-180-day expiry.
    - Each event maps only to its disclosed idempotent context flag; page view and submit do not create an ordered record or copy form data.
    - Campaign tables cover all allowed keys, trim/case, 32-character cap, duplicates, excluded keys, forbidden characters, and mutation proof against character stripping.
    - Storage access/get/set/remove and history failures stay contained; `clearContext` clears persistent and memory copies and returns truthful success/blocked information only.
  </behavior>
  <action>
Write the exhaustive tables and mutation probes first. Complete the tracer implementation as a total state machine: exact schema keys and domains, immutable reducers, injected UTC day source, visit ordinal `1..4`, day-only last-seen, derived bands, and read-time expiry. Treat localStorage as untrusted and reject the whole record rather than defaulting fields. Map the ten events exhaustively to only the five D-09 interaction concepts; no ordered array exists. Campaign processing may trim and lowercase before validating the complete candidate, then truncate the already-valid value to 32 characters; reject duplicate occurrences and remove all campaign keys from the canonical product URL attempt. Ensure the event sink and context update are independently failure-contained. `clearContext()` must reset page memory even if persistent removal throws and expose only the discriminant needed by D-12's truthful UI. Preserve D-06: no `QualifyValues`, request body, role, county, timeframe, portfolio, name, email, phone, organization, or message reaches this module.
  </action>
  <verify>
    <automated>npm run test:unit -- src/test/measurement.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every finite accepted domain and representative invalid/extra case appears in a named table.
    - Campaign and schema mutation probes fail against permissive character stripping and partial-record defaulting.
    - Clock-controlled transitions and expiry are deterministic in any timezone.
    - All browser exceptions result in bounded memory/no-op behavior, never a thrown caller error.
  </acceptance_criteria>
  <reversibility rating="costly">The versioned key and exact schema are a published browser contract; future changes require a version bump and intentional record retirement.</reversibility>
  <done>The complete finite data contract is green, mutation-resistant, and contains no channel for identity, form values, or ordered click history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the context remains absent from form and provider payload shapes</name>
  <files>src/test/build-output.test.ts, src/test/measurement.test.ts</files>
  <read_first>
    - `src/test/build-output.test.ts` exact per-file capability-map loop and built-bundle helper.
    - `src/test/measurement.test.ts` Task 1 finite tables.
    - `.planning/phases/03-build-privacy-bounded-engagement-context/03-CONTEXT.md` D-02, D-03, D-06, D-09.
  </read_first>
  <behavior>
    - Source and built bundle contain no event property-bag signature, visitor UUID/cookie/fingerprint, raw click array, or analytics copy of form fields.
    - Derivation fields exist only inside the local record and read-context result, never an analytics sink call or qualification body.
    - The no-op path retains no emitted-name history after repeated valid and invalid calls.
  </behavior>
  <action>
Extend the existing static/bundle contract with structural assertions that target actual exports/call shapes rather than broad prose strings. Enumerate the allowed stored record keys and the allowed `track` arity, and cross-check the qualification body contract so D-06 cannot regress. Prove repeated calls leave no event queue, retry timer, debug log, or emitted-name buffer. Keep negative gates scoped to production regions/files so test descriptions and documentation cannot self-trigger.
  </action>
  <verify>
    <automated>npm run build && npm run test:unit -- src/test/measurement.test.ts src/test/build-output.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The built artifact contains the bounded record contract but no provider SDK, identity seam, form-property analytics, or clickstream buffer.
    - Existing FormSubmit payload tests remain green and show no Phase 3 context field.
    - Lint, typecheck, focused tests, and production build pass.
  </acceptance_criteria>
  <reversibility rating="reversible">These enforcement assertions can evolve alongside an explicitly approved future provider while retaining the name-only contract.</reversibility>
  <done>Executable source and bundle evidence proves the bounded local context cannot silently become provider or lead payload data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serialized browser record -> state machine | User/script-controlled JSON must match the exact schema before any field is consumed. |
| URL values -> campaign context | Query strings may contain personal or polluted values and must fail closed. |
| form/controller state -> bare event API | Personal qualification values must remain outside measurement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | context schema | high | mitigate | Exact-key/version/domain guards plus mutation tests reject partial, extra, malformed, unknown, and expired records wholesale. |
| T-03-06 | Information Disclosure | campaign parser | high | mitigate | Unique three-key allowlist and whole-value validation reject personal-looking or polluted values instead of stripping characters. |
| T-03-07 | Information Disclosure | event/context reducer | high | mitigate | Bare-name API, exhaustive flag mapping, and bundle/form cross-check prevent property and lead joins. |
| T-03-08 | Denial of Service | storage/history exceptions | medium | mitigate | Unit tables exercise access/get/set/remove/replace exceptions and require bounded in-memory continuation. |
| T-03-SC | Tampering | package installs | high | mitigate | No package-manager action exists in this plan. |
</threat_model>

<verification>
- `npm run test:unit -- src/test/measurement.test.ts src/test/build-output.test.ts` passes in under the validation latency budget.
- `npm run typecheck && npm run lint && npm run build` passes.
- Mutation probes demonstrate the campaign and schema tests are non-vacuous.
</verification>

<success_criteria>
- Every measurement input and state transition is finite, deterministic, and bounded.
- Invalid data fails closed while browser capability failures fail open for the visitor journey.
- Static and runtime evidence excludes identity, form values, property payloads, and click history.
</success_criteria>

<output>
Create `.planning/phases/03-build-privacy-bounded-engagement-context/03-02-SUMMARY.md` when done.
</output>
