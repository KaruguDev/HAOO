---
phase: 02-submit-a-qualified-haoo-enquiry
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-07"]
files_modified:
  - src/products/copy.ts
  - src/components/OnboardingChoices.tsx
  - src/components/ProductHeader.tsx
  - src/test/haoo-page.test.tsx
  - src/test/product-shell-reuse.test.tsx
  - .github/workflows/deploy.yml
  - README.md
autonomous: true
requirements: [LEAD-01, LEAD-04, LEAD-06]

user_setup:
  - service: github-actions
    why: "The deployed build needs the FormSubmit random-token endpoint injected at build time (D-04). Without it the site falls back to the readable address, which still works but publishes info@haoo.online in the bundle."
    env_vars:
      - name: VITE_HAOO_FORM_ENDPOINT
        source: "GitHub repository -> Settings -> Secrets and variables -> Actions -> Variables tab -> New repository variable. Value is the full FormSubmit AJAX URL with exactly one nonblank target segment for the HAOO random token, e.g. https://formsubmit.co/ajax/{token}. Create it as a VARIABLE, not a secret: Vite inlines VITE_* values into the public bundle, so it is obfuscation, never secrecy."
    dashboard_config:
      - task: "Obtain the HAOO random-token endpoint from FormSubmit for info@haoo.online"
        location: "https://formsubmit.co - enter info@haoo.online, copy the returned random-token URL. Endpoint activation and the confirmation email round-trip remain LEAD-07 / Phase 5."

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor can reach #qualify from the product navigation and from each of the three onboarding-choice blocks (D-08, D-09, LEAD-01)."
    - "The navigation order is Benefits, Capabilities, Brochure, Send details, Onboarding, matching the page order (D-09)."
    - "Each onboarding block keeps WhatsApp visually primary and presents Send your details instead below the existing assisted contacts (D-08, D-10)."
    - "The three new entry links are native anchors with a 44px target, visible focus, wrapping text, and no dependency on form, network, analytics, or storage state (LEAD-06)."
    - "Entry-point copy is product-name parameterised and the generic shell contains no HAOO literal (D-11)."
    - "The deployed build receives the HAOO FormSubmit endpoint from a public Actions variable, retains its documented https fallback, and identifies activation as Phase 5 work (D-04)."
  artifacts:
    - "src/products/copy.ts exports qualifyEntryPointLabel"
    - "src/components/OnboardingChoices.tsx renders one #qualify anchor per component instance"
    - "src/components/ProductHeader.tsx includes Send details -> #qualify in PRODUCT_LINKS"
    - "src/test/haoo-page.test.tsx proves link counts, destinations, order, and placement"
    - ".github/workflows/deploy.yml injects VITE_HAOO_FORM_ENDPOINT into the Build step and README.md documents its public fallback semantics"
  key_links:
    - "qualifyEntryPointLabel(product.name) -> OnboardingChoices link accessible name"
    - "ProductHeader PRODUCT_LINKS -> #qualify section created by 02-01"
    - "ProductPage's three OnboardingChoices instances -> exactly three Send your details instead links"
    - "GitHub Actions vars.VITE_HAOO_FORM_ENDPOINT -> Vite build environment -> HAOO_PRODUCT.qualify.endpoint"
---

<objective>
Make the qualification form reachable everywhere a serious prospect decides how to proceed.

Purpose: extend the Phase 1 assisted-first journey with a written alternative while keeping WhatsApp primary and self-onboarding unconditional (D-08, D-09, D-10).

Output: three product-generic entry links, one desktop/mobile product-nav entry, and executable navigation contracts.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-CONTEXT.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-01-SUMMARY.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-07-SUMMARY.md
@src/products/copy.ts
@src/components/OnboardingChoices.tsx
@src/components/ProductHeader.tsx
@src/test/haoo-page.test.tsx
@src/test/product-shell-reuse.test.tsx
</context>

## Artifacts this phase produces

- `src/products/copy.ts`: `qualifyEntryPointLabel(productName)`
- `src/components/OnboardingChoices.tsx`: one native `#qualify` entry link per rendered block
- `src/components/ProductHeader.tsx`: `Send details` product-nav item
- `src/test/haoo-page.test.tsx`: navigation and entry-point contracts

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the product-generic written-enquiry entry point to all onboarding blocks</name>
  <files>src/products/copy.ts, src/components/OnboardingChoices.tsx, src/test/haoo-page.test.tsx, src/test/product-shell-reuse.test.tsx</files>
  <read_first>
    - `src/components/OnboardingChoices.tsx` — retain the existing assisted-first hierarchy, focus classes, and three-position reusable rendering.
    - `src/products/copy.ts` — use `requireIdentity` for every product-name copy builder and preserve the generic-source literal prohibition.
    - `src/test/haoo-page.test.tsx` — keep all four Phase 1 onboarding accessible names pinned to exactly three occurrences.
    - `src/test/product-shell-reuse.test.tsx` — extend its copy-builder identity and synthetic-product contracts.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md` sections Information Architecture, Component Inventory, and Copywriting Contract.
  </read_first>
  <behavior>
    - Test 1: the HAOO page contains exactly three links named `Send your details instead`, each with `href="#qualify"`.
    - Test 2: each link follows the existing WhatsApp/call/email assisted-contact group and does not replace or precede WhatsApp.
    - Test 3: all pre-existing Phase 1 onboarding accessible names still occur exactly three times.
    - Test 4: a synthetic ZENITH product renders `Send your details instead` without leaking a HAOO literal into a generic source.
  </behavior>
  <action>
First write the failing contracts, then export `qualifyEntryPointLabel(productName)` from `copy.ts`, routing the name through the existing identity guard even though the locked visible label is `Send your details instead`. Extend `OnboardingChoices` with one native anchor to `#qualify` after the current contact group, not inside or before the visually primary WhatsApp action. Use the UI-SPEC's accent text treatment, `min-h-11`, wrapping text, and the existing light/dark focus pairing appropriate to each surface. The link must remain a plain anchor with no click handler, form state, analytics call, or URL data derived from visitor input. Preserve D-08, D-10, D-11 and LEAD-06 exactly.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/haoo-page.test.tsx src/test/product-shell-reuse.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/products/copy.ts` exports `qualifyEntryPointLabel` and continues to contain no product-specific literal.
    - Rendering the HAOO product page produces exactly three `Send your details instead` anchors with `href="#qualify"`.
    - Every entry anchor has a 44px minimum target and a focus-visible ring pairing already recognized by the Phase 1 contrast contract.
    - The WhatsApp action remains the first filled assisted action in every onboarding block.
    - Existing onboarding link-count assertions remain unchanged and pass.
  </acceptance_criteria>
  <done>All three onboarding decision points expose the written-enquiry alternative without weakening WhatsApp, duplicating a Phase 1 accessible name, or introducing stateful navigation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Send details to desktop and mobile product navigation in page order</name>
  <files>src/components/ProductHeader.tsx, src/test/haoo-page.test.tsx</files>
  <read_first>
    - `src/components/ProductHeader.tsx` — both navigation presentations map the shared `PRODUCT_LINKS` constant.
    - `src/pages/ProductPage.tsx` — 02-01 created the concrete `#qualify` target between brochure and onboarding; 02-07 completed its inherited guard registration before this expansion.
    - `src/test/haoo-page.test.tsx` — extend its navigation-name and section-order assertions rather than creating selector-based duplication.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md` Placement and responsive navigation rules.
  </read_first>
  <behavior>
    - Test 1: desktop and mobile navigation each expose `Send details` with `href="#qualify"`.
    - Test 2: link order is Benefits, Capabilities, Brochure, Send details, Onboarding.
    - Test 3: activating either native anchor targets the single existing `#qualify` section.
  </behavior>
  <action>
Add `{ label: 'Send details', href: '#qualify' }` to `PRODUCT_LINKS` after Brochure and before Onboarding per D-09. Do not duplicate the item separately in desktop and mobile JSX; both must continue mapping the shared array. Extend the page contract to assert the two rendered nav occurrences, exact href, and exact order. Preserve native-anchor behavior and existing mobile disclosure semantics; add no router or scroll handler.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/haoo-page.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `PRODUCT_LINKS` has five entries in the locked order.
    - Both desktop and mobile navigation render `Send details` once from the shared array.
    - Both links target `#qualify`, and exactly one element with `id="qualify"` exists on the page.
    - No runtime router, click interception, or duplicate navigation list is introduced.
  </acceptance_criteria>
  <done>Returning prospects can jump directly to the qualification form from either product navigation presentation, and navigation order mirrors document order.</done>
</task>

<task type="auto">
  <name>Task 3: Inject the endpoint at build time and document it as obfuscation, not a secret</name>
  <files>.github/workflows/deploy.yml, README.md</files>
  <read_first>
    - `.github/workflows/deploy.yml` — read in full; there is no `env:` block anywhere in the file today and the Build step is `run: npm run build` with nothing passed to it.
    - `README.md` — the existing `## Contact form` section, whose activation instructions are the tone and structure to match.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-RESEARCH.md` Pitfalls 6 and 7, and § Environment Availability.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-01-SUMMARY.md` — confirm the endpoint constant and fallback landed before documenting their deployment wiring.
  </read_first>
  <action>
Add an `env:` block to the `- name: Build` step in `.github/workflows/deploy.yml` setting `VITE_HAOO_FORM_ENDPOINT: ${{ vars.VITE_HAOO_FORM_ENDPOINT }}`. Use the `vars` context because Vite exposes the value publicly; change no other step, permission, or action version.

Add a `## HAOO qualification form` section to `README.md` after the existing `## Contact form` section stating that the form on `/products/haoo/` submits by AJAX to FormSubmit for `info@haoo.online`; the endpoint comes from the GitHub Actions repository variable `VITE_HAOO_FORM_ENDPOINT`; and undefined, empty, whitespace-only, malformed, non-https, non-FormSubmit, or non-AJAX values fall back to `https://formsubmit.co/ajax/info@haoo.online`. State that a valid value must be an absolute `https://formsubmit.co/ajax/{target}` URL with exactly one non-empty target segment; after URL decoding that segment must not be blank or contain a slash, and credentials, query, and fragment are not accepted. Explicitly document that bare `/ajax`, `/ajax/`, encoded-whitespace targets, malformed encodings, and extra target path segments select the fallback, while readable-address and random-token targets are valid; surrounding whitespace around the full configured value is trimmed. State that this value is not a secret because Vite inlines `VITE_*` values into the public bundle: the random token is scraper obfuscation, not inbox protection. Instruct maintainers to use an Actions repository variable and never a committed `.env` file. Record endpoint activation and live-mail confirmation as Phase 5 `LEAD-07`, not a Phase 2 action. Add no captcha claim for this form because D-24 disables it.
  </action>
  <verify>
    <automated>grep -q 'vars.VITE_HAOO_FORM_ENDPOINT' .github/workflows/deploy.yml &amp;&amp; ! grep -q 'secrets.VITE_HAOO_FORM_ENDPOINT' .github/workflows/deploy.yml &amp;&amp; grep -q 'VITE_HAOO_FORM_ENDPOINT' README.md &amp;&amp; grep -qi 'not a secret' README.md &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` places `VITE_HAOO_FORM_ENDPOINT: ${{ vars.VITE_HAOO_FORM_ENDPOINT }}` on the Build step and never reads that name from `secrets`.
    - `README.md` names the variable, the exact one-decoded-nonblank-target absolute-https FormSubmit AJAX shape, normalization and fallback cases, valid readable-address/token examples, fallback URL, public-bundle limitation, and Phase 5 activation ownership.
    - The new README section makes no captcha-protection claim for the HAOO qualification form.
    - `npm run build` exits 0 with the variable unset, proving the documented fallback remains buildable.
    - No unrelated workflow step, action version, or permission changes.
  </acceptance_criteria>
  <done>Deployment injects the public HAOO endpoint through an Actions variable, and maintainers have accurate fallback, exposure, and activation guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native anchor -> document fragment | A visitor-triggered link resolves to a fixed in-document id; no untrusted data enters the destination. |
| product data -> generic navigation copy | Product identity crosses into generic rendering and must remain escaped and parameterised. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering | `#qualify` entry destinations | medium | mitigate | Pin every new href to the literal `#qualify` and assert exactly one matching target; no visitor or campaign value enters a URL. |
| T-02-13 | Denial of Service | mobile/desktop product navigation | medium | mitigate | Both presentations map one shared array, preventing one viewport from silently losing the entry point. |
| T-02-14 | Information Disclosure | entry-point activation | low | accept | Native fragment navigation emits no submitted values or provider data; no analytics work is introduced until Phase 3. |
| T-02-SC | Tampering | package installs | high | mitigate | This plan installs zero packages; the empty Package Legitimacy Audit in `02-RESEARCH.md` remains valid. |
</threat_model>

<verification>
- `npm run test:unit -- --run src/test/haoo-page.test.tsx src/test/product-shell-reuse.test.tsx` passes.
- `npm run typecheck && npm run lint` passes.
- The full page exposes three onboarding entry links and two nav links, all targeting the one `#qualify` section.
</verification>

<success_criteria>
- A visitor can reach the form from every onboarding decision point and from either navigation presentation.
- WhatsApp remains visually primary, self-onboarding remains unconditional, and all new controls are native keyboard-focusable anchors.
- Product-generic copy and Phase 1 accessible-name count contracts remain green.
</success_criteria>

<output>
Create `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-03-SUMMARY.md` when done
</output>
