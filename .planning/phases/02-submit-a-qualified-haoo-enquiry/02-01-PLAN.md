---
phase: 02-submit-a-qualified-haoo-enquiry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/products/types.ts
  - src/products/haoo.ts
  - src/components/QualifyForm.tsx
  - src/pages/ProductPage.tsx
  - src/test/build-output.test.ts
  - src/test/product-shell-reuse.test.tsx
  - src/test/qualify-form.test.tsx
autonomous: true
requirements: [LEAD-01, LEAD-04, LEAD-05, LEAD-06]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 1
  confidence: low

must_haves:
  truths:
    # --- Goal-backward (phase outcome) ---
    - "A visitor on /products/haoo/ reaches a #qualify section between the brochure section and the closing onboarding block, without leaving the page or opening a dialog (D-06, D-07)."
    - "Filling the required controls and activating the submit control issues exactly one POST to HAOO_PRODUCT.qualify.endpoint with Content-Type application/json (D-01, LEAD-04)."
    - "The request body carries _subject from product data, _template table, _captcha false, _honey, a Source origin note, and one readable key per supplied field (LEAD-04, D-05)."
    - "The request body carries no _cc, no _next, no _autoresponse, and no _replyto (D-05, RESEARCH Pitfall 5, threat T-02-04)."
    - "Success and failure are decided from response.ok alone; the provider response body is never read (RESEARCH Pitfall 4)."
    - "QualifyForm renders for a synthetic non-HAOO product and contains no HAOO literal in any product-generic source (D-11)."
    - "The form carries noValidate, so activating submit with invalid values always reaches handleSubmit, renders the custom accessible error path, and issues no request (D-22, LEAD-01)."
    # --- Edge probe lift: LEAD-01 (resolved, explicit) ---
    - "LEAD-01/empty: submitting an untouched form produces a required-field message for every required control, issues no fetch call, and leaves every control empty."
    - "LEAD-01/encoding: field length bounds are enforced by the native maxLength attribute counted in UTF-16 code units, and validate() re-checks the same bound with String.prototype.length so the browser and the validator agree on one definition of length."
    - "LEAD-01/idempotency: buildSubmissionBody called twice on the same values returns a deeply equal object, the submit control is natively disabled while state is submitting, and the tracer's minimal succeeded panel replaces the form so a second submission is not reachable; Plan 05 enriches that panel without changing this terminal-state invariant."
    - "LEAD-01/concurrency: a synchronous ref guard admits at most one fetch before React can commit disabled state; rapid repeated submit events while the promise is pending still issue exactly one request, and a rejected promise lands in failed and never in succeeded."
    # --- Edge probe lift: LEAD-04 (resolved, explicit) ---
    - "LEAD-04/empty: an empty optional value is omitted from the payload entirely, while _subject, _template, _captcha, _honey and Source are present on every request."
    - "LEAD-04/encoding: payload keys are compared byte-for-byte against the locked emailLabel strings and the body is JSON.stringify'd and sent as UTF-8 application/json."
    # --- UI-SPEC UI Considerations lift: #qualify section (8/8 explicit) ---
    - "UI/empty/#qualify-section: on first render the section shows its h2, lead, sub-lead and an unfilled form card; it renders from static copy and never from fetched data. Plan 06 owns the required-fields instruction and collection disclosure."
    - "UI/loading/#qualify-section: the section has no skeleton, no spinner and no progress bar; the only in-flight signal is the submit-button relabel plus the live region."
    - "UI/error/#qualify-section: in every failure mode the visitor still has WhatsApp, phone, email and self-onboarding available."
    - "UI/populated/#qualify-section: a fully completed form is the maximal state - single column at a 560px measure with DOM order identical to visual order, still fitting a 320px viewport."
    - "UI/partial/#qualify-section: a partly completed form is a supported state, never an error, and never blocks WhatsApp, phone, email or self-onboarding."
    - "UI/overflow/#qualify-section: single column at every width, 560px form card, 680px reading measure, controls at 100% of card width, no horizontal page overflow at 320px or at 200% zoom."
    - "UI/zero-one-many/#qualify-section: the section renders exactly once per product page and hosts exactly one form and exactly one role=status region."
    - "UI/long-text/#qualify-section: no line clamp, no ellipsis and no truncation anywhere in the section; text wraps and containers grow vertically."
    # --- UI-SPEC lift: text / email / tel fields (6/6 explicit) ---
    - "UI/empty/text-fields: every text, email and tel input renders empty with its visible label present and never a placeholder standing in for a label."
    - "UI/loading/text-fields: during submission each field keeps its value, stays readable, and is not disabled or blanked."
    - "UI/error/text-fields: an invalid field renders a p element with id {field}-error below the control, wired through aria-describedby alongside any help text."
    - "UI/partial/text-fields: a partly filled field keeps its value through validation failure and provider failure, and is not validated on first entry or on blur before the first submit attempt."
    - "UI/overflow/text-fields: inputs never receive a fixed pixel width and never create horizontal overflow at 320px."
    - "UI/long-text/text-fields: free-text entry is bounded by maxLength rather than silent clipping - name 80, organization 120, email 254, phone 30."
    # --- UI-SPEC lift: live status region (6/6 explicit) ---
    - "UI/empty/live-region: the live region is mounted unconditionally from first render with an empty string as its text and a reserved min-height."
    - "UI/loading/live-region: the live region is the loading affordance; it never mounts or unmounts to signal loading, only its text changes."
    - "UI/error/live-region: on failure the region reads the locked failure string, and because it lives outside the form card it cannot unmount at the moment it needs to announce."
    - "UI/partial/live-region: the region always holds exactly one state string and is never left mid-announcement."
    - "UI/overflow/live-region: the region reserves a minimum height so a state change does not shift layout, and its text wraps rather than clipping or scrolling."
    - "UI/long-text/live-region: the longest state string wraps within the reserved-height box without truncation."
    # --- UI-SPEC lift: submit / retry control (6/6 explicit) ---
    - "UI/empty/submit-control: the submit control is always present and enabled in the idle state, labelled Send my details, and is never rendered absent or inert."
    - "UI/loading/submit-control: in flight the control relabels to Sending and takes the native disabled attribute at 70% opacity with cursor-wait; the relabel is the primary signal and there is no spinner."
    - "UI/error/submit-control: after a failed submit the control returns to Send my details, enabled, and setValues is never called in the failed branch."
    - "UI/partial/submit-control: the control is enabled regardless of how much of the form is filled and is never disabled to express incompleteness."
    - "UI/overflow/submit-control: the control is w-full below md and w-auto from md upward with a 44px minimum height, and its label wraps rather than overflowing."
    - "UI/long-text/submit-control: both control labels wrap rather than truncate and the button keeps its 44px minimum height as it grows."
    # --- UI-SPEC lift: honeypot field (6/6 explicit) ---
    - "UI/empty/honeypot: the honeypot is a static off-screen input rendered unconditionally, never conditionally mounted."
    - "UI/loading/honeypot: the honeypot has no loading state."
    - "UI/error/honeypot: a filled honeypot is posted unchanged for FormSubmit to evaluate; the browser shows no honeypot-specific error or accusatory copy, and no captcha or third-party challenge widget exists to fail."
    - "UI/partial/honeypot: the honeypot has exactly two payload states, empty and non-empty, and FormSubmit—not client code—owns the spam decision."
    - "UI/overflow/honeypot: the honeypot is positioned absolutely off-screen at -10000px with h-px w-px and overflow-hidden and must never use display none."
    - "UI/long-text/honeypot: the honeypot label is never visible to sighted users but must still be a real label element, not an aria-label on an unlabelled input."
  artifacts:
    - "src/products/types.ts exports QualifyControl, QualifyOption, QualifyField, ProductQualifyForm and ProductDefinition.qualify"
    - "src/products/haoo.ts exports HAOO_PRODUCT.qualify with endpoint, subject, sourceNote and fields"
    - "src/components/QualifyForm.tsx exports default QualifyForm plus buildSubmissionBody, validateQualifyValues and isFieldRequired"
    - "src/pages/ProductPage.tsx renders section id=qualify aria-label='Send your details' between #brochure and #onboarding"
    - "src/test/qualify-form.test.tsx exists and asserts the end-to-end submission contract"
  key_links:
    - "ProductPage.tsx -> QualifyForm.tsx: the section passes product.qualify, product.contacts and product.name, never the HAOO module"
    - "QualifyForm.tsx -> HAOO_PRODUCT.qualify.endpoint: the only network destination in the product surface"
    - "build-output.test.ts per-file prohibition map -> haoo.ts and QualifyForm.tsx: the narrowed static boundary is the only thing permitting provider and network tokens in the product surface"
  prohibitions:
    - statement: "Phase 2 must NOT report a browser-observable send as a delivered email. Status and confirmation copy describe what the browser did, never what the mailbox received."
      status: unverified
      verification: judgment
---

<objective>
Land the Phase 2 tracer: one qualified enquiry travels from the HAOO product page, through a product-generic form component, to the FormSubmit AJAX endpoint - end to end, production quality, on the real page, in one commit.

Purpose: prove the whole architecture (product data -> generic component -> page composition -> provider request -> in-page state machine) on the phase's earliest tokens, so an architectural dead end is discovered after one commit instead of after five. Every later plan in this phase widens this proven path rather than discovering it.

Output: a working `#qualify` section on `/products/haoo/` with a three-field enquiry that validates, submits, announces, and recovers, plus the narrowed Phase 1 static boundary that legalises it. Plan 07 immediately follows with endpoint edge-case proof and guard registration; Plan 03 owns deployment workflow injection and maintainer documentation.

**Phase goal (ROADMAP, verbatim):** Interested prospects can voluntarily send HAOO the minimum structured details needed for useful human follow-up and recover clearly from validation or provider trouble.

> **Planner note (MVP_MODE):** the ROADMAP `**Goal:**` line for Phase 2 is not written in `As a … I want to … so that …` form, unlike Phase 1's. Per the MVP-mode rule the planner does not invent a user story. Run `/gsd-mvp-phase 2` if the user-story framing is wanted; the plans below are complete without it.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-CONTEXT.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-RESEARCH.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-PATTERNS.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-VALIDATION.md
</context>

## Artifacts this phase produces

New symbols and files created by Phase 2 (all plans). Nothing in this list exists in the codebase today; treat every entry as newly created, not as drift from an existing definition.

**New files**
- `src/components/QualifyForm.tsx`
- `src/components/QualifyFallback.tsx` (plan 05)
- `src/test/qualify-form.test.tsx`
- `src/test/qualify-data.test.ts` (plan 07)

**New exported types** (`src/products/types.ts`)
- `QualifyControl`, `QualifyOption`, `QualifyField`, `ProductQualifyForm`
- new required field `ProductDefinition.qualify`

**New exported values / functions**
- `src/products/haoo.ts`: `HAOO_PRODUCT.qualify` (with `endpoint`, `subject`, `sourceNote`, `fields`); module constants `QUALIFY_ENDPOINT`, `QUALIFY_ENDPOINT_FALLBACK`
- `src/components/QualifyForm.tsx`: default export `QualifyForm`; named `buildSubmissionBody`, `validateQualifyValues`, `isFieldRequired`; module constants `QUALIFY_STATUS_MESSAGES`, `QUALIFY_SUBMIT_LABEL`, `QUALIFY_SUBMITTING_LABEL`, `QUALIFY_SUMMARY_HEADING`
- `src/components/QualifyFallback.tsx` (plan 05): default export `QualifyFallback`
- `src/products/copy.ts` (plans 03/05/06): `qualifyEntryPointLabel`, `whatsappFallbackActionLabel`, `phoneFallbackActionLabel`, `emailFallbackActionLabel`, `qualifyConfirmationBody`, `qualifyCollectionNote`

**New configuration keys**
- `VITE_HAOO_FORM_ENDPOINT` (Vite build-time env var; GitHub Actions repository *variable*; typed in plan 07)
- `ImportMetaEnv.VITE_HAOO_FORM_ENDPOINT` in `src/vite-env.d.ts` (plan 07)

**New DOM identifiers / anchors**
- section `id="qualify"`, `aria-label="Send your details"`
- nav entry `Send details` -> `#qualify` in `PRODUCT_LINKS` (plan 03)
- honeypot control id `qualify-website`
- per-field ids `qualify-{fieldName}`, error ids `qualify-{fieldName}-error`, help ids `qualify-{fieldName}-help`

**New test-contract identifiers**
- `PRODUCT_SOURCE_BOUNDARY` map in `src/test/build-output.test.ts` (replaces the flat `PRODUCT_SOURCES` + inline regex list)

## Multi-Source Coverage Audit

Every item from every source artifact, mapped to the plan that delivers it. `COVERED` means a task exists whose `<action>` implements it.

**GOAL — ROADMAP Phase 2 success criteria**

| # | Success criterion | Plan | Status |
|---|---|---|---|
| 1 | Name, contact method, role, organization, portfolio band, location, timeframe through labelled controlled fields | 02-01, 02-02 | COVERED |
| 2 | Visitor can tell which fields are required, why details are collected, and that an engagement summary accompanies the submission | 02-06 | COVERED |
| 3 | Valid submission addressed to `info@haoo.online` with a recognizable HAOO subject and human-readable labels | 02-01, 02-07 | COVERED |
| 4 | Accessible validation, submitting, return, failure and retry guidance; values retained after a recoverable error | 02-04, 02-05 | COVERED |
| 5 | Keyboard and AT users can complete the form; honeypot and provider spam controls discourage automation | 02-01, 02-03, 02-04, 02-07 | COVERED |

**REQ — `REQUIREMENTS.md` phase requirement IDs**

| ID | Plan(s) | Status |
|---|---|---|
| LEAD-01 | 02-01, 02-03 | COVERED |
| LEAD-02 | 02-02 | COVERED |
| LEAD-03 | 02-06 | COVERED |
| LEAD-04 | 02-01, 02-07 | COVERED |
| LEAD-05 | 02-01, 02-04, 02-05 | COVERED |
| LEAD-06 | 02-01, 02-05, 02-07 | COVERED |

**RESEARCH — features and constraints from `02-RESEARCH.md`**

| Item | Plan | Status |
|---|---|---|
| Zero new runtime or dev dependencies | all | COVERED |
| Pattern 1 — provider request as a pure testable descriptor (`buildSubmissionBody`) | 02-01 | COVERED |
| Pattern 2 — field definitions as product data | 02-01, 02-02 | COVERED |
| Pattern 3 — explicit five-state submission machine with value retention | 02-01, 02-05 | COVERED |
| Pattern 4 — GOV.UK error summary plus focus move | 02-01, 02-04 | COVERED |
| Pattern 5 — conditionally-required field announced accessibly | 02-04 | COVERED |
| Pitfall 1 — narrow the Phase 1 static-boundary guard first | 02-01 | COVERED |
| Pitfall 2 — distinct accessible names on fallback and confirmation panels | 02-05 | COVERED |
| Pitfall 3 — persistently mounted `role="status"` region | 02-01 | COVERED |
| Pitfall 4 — branch on `response.ok`, never the body | 02-01 | COVERED |
| Pitfall 5 — omit `_autoresponse` | 02-01 | COVERED |
| Pitfall 6 — token documented as obfuscation, not secrecy | 02-03 | COVERED |
| Pitfall 7 — defensive resolver, workflow `env:` block, and documented fallback | 02-01, 02-03, 02-07 | COVERED |
| Pitfall 8 — arbitrary-hex ring tokens only; register new files in `FOCUS_SOURCES` | 02-05, 02-07 | COVERED |
| Pitfall 9 — labels not placeholders; every select gets an empty prompt | 02-01, 02-02 | COVERED |
| Pitfall 10 — `autocomplete` tokens on person-describing fields | 02-01, 02-02 | COVERED |
| 47-county reference data with human spelling verification | 02-02 | COVERED |
| Package Legitimacy Gate | n/a — zero packages proposed; gate not applicable | COVERED |
| Open Question 5 — migrate `src/App.tsx` contact form to AJAX | out of scope per CONTEXT `## Phase Boundary` and RESEARCH recommendation | EXCLUDED |

**CONTEXT — locked decisions D-01 … D-26**

| Decision | Plan | Status |
|---|---|---|
| D-01 fetch AJAX, in-page | 02-01 | COVERED |
| D-02 JS-dependent plus direct-contact fallback | 02-05 | COVERED |
| D-03 confirmation panel replaces the form | 02-01, 02-05 | COVERED |
| D-04 token endpoint from build-time env with defensive fallback resolution and deployment wiring | 02-01, 02-03, 02-07 | COVERED |
| D-05 deliver to `info@haoo.online` only, source note, no cc | 02-01 | COVERED |
| D-06 `#qualify` section in the existing page, no route, no modal | 02-01 | COVERED |
| D-07 placed between brochure and closing onboarding | 02-01 | COVERED |
| D-08 fourth "send details" link in all three `OnboardingChoices` | 02-03 | COVERED |
| D-09 product-nav entry for the section | 02-03 | COVERED |
| D-10 consultation framing, never a gate | 02-01, 02-03 | COVERED |
| D-11 product-generic component configured from product data | 02-01 | COVERED |
| D-12 ten-field set | 02-02 | COVERED |
| D-13 email required, phone optional by default | 02-02 | COVERED |
| D-14 preferred contact channel as a controlled choice | 02-02 | COVERED |
| D-15 phone conditionally required, announced accessibly | 02-04 | COVERED |
| D-16 role required select, organization optional text | 02-02 | COVERED |
| D-17 portfolio-size bands | 02-02 | COVERED |
| D-18 closed Kenyan county select plus Outside Kenya | 02-02 | COVERED |
| D-19 onboarding timeframe bands | 02-02 | COVERED |
| D-20 optional message, email-only, never in measurement | 02-02 | COVERED |
| D-21 "(optional)" markers plus a required-fields instruction | 02-02, 02-06 | COVERED |
| D-22 validate on submit then re-validate on correction | 02-01, 02-04 | COVERED |
| D-23 single `role="status"`; button disables and relabels | 02-01 | COVERED |
| D-24 honeypot plus `_captcha=false`, no challenge widget | 02-01 | COVERED |
| D-25 plain-language collection note near the submit control | 02-06 | COVERED |
| D-26 no engagement-summary payload or placeholder in Phase 2 | 02-01, 02-06 | COVERED |
| Deferred: cc to `info@zero-paperhub.com` | — | EXCLUDED (CONTEXT `## Deferred Ideas`) |
| Deferred: expandable "what we collect" list | — | EXCLUDED (CONTEXT `## Deferred Ideas`) |
| Deferred: progressive-enhancement plain-POST path | — | EXCLUDED (CONTEXT `## Deferred Ideas`) |
| Deferred: timing-based bot check | — | EXCLUDED (CONTEXT `## Deferred Ideas`) |

**No MISSING rows.** Every GOAL, REQ, RESEARCH and CONTEXT item is either COVERED by a plan or EXCLUDED with a named source authorising the exclusion.

## Flagged assumptions (spec-less probe fallback)

The edge probe surfaced 11 items across LEAD-01…LEAD-06. Eight were auto-resolved as `explicit` acceptance criteria and authored into `must_haves.truths` in the plan owning the requirement. Three rows classified `unclassified` and therefore stay **`unresolved`** — never auto-resolved with a backstop, never dropped:

| Requirement | Probe | Status | Owning plan |
|---|---|---|---|
| LEAD-06 | `unclassified — review manually` | **unresolved** — flagged assumption below | 02-01 |
| LEAD-02 | `unclassified — review manually` | **unresolved** — flagged in 02-02 | 02-02 |
| LEAD-03 | `unclassified — review manually` | **unresolved** — flagged in 02-06 | 02-06 |

**LEAD-06 flagged assumption (unresolved):** the probe did not classify the spam-control requirement into a known edge category, so no automatic acceptance criterion was derived. The planner assumes — and cannot prove from the source artifacts — that FormSubmit's evaluation of the submitted `_honey` value plus `_captcha=false` is a *sufficient* spam control for the HAOO inbox at launch volume. The browser does not infer spam or fabricate a success state; it posts the value and follows `response.ok` like every other submission. `02-RESEARCH.md` § Security Domain records "accepted residual risk" for spam volume and CONTEXT `## Deferred Ideas` defers the timing check. If the real answer is different, the resolution is a product decision, not a code fix. **Surfaced for human review; not auto-resolved.**

**No-silent-drop equality:** 11 probe-surfaced edges = 8 authored into `must_haves.truths` (as `explicit` strings) + 3 surfaced as flagged assumptions. Zero `backstop` markers were authored because every classified row admitted a defensible acceptance criterion.

**Prohibition recall (spec-less §B):** stage-1 recall produced 11 raw must-NOT candidates; stage-2 precision kept 5 bespoke values/transparency items, distributed one per plan into `must_haves.prohibitions`, each authored **descriptor-less** so it disposes flagged-unverified. Canon-referral drops (breadcrumb only, not minted): *email-header injection and HTML/script injection into the delivered email are canon injection — covered by `/gsd-secure-phase` and eslint; not minted here.* *Bundled-endpoint disclosure and PII-in-transit to a third-party processor are canon security — covered by `/gsd-secure-phase`; not minted here.* *Kenya DPA 2019 lawful basis, notice and retention are canon compliance — covered by `/gsd-secure-phase` and the STATE.md privacy/legal blocker; not minted here.*

<assumption_delta_decision>
**Transition detected:** singular -> plural on *form-to-provider integration*. Before this phase the codebase had exactly one form (`src/App.tsx`, native `action` POST with `_next`). Phase 2 adds a second form with a different submission mechanism (`fetch` AJAX, in-page state machine), and CONTEXT D-14 simultaneously turns "contact method" from three co-equal links into a chosen `preferredChannel`.

**Question asked:** is *provider submission* now the primary noun (promote a shared submission seam that both forms use), or is `QualifyForm` a second one-off alongside the contact form (add-alongside)?

**Decision: `add-alongside`.** Rationale: CONTEXT `## Phase Boundary` and D-01 explicitly scope the AJAX departure to the HAOO form, and `02-RESEARCH.md` Open Question 5 recommends *not* migrating `src/App.tsx` in this phase. Promoting now would drag an out-of-scope file into the phase and rewrite a shipped, working redirect flow for no visitor-facing gain.

**What would force a later promote:** a third form, or a decision to migrate the company contact form to the AJAX pattern. Either makes the submission machinery the primary noun and it should move behind one seam (`buildSubmissionBody` + the state machine) that both call sites use. `buildSubmissionBody` is already written as a pure function taking `(values, qualifyConfig)` precisely so that promotion is a move, not a rewrite.

**Suggested invariant test (optional):** a source contract asserting that exactly one module in `src/` contains a `formsubmit` endpoint literal per form, so a third hand-rolled provider call cannot appear unnoticed.
</assumption_delta_decision>

## Scope cohesion note

This tracer owns seven files: four production files establish the product-data → generic-form → page → provider route, while three tests keep the route buildable, product-generic, and inside the narrowed static boundary. Plan 07 is an immediate dependent verification slice that adds environment typing, endpoint edge-case contracts, and suite-wide focus/generic-source registration without weakening this plan's runnable end-to-end submission proof.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "send my details" - one path, page to provider</name>
  <reversibility rating="costly">D-01 builds the state machine, failure UI and every contract around an in-page AJAX flow; reverting to the redirect `_next` pattern would rewrite the component and its tests, though no data migration or published contract is involved.</reversibility>
  <files>src/products/types.ts, src/products/haoo.ts, src/components/QualifyForm.tsx, src/pages/ProductPage.tsx, src/test/build-output.test.ts, src/test/product-shell-reuse.test.tsx, src/test/qualify-form.test.tsx</files>
  <read_first>
    - `src/products/types.ts` — the `readonly`-property, exported-`type`-alias and `ProductDefinition` conventions this extension must match.
    - `src/products/haoo.ts` — the module-level UPPERCASE-constant and nested-object-literal shape the `qualify` block must mirror (see the `contacts` and `brochure` blocks).
    - `src/pages/ProductPage.tsx` — `containerClasses` / `headingClasses` / `bodyClasses` at lines 25-27 and the `#brochure` section at lines 185-192, which is the exact section shape to copy.
    - `src/components/BrochurePanel.tsx` — the product-generic component prop shape, `focusClasses` / `surfaceClasses` module constants, and local-`useState` recovery pattern.
    - `src/components/OnboardingChoices.tsx` lines 16-17 — the two verbatim focus-token strings; no new ring colour may be introduced.
    - `src/App.tsx` lines 548-623 — the shipped FormSubmit field set, the honeypot block at 564-573, and the `role="status"` block at 575-586 which is the known-defective analog this task must NOT copy.
    - `src/test/build-output.test.ts` lines 35-46 and 292-306 — the flat `PRODUCT_SOURCES` array and forbidden-regex list being replaced by a per-file map.
    - `src/test/product-shell-reuse.test.tsx` lines 55-103 — the synthetic ZENITH `ProductDefinition` literal that must gain a `qualify` block the moment `qualify` becomes required.
    - `src/test/haoo-page.test.tsx` lines 1-26 and 42-55 — the RTL query conventions, the `expectedSections` filter list, and the nav-link loop this task must not disturb.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md` §§ Copywriting Contract, Interaction Contract, Component Inventory — every user-visible string in this task is locked there.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-RESEARCH.md` §§ Architecture Patterns (Patterns 1 and 3), Common Pitfalls 1/3/4/5, Code Examples.
  </read_first>
  <action>
Deliver the thinnest complete path through every layer the phase touches, production quality, in one commit. Fields are limited to three for this tracer — `name`, `email`, `role` — because the remaining seven are pure data added in plan 02 without touching the component.

**Layer 1 — `src/products/types.ts` (data contract, full shape now so later plans do not churn this file).** Add exported `type QualifyControl = 'text' | 'email' | 'tel' | 'select' | 'textarea'` following the `ProductCapabilityIcon` union style. Add `interface QualifyOption` with `readonly value: string` and `readonly label: string`. Add `interface QualifyField` with `readonly name`, `readonly label`, `readonly emailLabel`, `readonly control: QualifyControl`, `readonly required: boolean`, `readonly requiredMessage: string`, and optional `readonly autoComplete`, `readonly maxLength: number`, `readonly rows: number`, `readonly options: readonly QualifyOption[]`, `readonly placeholderOption: string`, `readonly help: string`, `readonly formatPattern: string`, `readonly formatMessage: string`, `readonly lengthMessage: string`, and `readonly requiredWhen: { readonly field: string; readonly values: readonly string[]; readonly message: string }`. Add `interface QualifyFieldGroup` with `readonly legend: string` and `readonly fieldNames: readonly string[]`. Add `interface ProductQualifyForm` with `readonly endpoint`, `readonly subject`, `readonly sourceNote`, `readonly fields: readonly QualifyField[]`, and `readonly groups: readonly QualifyFieldGroup[]`. Add `readonly qualify: ProductQualifyForm` to `ProductDefinition` immediately after `brochure`. Every property `readonly`; every array `readonly T[]`. This product-configured grouping contract is required by D-11 and prevents the generic component from knowing HAOO's legends or field names.

**Layer 2 — `src/products/haoo.ts` (product data).** Add module constant `QUALIFY_ENDPOINT_FALLBACK = 'https://formsubmit.co/ajax/info@haoo.online'` and export a small `resolveQualifyEndpoint(configuredValue?: string)` used to derive `QUALIFY_ENDPOINT`. The resolver trims the configured value, parses it with `new URL(...)` inside a failure-safe branch, and accepts it only when the protocol is exactly `https:`, `host` is exactly `formsubmit.co`, credentials, query, and fragment are absent, and `pathname` has exactly the shape `/ajax/{target}` with one path segment after `/ajax/`. Extract that segment, decode it once with `decodeURIComponent` inside the same failure-safe branch, and require the decoded target to remain non-empty after `trim()` and contain no `/`; decoding failure also selects `QUALIFY_ENDPOINT_FALLBACK`. This rejects bare `/ajax`, `/ajax/`, `/ajax//`, whitespace-only targets, percent-encoded whitespace targets such as `/ajax/%20`, encoded slash targets, extra path segments, and malformed encodings instead of treating a route prefix as a usable recipient. Undefined, `''`, whitespace-only, malformed, `http:`, wrong-host, and wrong-path inputs likewise cannot become the network destination, while valid one-segment readable-address and random-token URLs are preserved after outer trimming. Do not use a nullish-only fallback because an empty Actions variable is not nullish. Hoist `QUALIFY_ENDPOINT = resolveQualifyEndpoint(import.meta.env.VITE_HAOO_FORM_ENDPOINT)` above `HAOO_PRODUCT` alongside `WHATSAPP_STARTER_TEXT` and `PHONE_NUMBER`. Add a `qualify` block after `brochure` with `endpoint: QUALIFY_ENDPOINT`, `subject: 'New HAOO qualification enquiry — ZERO-PAPER HUB'`, `sourceNote: 'Sent from the HAOO product page on ZERO-PAPER HUB (www.zero-paperhub.com/products/haoo/)'`, and a `fields` array holding exactly three entries for this task: `name` (label `Full name`, emailLabel `Full name`, control `text`, required, autoComplete `name`, maxLength 80, requiredMessage `Enter your full name`, lengthMessage `Shorten your full name to 80 characters or fewer`); `email` (label `Email address`, emailLabel `Email address`, control `email`, required, autoComplete `email`, maxLength 254, requiredMessage `Enter your email address`, formatMessage `Enter an email address in the format name@example.com`, lengthMessage `Shorten your email address to 254 characters or fewer`); `role` (label `Your role`, emailLabel `Role`, control `select`, required, autoComplete `off`, placeholderOption `Select your role`, options Landlord / Property manager / Agency / Organization / Other with value equal to label, requiredMessage `Select your role`). Add tracer grouping data in field order: `About you` owns `name` and `email`, while `About your portfolio` owns `role`; plan 02 expands this to the locked three-group final arrangement. Brand spelling in every new string is exactly `ZERO-PAPER HUB`.

**Layer 3 — `src/components/QualifyForm.tsx` (new, product-generic).** Props are `readonly qualify: ProductQualifyForm`, `readonly contacts: ProductContacts`, `readonly productName: string` — the module must import no product module and must contain no `HAOO` literal. Export module constants `QUALIFY_SUBMIT_LABEL = 'Send my details'`, `QUALIFY_SUBMITTING_LABEL = 'Sending…'`, `QUALIFY_SUMMARY_HEADING = 'There is a problem'`, and `QUALIFY_STATUS_MESSAGES` mapping `submitting` to `Sending your details…`, `succeeded` to `Your details were sent.`, `failed` to `We couldn't send your details.`, and `idle` to the empty string.

Export a pure `buildSubmissionBody(values, qualify)` returning a flat `Record<string, string>` containing `_subject` from `qualify.subject`, `_template` `'table'`, `_captcha` `'false'`, `_honey` from `values._honey ?? ''`, then one entry per field keyed by `field.emailLabel` whose value is the trimmed value, omitting entries whose trimmed value is empty, then `Source` from `qualify.sourceNote`. Do not emit `_cc`, `_next`, `_autoresponse` or `_replyto` under any condition.

Export a pure `validateQualifyValues(values, qualify)` returning `Record<string, string>` keyed by field name. It enforces computed presence using `isFieldRequired(field, values)`, email format using `field.formatMessage`, `String.prototype.length <= field.maxLength` using `field.lengthMessage`, and for every select rejects any non-empty value that is not byte-equal to one of `field.options[].value` using the configured format message or required message. These checks run on the controlled state immediately before fetch, so programmatic over-bound values and manipulated select values issue zero requests rather than relying on native attributes. Export `isFieldRequired(field, values)` returning `field.required` for now — plan 04 makes it interpret the generic `requiredWhen` descriptor.

Render the form as `<form noValidate onSubmit={handleSubmit}>`. Keep native `required`, input `type`, and `maxLength` attributes as semantic/browser hints, but deliberately disable the browser's submission blocking so every submit-button activation reaches the single custom validation path and accessible error summary. Component state is `values` (a `Record<string,string>` seeded from field names plus `_honey`, all empty), `errors`, `submitted` (boolean, has a submit been attempted), and `state` typed `'idle' | 'submitting' | 'succeeded' | 'failed'`; also create `const inFlightRef = useRef(false)` as the synchronous concurrency authority and an error-summary ref. `handleSubmit` calls `event.preventDefault()` first and immediately returns when `inFlightRef.current` is true. It then runs `validateQualifyValues`, sets `errors` and `submitted`, and returns without touching `values` when any error exists. After React commits an invalid submission, move focus to the error-summary container—not the first invalid control and not the inner alert. Plan 04 preserves this target while adding per-field correction and repeat-submit re-announcement. On clean validation, set `inFlightRef.current = true` synchronously before the first await, set `state` to `submitting`, and always call `fetch(qualify.endpoint, { method: 'POST', headers: { 'Content-Type': 'application/json', Accept: 'application/json' }, body: JSON.stringify(buildSubmissionBody(values, qualify)) })`, including when `_honey` is non-empty so FormSubmit—not the client—evaluates the spam signal. Set `state` to `succeeded` only when `response.ok` is true and `failed` otherwise, with a `catch` that sets `failed`; release `inFlightRef.current` in `finally` so retry is possible after settlement. Never call `response.json()`. Never call `setValues` in the failure branch. React state and the native disabled attribute remain visual/accessibility feedback, not the concurrency primitive.

Markup: while state is not `succeeded`, render a `<form onSubmit>` inside a white card (`rounded-2xl`, `border border-[#DFE4F0]`, `max-w-[560px]`, `p-6 md:p-8`). First child is the honeypot, copied from `src/App.tsx:564-573` with the id and `htmlFor` changed to `qualify-website` and the visible label text kept. When `errors` is non-empty, render above the fieldsets a `tabIndex={-1}` summary container with an inner `role="alert"`, the heading `QUALIFY_SUMMARY_HEADING`, and links to invalid controls in configured DOM order; the container is the invalid-submit focus target. Render each field's configured message inline with a stable error id and wire it with `aria-invalid` and `aria-describedby`. Plan 04 enriches this same summary with correction-driven removal and an attempt counter for repeat announcements; it must not introduce a different focus target. Render `qualify.groups` in order as native borderless `<fieldset>` elements with native `<legend>` children and render each group's `fieldNames` by resolving them against `qualify.fields`; reject duplicate or unknown names in tests so every configured field appears exactly once. Within each field row, compute `const required = isFieldRequired(field, values)`. The `<label htmlFor={'qualify-' + field.name}>` carries `field.label` and, only when `required` is false, a separate neutral ` (optional)` span; this suffix is derived from current computed requiredness per D-21 and must disappear and return with conditional rules. Plan 06 exclusively owns adding the visible required-fields instruction and collection disclosure. Follow the label with the native control (`input` for text/email/tel, `select` for select with a leading `<option value="">{field.placeholderOption}</option>` then one option per `field.options`, `textarea` for textarea) with `id`, `name`, `value`, `onChange`, `required={required}`, `aria-required={required}`, `autoComplete`, `maxLength`, and the border token `border-[#6E7A94]`. Do not use `border-[#DFE4F0]` or `border-gray-200` on a control. Every control and the submit button carry the verbatim light focus string from `OnboardingChoices.tsx:16`; introduce no named Tailwind ring colour. Entered text is 16px (`text-base`) so iOS does not zoom on focus. Then the submit `<button type="submit" disabled={state === 'submitting'}>` rendering `QUALIFY_SUBMITTING_LABEL` while submitting and `QUALIFY_SUBMIT_LABEL` otherwise, `w-full md:w-auto`, `min-h-11`, `bg-[#4054C6] hover:bg-[#3345A7] active:bg-[#29388A]`, `disabled:cursor-wait disabled:opacity-70`. When state is `succeeded`, replace the form card with one minimal accessible confirmation panel containing a `tabIndex={-1}` heading and the browser-observable text `Your details were sent.`; focus its heading after the state transition. This production-quality tracer terminal state makes the form and submit control unreachable, so resubmission is impossible. Plan 05 enriches this existing panel with approved response-time and direct-contact content without changing its replacement, focus, single-panel, or truthful-status contracts.

Outside the `<form>` element and outside the card, render the live region as a single `<p role="status" className="min-h-[1.5rem] text-sm">{statusMessage}</p>`, mounted unconditionally from the component's first render, whose text is the empty string while idle. Exactly one `role="status"` node exists in the component. Do not conditionally mount it, do not toggle `aria-live`, and do not copy the conditional shape at `src/App.tsx:575-586`.

**Layer 4 — `src/pages/ProductPage.tsx` (composition).** Import `QualifyForm`. Insert a new `<section id="qualify" aria-label="Send your details" className="scroll-mt-4 py-12 md:py-16">` between the `#brochure` section (line 185) and the `#onboarding` section (line 193), copying the `#brochure` section shape exactly: `containerClasses` wrapper, one `<h2 className={headingClasses}>Send your details</h2>`, a lead paragraph `<p className={'mt-4 max-w-[680px] ' + bodyClasses}>{product.assistedInvitation}</p>`, a sub-lead paragraph with the locked string `Prefer writing to chatting? Share a few details and we'll reply with the onboarding path that fits your portfolio. This is not a sign-up — you can still start on your own at any time.`, then `<QualifyForm qualify={product.qualify} contacts={product.contacts} productName={product.name} />`. The `aria-label` must not be any of Benefits, Capabilities, Rental journey, Brochure or Onboarding. `ProductPage.tsx` itself must contain no `<form`, no `fetch(` and no `formsubmit` token — it renders the component and nothing more.

**Layer 5 — `src/test/build-output.test.ts` (narrowed static boundary, Pitfall 1).** Replace the flat `PRODUCT_SOURCES` array at lines 35-46 and the inline forbidden-regex list at lines 292-306 with a data-driven per-file map. Declare named regex groups: `ALWAYS_FORBIDDEN` holding `/dangerouslySetInnerHTML/`, `/localStorage|sessionStorage|document\.cookie|indexedDB/`, `/gtag\(|dataLayer|analytics\./`, `/react-router|createBrowserRouter/`, `/document\.referrer|navigator\.userAgent|window\.location/` and `/supabase/i`; `NETWORK_FORBIDDEN` holding `/\bfetch\s*\(|XMLHttpRequest|navigator\.sendBeacon/`; `PROVIDER_FORBIDDEN` holding `/formsubmit/`; and `FORM_MARKUP_FORBIDDEN` holding `/FormData|<form\b/`. Declare `PRODUCT_SOURCE_BOUNDARY` as a `Record<string, readonly RegExp[]>` listing every product source with the groups that still apply to it: the six untouched files (`BrochurePanel.tsx`, `ProductHeader.tsx`, `ProductsSection.tsx`, `copy.ts`, `registry.ts`, `types.ts`) and `ProductPage.tsx` keep all four groups; `src/products/haoo.ts` keeps all groups except `PROVIDER_FORBIDDEN`; `src/components/QualifyForm.tsx` keeps `ALWAYS_FORBIDDEN` and `PROVIDER_FORBIDDEN` only. Keep the existing JSDoc comment but restate it for Phase 2, naming which file is permitted which token and why. Keep the assertion that every listed source is non-empty and keep the two `components.json` / `src/components/ui` absence assertions. Do not delete a regex without replacing the guarantee, and do not weaken any group for a file that does not need it.

**Layer 6 — `src/test/product-shell-reuse.test.tsx` (type compatibility).** Adding a required `qualify` to `ProductDefinition` breaks the synthetic ZENITH literal at lines 55-103 under `tsc`. Add a `qualify` block to that literal built from `HAOO_PRODUCT.qualify` field definitions with `endpoint: 'https://formsubmit.co/ajax/hello@zenith.example'`, `subject: 'New ZENITH qualification enquiry — ZERO-PAPER HUB'` and a ZENITH `sourceNote`, so the existing `expect(container.textContent).not.toContain('HAOO')` assertion at line 125 now also proves the form is product-generic. Change nothing else in this file in this task.

**Layer 7 — `src/test/qualify-form.test.tsx` (new end-to-end contract).** Import `describe`, `expect`, `it`, `vi`, `afterEach` explicitly from `vitest` (`globals: false`), and `fireEvent`, `render`, `screen`, `waitFor`, `within` from `@testing-library/react`; use `fireEvent`, never `userEvent`, which is not installed. Add `afterEach(() => { vi.unstubAllGlobals(); })`. Render `<ProductPage product={HAOO_PRODUCT} />` through a local `renderPage()` helper and scope queries with `within(screen.getByRole('region', { name: 'Send your details' }))`. Write the tracer contract as named tests whose titles match the `-t` filters in `02-VALIDATION.md`: `collects a name and a usable contact method`, `posts a readable, correctly-addressed payload`, `announces every submission state`, and `traps bots without blocking assistive technology`. Stub the network with `vi.stubGlobal('fetch', vi.fn().mockResolvedValue({ ok: true }))` and assert on the *request*, never a response body: URL equals `HAOO_PRODUCT.qualify.endpoint`, method `POST`, `Content-Type` `application/json`, parsed body has `_captcha` `'false'`, `_template` `'table'`, `_subject` containing `HAOO`, keys containing `Full name` / `Email address` / `Role` / `Source`, and `not.toHaveProperty` for each of `_cc`, `_next`, `_autoresponse`, `_replyto`. Assert the rendered form has `noValidate === true`, then activate its submit button while required fields are empty; prove the custom required messages and accessible error summary render, focus moves to the summary container, and fetch is called zero times. Assert specifically that neither the first invalid control nor the inner alert is the focus target. This button-activation contract must fail if native constraint validation can bypass `handleSubmit`. Assert the tracer renders its configured native groups and legends in DOM order, labels optional fields from computed requiredness, and mounts the `role="status"` node at first render with empty text content. Add direct pure-validator or controlled-state tests proving a select value outside its configured option allowlist and a programmatically supplied value longer than `maxLength` both return field errors; submit each manipulated state and assert fetch is called zero times. For the provider-owned spam contract, fill `_honey`, submit, assert `fetch` is called once, and assert the parsed JSON body contains the exact non-empty `_honey` value. For concurrency, use a deferred fetch promise, fire two valid submit events back-to-back before resolving it, assert exactly one request exists while pending, then resolve and assert the normal terminal transition. After a successful response, assert the minimal confirmation panel heading receives focus, the form and submit button are absent, and no second submission is reachable; Plan 05 extends this same test with approved confirmation content. This test must fail if protection relies only on React state or the button's disabled attribute.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/qualify-form.test.tsx src/test/build-output.test.ts src/test/product-shell-reuse.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/products/types.ts` contains `export type QualifyControl`, `export interface QualifyField`, `export interface ProductQualifyForm`, and `readonly qualify: ProductQualifyForm;` inside `ProductDefinition`.
    - `src/products/haoo.ts` contains `VITE_HAOO_FORM_ENDPOINT` and the literal `https://formsubmit.co/ajax/info@haoo.online`.
    - `src/components/QualifyForm.tsx` contains `export function buildSubmissionBody(`, `export function validateQualifyValues(`, and `export function isFieldRequired(`.
    - `grep -v '^\s*[/*]' src/components/QualifyForm.tsx | grep -c 'role="status"'` returns `1` (comment lines filtered so a descriptive comment cannot inflate the count).
    - Behavior: with `fetch` stubbed to resolve an object whose `ok` is `true` and whose `json` method throws, a valid submission still reaches the succeeded state — proving the provider response body is never read.
    - Behavior: `Object.keys(parsedBody).sort()` from the stubbed request equals exactly `['Email address','Full name','Role','Source','_captcha','_honey','_subject','_template']`, which fails if any additional provider option is emitted.
    - `src/pages/ProductPage.tsx` contains `id="qualify"` and `aria-label="Send your details"`, and the `#qualify` section appears at a byte offset after `id="brochure"` and before `id="onboarding"`.
    - `src/test/build-output.test.ts` contains `PRODUCT_SOURCE_BOUNDARY` and each of `ALWAYS_FORBIDDEN`, `NETWORK_FORBIDDEN`, `PROVIDER_FORBIDDEN`, `FORM_MARKUP_FORBIDDEN`.
    - Behavior: rendering `ProductPage` with `HAOO_PRODUCT` and submitting a valid name, email and role issues exactly one `fetch` whose parsed JSON body has keys `Full name`, `Email address`, `Role`, `Source`, `_subject`, `_template`, `_captcha`, `_honey` and no others.
    - Behavior: a `fetch` resolving with `{ ok: false }` leaves every entered value present in the DOM and puts `We couldn't send your details.` in the `role="status"` node.
    - Behavior: a non-empty `_honey` value is included byte-for-byte in exactly one provider request; the browser renders no honeypot-specific error and derives the terminal state only from `response.ok`.
    - Behavior: two valid submit events fired synchronously against one deferred provider promise issue exactly one `fetch`; after settlement the guard releases and a failed state can be retried.
    - Behavior: an invalid submit focuses the `tabIndex={-1}` error-summary container, not the first invalid control or the inner alert, and issues zero requests.
    - Behavior: a successful response replaces the form with one minimal confirmation panel, focuses its heading, preserves the external status region, and leaves no reachable submit control; Plan 05 enriches this panel rather than creating a second terminal-state contract.
    - Behavior: configured groups render as native fieldsets with native legends in configured DOM order, and every configured field belongs to exactly one group.
    - Behavior: optional-label text is derived from `isFieldRequired`; source labels stay unchanged and a computed-optional field has the visible `(optional)` suffix.
    - Behavior: an out-of-allowlist select value and a value whose `String.prototype.length` exceeds its configured `maxLength` each produce a validation error and zero fetch calls.
    - `npm run test:unit` exits 0 with no fewer than 65 previously-passing tests still passing.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>A visitor can open `/products/haoo/`, scroll to `Send your details`, correct an invalid submission from the focused summary, enter a name, an email address and a role, press `Send my details`, and have exactly one correctly-addressed JSON request reach the FormSubmit AJAX endpoint. Submitting / sent / failed are announced in a persistently mounted status region, failure retains values, and success replaces the form so resubmission is unreachable. The Phase 1 static boundary is restated rather than deleted, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser -> QualifyForm state | Untrusted free text and select values enter component state. React escapes on render; there is no `dangerouslySetInnerHTML` (guard-enforced). |
| QualifyForm -> formsubmit.co (cross-origin POST) | Personal data leaves the origin to a third-party processor over the public internet. |
| GitHub Actions build -> public bundle | `VITE_*` values are statically inlined into `dist/assets/*.js` and are world-readable. |
| static site -> visitor | No server, no session, no credential, no protected resource. ASVS V2/V3/V4 are out of scope for this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | build-time env inlining | medium | accept | The token is public by construction (Vite documents this verbatim). Plan 03 is the future dependent owner of README disclosure and workflow injection; residual risk remains that anyone reading the bundle can discover the endpoint. |
| T-02-04 | Tampering | `buildSubmissionBody` | high | mitigate | No visitor-supplied value reaches a header-shaped provider option: `_cc`, `_replyto`, `_next` and `_autoresponse` are never emitted, and the recipient is fixed in build data. Asserted by `not.toHaveProperty` in `qualify-form.test.tsx` (task 1). |
| T-02-05 | Spoofing / DoS | provider inbox | medium | mitigate | `_honey` is rendered off-screen and posted unchanged with `_captcha=false` for FormSubmit to evaluate; client code never manufactures success for a filled field. Residual spam risk is accepted per `02-RESEARCH.md`; a timing check is a documented Deferred Idea. |
| T-02-06 | Repudiation | success reporting | medium | mitigate | Success is derived from `response.ok` alone and never from a parsed body, so a provider body change cannot make the page claim an enquiry was sent when it was rejected. Status copy says `Your details were sent.`, never that it was delivered. |
| T-02-07 | Tampering | Phase 1 static boundary | high | mitigate | `PRODUCT_SOURCE_BOUNDARY` narrows the guard per file rather than deleting regexes: storage, analytics, injection, router and supabase prohibitions remain on every product source including the two newly permitted ones (task 1). |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable by construction — Phase 2 installs zero packages (`02-RESEARCH.md` § Package Legitimacy Audit records an empty table). Any plan that later proposes a dependency must re-run the Package Legitimacy Gate and insert a blocking human checkpoint for `[ASSUMED]`/`[SUS]` verdicts. `npm ci` against the existing lockfile is the only install performed. |
</threat_model>

<verification>
- `npm test` (build then full suite) exits 0 with every Phase 1 contract still passing.
- `npm run typecheck` and `npm run lint` exit 0.
- `npm run test:unit -- --run src/test/build-output.test.ts` passes, proving the narrowed boundary still forbids storage, analytics, injection, router and supabase tokens in every product source.
- `npm run test:unit -- --run src/test/product-shell-reuse.test.tsx` passes, proving the form renders for the synthetic ZENITH product with no `HAOO` literal reaching a product-generic source.
- `npm run test:unit -- --run src/test/qualify-form.test.tsx` passes the noValidate, provider-owned `_honey`, synchronous in-flight guard, payload, and response-state contracts before Plan 07 adds broader guard registration.
- Manual (deferred to Phase 5 / `LEAD-07`, not this plan): a live submission arrives at `info@haoo.online` with the HAOO subject and readable labels.
</verification>

<success_criteria>
- A visitor can submit a name, email address and role from `/products/haoo/` without leaving the page.
- Exactly one JSON POST reaches `HAOO_PRODUCT.qualify.endpoint` per successful submission, carrying readable field labels and no cc, reply-to, redirect or autoresponse option.
- Submitting, sent and failed states are announced through a single persistently mounted `role="status"` region.
- Entered values survive a failed provider attempt.
- The Phase 1 static boundary and synthetic-product compatibility contracts pass, narrowed rather than deleted; Plan 07 immediately registers the new component with the suite-wide focus and generic-source scans.
- The tracer's runtime resolver is blank-safe and accepts only an absolute `https://formsubmit.co/ajax/{target}` destination whose decoded target is one nonblank segment; Plan 07 pins every invalid-input edge and Plan 03 owns injecting configuration into deployed builds.
</success_criteria>

<output>
Create `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-01-SUMMARY.md` when done
</output>
