---
phase: 02-submit-a-qualified-haoo-enquiry
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - src/products/types.ts
  - src/products/haoo.ts
  - src/products/copy.ts
  - src/components/QualifyForm.tsx
  - src/test/qualify-form.test.tsx
autonomous: false
requirements: [LEAD-03]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The form states that all fields are required unless marked optional, and every optional label visibly carries `(optional)` with no asterisks (D-21, LEAD-03)."
    - "Immediately above the submit control, a prominent two-sentence notice explains the reply purpose and the coarse anonymous page-use summary in human-approved language (D-25, LEAD-03)."
    - "The submit control references the disclosure through `aria-describedby`, and the note remains visible and readable in idle, invalid, submitting, and failed states."
    - "Phase 2 sends no engagement-summary value, placeholder, signal list, analytics field, visitor identifier, or free-text measurement payload (D-20, D-26)."
    - "The disclosure is configuration data consumed by the generic form and contains no product-specific literal in generic sources (D-11)."
    - "No plan or UI text asserts Kenya Data Protection Act compliance; privacy/legal approval is recorded as an owned release condition."
  artifacts:
    - "src/products/types.ts declares the qualification disclosure configuration shape"
    - "src/products/haoo.ts provides the approved purpose and page-context notice"
    - "src/products/copy.ts exports qualifyCollectionNote helpers without product-specific literals"
    - "src/components/QualifyForm.tsx renders the required-fields note and disclosure immediately above submit"
    - "src/test/qualify-form.test.tsx contains the `discloses what is collected and what is required` contract"
  key_links:
    - "HAOO_PRODUCT.qualify disclosure data -> QualifyForm visible notice -> submit button aria-describedby"
    - "buildSubmissionBody exact-key contract -> D-26 absence of engagement-summary payload"
    - "optional field definitions -> rendered `(optional)` suffix -> required-fields instruction"
  prohibitions:
    - statement: "Phase 2 must not emit an engagement-summary field or detailed signal list; Phase 3 defines and implements the actual bounded context."
      status: verified
      verification: automated
---

<objective>
Finish the form's trust contract: tell visitors what is required, why HAOO receives their details, and what page-use context will accompany a future submission—without pretending Phase 2 already sends that context.

Purpose: satisfy LEAD-03 and D-21, D-25, D-26 while preserving the Phase 3 measurement boundary and the open privacy/legal ownership decision.

Output: human-approved visible disclosure, semantic wiring to the submit action, and negative payload contracts that prevent premature enrichment.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-CONTEXT.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-RESEARCH.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-05-SUMMARY.md
@src/products/types.ts
@src/products/haoo.ts
@src/components/QualifyForm.tsx
@src/test/qualify-form.test.tsx
</context>

## Artifacts this phase produces

- `src/products/types.ts`: product-configured qualification disclosure fields
- `src/products/haoo.ts`: approved purpose and coarse-page-context sentences
- `src/products/copy.ts`: `qualifyCollectionNote` product-name-parameterised helper(s)
- `src/components/QualifyForm.tsx`: visible required-fields note and disclosure inset linked to submit
- `src/test/qualify-form.test.tsx`: disclosure, required-marker, and no-summary-payload contracts

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Obtain privacy/legal approval for the collection disclosure before production collection</name>
  <decision>Approve or replace the collection disclosure and state whether approval gates merge or production activation.</decision>
  <context>The draft promises reply-only use, no mailing-list sale/addition, and a coarse anonymous page-use summary excluding message text, exact portfolio numbers, and cross-site identifiers. Retention, processor, storage, and Kenya Data Protection Act conclusions remain owned by privacy/legal; the executor must not infer them.</context>
  <options>
    <option id="approve-merge">
      <name>Approve and merge</name>
      <pros>Ships the draft wording as the code-merge contract.</pros>
      <cons>Privacy/legal accepts every sentence before implementation.</cons>
    </option>
    <option id="approve-activation">
      <name>Gate activation</name>
      <pros>Allows implementation while keeping production collection blocked until final approval.</pros>
      <cons>Phase 5 must explicitly verify the approval before endpoint activation.</cons>
    </option>
    <option id="replace-copy">
      <name>Replace wording</name>
      <pros>Ships language supplied by the responsible privacy/legal owner.</pros>
      <cons>Requires exact replacement sentences and a merge-versus-activation gate decision now.</cons>
    </option>
  </options>
  <resume-signal>Select `approve-merge`, `approve-activation`, or provide exact replacement copy plus the chosen gate.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the approved collection notice and prove Phase 2 sends no engagement summary</name>
  <files>src/products/types.ts, src/products/haoo.ts, src/products/copy.ts, src/components/QualifyForm.tsx, src/test/qualify-form.test.tsx</files>
  <read_first>
    - `src/products/types.ts` and `src/products/haoo.ts` — extend the existing `ProductQualifyForm` data contract, not component-local HAOO copy.
    - `src/products/copy.ts` — preserve the identity guard and generic-source literal ban.
    - `src/components/QualifyForm.tsx` — render the note inside the retained form card immediately above submit and leave it mounted through failed state.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-CONTEXT.md` D-20, D-21, D-25, D-26.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md` Disclosure note, required-fields copy, colours, and all six disclosure UI considerations.
    - `src/test/qualify-form.test.tsx` — extend the exact payload-key and state contracts from prior plans.
    - The Task 1 approved wording and gate status.
  </read_first>
  <behavior>
    - Test 1: `discloses what is collected and what is required` finds the required-fields note, all optional suffixes, and both approved disclosure sentences.
    - Test 2: the submit button's `aria-describedby` includes the disclosure id and the description remains present in idle, invalid, submitting, and failed states.
    - Test 3: no label uses an asterisk and exactly the configured optional fields carry `(optional)`; conditional phone behavior from 02-04 remains correct.
    - Test 4: complete and partial request bodies contain only provider options, Source, and supplied field email labels—no engagement/context/analytics/identifier/score key or placeholder section.
    - Test 5: the message value appears only under the readable `Message` email key and nowhere in disclosure metadata or any secondary object.
  </behavior>
  <action>
Write the failing LEAD-03 test first. Extend the product qualification config with disclosure data containing the Task 1 approved wording, keeping product identity supplied through data or identity-checked copy builders per D-11. Render `All fields are required unless marked optional.` before the form card content where the UI-SPEC fixes it, and render the approved two-sentence disclosure in the `#FBFCFF` inset immediately above the submit button. Assign a stable disclosure id and include it in the button's `aria-describedby`. Keep the note visible in idle, invalid, submitting, and failed states; the form's successful replacement may remove it because the submission is complete. Preserve the derived `(optional)` suffix and conditional phone label behavior. Strengthen the payload test to enumerate allowed keys and fail on any engagement-summary, placeholder, analytic, identifier, score, or detailed-signal field. Do not add tracking, storage, campaign data, a detailed signal list, a consent checkbox, or a claim of legal compliance. D-25 is disclosure now; D-26 keeps actual summary data exclusively in Phase 3/4.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/qualify-form.test.tsx -t "discloses what is collected and what is required|posts a readable, correctly-addressed payload" && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - The required-fields instruction is visible and every optional label is derived from requiredness rather than embedded in source labels.
    - The exact human-approved two-sentence disclosure appears immediately above submit and is referenced by the submit button.
    - The disclosure remains present through validation, submitting, and recoverable provider failure.
    - Parsed payload keys are limited to the configured supplied field labels plus `_subject`, `_template`, `_captcha`, `_honey`, and `Source`.
    - No engagement summary, placeholder, score, identifier, analytics field, or detailed signal list is emitted.
    - The free-text message is sent only as its ordinary email field and is not copied into any context value.
    - Full build, unit suite, typecheck, and lint pass.
  </acceptance_criteria>
  <done>Visitors receive the approved, prominent collection explanation and required-field guidance, while executable contracts keep Phase 2 free of premature engagement-summary or analytics data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved notice -> public UI | Human-reviewed privacy wording becomes a durable promise visible to every prospect. |
| form values -> provider payload | Personal and free-text data must not leak into future context or measurement-shaped fields. |
| Phase 2 -> Phase 3 | This phase discloses a future coarse summary but must not implement its signals or storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-23 | Information Disclosure | `buildSubmissionBody` | high | mitigate | Exact-key assertions permit only provider options, Source, and supplied email labels; no analytics, identifier, score, or summary field can appear silently. |
| T-02-24 | Repudiation | public collection promise | high | transfer | Exact wording and release-gate status require privacy/legal owner approval; the implementation makes no independent compliance claim. |
| T-02-25 | Information Disclosure | free-text message | high | mitigate | Message is email-only under its readable key and is explicitly absent from every secondary/context value; no storage or analytics is added. |
| T-02-26 | Tampering | hidden or disconnected notice | medium | mitigate | Component test requires visible exact copy immediately above submit and a submit-button `aria-describedby` connection across all recoverable states. |
| T-02-SC | Tampering | package installs | high | mitigate | No dependency is added; the Phase 2 Package Legitimacy Audit remains an explicit zero-package decision. |
</threat_model>

<verification>
- `npm run test:unit -- --run src/test/qualify-form.test.tsx` passes all LEAD-01 through LEAD-06 component contracts.
- `npm run typecheck && npm run lint && npm test` passes.
- Privacy/legal-approved wording and its release-gate status are recorded in `02-06-SUMMARY.md` without asserting a compliance conclusion.
</verification>

<success_criteria>
- Visitors can identify required versus optional fields and understand why details are collected.
- The engagement-context disclosure is visible, plain-language, approved, and semantically connected to submit.
- Phase 2 sends only form fields plus the source note; the engagement summary remains a later-phase implementation.
</success_criteria>

<output>
Create `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-06-SUMMARY.md` when done
</output>
