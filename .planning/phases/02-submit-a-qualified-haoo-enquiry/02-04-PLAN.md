---
phase: 02-submit-a-qualified-haoo-enquiry
plan: 04
type: execute
wave: 4
depends_on: ["02-02"]
files_modified:
  - src/components/QualifyForm.tsx
  - src/products/haoo.ts
  - src/test/qualify-form.test.tsx
autonomous: true
requirements: [LEAD-01, LEAD-05]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No validation message appears on first entry or blur before the first submit attempt (D-22)."
    - "An invalid submit shows byte-identical inline and summary messages in DOM field order, focuses the summary, issues no request, and retains all values (D-22, LEAD-05)."
    - "After the first failed submit, editing a field re-validates that field and removes its message as soon as it becomes valid (D-22)."
    - "A second invalid submit re-focuses and re-announces the summary even when the error set is unchanged (D-22)."
    - "Choosing WhatsApp or Phone call makes phone required in native, ARIA, label, validation, and live-announcement behavior; choosing Email reverses the rule and clears its error (D-15)."
    - "Every validation and focus state remains keyboard-operable at 320px and 200% zoom with no truncation or inert-submit pattern (LEAD-05)."
  artifacts:
    - "src/components/QualifyForm.tsx extends the tracer's summary-container focus with correction re-validation, repeated-submit announcement, ordered errors, and conditional requiredness"
    - "src/test/qualify-form.test.tsx contains validation, focus, correction, and retained-value contracts"
  key_links:
    - "HAOO_PRODUCT.qualify.fields -> validateQualifyValues -> inline errors and ordered summary"
    - "preferredChannel change -> isFieldRequired(phone) -> required/aria-required/label/status message"
    - "submit attempt counter -> summary focus effect -> repeat announcement"
---

<objective>
Turn the full ten-field form into an accessible correction loop that explains every problem and never discards work.

Purpose: satisfy D-15, D-22, LEAD-01, and LEAD-05 with explicit validation timing, error-summary focus, conditional contact reachability, and retained values.

Output: production validation behavior plus deterministic component tests for every recovery edge.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-CONTEXT.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-RESEARCH.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-VALIDATION.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-02-SUMMARY.md
@src/components/QualifyForm.tsx
@src/test/qualify-form.test.tsx
</context>

## Artifacts this phase produces

- `src/components/QualifyForm.tsx`: tracer error-summary contract extended with per-field correction validation, attempt-counter repeat-focus loop, and conditional phone requiredness
- `src/test/qualify-form.test.tsx`: `retains entered values`, validation timing, repeat-announcement, and phone-requiredness tests

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tracer validation with correction re-validation and repeat announcement</name>
  <files>src/products/haoo.ts, src/components/QualifyForm.tsx, src/test/qualify-form.test.tsx</files>
  <read_first>
    - `src/components/QualifyForm.tsx` — use the pure validator and controlled values created by 02-01; do not fork a second validation path.
    - `src/products/haoo.ts` — consume the ten fields and their locked message strings created by 02-02.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md` Validation timing, Error summary, Inline error, and validation-message tables.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-RESEARCH.md` Pattern 4 and Pitfall 9.
    - `src/test/qualify-form.test.tsx` — preserve the request/state contracts and use `fireEvent`, not an uninstalled event library.
  </read_first>
  <behavior>
    - Test 1: first typing and blur render no error before submission.
    - Test 2: submitting empty required fields renders the locked messages inline and in a `role="alert"` summary in field order, focuses its `tabIndex={-1}` container, and calls fetch zero times.
    - Test 3: summary links target the corresponding controls and every invalid control has `aria-invalid` plus `aria-describedby` for its exact inline message.
    - Test 4: correcting a field after the first failed attempt removes only that field's inline and summary error while preserving all values.
    - Test 5: a second invalid submit with the same errors focuses the summary again.
    - Test 6: appending an unconfigured option to a native select, choosing it, and submitting produces the configured field error and zero fetch calls.
    - Test 7: programmatically setting a text value beyond its configured maxLength and submitting produces its configured length error and zero fetch calls.
  </behavior>
  <action>
Write the failing extension tests first. Preserve Plan 01's summary markup, configured-message rendering, and focus target exactly: every invalid submit continues to focus the outer `tabIndex={-1}` summary container after React commits, never the first invalid control or inner alert. Extend D-22's existing `submitted` state with an incrementing invalid-attempt counter so a repeated invalid submit re-focuses and re-announces that same summary even when error keys are unchanged. On submit, call the one pure validator across the product-data field order; preserve its plan-01 checks for configured option allowlist membership and `String.prototype.length <= maxLength` so DOM manipulation cannot bypass them. In the component test, append an extra `<option>` to a native select before `fireEvent.change`, and use `fireEvent.change` to place an over-bound value into a controlled text input; each subsequent submit must show the configured error and call fetch zero times. Reuse the exact message string for the summary link and inline paragraph. After the first failed attempt only, field changes revalidate the changed field, update the existing summary in place, and retain its DOM-order links and `aria-invalid` / `aria-describedby` wiring. Do not create a second summary or focus path, do not validate on initial change or blur, do not disable submit to represent incompleteness, and do not clear any value during validation.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/qualify-form.test.tsx -t "retains entered values|validation|problem" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - An untouched invalid submit produces one summary entry per invalid required field and zero fetch calls.
    - Summary and inline messages are byte-identical and appear in the field array's DOM order.
    - The summary container, not the inner alert, is the focus target and carries `tabIndex={-1}`.
    - A repeat invalid submit triggers a new focus call even when error keys are unchanged.
    - Correcting one field removes only its error; every other entered value and error remains intact.
    - No validation message renders before the first submit attempt.
    - Manipulated select values and programmatically over-bound text values are rejected before fetch and use configured error strings.
  </acceptance_criteria>
  <done>Invalid submissions produce an accessible, focusable correction loop with exact messages and retained values, while first-entry typing stays quiet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make phone conditionally required and announce the rule change</name>
  <reversibility rating="reversible">D-15 is isolated behind `isFieldRequired` and can be changed locally without altering the provider contract.</reversibility>
  <files>src/products/haoo.ts, src/components/QualifyForm.tsx, src/test/qualify-form.test.tsx</files>
  <read_first>
    - `src/components/QualifyForm.tsx` — extend the single `isFieldRequired` seam and the persistently mounted status region.
    - `src/products/haoo.ts` — configure the phone field's generic `requiredWhen` descriptor; the component must not identify HAOO fields or option values itself.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-CONTEXT.md` D-13 through D-15.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md` Conditionally required phone and Live status region copy.
    - `src/test/qualify-form.test.tsx` — add focused behavior cases beside the validation tests from task 1.
  </read_first>
  <behavior>
    - Test 1: phone starts optional, has no required attributes, and its label contains `(optional)`.
    - Test 2: choosing WhatsApp or Phone call adds native required and `aria-required`, removes `(optional)`, and announces the locked channel-specific sentence.
    - Test 3: submitting with one of those channels and empty phone shows the locked phone-required message.
    - Test 4: changing the channel to Email reverses requiredness, restores `(optional)`, clears any phone error, and permits a valid submit without phone.
    - Test 5: a permissive valid Kenyan phone format passes; disallowed characters produce the locked format message.
    - Test 6: a synthetic product with different controller, dependent field, and trigger value changes requiredness through the same descriptor without HAOO field names or option values in `QualifyForm.tsx`.
  </behavior>
  <action>
Configure HAOO's `phone` field with `requiredWhen: { field: 'preferredChannel', values: ['WhatsApp', 'Phone call'], message: 'A phone number is now required because you asked us to reach you by {value}.' }` per D-11 and D-15. Implement `isFieldRequired(field, values)` generically: return true for base-required fields, otherwise evaluate the optional descriptor by reading `values[field.requiredWhen.field]` and testing exact membership in `field.requiredWhen.values`. QualifyForm must contain no `phone`, `preferredChannel`, `WhatsApp`, or `Phone call` decision literals; it uses the descriptor for native `required`, `aria-required`, the derived optional-label suffix, validation, error clearing, and announcement copy with `{value}` replaced by the selected controller value. Do not add a second live region. When a descriptor stops matching, clear only the dependent required error and keep its typed value. Apply the configured permissive phone format without normalising or rewriting visitor input. Add a synthetic-product test whose controller name, dependent name, trigger value, and message differ from HAOO's and prove requiredness, suffix, validator, reversal, and announcement all follow configuration.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/qualify-form.test.tsx -t "phone|required|retains entered values" && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Phone is optional by default and required for exactly the WhatsApp and Phone call selections.
    - Native requiredness, `aria-required`, label suffix, validator, and live announcement change together.
    - Returning to Email clears the phone-required error without clearing the phone value.
    - The status section still contains exactly one persistently mounted `role="status"` node.
    - Valid Kenyan formatting is accepted without E.164 normalisation; invalid characters receive the locked message.
    - `src/products/haoo.ts` owns phone's `requiredWhen` descriptor, while `QualifyForm.tsx` contains no HAOO conditional field names or option-value literals.
    - A synthetic product with a different conditional descriptor passes the same requiredness, label, validation, reversal, and announcement assertions.
  </acceptance_criteria>
  <done>The chosen contact channel is always reachable, and the phone rule is exposed consistently to sighted, keyboard, and assistive-technology users.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted controls -> client validator | Visitor-entered values and manipulated select values are evaluated before any network request. |
| validator -> accessible DOM | Error text and ids enter rendered markup and must remain escaped, stable, and correctly associated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15 | Tampering | manipulated select values | high | mitigate | Validate controlled fields against their configured option allowlists before fetch, not merely for non-empty presence. |
| T-02-16 | Denial of Service | repeated submit attempts | medium | mitigate | Invalid attempts remain synchronous and issue zero requests; repeat focus is keyed by an integer counter without stacking summaries. |
| T-02-17 | Information Disclosure | validation UI | medium | mitigate | Error surfaces render fixed configured messages, never provider internals or echoed free text. |
| T-02-18 | Spoofing | unreachable preferred channel | medium | mitigate | D-15 conditionally requires phone for WhatsApp/Phone call and tests reversal to Email. |
| T-02-SC | Tampering | package installs | high | mitigate | No packages are installed; existing native controls, React state, and Vitest contracts are sufficient. |
</threat_model>

<verification>
- `npm run test:unit -- --run src/test/qualify-form.test.tsx` passes.
- `npm run typecheck && npm run lint` passes.
- Automated tests prove validation timing, repeat focus, DOM-order summary, correction behavior, value retention, and conditional phone semantics.
</verification>

<success_criteria>
- Every invalid submission explains every problem in an announced, focusable summary and beside the affected fields.
- Entered values survive validation and subsequent correction.
- The selected contact channel is reachable, and requiredness changes are both visible and announced.
</success_criteria>

<output>
Create `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-04-SUMMARY.md` when done
</output>
