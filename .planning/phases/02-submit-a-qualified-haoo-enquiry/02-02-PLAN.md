---
phase: 02-submit-a-qualified-haoo-enquiry
plan: 02
type: execute
wave: 3
depends_on: ["02-01", "02-07"]
files_modified:
  - src/products/haoo.ts
  - src/test/qualify-data.test.ts
  - src/test/qualify-form.test.tsx
autonomous: false
requirements: [LEAD-02]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward (LEAD-02) ---
    - "A visitor can state role, organization, portfolio-size band, location and onboarding timeframe through clearly labelled controlled fields (LEAD-02, D-12, D-16, D-17, D-18, D-19)."
    - "A visitor can state a preferred contact channel as a controlled choice of WhatsApp, Phone call or Email (D-14)."
    - "Email is a required field and phone is optional by default, so name plus email alone satisfy LEAD-01's usable contact method (D-13)."
    - "Every option list ships in product data, not in JSX, and is assertable without rendering (D-11, RESEARCH Pattern 2)."
    - "The county select offers the 47 counties of the First Schedule in official code order 1-47 followed by Outside Kenya, and offers no free-text alternative (D-18)."
    - "Every select carries a leading non-selectable prompt option whose value is the empty string, so an untouched required select cannot submit a real value (RESEARCH Pitfall 9)."
    - "Optional fields carry an '(optional)' suffix inside the label and no field carries an asterisk (D-21)."
    - "Fields describing the person carry a WCAG 1.3.5 autocomplete token (name, email, tel, organization, address-level1); fields describing the enquiry carry off (RESEARCH Pitfall 10)."
    - "The optional free-text message is bounded at 1000 characters and its value is email-only - it is never written to any measurement or engagement-summary payload (D-20, D-26)."
    - "A complete valid submission carries all ten readable email labels: Full name, Email address, Preferred contact channel, Phone number, Role, Organization, Portfolio size, Location, Onboarding timeframe, Message (LEAD-04)."
    # --- UI-SPEC UI Considerations lift: select fields (8/8 explicit) ---
    - "UI/empty/select-fields: every select opens on a leading non-selectable prompt option with value empty string, so required-select validation is a value !== empty-string check."
    - "UI/loading/select-fields: selects have no skeleton or spinner; during submission each keeps its value, stays readable, and is not disabled or blanked."
    - "UI/error/select-fields: an unselected required select renders its locked Select-something message inline and in the summary."
    - "UI/populated/select-fields: the county select is populated with a fixed 48-option list rendered natively - options are never fetched, filtered or virtualised."
    - "UI/partial/select-fields: a chosen select value survives validation failure and provider failure and is not validated before the first submit attempt."
    - "UI/overflow/select-fields: the county select relies on native option rendering and must not be given a fixed width that clips the longest option name."
    - "UI/zero-one-many/select-fields: a select holds exactly one value at a time and the county list is fixed at 48 options, so there is no no-options-available state to design."
    - "UI/long-text/select-fields: long county names wrap or extend natively in the option list rather than being cut."
    # --- UI-SPEC UI Considerations lift: textarea (8/8 explicit) ---
    - "UI/empty/textarea: the textarea renders empty and stays valid when empty - it is optional, labelled (optional), and its value is email-only."
    - "UI/loading/textarea: the textarea has no skeleton or spinner; during submission it keeps its value, stays readable, and is not disabled or blanked."
    - "UI/error/textarea: the textarea has exactly one error condition - over 1000 characters - with a locked message rather than silent clipping."
    - "UI/populated/textarea: a populated textarea shows the entered text at 4 rows and scrolls internally beyond that, up to the 1000-character bound."
    - "UI/partial/textarea: a partly filled textarea keeps its value through validation failure and provider failure."
    - "UI/overflow/textarea: the textarea is resizable vertically only and scrolls internally past 4 rows."
    - "UI/zero-one-many/textarea: exactly one textarea exists, it holds at most one value, and there is no repeatable or add-another variant."
    - "UI/long-text/textarea: the textarea is bounded at 1000 characters, grows to 4 rows and then scrolls internally, never truncating silently."
  artifacts:
    - "src/products/haoo.ts HAOO_PRODUCT.qualify.fields contains exactly ten entries in the locked DOM order"
    - "src/products/haoo.ts exports KENYAN_COUNTY_OPTIONS with 48 entries"
    - "src/test/qualify-data.test.ts asserts the county, role, channel, portfolio-band and timeframe option lists byte for byte"
    - "src/test/qualify-form.test.tsx contains a test named 'renders every qualification option'"
  key_links:
    - "haoo.ts qualify.fields -> QualifyForm field rendering: the component reads the array and never hard-codes a field, so adding a field is a data change"
    - "QualifyField.emailLabel -> buildSubmissionBody payload key -> delivered email field label: this is the whole of LEAD-04's human-readable labels"
    - "QualifyField.options -> rendered option elements: a mismatch between shipped data and rendered options is what the 'renders every qualification option' contract catches"
  prohibitions:
    - statement: "The optional free-text message must NOT reach any analytics event, browser-stored value, or engagement-summary payload, in this phase or any later one. It is an email-only field by construction, not by convention."
      status: unverified
      verification: judgment
---

<objective>
Widen the proven tracer path from three fields to the full locked qualification set, entirely through product data. The component written in plan 01 remains product-generic: every remaining field, option list, prompt, autocomplete token, length bound, and the final three-group arrangement is added to `HAOO_PRODUCT.qualify` and asserted directly against the shipped data.

Purpose: LEAD-02 is the phase's information-capture requirement, and its value depends on the option lists being exactly right — a county the HAOO team cannot recognise, or a role that does not route follow-up, is worse than a free-text box. Keeping the fields as data is also what makes the component reusable for a second product (D-11).

Output: a ten-field HAOO enquiry with five closed option lists, human-verified county spelling, and data contracts that fail if the shipped list drifts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-CONTEXT.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-RESEARCH.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-01-SUMMARY.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-07-SUMMARY.md
</context>

## Artifacts this plan produces

New symbols created here (none of these exist before this plan):

- `src/products/haoo.ts`: module constants `KENYAN_COUNTY_OPTIONS`, `ROLE_OPTIONS`, `PORTFOLIO_BAND_OPTIONS`, `TIMEFRAME_OPTIONS`, `CONTACT_CHANNEL_OPTIONS`
- seven new `QualifyField` entries in `HAOO_PRODUCT.qualify.fields`: `preferredChannel`, `phone`, `organization`, `portfolioBand`, `county`, `timeframe`, `message`
- new DOM ids `qualify-preferredChannel`, `qualify-phone`, `qualify-organization`, `qualify-portfolioBand`, `qualify-county`, `qualify-timeframe`, `qualify-message`
- new test names `renders every qualification option` (in `qualify-form.test.tsx`) and `county` (in `qualify-data.test.ts`)

## Flagged assumption (spec-less probe fallback)

The edge probe classified LEAD-02 as `unclassified — review manually`, so no acceptance criterion was auto-derived and the row stays **`unresolved`**. It is surfaced here rather than dropped.

**LEAD-02 flagged assumption (unresolved):** the planner assumes the five closed option lists are *complete enough* for real HAOO follow-up routing — that no prospect segment falls outside `Landlord / Property manager / Agency / Organization / Other`, that `200+ units` is an adequate top band, and that `Outside Kenya` is a sufficient single bucket for non-Kenyan locations. None of these can be derived from the source artifacts; they come from CONTEXT D-16/D-17/D-18 as recorded choices. If a segment is missing, the failure mode is silent: a prospect picks `Other` and the routing information is lost. **Surfaced for human review; not auto-resolved and not backstopped.**

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the exact spelling of the closed option lists before they reach a real inbox</name>
  <what-built>A reviewed candidate set of the 47 Kenyan counties, portfolio bands, timeframe labels, role options, and the Outside Kenya bucket, ready to be pinned as product data.</what-built>
  <how-to-verify>
Compare the four contested county spellings and all en-dash/apostrophe codepoints in the human-check below against an authoritative Kenyan government source. Also review whether the role and Outside Kenya list shapes cover HAOO's real prospect-routing needs.
  </how-to-verify>
  <verify>
    <human-check>
Four Kenyan county names carry punctuation that varies between published transcriptions, and the authoritative KLRC First Schedule page could not be reached during research (it issued a 307 redirect to an unrelated host, which was not followed). These values will be written into emails delivered to `info@haoo.online`, and in Phase 3 into the engagement summary. A wrong character is a permanent data-quality defect in a closed list, so it is confirmed by a human before it is shipped, not after.

**Confirm each of the following against an authoritative Kenyan government county list:**

| Name as planned | Codepoint detail | Alternatives seen in the wild |
|---|---|---|
| `Taita–Taveta` | en dash U+2013 between the two words | `Taita-Taveta` (hyphen), `Taita Taveta` (space) |
| `Tharaka-Nithi` | plain hyphen U+002D | `Tharaka Nithi` (space) |
| `Elgeyo-Marakwet` | plain hyphen U+002D | `Elgeyo/Marakwet` (solidus, used on some IEBC and Gazette documents) |
| `Murang'a` | typographic apostrophe U+2019 | `Murang'a` (straight apostrophe U+0027) |

**Also confirm the non-county en dashes**, which use the same U+2013 character: the portfolio bands `1–5 units`, `6–20 units`, `21–50 units`, `51–200 units` and the timeframe `In 1–3 months`. `200+ units`, `Ready now`, `In 3+ months` and `Just exploring` contain no dash.

**Also confirm the two list-shape decisions**, which are recorded choices rather than derived facts (see the flagged assumption above):

1. Role options are exactly `Landlord`, `Property manager`, `Agency`, `Organization`, `Other` — no missing HAOO prospect segment.
2. Location is a closed county select plus a single `Outside Kenya` bucket — no county-level detail is needed for non-Kenyan prospects.

Reply with either **approved as planned** or the corrected values. If a correction is given, task 2 ships the corrected values and task 3's data contract pins them.
    </human-check>
  </verify>
  <resume-signal>Reply `approved as planned` or provide the exact corrected values and list-shape changes.</resume-signal>
  <done>A human has confirmed or corrected the four county spellings, the en-dash usage in the band and timeframe labels, and the two list-shape decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the ten-field qualification set and five closed option lists as product data</name>
  <files>src/products/haoo.ts</files>
  <read_first>
    - `src/products/haoo.ts` — the `qualify` block added in plan 01, and the module-level UPPERCASE-constant convention used by `WHATSAPP_STARTER_TEXT` and `PHONE_NUMBER`.
    - `src/products/types.ts` — the `QualifyField` and `QualifyOption` shapes; every property added here must already exist on the type (plan 01 declared the full shape, so this task adds no type).
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-UI-SPEC.md` § Copywriting Contract -> Fields, Select prompts and options, and Validation messages — every label, email label, prompt, option label, autocomplete token, max length and message string is locked there and must be reproduced exactly.
    - `src/App.tsx` lines 586-618 — the shipped `(optional)` treatment and native `autoComplete` / `maxLength` usage.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-RESEARCH.md` § Standard Stack -> Reference Data (the 47-county list in code order) and Pitfalls 9 and 10.
    - The task-1 checkpoint outcome — ship the corrected values if a correction was given.
  </read_first>
  <action>
Add five module-level option constants above `HAOO_PRODUCT`, each typed `readonly QualifyOption[]`, each entry having `value` identical to `label` so the delivered email carries the human string and no lookup table is needed:

- `CONTACT_CHANNEL_OPTIONS`: `WhatsApp`, `Phone call`, `Email`.
- `ROLE_OPTIONS`: `Landlord`, `Property manager`, `Agency`, `Organization`, `Other`.
- `PORTFOLIO_BAND_OPTIONS`: `1–5 units`, `6–20 units`, `21–50 units`, `51–200 units`, `200+ units` — the first four use an en dash U+2013.
- `TIMEFRAME_OPTIONS`: `Ready now`, `In 1–3 months`, `In 3+ months`, `Just exploring` — `In 1–3 months` uses an en dash U+2013.
- `KENYAN_COUNTY_OPTIONS`: the 47 counties of the First Schedule in official code order 1-47 — `Mombasa`, `Kwale`, `Kilifi`, `Tana River`, `Lamu`, `Taita–Taveta`, `Garissa`, `Wajir`, `Mandera`, `Marsabit`, `Isiolo`, `Meru`, `Tharaka-Nithi`, `Embu`, `Kitui`, `Machakos`, `Makueni`, `Nyandarua`, `Nyeri`, `Kirinyaga`, `Murang'a`, `Kiambu`, `Turkana`, `West Pokot`, `Samburu`, `Trans-Nzoia`, `Uasin Gishu`, `Elgeyo-Marakwet`, `Nandi`, `Baringo`, `Laikipia`, `Nakuru`, `Narok`, `Kajiado`, `Kericho`, `Bomet`, `Kakamega`, `Vihiga`, `Bungoma`, `Busia`, `Siaya`, `Kisumu`, `Homa Bay`, `Migori`, `Kisii`, `Nyamira`, `Nairobi` — followed by a 48th entry `Outside Kenya`. Use exactly the character forms confirmed in task 1.

Expand `HAOO_PRODUCT.qualify.fields` from the three tracer entries to the full ten, in this DOM order, which is also the order the email labels appear: `name`, `email`, `preferredChannel`, `phone`, `role`, `organization`, `portfolioBand`, `county`, `timeframe`, `message`. The preferred-channel select must precede the phone field so plan 04's conditional-required flip happens after the visitor chooses, never retroactively on a field they already left. Replace the tracer grouping with exactly three product-configured groups in this order: `About you` owns `name`, `email`, `preferredChannel`, and `phone`; `About your portfolio` owns `role`, `organization`, `portfolioBand`, and `county`; `Getting started` owns `timeframe` and `message`. Each field name appears once and the flattened group order equals the fields array order. QualifyForm consumes only this generic grouping metadata and renders native fieldset/legend elements; no HAOO legend or field-name knowledge belongs in JSX (D-11 and the UI-SPEC fieldset contract).

Field values, all locked by the UI-SPEC Fields table:

- `preferredChannel` — label `How should we reach you?`, emailLabel `Preferred contact channel`, control `select`, required `true`, autoComplete `off`, placeholderOption `Select a channel`, options `CONTACT_CHANNEL_OPTIONS`, requiredMessage `Select how we should reach you`.
- `phone` — label `Phone number`, emailLabel `Phone number`, control `tel`, required `false`, autoComplete `tel`, maxLength 30, lengthMessage `Shorten your phone number to 30 characters or fewer`, requiredMessage `Enter a phone number so we can reach you on the channel you chose`, formatPattern permitting digits, spaces, hyphens, parentheses and a leading plus, formatMessage `Enter a phone number using digits, spaces, or +`. Do not add `requiredWhen` here — that is plan 04.
- `organization` — label `Organization`, emailLabel `Organization`, control `text`, required `false`, autoComplete `organization`, maxLength 120, lengthMessage `Shorten your organization to 120 characters or fewer`.
- `portfolioBand` — label `How many units do you manage?`, emailLabel `Portfolio size`, control `select`, required `true`, autoComplete `off`, placeholderOption `Select a range`, options `PORTFOLIO_BAND_OPTIONS`, requiredMessage `Select how many units you manage`.
- `county` — label `Where are your properties?`, emailLabel `Location`, control `select`, required `true`, autoComplete `address-level1`, placeholderOption `Select a county`, options `KENYAN_COUNTY_OPTIONS`, requiredMessage `Select where your properties are`.
- `timeframe` — label `When would you like to start?`, emailLabel `Onboarding timeframe`, control `select`, required `true`, autoComplete `off`, placeholderOption `Select a timeframe`, options `TIMEFRAME_OPTIONS`, requiredMessage `Select when you would like to start`.
- `message` — label `Anything else we should know?`, emailLabel `Message`, control `textarea`, required `false`, autoComplete `off`, rows 4, maxLength 1000, lengthMessage `Shorten your message to 1000 characters or fewer`.

Leave the three tracer fields' values unchanged except to confirm `name` requiredMessage is `Enter your full name`, `email` requiredMessage is `Enter your email address` and formatMessage is `Enter an email address in the format name@example.com`, and `role` requiredMessage is `Select your role` with placeholderOption `Select your role`.

Do not put the `(optional)` suffix into the `label` string. The component derives it from `isFieldRequired(field, values) === false`, which is what keeps the marker and the validation rule from drifting apart, including the phone transition added in plan 04. Do not add an asterisk to any label. Add no field not listed above; in particular add no consent checkbox, no marketing opt-in and no lead-score field — none is in the phase boundary.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/qualify-data.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/products/haoo.ts` exports or declares `KENYAN_COUNTY_OPTIONS`, `ROLE_OPTIONS`, `PORTFOLIO_BAND_OPTIONS`, `TIMEFRAME_OPTIONS` and `CONTACT_CHANNEL_OPTIONS`.
    - Behavior: `HAOO_PRODUCT.qualify.fields.map(f => f.name)` equals `['name','email','preferredChannel','phone','role','organization','portfolioBand','county','timeframe','message']`.
    - Behavior: `HAOO_PRODUCT.qualify.fields.map(f => f.emailLabel)` equals `['Full name','Email address','Preferred contact channel','Phone number','Role','Organization','Portfolio size','Location','Onboarding timeframe','Message']`.
    - Behavior: the `county` field's `options` array has length 48, its first entry label is `Mombasa`, its 47th is `Nairobi` and its 48th is `Outside Kenya`.
    - Behavior: every field whose `control` is `select` has a non-empty `placeholderOption` and a non-empty `options` array.
    - Behavior: exactly three fields have `required === false` — `phone`, `organization`, `message` — and every other field has `required === true`; the count of optional fields is 3 and the count of required fields is 7.
    - Behavior: `HAOO_PRODUCT.qualify.fields.filter(f => f.autoComplete === 'off').map(f => f.name)` contains `preferredChannel`, `role`, `portfolioBand`, `timeframe` and `message`, and the person-describing fields carry `name`, `email`, `tel`, `organization` and `address-level1` respectively.
    - Behavior: no `label` string in the fields array contains the substring `(optional)` or an asterisk character.
    - Behavior: `HAOO_PRODUCT.qualify.groups.map(group => group.legend)` equals `['About you','About your portfolio','Getting started']`, flattening `fieldNames` equals the ten-field DOM order, and no field name is missing or repeated.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>All ten locked fields and all five closed option lists ship as product data with the human-confirmed character forms, and the component from plan 01 renders them without a single code change.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the shipped option lists and the ten readable email labels with data and render contracts</name>
  <files>src/test/qualify-data.test.ts, src/test/qualify-form.test.tsx</files>
  <read_first>
    - `src/test/qualify-data.test.ts` — the endpoint contract created in plan 07, whose style and explicit `vitest` imports this extension must match (`globals: false`).
    - `src/test/qualify-form.test.tsx` — the tracer contract created in plan 01, including the `renderPage()` helper, the `within(screen.getByRole('region', { name: 'Send your details' }))` scoping and the `fireEvent` usage (`@testing-library/user-event` is not installed).
    - `src/products/haoo.ts` — the option constants and field array added in task 2.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-VALIDATION.md` § Per-Task Verification Map — the two test names in this task are the exact `-t` filters recorded there.
  </read_first>
  <action>
Extend `src/test/qualify-data.test.ts` with a test named `county` asserting the shipped list against a literal expectation array written inside the test — do not derive the expectation from the source constant, or the contract proves nothing. Assert length 48, deep equality of the `label` sequence against the literal 47-county code-order list plus `Outside Kenya`, that `value` equals `label` for every entry, that every label is unique, and that no label has leading or trailing whitespace. Assert the four contested names by codepoint rather than by eye using the exact characters approved at task 1; do not force U+2019 for Murang'a if the reviewer approved U+0027, and do not force an en dash where the reviewer approved a plain hyphen. Add sibling assertions for `ROLE_OPTIONS`, `PORTFOLIO_BAND_OPTIONS`, `TIMEFRAME_OPTIONS` and `CONTACT_CHANNEL_OPTIONS` against literal expectation arrays, including the checkpoint-approved dash codepoints.

Add a test named `fields` asserting the field `name` order, the `emailLabel` order, the required/optional split, the `autoComplete` token per field, the `maxLength` values (80, 254, 30, 120, 1000), that every select has a non-empty `placeholderOption`, and that no `label` contains `(optional)` or an asterisk.

In the same data contract assert the exact three group legends and field-name memberships, uniqueness, and flattened order. Extend the render contract to query all three native groups with `getByRole('group', { name: legend })`, assert their DOM order, and assert each control is contained by the configured group. This proves the UI-SPEC's `fieldset`/`legend` structure rather than treating the legends as prose.

Extend `src/test/qualify-form.test.tsx` with a test named `renders every qualification option` that renders the page, scopes to the qualify region, and for every field in `HAOO_PRODUCT.qualify.fields` finds a control by its accessible name derived from `field.label` — allowing for the `(optional)` suffix the component appends to optional fields — and asserts: the control's tag matches the declared `control`; for selects, that the rendered `option` element count equals `options.length + 1` and that the first option has an empty `value` and text equal to `placeholderOption`; that `getAttribute('autocomplete')` equals the declared token; that `getAttribute('maxlength')` equals the declared bound where one is declared; and that `getAttribute('required')` is present exactly for the fields declared `required` at this stage. Assert every option label from every list appears in the rendered document so a truncated or filtered list fails.

Extend the existing `posts a readable, correctly-addressed payload` test so it fills all ten controls with valid values and asserts the parsed body key set sorted equals the ten locked email labels plus `Source`, `_subject`, `_template`, `_captcha` and `_honey`. Add one assertion that an untouched optional field is absent from the payload entirely rather than present with an empty string.

Every test file must import `describe`, `expect`, `it` (and `vi`/`afterEach` where used) explicitly from `vitest`.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/qualify-data.test.ts src/test/qualify-form.test.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/qualify-data.test.ts` contains test names `county` and `fields`, and `src/test/qualify-form.test.tsx` contains a test named `renders every qualification option`.
    - CLI: `npm run test:unit -- --run src/test/qualify-data.test.ts -t "county"` exits 0 and reports at least one passing test.
    - CLI: `npm run test:unit -- --run src/test/qualify-form.test.tsx -t "renders every qualification option"` exits 0 and reports at least one passing test.
    - Behavior: changing any single county label in `src/products/haoo.ts` makes `qualify-data.test.ts` fail — the expectation is a literal array in the test, not a re-read of the source constant.
    - Behavior: removing any option from any shipped list makes `renders every qualification option` fail because the rendered option count no longer equals `options.length + 1`.
    - Behavior: the rendered form exposes exactly three native fieldset groups named `About you`, `About your portfolio`, and `Getting started` in that DOM order, with every control inside its configured group.
    - Behavior: the payload assertion fails if an untouched optional field is emitted as an empty string.
    - CLI: `npm test` exits 0 and the reported test count is strictly greater than the plan-01 baseline.
  </acceptance_criteria>
  <done>The shipped option lists and the ten readable email labels are pinned by contracts that fail on drift, including codepoint-level assertions on the four contested county names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser -> select and textarea values | Closed selects carry no untrusted free text; two fields (`organization`, `message`) do. |
| QualifyForm -> formsubmit.co | Ten field values now cross to a third-party processor instead of three. |
| Phase 2 field values -> Phase 3 measurement | A future phase will read coarse bands from this data; the free-text fields must never be readable by it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | `organization` and `message` free-text fields | medium | mitigate | Bounded by native `maxLength` (120 / 1000) declared in product data and re-checked in `validateQualifyValues`; React escapes on render and the provider escapes on email composition; `dangerouslySetInnerHTML` remains guard-forbidden. No client-side sanitiser is added — there is no server and nothing to sanitise for. |
| T-02-09 | Information Disclosure | `message` value crossing into Phase 3 measurement | high | mitigate | D-20 makes the field email-only. `buildSubmissionBody` is the single consumer of field values and writes only into the provider payload; the prohibition in `must_haves` records the constraint for the Phase 3 planner and the verifier. |
| T-02-10 | Information Disclosure | closed selects vs free text for role, portfolio and location | medium | mitigate | D-16/D-17/D-18 make five of the ten fields closed lists, so exact portfolio counts and precise addresses cannot be entered at all. This is a design-level reduction of what personal data can exist, not a filter applied afterwards. |
| T-02-11 | Repudiation | option-list drift | medium | mitigate | Literal-array data contracts with codepoint assertions on the four contested county names, plus a blocking human checkpoint before the values ship, so a wrong value cannot reach a real inbox unnoticed. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no packages and adds no import. `02-RESEARCH.md` § Package Legitimacy Audit records an empty table; any proposed dependency re-opens the gate with a blocking human checkpoint. |
</threat_model>

<verification>
- `npm test` exits 0 with a strictly higher test count than the plan-01 baseline.
- `npm run typecheck` and `npm run lint` exit 0.
- `npm run test:unit -- --run src/test/qualify-data.test.ts -t "county"` proves the 48-option list matches the human-confirmed characters.
- `npm run test:unit -- --run src/test/qualify-form.test.tsx -t "renders every qualification option"` proves every shipped option reaches the DOM.
- `npm run test:unit -- --run src/test/product-shell-reuse.test.tsx` still passes, proving no HAOO literal leaked into a product-generic source while the field set grew.
</verification>

<success_criteria>
- A visitor can state role, organization, portfolio-size band, county and onboarding timeframe through labelled controlled fields, and choose a preferred contact channel.
- Every option list ships in product data and is pinned by a literal-expectation contract.
- The county list is the 47 First Schedule counties in code order plus `Outside Kenya`, with human-confirmed punctuation.
- A complete submission carries ten readable email labels and omits untouched optional fields.
- The plan-01 component required no code change to render the widened field set.
</success_criteria>

<output>
Create `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-02-SUMMARY.md` when done
</output>
