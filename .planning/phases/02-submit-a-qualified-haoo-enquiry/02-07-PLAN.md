---
phase: 02-submit-a-qualified-haoo-enquiry
plan: 07
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/vite-env.d.ts
  - src/test/qualify-data.test.ts
  - src/test/focus-contrast.test.ts
  - src/test/product-shell-reuse.test.tsx
autonomous: true
requirements: [LEAD-04, LEAD-06]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The endpoint resolver trims VITE_HAOO_FORM_ENDPOINT and accepts it only when it is an absolute https FormSubmit AJAX URL with exactly one decoded, nonblank target segment; undefined, empty, whitespace-only, malformed, non-https, wrong-host, wrong-path, bare-route, encoded-empty-like, encoded-slash, extra-segment, credentialed, query, and fragment values all resolve to the documented fallback (D-04, LEAD-04)."
    - "The endpoint contract proves HAOO_PRODUCT.qualify.endpoint is non-empty, absolute, https, hosted by formsubmit.co, and has exactly `/ajax/{target}` with one decoded nonblank target before later plans expand the field data."
    - "QualifyForm.tsx is registered with both the Phase 1 focus-contrast scan and the case-insensitive product-generic source scan without expanding either guard's allowlist (D-11, LEAD-06)."
    - "ImportMetaEnv declares the optional public VITE_HAOO_FORM_ENDPOINT value consumed by the tracer, while deployment documentation continues to treat it as obfuscation rather than secrecy (D-04)."
  artifacts:
    - "src/vite-env.d.ts declares ImportMetaEnv.VITE_HAOO_FORM_ENDPOINT"
    - "src/test/qualify-data.test.ts asserts endpoint resolution and the tracer product-data invariants"
    - "src/test/focus-contrast.test.ts includes src/components/QualifyForm.tsx in FOCUS_SOURCES"
    - "src/test/product-shell-reuse.test.tsx includes src/components/QualifyForm.tsx in GENERIC_PRODUCT_SOURCES"
  key_links:
    - "VITE_HAOO_FORM_ENDPOINT typing -> haoo.ts resolveQualifyEndpoint -> HAOO_PRODUCT.qualify.endpoint"
    - "resolveQualifyEndpoint exact `/ajax/{target}` parser + decoded-target validation -> invalid-input table -> QUALIFY_ENDPOINT_FALLBACK: a route prefix, blank/encoded-empty-like target, or unsafe build input cannot become the request destination"
    - "QualifyForm.tsx -> FOCUS_SOURCES and GENERIC_PRODUCT_SOURCES: the tracer component remains inside inherited accessibility and reuse boundaries"
---

<objective>
Pin the tracer's endpoint boundary and register its new component with every inherited Phase 1 guard before any field or navigation expansion begins.

Purpose: keep Plan 01 to its seven-file end-to-end production path while making D-04 endpoint safety and D-11/LEAD-06 guard coverage independently executable and explicit.

Output: build-time env typing, table-driven endpoint contracts, and suite-wide focus/product-generic source registration.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-CONTEXT.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-RESEARCH.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-PATTERNS.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-VALIDATION.md
@.planning/phases/02-submit-a-qualified-haoo-enquiry/02-01-SUMMARY.md
@src/products/haoo.ts
@src/components/QualifyForm.tsx
@src/test/focus-contrast.test.ts
@src/test/product-shell-reuse.test.tsx
</context>

## Artifacts this phase produces

- `src/vite-env.d.ts`: explicit optional `VITE_HAOO_FORM_ENDPOINT` typing
- `src/test/qualify-data.test.ts`: endpoint-resolution and initial product-data contracts
- `src/test/focus-contrast.test.ts`: `QualifyForm.tsx` focus-source registration
- `src/test/product-shell-reuse.test.tsx`: `QualifyForm.tsx` generic-source registration

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin endpoint resolution and close the inherited guard registrations</name>
  <files>src/vite-env.d.ts, src/test/qualify-data.test.ts, src/test/focus-contrast.test.ts, src/test/product-shell-reuse.test.tsx</files>
  <read_first>
    - `src/vite-env.d.ts` — keep the existing Vite client reference and add only the public endpoint typing required by D-04.
    - `src/products/haoo.ts` — consume the `QUALIFY_ENDPOINT`, `QUALIFY_ENDPOINT_FALLBACK`, `resolveQualifyEndpoint`, and three-field `HAOO_PRODUCT.qualify` delivered by 02-01; do not move or duplicate runtime resolution into a test helper.
    - `src/test/focus-contrast.test.ts` — preserve `RING_COLOR_TOKENS` and extend the data-driven `FOCUS_SOURCES` list.
    - `src/test/product-shell-reuse.test.tsx` — preserve the synthetic ZENITH qualification data added by 02-01 and extend `GENERIC_PRODUCT_SOURCES` only.
    - `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-RESEARCH.md` Pitfalls 6–8 and Security Domain V9.
  </read_first>
  <behavior>
    - Test 1: `HAOO_PRODUCT.qualify.endpoint` is a non-empty absolute `https://formsubmit.co/ajax/{target}` URL with one decoded, nonblank target segment.
    - Test 2: undefined, empty, whitespace-only, malformed, http, wrong-host, wrong-path, bare `/ajax`, `/ajax/`, `/ajax//`, `/ajax/%20`, `/ajax/%09`, `/ajax/%2F`, malformed percent encoding, extra path segments, credentials, query, and fragment configured values resolve to the readable-address fallback.
    - Test 3: whitespace-padded valid readable-address and random-token URLs are trimmed and preserved with explicit expected resolution values.
    - Test 4: the tracer's field names and email labels are non-empty and unique, and subject/source strings retain the approved identities.
    - Test 5: the focus scan finds at least one approved 3:1 ring/offset pairing in QualifyForm without a new named ring token.
    - Test 6: the generic-source scan rejects any case-insensitive HAOO literal introduced into QualifyForm.
  </behavior>
  <action>
Keep the existing triple-slash Vite reference in `src/vite-env.d.ts`; add `ImportMetaEnv` with optional readonly `VITE_HAOO_FORM_ENDPOINT?: string` and `ImportMeta` with readonly `env: ImportMetaEnv`, matching D-04's public build-time configuration boundary.

Create `src/test/qualify-data.test.ts` with explicit Vitest imports (`globals: false`). Add a test titled `endpoint` that parses `HAOO_PRODUCT.qualify.endpoint` and asserts a non-empty string, protocol `https:`, host `formsubmit.co`, empty username/password/search/hash, and a pathname that matches exactly `/ajax/{target}`. Decode the single target with `decodeURIComponent`, assert it remains non-empty after `trim()` and contains no slash, and fail the test if decoding throws. Exercise `resolveQualifyEndpoint` with one table whose rows carry `configuredValue` and an explicit `expectedResolution`: include `undefined`, empty and whitespace-only strings, a malformed URL, `http:`, wrong host, wrong path, bare `https://formsubmit.co/ajax`, `https://formsubmit.co/ajax/`, `/ajax//`, encoded spaces and tabs (`%20`, `%09`), encoded slash (`%2F`), malformed percent encoding, extra path segments, credentials, query, and fragment; every invalid row expects `https://formsubmit.co/ajax/info@haoo.online`. Include whitespace-padded `https://formsubmit.co/ajax/info@haoo.online` and a representative one-segment random token as valid rows whose trimmed values are preserved exactly. This table must distinguish the route prefix from a usable D-04/LEAD-04 recipient and token. Pin the tracer data by checking the HAOO subject, ZERO-PAPER HUB source note, and non-empty unique field names/email labels. These assertions verify the resolver delivered in 02-01; do not add a nullish-only fallback, accept arbitrary https hosts, or reduce pathname validation to `startsWith('/ajax/')`.

Add `src/components/QualifyForm.tsx` to `FOCUS_SOURCES` and `GENERIC_PRODUCT_SOURCES`. Do not add a named focus token to `RING_COLOR_TOKENS`; fix the component utility if the existing arbitrary-hex/white pair does not pass. Do not weaken the literal ban, add an exception, or remove the synthetic ZENITH render. This closes the inherited D-11 and LEAD-06 boundaries before plans 02 and 03 modify these same test files.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/qualify-data.test.ts src/test/focus-contrast.test.ts src/test/product-shell-reuse.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/vite-env.d.ts` contains `ImportMetaEnv.VITE_HAOO_FORM_ENDPOINT` without removing the Vite client reference.
    - The endpoint table covers undefined, blank, whitespace-only, malformed URL/encoding, non-https, wrong-host, wrong-path, bare `/ajax`, `/ajax/`, empty/encoded-empty-like targets, encoded slash, extra segment, credentials, query, fragment, valid readable-address, and valid-token inputs with an explicit expected resolution for every row.
    - Every invalid configured value resolves to the readable-address fallback; valid readable-address and random-token inputs are trimmed and retained.
    - `FOCUS_SOURCES` and `GENERIC_PRODUCT_SOURCES` both contain `src/components/QualifyForm.tsx`.
    - `RING_COLOR_TOKENS` retains only its existing `white` and `blue-700` named entries.
    - Focus, genericity, endpoint, and typecheck commands exit zero.
  </acceptance_criteria>
  <done>The tracer endpoint is pinned to an exact https FormSubmit `/ajax/{target}` destination with one decoded nonblank target segment, and QualifyForm participates in the Phase 1 focus and product-generic regression guards before expansion plans run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions/Vite env -> `haoo.ts` | Untrusted or blank build configuration selects the public cross-origin submission destination. |
| QualifyForm source -> inherited test guards | New interactive/provider code must remain within existing accessibility and product-generic boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `fetch` to `qualify.endpoint` | high | mitigate | `qualify-data.test.ts` asserts the resolved endpoint protocol is exactly `https:` so submitted personal data cannot travel in cleartext. |
| T-02-02 | Denial of Service | `resolveQualifyEndpoint` | high | mitigate | Table-driven tests prove missing, blank, whitespace-only, malformed, non-https, wrong-host/path, bare AJAX-route, decoded-empty-like, encoded-slash, extra-segment, credentialed, query, and fragment values all select the non-empty fallback; valid readable-address and token targets remain accepted. |
| T-02-27 | Tampering | focus/generic source registries | high | mitigate | The new component is added to both existing data-driven scans without allowlist expansion or product-name exceptions. |
| T-02-SC | Tampering | package installs | high | mitigate | No dependency is added; the Phase 2 Package Legitimacy Audit remains an explicit zero-package decision. |
</threat_model>

<verification>
- `npm run test:unit -- --run src/test/qualify-data.test.ts src/test/focus-contrast.test.ts src/test/product-shell-reuse.test.tsx` passes.
- `npm run typecheck && npm run lint` passes.
- The plan touches only its four declared files and leaves the production tracer implementation owned by 02-01.
</verification>

<success_criteria>
- Unsafe, blank, bare-route, encoded-empty-like, or multi-segment build configuration cannot become the submission destination; valid readable-address and random-token targets remain usable.
- The Vite endpoint key has an explicit optional public type.
- QualifyForm is covered by inherited focus-contrast and product-generic source contracts.
- Plans 02 and 03 can safely extend the registered test files after this plan completes.
</success_criteria>

<output>
Create `.planning/phases/02-submit-a-qualified-haoo-enquiry/02-07-SUMMARY.md` when done
</output>
