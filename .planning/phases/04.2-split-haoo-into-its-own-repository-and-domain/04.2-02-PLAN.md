---
phase: 04.2-split-haoo-into-its-own-repository-and-domain
plan: 02
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified:
  - "HAOO: index.html (moved from products/haoo/index.html)"
  - "HAOO: CNAME"
  - "HAOO: vite.config.ts"
  - "HAOO: src/App.tsx"
  - "HAOO: src/test/build-output.test.ts"
  - "HAOO: src/test/focus-contrast.test.ts"
  - "HAOO: src/test/product-shell-reuse.test.tsx"
  - "HAOO: scripts/assert-phase1-red.mjs"
  - "HAOO: .github/workflows/deploy.yml"
  - ".planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-DEFERRED-ITEMS.md"
autonomous: true
requirements: [SPLT-01, SPLT-02, PROD-05, PROD-06, QUAL-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor can open the HAOO page at the HAOO production host and refresh it, and the document served is a physical file at that path."
    - "The HAOO repository's history contains every commit the source repository's history contains, so `git log --follow` and `git blame` reach the Phase 1 through 04.1 rationale from the code it constrains, and every commit SHA cited across `.planning/` still resolves."
    - "The HAOO repository builds, typechecks, lints and passes its full build-first suite with no ZERO-PAPER HUB source in the tree."
    - "Every suite inventory that named a pruned file was narrowed in the same commit that pruned it, and every case that could not survive names its successor."
    - "The published HAOO document's canonical URL, both Open Graph URLs and the Twitter image all name the same recorded HAOO host, and the `CNAME` file pins that same host."
  artifacts:
    - "HAOO repository at KaruguDev/<name recorded in 04.2-SPLIT-CONTRACT.md>, with a green Pages deployment on the HAOO host"
    - "HAOO: index.html — the published HAOO document at the site root"
    - "HAOO: CNAME pinned to the recorded host"
    - ".planning/phases/04.2-.../04.2-DEFERRED-ITEMS.md — every narrowing and withdrawal from task 1, recorded"
  key_links:
    - "CNAME literal → the `uploads exactly the built dist tree` case, which is the cheapest guard against the canonical, the social URLs and the card link disagreeing about one hostname."
    - "`PRODUCT_SOURCE_BOUNDARY` non-empty guard → the atomicity requirement: it goes red the instant a mapped source leaves the tree, so pruning and narrowing are one commit."
    - "Deleted product registry → the shell-reuse case that iterated it, which is why PROD-06's withdrawal lands in this commit and not a later one (D-05)."
  prohibitions:
    - "Never clone with `--depth`. A shallow clone has no history to preserve and defeats D-01 entirely."
    - "Never use a history-rewriting tool. Every SHA must survive (D-01, D-02)."
    - "Never weaken an assertion to make a move fit. A case either moves, narrows with its removal recorded, or is retired with a named successor — nothing is deleted silently (SC6)."
    - "Never run the unit-only test script to validate this work. It skips the build, so every artifact assertion would validate a stale build output."
    - "Do not push over HTTPS with the `gh` token — it lacks the `workflow` scope and will refuse the deploy workflow file. Push over SSH."
    - "Do not narrow or re-point the four product asset paths, the PDF alternate link, the five recovery links, or the two SHA-256 pins. Under the recorded default they are deliberately unchanged."
---

<objective>
This is the phase's tracer: one HAOO page, wired through every layer the split touches — git history,
source tree, build config, test suite, CI workflow, Pages claim and DNS — and proved end to end on
the HAOO production host before any expansion plan starts.

It is production-quality, not a prototype. The suite that guards it is the same suite at the same
strength, and the deployment it produces is the one that ships permanently. What it deliberately
leaves for expansion is breadth, not depth: the remaining URL literals in the test fixtures
(04.2-03), the data-controller sentence (04.2-04), the report's cutover bound (04.2-05) and the
whole ZERO-PAPER HUB side (04.2-06 onward).

Purpose: prove the two-repository architecture works before ten layers of it are committed.
Output: a live HAOO site on its own domain, from its own repository, with its own green suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-SPLIT-CONTRACT.md
@.planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-CONTEXT.md
@.planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-PATTERNS.md
@.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/deferred-items.md
</context>

<!-- planner-discipline-allow: HomePage -->
<!-- planner-discipline-allow: downloadCompanyProfile -->
<!-- planner-discipline-allow: NAV_LINKS -->
<!-- planner-discipline-allow: three public build variables -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Clone without rewriting, prune the ZERO-PAPER HUB half, and narrow every inventory that named a pruned file — one commit</name>
  <reversibility rating="one-way">D-01, already rated one-way and locked by the owner in CONTEXT.md: once the pruned clone is pushed and worked on, re-deriving a different history shape means rewriting every SHA in the new repository, and `.planning/` cites commit SHAs directly. No new gate is inserted — plan 04.2-01 task 2 already put the repository choice to the owner.</reversibility>
  <precondition>04.2-01 task 1 is cleared (the HAOO domain returns 404 at the Pages edge and both challenge TXT records resolve) and 04.2-01 task 3 is committed, so `04.2-SPLIT-CONTRACT.md` records the host, document path, repository name and checkout path. Additionally: the local ZERO-PAPER HUB checkout has no unpushed commits — `git clone` copies `origin`, not the working state, so anything unpushed would be missing from the new repository.</precondition>
  <files>
    HAOO: index.html, CNAME, vite.config.ts, src/App.tsx,
    src/test/build-output.test.ts, src/test/focus-contrast.test.ts,
    src/test/product-shell-reuse.test.tsx, scripts/assert-phase1-red.mjs,
    and the deletion of every ZERO-PAPER HUB-only path enumerated in the action
  </files>
  <read_first>
    .planning/phases/04.2-.../04.2-SPLIT-CONTRACT.md (§Domain, §Published document path, §Repository),
    src/App.tsx lines 655-661 (the branch that disappears) and lines 140-145 (the contact constants that stay behind),
    products/haoo/index.html (all 49 lines — the document being moved),
    src/test/build-output.test.ts lines 20-62 (the path and URL constants), lines 129-158 (the per-file boundary map), lines 195-215 (the build input list), lines 550-600 (the physical-document and metadata cases), lines 742-757 (the artifact and boundary cases),
    src/test/focus-contrast.test.ts lines 27-42 (the closed focus-source list and its doc comment),
    src/test/product-shell-reuse.test.tsx lines 190-300 (the synthetic-product cases that become the named successors, and the registry-iterating case that cannot survive),
    scripts/assert-phase1-red.mjs lines 1-20 (the suite list and marker list),
    src/measurement/posthog.ts lines 91-108 (the project's canonical in-code withdrawal shape — reproduce all five of its elements),
    .planning/phases/04.1-.../deferred-items.md (the per-entry heading shape for recording a narrowing out of band)
  </read_first>
  <behavior>
    Test expectations that must hold at the end of this single commit, in the HAOO repository:
    - The build-first suite is green in full. Not one file, not the unit-only script — the whole suite after a real build.
    - The physical-document case still passes against the document's new published path, under a renamed case name that no longer claims the document is nested.
    - The root metadata case asserts the HAOO document's own title, description, canonical, Open Graph type/title/url/image, site name, Twitter card and favicon — from BOTH the source and the built HTML in one loop, so a build that drops a tag fails — and still asserts the scaffolding-vendor negative check.
    - The per-file boundary case passes with the map narrowed to sixteen keys; its non-empty source guard still fires for every remaining key.
    - The focus-contrast case passes with six sources listed, and its own `pairs.length > 0` guard still holds.
    - The shell-reuse suite passes with the registry-iterating case retired and the three synthetic-product cases green, including the one asserting the rendered synthetic product's text contains no HAOO name.
    - The build-freshness case passes, which is only possible if the build input list no longer names a deleted path or the same path twice.
    - The artifact case passes with the `CNAME` literal equal to the recorded HAOO host.
  </behavior>
  <action>
Everything below lands in ONE commit in the new repository. This is not stylistic: the per-file
boundary map's non-empty source guard goes red the instant a mapped source leaves the tree, so
deleting sources and narrowing the map are the same operation. Splitting it produces a red tree at
every intermediate step.

**Step A — stand the repository up without rewriting history (D-01, D-02, D-03).**
Push any local commits in the ZERO-PAPER HUB checkout first. Create the repository named in the
split contract, empty, with no push flag. Full-clone the source repository over SSH into the sibling
checkout path the contract records; then re-point that clone's `origin` at the new repository.
Record `git rev-list --count HEAD` from both checkouts and assert equality before touching a file:
that equality, not a memorized number, is the proof nothing was rewritten. Do not pass a depth flag,
and do not reach for a history-rewriting tool — both defeat the decision this step exists to honour.
`.planning/` arrives whole with the clone and stays (D-03).

**Step B — delete the ZERO-PAPER HUB half.** `git rm` exactly these paths:
the site root document; the products-section component; the product registry module; the
products-section test; the four files under the marketing asset directory; the two company-branded
images at the public root; the two inert static-host files (a rewrite config and a duplicate domain
text file, both no-ops on this host); and the two scaffolding-tool files under the hidden tool
directory. Nothing else.

**Step C — the product shell stays whole and moves to the site root (D-04, split contract
§Published document path).** `git mv` the HAOO document to the repository root so it becomes the
site's only published document, and make `vite.config.ts` carry exactly one build input pointing at
it. Delete the `data-page` attribute from the document's `body` element: with one document per
repository the runtime branch is gone, and shrinking the branch instead of removing it is the
anti-pattern the refreshed architecture map names. Correspondingly, `src/App.tsx` keeps only the
default export returning the product page for the HAOO product, plus its imports; delete the home
page component, the company-profile download function, the three static content collections, the
scroll-reveal hook and the two contact-form constants — they belong to the other repository and
travel there in plan 04.2-06.

Leave the four product asset paths, the PDF alternate link, the five recovery links, the brochure
download name and both SHA-256 pins **exactly as they are**. Under the recorded default the assets
stay at their existing relative path so a bookmarked URL maps one-to-one across the two hosts;
changing them here would be a large fan-out bought for nothing.

**Step D — the favicon coupling.** The document's icon link points at a company-branded image at the
public root, which step B deleted. Re-point it at the HAOO logo already published under the product
asset directory. Retain the `og:site_name` value naming the parent company unchanged — D-07 keeps
the relationship visible, and this is a decision, not an oversight, so say so in one comment.

**Step E — pin the host.** Set `CNAME` to the host recorded in the split contract. Re-point the
document's canonical link, both Open Graph URLs and the Twitter image to that host, and change the
matching byte-exact constants in the build-output suite to match. Change the constants; never loosen
an assertion to a substring that both hostnames would satisfy.

**Step F — narrow every inventory that named a pruned file, and record each narrowing.** Six of
them, and the rule throughout is the one Phase 2 to Phase 3 established: narrow per file, never
delete the underlying regex group.
1. The per-file boundary map loses its two keys for the pruned component and the pruned registry
   module. The always-forbidden and full-boundary regex groups are untouched and keep applying to
   the sixteen remaining keys.
2. The closed focus-source list loses its one entry for the pruned component. Keep its doc comment
   and extend it to record what was removed and why; the case's own non-empty pair guard must still
   hold with six sources.
3. The red-marker assertion script loses the pruned test file and its marker, leaving three. Record
   this as a narrowing with the ZERO-PAPER HUB successor named — plan 04.2-06 gives that side its
   own single-marker version.
4. The shell-reuse suite loses its import of the pruned registry and its case that iterated the
   registered collection. Note first that a *second* case in that file also reaches through the
   registry — the literal-rejection case takes the product name from the collection's first element.
   That case is one of PROD-06's three named successors and must stay green, so re-source its name
   from the product definition module directly rather than through a collection that no longer
   exists. Then the iterating case: it cannot survive, because with one product the claim it made is
   vacuous rather than false. Retire it in place with the withdrawal shape this project already uses,
   reproducing all five load-bearing elements of the canonical in-code example: first line naming
   this plan and the successors, the old claim stated in full, what stopped being true and why, what
   the successors prove instead, and why it is retained rather than deleted. Name all three
   successors explicitly — the synthetic-product render case, the byte-for-byte product-name-string
   case, and the case rejecting product-name literals in product-generic executable source. This is
   D-05, and it lands in this commit precisely because this is the commit that makes the shell
   single-product.
5. The build input list must stop naming the deleted root document and must stop naming the moved
   document twice now that the two path constants resolve to the same file. Collapse the two
   source/built HTML constant pairs into one pair. The freshness case is what will tell you if you
   got this wrong.
6. The two metadata cases now describe the same document. Rename the product-metadata case to
   `publishes first-party HAOO root canonical and social metadata`, and have it absorb the two
   assertions the retired root-metadata case carried that it does not already have: that the icon
   asset exists in both the public tree and the built output, and the scaffolding-vendor negative
   check. Then retire the old root-metadata case with that renamed case named as its successor, and
   remove the now-duplicate root title, description, URL and image constants, recording their
   removal. Rename the physical-document case so it no longer claims the document is nested; the
   red marker inside it does not change.

**Step G — prove it.** Install from the lockfile in the new checkout, then run typecheck, lint and
the build-first suite. The build-first script is mandatory here: the unit-only script skips the
build, and this commit changes the build input set, both HTML path constants and the build config,
so a unit-only pass would be a pass against a stale build output and would prove nothing.
  </action>
  <verify>
    <automated>cd "$HAOO_CHECKOUT" &amp;&amp; test "$(git rev-list --count HEAD~1)" = "$(git -C "$ZPH_CHECKOUT" rev-list --count origin/main)" &amp;&amp; echo "history preserved"</automated>
    <automated>cd "$HAOO_CHECKOUT" &amp;&amp; npm ci &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm test</automated>
    <automated>cd "$HAOO_CHECKOUT" &amp;&amp; npx vitest run src/test/build-output.test.ts -t 'narrowed static boundary'</automated>
    <automated>cd "$HAOO_CHECKOUT" &amp;&amp; npx vitest run src/test/build-output.test.ts -t 'publishes first-party HAOO root canonical and social metadata'</automated>
    <automated>cd "$HAOO_CHECKOUT" &amp;&amp; npx vitest run src/test/build-output.test.ts -t 'uploads exactly the built dist tree'</automated>
    <automated>cd "$HAOO_CHECKOUT" &amp;&amp; npx vitest run src/test/product-shell-reuse.test.tsx</automated>
    <automated>cd "$HAOO_CHECKOUT" &amp;&amp; npx vitest run src/test/focus-contrast.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git -C "$HAOO_CHECKOUT" rev-list --count HEAD~1` equals `git -C "$ZPH_CHECKOUT" rev-list --count origin/main` — the clone rewrote nothing.
    - `git -C "$HAOO_CHECKOUT" log --follow --oneline src/products/haoo.ts | wc -l` is greater than 20, proving history follows the moved product definition back through prior phases.
    - `git -C "$HAOO_CHECKOUT" cat-file -e 7ba2c89^{commit}` exits 0 — a SHA cited in `.planning/` still resolves in the new repository.
    - `cd "$HAOO_CHECKOUT" && npm run typecheck && npm run lint && npm test` all exit 0.
    - `git -C "$HAOO_CHECKOUT" grep -l -E 'HomePage|downloadCompanyProfile|NAV_LINKS' -- src/ | wc -l` equals 0.
    - `test -f "$HAOO_CHECKOUT/index.html" && ! test -e "$HAOO_CHECKOUT/products/haoo/index.html"` — the document is at the site root and nowhere else.
    - `test -f "$HAOO_CHECKOUT/dist/index.html"` after the build, and `grep -c 'rel="canonical"' "$HAOO_CHECKOUT/dist/index.html"` equals 1.
    - `! grep -q 'zero-paperhub.com' "$HAOO_CHECKOUT/index.html"` — no URL in the published document names the other host.
    - `cat "$HAOO_CHECKOUT/CNAME"` equals the host recorded under `## Domain` in `04.2-SPLIT-CONTRACT.md`, with no trailing content.
    - `! test -e "$HAOO_CHECKOUT/src/products/registry.ts" && ! test -e "$HAOO_CHECKOUT/src/components/ProductsSection.tsx" && ! test -e "$HAOO_CHECKOUT/src/test/products-section.test.tsx"`.
    - `grep -c "'src/" "$HAOO_CHECKOUT/src/test/build-output.test.ts"` inside the boundary map block yields 16 keys — verified by running the boundary case, whose non-empty source guard fails loudly on any stale key.
    - `grep -c 'src/' "$HAOO_CHECKOUT/src/test/focus-contrast.test.ts"` inside the focus-source block yields 6 entries, and the focus suite is green.
    - `grep -c 'phase1-red' "$HAOO_CHECKOUT/scripts/assert-phase1-red.mjs"` counts 3 markers, and `npm run test:phase1:red` exits 0.
    - `grep -c 'WITHDRAWN by plan `04.2-02`' "$HAOO_CHECKOUT/src/test/product-shell-reuse.test.tsx"` is at least 1, and that comment block names all three successor case titles verbatim.
    - `git -C "$HAOO_CHECKOUT" log -1 --name-only | wc -l` shows the prune, the move, the config change and all six inventory narrowings in a single commit — no intermediate commit exists in which the suite is red.
    - The two SHA-256 pins in the build-output suite are byte-unchanged: `git -C "$HAOO_CHECKOUT" diff HEAD~1 HEAD -- src/test/build-output.test.ts | grep -c '^[-+].*SHA256' ` equals 0.
  </acceptance_criteria>
  <done>
The HAOO repository exists, holds the complete unrewritten history of the source repository plus one
deleting commit, contains no ZERO-PAPER HUB source, publishes the HAOO document at the site root
under the recorded host, and passes typecheck, lint and the full build-first suite. Every inventory
that named a pruned file was narrowed in that same commit, and the one case that could not survive
carries an in-code withdrawal naming its three successors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Carry the deploy workflow, push over SSH, and claim the HAOO domain for this repository</name>
  <precondition>Task 1 is committed and green locally. The HAOO domain returns 404 at the GitHub Pages edge per 04.2-01's recorded evidence — if it does not, the custom-domain claim below will not take and the third-party site is still live.</precondition>
  <files>
    HAOO: .github/workflows/deploy.yml
  </files>
  <read_first>
    .github/workflows/deploy.yml (all 108 lines — the workflow name, the coverage-audit step and its restated comment, the four browser-prefixed build variable assignments and their justification block, the deliberate Build-then-unit-test ordering, and the least-privilege permissions block),
    .planning/phases/04.2-.../04.2-SPLIT-CONTRACT.md (§Domain, §Build variables — a counting correction)
  </read_first>
  <action>
**Correct the workflow for the repository it now lives in.** Two changes, no more. Rename the
workflow so it names the HAOO site rather than the parent site. Then correct the counting error the
split contract records: the justification comment says "the three public build variables" while the
step below it assigns four browser-prefixed names — the fourth, the form endpoint, is real,
deliberately unset, and falls back correctly. Say "the three PostHog build variables" or "the four
browser-prefixed assignments"; do not leave a sentence in CI that a reader can check against the
file below it and find wrong. This is the same discipline 04.1 applied when it rewrote a step's
comment in the same commit that changed what the step asserts, explicitly so a false statement would
not stand in CI.

Everything else in the workflow is correct for the HAOO repository and must survive byte-identical:
the capability-coverage step (its argument is a path under `.planning/`, which arrived whole with
the clone, so it still resolves), the four build-variable assignments (each must remain exactly one
repository-variable expression — the value-side rule is what makes the "no secrets here" paragraph
enforceable rather than merely stated), the deliberate ordering that runs the unit-only script
*after* Build so the suite scans the very artifact about to be uploaded, and the least-privilege
permissions block.

**Push, over SSH.** The available token's scopes do not include `workflow`, so an HTTPS push of a
file under the workflow directory will be refused outright. The clone's remote is already SSH from
task 1; push the branch and set upstream.

**Claim the domain for this repository.** Configure the repository's Pages site to build from the
GitHub Actions workflow and to serve the host recorded in the split contract. Use the Pages API
rather than the web UI — this half is automatable, unlike the account-level verification in 04.2-01.
Then let the workflow run and confirm the deployment completed.

Do **not** enable HTTPS enforcement here. GitHub issues the certificate asynchronously and the
option can take up to 24 hours to become available; plan 04.2-09 owns that gate and the wait. Do not
build a second recovery document for the other leg of the domain either — GitHub supplies the
counterpart redirect automatically, and hand-rolling it is one of the "use the platform's own
mechanism" cases this phase's research names.
  </action>
  <verify>
    <automated>cd "$HAOO_CHECKOUT" &amp;&amp; git push -u origin main &amp;&amp; git rev-parse HEAD = "$(git rev-parse origin/main)"</automated>
    <automated>gh api "repos/KaruguDev/$HAOO_REPO/pages" --jq '.cname' | grep -qx "$(cat "$HAOO_CHECKOUT/CNAME")"</automated>
    <automated>gh run list --repo "KaruguDev/$HAOO_REPO" --limit 1 --json conclusion --jq '.[0].conclusion' | grep -qx success</automated>
    <automated>cd "$HAOO_CHECKOUT" &amp;&amp; grep -c 'vars\.VITE_HAOO_' .github/workflows/deploy.yml | grep -qx 4</automated>
    <automated>cd "$HAOO_CHECKOUT" &amp;&amp; npx vitest run src/test/build-output.test.ts -t 'report credential out of the deploy workflow'</automated>
  </verify>
  <acceptance_criteria>
    - `git -C "$HAOO_CHECKOUT" rev-parse HEAD` equals `git -C "$HAOO_CHECKOUT" rev-parse origin/main`.
    - `gh api repos/KaruguDev/$HAOO_REPO/pages --jq '.cname'` returns exactly the contents of the repository's `CNAME` file.
    - `gh api repos/KaruguDev/$HAOO_REPO/pages --jq '.build_type'` returns `workflow`.
    - The most recent workflow run on the HAOO repository concluded `success`.
    - `grep -c 'permissions:' .github/workflows/deploy.yml` is 1 and the block still lists exactly the three least-privilege scopes — no scope was widened to make the push or the deploy work.
    - The workflow's Test step still runs the unit-only script and still appears after the Build step — the ordering that makes the suite scan the shipping artifact is unchanged.
    - `grep -c 'three public build variables' .github/workflows/deploy.yml` equals 0, and the replacement sentence names either the three PostHog variables or the four assignments.
    - `grep -c 'Deploy ZERO-PAPERHUB' .github/workflows/deploy.yml` equals 0.
  </acceptance_criteria>
  <done>
The HAOO repository's workflow names the HAOO site, states its variable count truthfully, and
retains its coverage step, its four repository-variable assignments, its Build-then-scan ordering
and its least-privilege permissions. The branch is pushed over SSH, the Pages site is configured to
serve the recorded host from the workflow, and the first deployment run concluded successfully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the deployed HAOO page end to end, and record every narrowing this plan made</name>
  <precondition>Task 2's deployment run concluded successfully. GitHub's certificate for a newly claimed custom domain can take up to 24 hours; if HTTPS is not yet available, assert over HTTP and record that the HTTPS assertion is owed to plan 04.2-09.</precondition>
  <files>
    .planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-DEFERRED-ITEMS.md
  </files>
  <read_first>
    .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/deferred-items.md (the per-entry heading shape this project uses, and D3's discipline of recording the measured red diff rather than a predicted one),
    the task 1 commit diff in the HAOO checkout — the record must be derived from what actually changed, not from what this plan predicted
  </read_first>
  <action>
**Prove the one path works.** Fetch the deployed HAOO document from the recorded host and assert
four things about the response: it answers with a success status, its title is the HAOO product
title, its canonical link names the recorded host, and the built asset it references for the
analytics vendor chunk is present in the artifact. Then fetch the brochure PDF at its published
asset path and confirm it answers. Then fetch the document a second time with a cache-busting query
to demonstrate the direct-navigation-and-refresh property is a physical file rather than a rewrite
rule — this host has no rewrite mechanism, so the property holds only because the file exists.

Record the measured outputs in the plan summary. These are the evidence for success criterion 3 and
for the amended QUAL-04.

**Record every narrowing.** Create `04.2-DEFERRED-ITEMS.md` in the phase directory and write one
entry per narrowing or withdrawal task 1 made, using this project's established per-entry headings:
what it was found during, the file it lives in, why it will fire, when it will fire, why it was not
fixed differently, and the suggested handling in the plan that owns the other side. Derive each
entry from the actual commit diff, not from this plan's prose, and record the measured before/after
for each list that shrank — the eighteen-to-sixteen boundary map, the seven-to-six focus source
list, the four-to-three marker list, the removed root metadata constants, and the retired
registry-iterating case with its three named successors.

The ZERO-PAPER HUB side's narrowings are not yet made; add a short forward-pointing section naming
plan 04.2-06 as their owner, so a reader of this file mid-phase does not conclude the other half was
forgotten.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}\n' "https://$HAOO_HOST/" | grep -qE '^(200|301)$'</automated>
    <automated>curl -sL "https://$HAOO_HOST/" | grep -q 'HAOO Property Management'</automated>
    <automated>curl -sL "https://$HAOO_HOST/" | grep -q "rel=\"canonical\" href=\"https://$HAOO_HOST/\""</automated>
    <automated>curl -sL "https://$HAOO_HOST/" | grep -qE 'assets/posthog-sdk-[A-Za-z0-9_-]+\.js'</automated>
    <automated>curl -s -o /dev/null -w '%{http_code}\n' "https://$HAOO_HOST/products/haoo/HAOO-Marketing-Brochure.pdf" | grep -qx 200</automated>
    <automated>test -f .planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-DEFERRED-ITEMS.md &amp;&amp; grep -c '^\*\*Found during:\*\*' .planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-DEFERRED-ITEMS.md | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `curl -sL https://$HAOO_HOST/` returns a document whose title is `HAOO Property Management | ZERO-PAPER HUB` and whose canonical link names the recorded host.
    - The fetched document references a built vendor analytics chunk under the assets directory, matching `assets/posthog-sdk-*.js` — the deployed build is the capturing one, not a reduced build.
    - `curl -s -o /dev/null -w '%{http_code}' https://$HAOO_HOST/products/haoo/HAOO-Marketing-Brochure.pdf` returns `200`.
    - A second fetch of the document with an arbitrary query string returns the same status and title — direct navigation and refresh both resolve.
    - `04.2-DEFERRED-ITEMS.md` exists and contains at least five entries, each with all six of the established headings (`Found during`, `File`, `Why it will fire`, `When it will fire`, `Not fixed here because`, `Suggested handling in`).
    - Each entry's before/after figures match the actual commit diff: `git -C "$HAOO_CHECKOUT" diff HEAD~1 HEAD -- src/test/build-output.test.ts src/test/focus-contrast.test.ts scripts/assert-phase1-red.mjs src/test/product-shell-reuse.test.tsx` corroborates every number written down.
    - The file names plan 04.2-06 as the owner of the ZERO-PAPER HUB-side narrowings.
    - If HTTPS was not yet available, the summary records that fact with the timestamp of the attempt and states that the HTTPS assertion is owed to plan 04.2-09.
  </acceptance_criteria>
  <done>
The HAOO page answers on its own production host with the correct title, canonical and vendor chunk;
the published brochure answers at its asset path; refresh resolves because the document is a physical
file. Every narrowing and withdrawal this plan made is recorded out of band with measured
before/after figures and a named owner for the other repository's half.
  </done>
</task>

</tasks>

<artifacts_this_plan_produces>
| Kind | Item |
|---|---|
| Repository | `KaruguDev/<name recorded in 04.2-SPLIT-CONTRACT.md>`, full history, SSH remote |
| File (moved) | HAOO `index.html` — the published HAOO document at the site root |
| File | HAOO `CNAME` — the recorded HAOO host, one literal |
| File | `.planning/phases/04.2-.../04.2-DEFERRED-ITEMS.md` |
| Renamed test case | `[phase1-red:build] emits a physical HAOO document at its published path` (was `… a physical nested HAOO document`) |
| Renamed test case | `publishes first-party HAOO root canonical and social metadata` (absorbs the retired ZPH-shaped root-metadata case) |
| Retired test case | `derives product-named shell surfaces for every registered product` — withdrawn in place, three successors named |
| Retired test case | `publishes first-party root canonical and social metadata` — successor named |
| Live endpoint | `https://<recorded HAOO host>/` serving the HAOO document from the HAOO repository |
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git remote → new repository | The clone carries every object from the source repository, including `.planning/`. Anything private in the source history is now in a second repository. |
| GitHub Actions runner → published artifact | Build variables are inlined into world-readable JavaScript by construction; the value-side rule is the only thing keeping a credential out. |
| GitHub Pages edge → the HAOO host | The claim made in task 2 is what stops the host reverting to a third party's content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-06 | Information Disclosure | the deploy workflow's Build environment, carried into a second repository | high | mitigate | The four browser-prefixed assignments must each remain exactly one repository-variable expression, never a secret, a literal or another context. The workflow-reading case in the build-output suite is run in task 2's verify against the new repository specifically — the rule is duplicated per repository, never assumed inherited. |
| T-04.2-07 | Spoofing | the HAOO custom-domain claim | high | mitigate | Task 2 claims the host for this repository via the Pages API, and task 3 proves the deployed document is this project's. The account-level verification from 04.2-01 is what prevents the claim being taken again after any future unlink. |
| T-04.2-08 | Repudiation | build-output assertions against a stale build | medium | mitigate | Task 1 runs the build-first script, never the unit-only one; this commit changes both HTML path constants, the build input list and the build config, so a unit-only pass would validate an artifact that no longer corresponds to the tree. The freshness case is the backstop. |
| T-04.2-09 | Tampering | assertion strength lost during the move | high | mitigate | Every case moves, narrows with the removal recorded, or is retired with named successors — enumerated in task 1 step F and recorded out of band in task 3. Task 3's acceptance criteria require the recorded figures to be corroborated by the actual diff, so a claimed narrowing that did not happen fails. |
| T-04.2-10 | Elevation of Privilege | the workflow's token scopes | medium | mitigate | Push over SSH rather than widening the token to `workflow` scope; the workflow's own least-privilege permissions block must survive byte-identical, asserted in task 2's criteria. |
| T-04.2-SC | Tampering | package-manager install | high | mitigate | The only install is `npm ci` from the byte-identical lockfile carried by the clone. No package is added, upgraded, or newly recommended, so the 04.1 legitimacy audit stands. If any dependency is added here, stop and run the package-legitimacy gate with a blocking human checkpoint. |
</threat_model>

<verification>
- History: commit count equality with the source, `log --follow` reaching prior phases, and a cited SHA still resolving.
- Local: `npm run typecheck && npm run lint && npm test` green in the HAOO checkout.
- Tree: no ZERO-PAPER HUB source under `src/`, no products-section component, registry module or products-section test.
- Suite: boundary map at sixteen keys, focus sources at six, red markers at three, shell-reuse green with the retired case's successors named.
- Host: `CNAME` equals the recorded host; the Pages API reports the same host and a workflow build type; the latest run concluded success.
- Live: the document answers with the HAOO title, the correct canonical and the vendor analytics chunk; the brochure answers at its asset path; refresh resolves.
- Record: at least five out-of-band entries with the six established headings, each corroborated by the diff.
</verification>

<success_criteria>
- The HAOO repository builds, typechecks, lints, tests and deploys with no ZERO-PAPER HUB source in
  its tree (SC1, first half; SPLT-01, first half).
- The published HAOO document's canonical, both Open Graph URLs and the Twitter image all name the
  recorded HAOO host, and the `CNAME` file pins the same host (SC2, SPLT-02, PROD-05).
- Direct navigation and refresh work on the HAOO production host for the document and the published
  brochure asset (SC3, QUAL-04).
- Every invariant that could not survive unchanged was withdrawn with a named successor in the same
  commit, never silently dropped (SC6, PROD-06 successor, D-05).
- Every commit SHA cited in `.planning/` still resolves in the new repository (D-01).
</success_criteria>

<output>
Create `.planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-02-SUMMARY.md` when done.
</output>
