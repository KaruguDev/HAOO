---
phase: 04-report-and-enrich-the-haoo-funnel-truthfully
plan: 06
type: execute
wave: 4
depends_on: ["04-05"]
files_modified:
  - src/measurement/plausible.ts
  - src/test/fixtures/plausible-preload-contract.ts
  - src/test/measurement.test.ts
  - scripts/verify-phase4-coverage.mjs
  - .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements: [MEAS-01, MEAS-08]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A configured browser sink mirrors Plausible's documented preload contract: initialization options live in `plausible.o`, while `plausible.q` contains only real name-only event calls."
    - "Automatic pageview capture is disabled before the managed script loads, and each accepted HAOO action still produces exactly one bare event-name call with provider failure isolated from the visitor journey."
    - "The provider remains unset in production: no deployment variable, account action, dashboard goal, or live collection is enabled by this gap plan."
    - "The canonical Phase 4 roadmap goal is a valid MVP user story with the same product-owner, aggregate-interest, readable-enquiry-context, no-identity, and no-hidden-score intent."
  prohibitions:
    - "No initialization call may be encoded as an event-shaped queue item, because that can leave automatic pageviews enabled and pollute aggregate counts."
    - "No provider call may carry properties, form values, browser context, a stable identifier, or more than the single allowlisted event-name argument."
    - "No automatic pageview, outbound-link, download, or form capture may be enabled alongside the explicit ten-event seam."
    - "No report or provider wording may imply a cohort, conversion, customer, completed onboarding, or person-level progression from aggregate occurrences."
  artifacts:
    - path: "src/measurement/plausible.ts"
      provides: "Vendor-compatible preload options slot and unchanged failure-isolated name-only sink"
      exports: ["PlausibleGlobal", "createPlausibleEventSink"]
    - path: "src/test/fixtures/plausible-preload-contract.ts"
      provides: "Independent test oracle transcribed from the documented Plausible preload snippet"
      exports: ["installPlausibleVendorPreload"]
    - path: ".planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md"
      provides: "Full Plausible browser and Stats API capability matrix with explicit opt-outs and deferred-production boundary"
    - path: "scripts/verify-phase4-coverage.mjs"
      provides: "Deterministic project-specific audit for required Plausible/FormSubmit rows, reasoned opt-outs, and deferred production"
      exports: ["auditPhase4Coverage"]
    - path: ".planning/ROADMAP.md"
      provides: "MVP-valid Phase 4 user-story goal"
  key_links:
    - from: "src/test/fixtures/plausible-preload-contract.ts"
      to: "src/measurement/plausible.ts"
      via: "The test compares independently installed vendor-preload state with the production adapter state"
      pattern: "plausible.o"
    - from: "src/measurement/index.ts"
      to: "src/measurement/plausible.ts"
      via: "The unchanged measurement facade resolves the corrected sink after campaign cleanup"
      pattern: "createPlausibleEventSink"
---

<objective>
Close CR-01 by making the configured collection path match the provider's documented preload contract without enabling production analytics.

**As a** HAOO product owner, **I want to** trust that each explicit browser action can become one aggregate event, **so that** the staged counts required by D-01 through D-04 are not inflated by automatic or initialization-shaped events.

Purpose: MEAS-01 needs trustworthy occurrence counts and MEAS-08 requires name-only, literal evidence. The existing test copied the implementation's invented queue shape, so this repair establishes an independent provider oracle and retains the C-3 decision to defer production enablement.
Output: corrected preload behavior, an independent vendor-contract fixture, explicit API coverage, and an MVP-valid roadmap goal.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-CONTEXT.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-RESEARCH.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-REVIEW.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-VERIFICATION.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-05-SUMMARY.md
@src/measurement/plausible.ts
@src/measurement/index.ts
@src/test/measurement.test.ts
</context>

## Artifacts this phase produces

- `PlausibleGlobal.o?: PlausibleInitOptions` in `src/measurement/plausible.ts`.
- `installPlausibleVendorPreload` in `src/test/fixtures/plausible-preload-contract.ts`.
- A configured preload behavior where `plausible.init(options)` assigns `plausible.o` and never appends an initialization-shaped item to `plausible.q`.
- A capability-matrix row for the preload options slot and initializer, plus Stats echoed-query provenance rows and the deferred-production boundary in `COVERAGE.md`.
- `auditPhase4Coverage` in `scripts/verify-phase4-coverage.mjs`, enforcing the required browser, Stats, and FormSubmit rows plus reasoned opt-outs and deferred production.
- A Phase 4 `**Goal**` line in valid `As a ..., I want to ..., so that ...` form.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Repair one configured HAOO event from facade initialization through the documented Plausible preload state</name>
  <files>src/measurement/plausible.ts, src/test/fixtures/plausible-preload-contract.ts, src/test/measurement.test.ts</files>
  <read_first>
    - `src/measurement/plausible.ts` — current `PlausibleGlobal`, `ensureProvider`, script insertion, and name-only sink.
    - `src/measurement/index.ts` — initialization order and the provider-adapter seam that must remain unchanged.
    - `src/test/measurement.test.ts` — current misleading initialization/queue assertion, `recordingScope`, and provider-failure regression cases.
    - `04-REVIEW.md` CR-01 and `04-VERIFICATION.md` first gap — exact defect and verifier expectation.
    - Official Plausible preload snippet recorded in research/review: queue calls use `plausible.q`, while `plausible.init` assigns options to `plausible.o`.
  </read_first>
  <behavior>
    - "RED: the independent vendor fixture and production adapter disagree before the fix because production queues an initialization-shaped call instead of populating the options slot."
    - "GREEN: before any event, `scope.plausible.o` equals `{ domain: SITE_DOMAIN, autoCapturePageviews: false }` and `scope.plausible.q` is empty or absent."
    - "GREEN: forwarding `haoo_page_view` produces exactly `[['haoo_page_view']]` in the preload queue and no second event or property argument."
    - "GREEN: a pre-existing provider function with an initializer receives the same options without widening event arity."
    - "REGRESSION: absent, blocked, and throwing providers still preserve local context and the visitor action."
  </behavior>
  <action>
    First create `src/test/fixtures/plausible-preload-contract.ts` as a test-only, independent transcription of the official preload semantics. It must define its own small scope/global shape rather than importing `PlausibleGlobal`, `PlausibleInitOptions`, `ensureProvider`, or any production implementation detail; export `installPlausibleVendorPreload(scope)` so the test can establish the vendor oracle and observe its `q` and `o` state.

    Replace the current circular initialization assertion with a contract test that compares the vendor fixture's state to the state produced by `createPlausibleEventSink` for the same domain and first accepted event. Prove the RED failure before editing production code.

    In `src/measurement/plausible.ts`, add the documented `o?: PlausibleInitOptions` slot to `PlausibleGlobal` and change the installed stub's initializer to assign `stub.o = options` rather than enqueueing an initialization call. Preserve the single bare-name call signature, script de-duplication, initialization-before-forwarding order, and every failure-isolation branch already shipped. Do not change `src/measurement/index.ts`, the five-member facade, provider resolution, deployment workflow, or public build variables. This implements D-01/D-04's trustworthy-count boundary while retaining D-02/D-03 report behavior untouched.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/measurement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test suite contains an independent preload fixture that imports no production measurement module.
    - The old assertion expecting an initialization-shaped queue entry is gone.
    - Before the first event, `plausible.o` holds the exact domain and `autoCapturePageviews: false`, and no queue entry exists for initialization.
    - After one accepted event, the queue contains exactly one one-argument event call.
    - All ten allowlisted events retain exact one-argument forwarding and all existing failure-isolation tests pass.
    - `.github/workflows/deploy.yml` remains unchanged and no production provider variable is set.
  </acceptance_criteria>
  <done>One configured HAOO event traverses the existing facade into vendor-compatible preload state with automatic pageviews disabled, no duplicate event, and no widened payload.</done>
</task>

<task type="auto">
  <name>Task 2: Certify the provider scope and restore canonical MVP goal verification</name>
  <files>scripts/verify-phase4-coverage.mjs, .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md` — current full-coverage Plausible/FormSubmit matrix.
    - `.planning/ROADMAP.md` Phase 4 — current goal, MVP mode, requirements, and success criteria.
    - `04-CONTEXT.md` D-01 through D-04 — locked report intent that the goal wording must retain.
    - `04-VERIFICATION.md` MVP Format Preflight — exact canonical validation failure.
    - `/home/paul/.codex/gsd-core/bin/gsd-tools.cjs` roadmap and user-story query contracts — the verifier must consume the stored goal rather than a duplicated literal.
  </read_first>
  <action>
    Reconcile `COVERAGE.md` from a default-INTEGRATE baseline. Keep the deliberately narrow browser sink and aggregate Stats query explicit: include custom event calls, `plausible.q`, `plausible.o`, `plausible.init`, ten goals, the Stats request fields, echoed-query provenance, results validation, and local bearer authentication. Retain a one-line reason for every OPT-OUT covering automatic capture, properties/identity, unsupported person/session metrics, provider comparisons/percentages, funnels/journeys, public dashboards, and production enablement. State plainly that this execution changes code and fixtures only; the provider remains unset pending separate privacy-owner approval.

    Add `scripts/verify-phase4-coverage.mjs` with an exported `auditPhase4Coverage(markdown)` and a direct CLI entry point. Parse the three named markdown tables rather than searching prose. Require the Plausible browser rows for custom event name, `window.plausible.q`, `window.plausible.o`, `plausible.init`, ten exact goals, automatic pageviews, event properties, outbound links, downloads, and forms; require the Stats rows for endpoint, events metric, goal dimension/filter, bounded/all date ranges, local bearer auth, all echoed-query provenance groups, results rows, person/session metrics, conversion/percentage metrics, comparisons, public/shared dashboards, and funnels; require the FormSubmit rows for AJAX JSON POST, named fields, subject/template/captcha/honeypot, HAOO engagement context, cc/next/autoresponse/reply-to, uploads, and webhooks. Assert each required row has its expected INTEGRATE or OPT-OUT decision, reject every OPT-OUT row whose reason cell is blank or whitespace, and require the Operational boundary to say production analytics remains OPT-OUT/deferred, the provider selector remains unset, and both local report variables stay outside browser/Vite configuration. Exit non-zero with row-specific diagnostics on any mismatch.

    Change only Phase 4's roadmap goal to: `As a HAOO product owner, I want to understand aggregate HAOO interest and receive transparent, human-readable context with voluntarily submitted enquiries, so that I can act without identity tracking or a hidden score.` Keep `Mode: mvp`, requirements, success criteria, and the goal's original intent unchanged. This is the verifier-required metadata repair, not a scope change.
  </action>
  <verify>
    <automated>PHASE4_GOAL="$(node /home/paul/.codex/gsd-core/bin/gsd-tools.cjs query roadmap.get-phase 04 --pick goal)"; test "$(node /home/paul/.codex/gsd-core/bin/gsd-tools.cjs query user-story.validate --story "$PHASE4_GOAL" --pick valid)" = "true" &amp;&amp; node scripts/verify-phase4-coverage.mjs .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - The user-story validation command prints `true`.
    - The validated story is read from Phase 4's actual stored roadmap goal; deleting or corrupting that edit makes the automated verification fail.
    - Phase 4 remains in MVP mode and its requirement IDs and three success criteria are unchanged.
    - The capability matrix distinguishes queue events from preload options and distinguishes supported aggregate query/provenance fields from identity, property, automatic-capture, person/session, comparison-percentage, and funnel capabilities.
    - Every OPT-OUT row carries a one-line reason, and production enablement is still explicitly deferred.
    - The project-specific coverage audit confirms every required Plausible browser, Plausible Stats, and FormSubmit capability row with its expected decision and fails on a missing row, blank OPT-OUT reason, or weakened deferred-production boundary.
  </acceptance_criteria>
  <done>The provider contract is fully scoped in the capability matrix and the canonical Phase 4 goal is a verifier-valid user story without changing the phase outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application preload -> managed provider script | Options and queued name-only events cross from local code into third-party code. |
| explicit component actions -> measurement facade | Only closed event literals may cross; visitor context and form values must not. |
| local report process -> Stats API | Credentialed aggregate queries remain disabled operationally until separate approval/setup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-21 | Tampering | `src/measurement/plausible.ts` provider contract | high | mitigate | Independent vendor fixture asserts `plausible.o` preload semantics and excludes initialization from the event queue. |
| T-04-22 | Information Disclosure | browser provider seam | high | mitigate | Existing one-argument tests and failure-isolation contracts keep properties, form values, identifiers, and automatic capture out of the sink. |
| T-04-23 | Repudiation | Stats echoed query metadata | high | mitigate | Plan 04-07 validates site, metric, dimension, goal filter, and dates before counts are accepted; production remains unset until both plans pass. |
| T-04-24 | Information Disclosure | report credential/browser boundary | high | mitigate | Existing source/bundle scans remain mandatory and 04-07 documents both local-only report variables without values. |
| T-04-25 | Tampering | report artifact replacement | high | mitigate | Plan 04-07 adds owned temporary-file cleanup and a real-filesystem throwing-rename contract before phase re-verification. |
| T-04-26 | Denial of Service | provider load/call | medium | mitigate | Existing blocked, missing, and throwing-provider tests remain green and local context updates independently. |
| T-04-SC | Tampering | package supply chain | high | mitigate | No package installation occurs; any later dependency addition requires the package-legitimacy gate. |
</threat_model>

<verification>
- `npm run test:unit -- --run src/test/measurement.test.ts` passes with the independent preload fixture.
- `npm run lint && npm run typecheck` pass after the global-shape change.
- The Phase 4 user story validates canonically and `COVERAGE.md` records every integration/opt-out decision.
- Git diff confirms `.github/workflows/deploy.yml` is untouched and production provider variables remain unset.
</verification>

<success_criteria>
- CR-01 is closed: preload options use `plausible.o`, the queue contains only real events, automatic capture is disabled, and name-only/failure-isolation behavior is unchanged.
- MEAS-01 is no longer vulnerable to initialization-shaped or automatic duplicate counts in the code path, while live enablement remains deferred.
- MEAS-08 remains bounded to literal aggregate occurrences and the MVP goal is valid for canonical user-flow verification.
</success_criteria>

<output>
Create `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-06-SUMMARY.md` when done.
</output>
