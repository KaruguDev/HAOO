---
phase: 04-report-and-enrich-the-haoo-funnel-truthfully
plan: 07
type: execute
wave: 4
depends_on: ["04-01", "04-05"]
files_modified:
  - src/reporting/query-provenance.ts
  - src/reporting/generate.ts
  - scripts/generate-haoo-report.mjs
  - src/test/fixtures/haoo-report-cli-fetch-preload.mjs
  - src/test/haoo-report.test.ts
  - README.md
  - .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md
autonomous: true
gap_closure: true
requirements: [MEAS-01, MEAS-05, MEAS-08]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every accepted Stats API response echoes the exact requested site, `events` metric, `event:goal` dimension, exact ten-name goal filter, and requested inclusive calendar range before any result count can enter the report model."
    - "For all time, the echoed end day equals the report day and the echoed start is a real ISO date or timestamp that is ordered, not future, and never invented from malformed provider text."
    - "A query, validation, partial-write, or final-rename failure leaves the previous destination byte-for-byte unchanged and removes the temporary sibling owned by that invocation."
    - "Concurrent report generations for the same output are fail-closed through exclusive temporary-file ownership: a losing invocation never overwrites or removes the active invocation's temporary file or the prior report."
    - "Owner documentation names both local report variables, explains that `PLAUSIBLE_SITE_ID` is the exact Plausible-configured domain, and gives a non-secret command containing both variable names."
    - "MEAS-05's shipped engagement summary retains its exact visit/recency thresholds, one-step-either-side boundaries, 32-character campaign cap, count-band wording, and no-score/no-identifier payload while report code changes."
  prohibitions:
    - "No count may be rendered under a locally requested site or period label when any echoed query field is absent, malformed, extra in a closed list, reordered where order is semantic, or different from the request."
    - "No impossible calendar day, future all-time start, start-after-end range, or stale all-time end day may become owner-facing provenance."
    - "No failed invocation may replace the prior report or leave an owned partial business-data file beside it."
    - "No missing-configuration error or documentation example may print, embed, or commit either environment-variable value."
    - "No engagement summary may expose `visitOrdinal`, `lastSeenDay`, raw storage, form answers, an opaque score, a stable identifier, or rounded threshold behavior."
    - "No report may calculate a percentage or describe aggregate occurrences as people, cohorts, conversions, delivery, customers, or completed onboarding per D-01 and D-04."
  artifacts:
    - path: "src/reporting/query-provenance.ts"
      provides: "Pure fail-closed validator for the Stats API echoed-query trust boundary"
      exports: ["validateEchoedQuery", "EchoedQueryProvenance"]
    - path: "src/reporting/generate.ts"
      provides: "Provenance-gated query orchestration plus owned temporary-file cleanup and exclusive reservation"
      exports: ["ReportFs", "generateHaooReport"]
    - path: "scripts/generate-haoo-report.mjs"
      provides: "Real filesystem capabilities and variable-name-only configuration diagnostics"
    - path: "src/test/fixtures/haoo-report-cli-fetch-preload.mjs"
      provides: "Node preload that replaces global fetch before CLI evaluation, serves only local Stats fixtures, and records request counts"
      exports: ["installCliFetchFixture"]
    - path: "README.md"
      provides: "Runnable owner report instructions for both local variables and exact-domain site scope"
    - path: ".planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md"
      provides: "Deferred-production checklist and complete local report invocation"
  key_links:
    - from: "src/reporting/generate.ts"
      to: "src/reporting/query-provenance.ts"
      via: "`queryRange` validates `body.query` against its independently constructed expected request before `parseGoalCounts`"
      pattern: "validateEchoedQuery"
    - from: "scripts/generate-haoo-report.mjs"
      to: "src/reporting/generate.ts"
      via: "CLI injects exclusive temporary reservation, removal, write, and rename capabilities"
      pattern: "generateHaooReport"
    - from: "README.md"
      to: "scripts/generate-haoo-report.mjs"
      via: "The documented two-variable invocation matches the CLI's exact required names"
      pattern: "PLAUSIBLE_SITE_ID"
---

<objective>
Close CR-02, WR-01, and WR-02 by accepting only query-correlated aggregate counts, making report replacement fail-closed after filesystem writes, and giving the owner complete non-secret run instructions.

**As a** HAOO product owner, **I want to** generate a report whose site, periods, counts, and local artifact are all validated, **so that** I can act on literal browser evidence without stale provenance, leaked credentials, or a damaged prior report.

Purpose: D-01 through D-04 govern presentation, but truthful labels are insufficient if the response belongs to another site/range or a failed replacement leaves fresh business data behind. This slice closes those trust boundaries while regression-protecting the already satisfied MEAS-05 summary.
Output: echoed-query validation, adversarial response fixtures, exclusive temporary ownership and cleanup, variable-name-only CLI errors, and complete owner documentation.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-CONTEXT.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-RESEARCH.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-PATTERNS.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-REVIEW.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-VERIFICATION.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-01-SUMMARY.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-05-SUMMARY.md
@src/reporting/generate.ts
@src/reporting/stats-response.ts
@src/test/haoo-report.test.ts
@scripts/generate-haoo-report.mjs
@README.md
</context>

## Artifacts this phase produces

- `EchoedQueryProvenance` and `validateEchoedQuery` in `src/reporting/query-provenance.ts`.
- A response-fixture builder in `src/test/haoo-report.test.ts` whose echoed query is authored independently from the outgoing request under test.
- `ReportFs.reserveTempSync` and `ReportFs.rmSync` injected filesystem capabilities, with exclusive ownership of `${outputPath}.tmp`.
- CLI missing-configuration output that lists `PLAUSIBLE_STATS_API_KEY` and/or `PLAUSIBLE_SITE_ID` by name only.
- `installCliFetchFixture` in `src/test/fixtures/haoo-report-cli-fetch-preload.mjs`, loaded with Node `--import` before the CLI to forbid live network access and record fetch calls.
- README and `04-USER-SETUP.md` report instructions defining exact-domain site scope and showing both variable names in one non-secret invocation.
- Regression evidence for MEAS-05's exact min/max/threshold/capped-band behavior and privacy boundary.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Gate one bounded response end to end on independently echoed site, query, and date provenance</name>
  <files>src/reporting/query-provenance.ts, src/reporting/generate.ts, src/test/haoo-report.test.ts</files>
  <read_first>
    - `src/reporting/generate.ts` — `queryRange`, `resolvedStartDay`, report-day calculation, and seven-query orchestration.
    - `src/reporting/stats-response.ts` — existing result-row validation that must run only after provenance succeeds.
    - `src/test/haoo-report.test.ts` — `goalRows`, `stubFetch`, `EVERY_PERIOD`, and report assertions whose fixtures currently omit bounded query metadata.
    - `04-REVIEW.md` CR-02 and `04-VERIFICATION.md` second gap — exact mismatch and date cases required.
    - Plausible Stats API response contract in `04-RESEARCH.md`: the response echoes the backend-resolved `query`, including timestamps in `date_range`.
  </read_first>
  <behavior>
    - "RED: a result body with valid rows but a wrong site, range, metric, dimension, goal filter, or malformed query is currently accepted; each new adversarial contract must fail before implementation."
    - "GREEN: a bounded response is accepted only when site ID is byte-exact; metrics are exactly `['events']`; dimensions are exactly `['event:goal']`; the sole filter is exactly `['is', 'event:goal', [...HAOO_REPORT_EVENTS]]`; and both valid ISO timestamps resolve to the requested inclusive days."
    - "GREEN: all-time accepts a real non-future ISO start on or before the report day and requires the exact report day as its valid ISO end."
    - "GREEN: wrong site, wrong/extra metric, wrong/extra dimension, missing/extra/wrong goal filter, wrong range, impossible day, future start, start after end, malformed query, and stale/future all-time end each abort the whole report before rendering or writing."
  </behavior>
  <action>
    Add `src/reporting/query-provenance.ts` as a pure fail-closed parser. Export `validateEchoedQuery(body, expected)` and a small `EchoedQueryProvenance` result carrying the validated start/end calendar days. Rebuild accepted values field-by-field; do not spread the untrusted query object. Validate the required query members exactly while tolerating provider-owned extra top-level query members such as normalized `order_by` or `include`, because the official response may add them. ISO validation must reject calendar-looking normalization such as February 30, reject non-ISO strings, and compare calendar-day prefixes without converting an offset timestamp into a different UTC day.

    For bounded queries, require both resolved calendar days to equal the requested `PeriodWindow`. For `all`, require the resolved end day to equal `today`, require the start day to be valid and no later than both the end and today, and return that validated start for the all-time heading. Site, metric, dimension, and filter comparisons are identical for all seven responses.

    In `queryRange`, construct the request body once, pass the body plus the exact expected site/range/report day/event tuple to `validateEchoedQuery`, and only then call `parseGoalCounts`. Remove the permissive `resolvedStartDay` helper. A response failing either provenance or rows returns `null`, preserving the existing all-or-nothing seven-query abort.

    Rewrite bounded and all-time fixtures so every accepted fixture carries independently authored echoed query metadata rather than deriving it from the outgoing request or importing production constants for the expected shape. Add the complete adversarial table named in `<behavior>`, then retain D-01 through D-04 rendering assertions unchanged. This closes CR-02 and MEAS-08 without adding provider percentages or cohort semantics.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/haoo-report.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the seven successful response fixtures contains an independently authored `query` object.
    - Counts are parsed only after the echoed site, one metric, one dimension, exact goal filter, and date range pass validation.
    - Dedicated tests reject wrong-site, wrong-range, wrong-query-shape, impossible-date, future-start, start-after-end, and stale/future all-time-end responses.
    - A failed provenance check leaves the report filesystem untouched and returns the period-specific existing failure result.
    - Valid timestamp offsets preserve their provider-local calendar day instead of being shifted through UTC.
  </acceptance_criteria>
  <done>A valid bounded Stats response now travels from fetch through independently verified provenance and row parsing into the correctly dated report, while every mismatched response fails before a byte is written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the prior report and remove the owned temporary sibling after post-write failure</name>
  <files>src/reporting/generate.ts, scripts/generate-haoo-report.mjs, src/test/haoo-report.test.ts</files>
  <read_first>
    - `src/reporting/generate.ts` — `ReportFs`, temporary sibling creation, rename, and catch path.
    - `scripts/generate-haoo-report.mjs` — real `node:fs` capability injection.
    - `src/test/haoo-report.test.ts` — `memoryFs`, current pre-write rejection case, and real temporary-directory utilities.
    - `04-REVIEW.md` WR-01 and `04-VERIFICATION.md` third gap — required throwing-rename real-filesystem evidence.
  </read_first>
  <behavior>
    - "RED: when a real temporary file has been written and injected `renameSync` throws, the previous destination remains but `${outputPath}.tmp` currently survives."
    - "GREEN: a throwing write-after-create or final rename removes the temporary sibling owned by that invocation and leaves the previous destination byte-for-byte unchanged."
    - "GREEN: exclusive reservation lets only one invocation own `${outputPath}.tmp`; a concurrent loser fails before write and never removes the winner's temporary file."
    - "GREEN: query/validation failures still perform no report write, and successful replacement remains atomic."
  </behavior>
  <action>
    Extend `ReportFs` with explicit `reserveTempSync(path)` and `rmSync(path, { force: true })` capabilities. In the CLI, implement reservation with an atomic exclusive create using Node `openSync(path, 'wx')` followed by `closeSync`, and inject `rmSync` from `node:fs`. This defines the concurrency guarantee instead of relying on a racy existence check: one invocation owns the fixed temporary sibling; another invocation targeting the same output fails closed and must not delete a file it did not reserve.

    In `generateHaooReport`, reserve `${outputPath}.tmp` only after all seven queries validate and the document renders in memory. Track ownership separately from whether the subsequent write returned. On any write or rename failure, force-remove the owned temporary sibling inside a nested failure-isolated cleanup, then return failure. Clear ownership after a successful rename so the destination is never removed. Do not alter, truncate, or pre-delete the prior destination.

    State the interruption boundary truthfully in code comments and tests: caught filesystem failures clean the owned temp; a competing run fails without touching it; an uncatchable process termination can leave only the reserved temp/lockout, never a partially replaced destination, and the next run fails closed rather than guessing ownership. Do not claim impossible cleanup after `SIGKILL`.

    Add the verifier-required real-filesystem test using `mkdtempSync`, a sentinel prior report, real reserve/write/removal functions, and an injected rename that throws. Assert the sentinel bytes are identical and the exact sibling is absent. Add a partial-write-then-throw test and an exclusive-reservation/concurrent-loser test. Always remove the test directory in `finally`.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/haoo-report.test.ts -t "temporary|rename|concurrent|previous report"</automated>
  </verify>
  <acceptance_criteria>
    - A real-filesystem throwing-rename test writes the temporary sibling, forces final rename failure, and proves the old destination is byte-identical and the sibling is absent afterward.
    - A post-create write failure also cleans the owned sibling.
    - Exclusive reservation is atomic in the real CLI adapter; a second invocation cannot acquire or delete another invocation's sibling.
    - Query/provenance failure writes nothing, successful generation still performs one final rename, and cleanup failure never overwrites the primary failure reason or logs business data.
    - The code and documentation make no guarantee of cleaning files after uncatchable process termination; they do guarantee the previous destination is never partially replaced and the next run fails closed on an unowned sibling.
  </acceptance_criteria>
  <done>Every caught post-write failure removes its owned temporary report and preserves the prior report exactly, while concurrent invocations cannot corrupt or delete each other's work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the owner command self-diagnosing and document both local report variables</name>
  <files>scripts/generate-haoo-report.mjs, src/test/fixtures/haoo-report-cli-fetch-preload.mjs, src/test/haoo-report.test.ts, README.md, .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md</files>
  <read_first>
    - `scripts/generate-haoo-report.mjs` — current generic missing-configuration branch and credential boundary.
    - `src/test/haoo-report.test.ts` owner-command tests — subprocess conventions and secret non-rendering assertions.
    - Node `--import` preload behavior — the fixture must replace `globalThis.fetch` before `scripts/generate-haoo-report.mjs` evaluates.
    - `README.md` HAOO measurement/report section — public build variables and incomplete report credential instructions.
    - `04-USER-SETUP.md` — deferred-production checklist and current one-variable invocation.
    - `src/test/qualify-form.test.tsx` summary threshold/cap matrix — MEAS-05 regression evidence that must remain green.
  </read_first>
  <behavior>
    - "RED: starting the CLI with neither variable or with either one missing identifies every absent variable name and never prints any supplied value."
    - "GREEN: missing-variable diagnostics list exactly the absent names in deterministic order before the existing generic failure guidance."
    - "GREEN: every missing-variable subprocess records zero fetch calls; the both-present subprocess records only fixture-served Stats requests and cannot reach the network."
    - "GREEN: the documented invocation contains both variable names without an example credential or domain value and explains exact-domain matching."
    - "REGRESSION: visit-band and recency thresholds retain their exact minimum, maximum, and one-step-either-side cases; campaign values remain capped at 32 accepted characters with no numeric score, stable identifier, or rounding-derived band."
  </behavior>
  <action>
    In the CLI, build a deterministic list of absent required environment-variable names from trimmed values. If non-empty, print a name-only diagnostic such as `Missing required environment variables: ...` followed by the existing terminal guidance, then exit 1. Never concatenate, serialize, log, or echo an environment-variable value.

    Create `src/test/fixtures/haoo-report-cli-fetch-preload.mjs` as a test-only Node preload. Export `installCliFetchFixture`, install it immediately on module evaluation, replace `globalThis.fetch` before the CLI imports, reject any endpoint other than the expected Stats URL, serve independently authored local query/result fixtures for the seven expected requests, and write a request-count/URL audit JSON file to a test-provided temporary path during process exit. The fixture must never delegate to the original fetch. Launch all four subprocess cases with `node --import ./src/test/fixtures/haoo-report-cli-fetch-preload.mjs scripts/generate-haoo-report.mjs` and isolated temporary output/audit paths. Assert the both-missing, API-key-only, and site-only processes exit before networking with an audit count of zero. Assert the both-present process succeeds with exactly seven fixture-served requests and no URL outside the fixture allowlist. Use a non-sensitive fixture domain for the report's intentional site-scope label; use distinct secret/header sentinel tokens and assert those tokens, plus every supplied value in the missing-variable cases, are absent from stdout, stderr, and the generated report. This seam is test-only: do not add a production environment switch, dependency, or network fallback.

    Update README and `04-USER-SETUP.md` together. Document `PLAUSIBLE_SITE_ID` as the domain exactly as configured for the Plausible site, including its hostname/domain spelling rather than a report label or URL. Document `PLAUSIBLE_STATS_API_KEY` as a local-process secret. Include one non-secret shell invocation containing both names by referencing already-set shell variables; do not include placeholder credential values. Preserve the separate three public `VITE_HAOO_*` variables and explicitly state production collection remains disabled pending privacy-owner approval, ten dashboard goals, and deployment setup.

    Run the existing MEAS-05 matrix without changing its approved values or sentence copy. Its visit-count threshold rows, one-step retention boundary rows, last-seen bands, and 32-character accepted campaign cap are the regression oracle; do not introduce arithmetic rounding, truncate the finished summary, or expose stored numeric members while editing shared tests/docs.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/haoo-report.test.ts src/test/qualify-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Missing configuration output names exactly whichever of `PLAUSIBLE_STATS_API_KEY` and `PLAUSIBLE_SITE_ID` are absent and prints no supplied value.
    - All missing-variable subprocess cases record zero fetch calls; their supplied sentinel value, when present, appears in neither stdout nor stderr.
    - The both-present subprocess uses the Node preload before CLI evaluation, records exactly seven fixture-only Stats requests, rejects any other URL, and cannot invoke the original network fetch.
    - Secret and authorization sentinel tokens are absent from stdout, stderr, and the generated report; the fixture domain appears only in the report's intentional site-scope field.
    - README and `04-USER-SETUP.md` both define the exact-domain meaning of `PLAUSIBLE_SITE_ID` and show one non-secret invocation containing both report variable names.
    - Both documents distinguish local report inputs from the three public browser build variables and keep production enablement deferred.
    - Existing MEAS-05 threshold tests cover exact minima/maxima and one step on each side; the exact capped-band/count behavior passes without rounding ambiguity.
    - Existing payload prohibitions for numeric stored members, raw storage, form-answer leakage, score, and stable identity remain green.
  </acceptance_criteria>
  <done>The owner can diagnose and run the local report from complete instructions without exposing values, while the shipped enquiry-summary thresholds and privacy boundary remain exactly intact.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Owner understands aggregate interest and receives transparent readable enquiry context without identity/score | 04-06, 04-07 | COVERED | MVP goal format is repaired in 04-06; trustworthy report flow is repaired in 04-07. |
| REQ | MEAS-01 | Aggregate counts for all ten HAOO actions | 04-06, 04-07 | COVERED | Correct preload plus query provenance and artifact safety. |
| REQ | MEAS-05 | Disclosed readable summary without opaque score | 04-07 | COVERED | Already satisfied; exact thresholds, caps, and payload prohibitions are regression gates. |
| REQ | MEAS-08 | Literal evidence language rather than delivery/customer/onboarding claims | 04-06, 04-07 | COVERED | No duplicate capture and no mismatched provenance can feed truthful labels. |
| RESEARCH | — | Name-only Plausible sink with automatic pageviews disabled | 04-06 | COVERED | Uses documented preload options slot and independent oracle. |
| RESEARCH | — | Local credentialed aggregate Stats query; no browser credential | 04-07 | COVERED | Exact echoed-query validation and complete local-variable docs. |
| RESEARCH | — | Atomic write-on-success report artifact | 04-07 | COVERED | Exclusive temp ownership, caught-failure cleanup, previous-file preservation. |
| CONTEXT | D-01 | Four-stage literal occurrence hierarchy, not progression | 04-06, 04-07 | COVERED | Existing renderer retained; source counts made trustworthy. |
| CONTEXT | D-02 | Compact totals with separate expandable event details | 04-07 | COVERED | Existing report UI regression suite remains unchanged and green. |
| CONTEXT | D-03 | 7/30/90/all-time; prior equivalent bounded comparisons only | 04-07 | COVERED | Every one of seven echoed ranges is validated exactly. |
| CONTEXT | D-04 | Counts/changes only; no conversion percentages | 04-06, 04-07 | COVERED | Capability opt-outs and full report regression preserve the prohibition. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stats API response -> report model | Untrusted echoed query metadata and result rows must both validate before owner-facing labels/counts are built. |
| local process environment -> CLI | A secret key and exact site domain enter locally; only missing names may leave through diagnostics. |
| rendered document -> local filesystem | Business counts cross into a temporary artifact and then atomically replace the prior complete report. |
| explicit browser events -> provider | Plan 04-06 keeps collection name-only and disables automatic/duplicate capture; production remains unset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-27 | Spoofing | echoed `query.site_id` | high | mitigate | Byte-exact site comparison rejects counts from a stale proxy, wrong site, or test fixture before row parsing. |
| T-04-28 | Tampering | echoed metrics/dimensions/filter/range | high | mitigate | Pure validator checks every required query member and real ordered ISO days for all seven responses. |
| T-04-29 | Information Disclosure | CLI environment and diagnostics | high | mitigate | Missing-variable output contains names only; source/bundle/report credential scans and sentinel-value tests remain green. |
| T-04-30 | Tampering | temporary report replacement | high | mitigate | Atomic exclusive reservation, owned cleanup, byte-identical prior-file assertions, and a real-filesystem throwing-rename test. |
| T-04-31 | Denial of Service | concurrent report runs | medium | mitigate | Atomic exclusive temp reservation rejects a losing invocation without touching active or prior artifacts; stale unowned temp fails closed. |
| T-04-32 | Repudiation | provider-contract drift and automatic capture | high | mitigate | 04-06 independent preload fixture and one-name/one-call tests are mandatory dependencies for phase re-verification. |
| T-04-33 | Information Disclosure | engagement summary | high | mitigate | Existing explicit readable-member pick list, exact threshold matrix, cap tests, and payload negative assertions run in Task 3. |
| T-04-SC | Tampering | package supply chain | high | mitigate | No package installation occurs; Node built-ins and existing Vitest infrastructure only. |
</threat_model>

<verification>
- `npm run test:unit -- --run src/test/haoo-report.test.ts src/test/qualify-form.test.tsx` passes all provenance, filesystem, CLI, and MEAS-05 regression contracts.
- `npm run lint && npm run typecheck && npm test` pass after both Wave 4 plans complete.
- A real temporary-directory mutation proves a throwing rename leaves the prior report byte-identical and no owned `.tmp` sibling.
- README and `04-USER-SETUP.md` agree on both local variables, exact site-domain meaning, and deferred production status.
- Re-run Phase 4 verification; live production collection remains a separate human gate and must not be claimed by fixture evidence.
</verification>

<success_criteria>
- CR-02 is closed: no response count is accepted without exact echoed query provenance and valid bounded/all-time dates.
- WR-01 is closed: caught post-write failures remove the owned temporary sibling and never alter the prior destination.
- WR-02 is closed: both report variables, exact-domain meaning, and a non-secret invocation are documented; missing-variable errors name absent variables only.
- MEAS-05 remains satisfied with exact thresholds/caps and no score or stable identifier.
- MEAS-01 and MEAS-08 are fixture-verifiable and safe to enable later, while production analytics remains disabled pending the separate privacy-owner gate.
</success_criteria>

<output>
Create `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-07-SUMMARY.md` when done.
</output>
