---
phase: 04-report-and-enrich-the-haoo-funnel-truthfully
plan: 12
type: execute
wave: 2
depends_on: ["04-11"]
files_modified:
  - src/measurement/plausible.ts
  - src/test/measurement.test.ts
  - .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [MEAS-01, MEAS-08]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No pre-existing provider global may be replaced, wrapped, or mutated by this adapter when its initializer is absent, throwing, or non-recording — and that now holds for a non-callable global too: a truthy non-function `scope.plausible` causes the adapter to refuse before anything is installed, so it returns no sink, appends no script element, and leaves the original value at its identical reference."
    - "The adopted-versus-installed classification is decided before any assignment to the scope: a stub is installed only when the scope carries no provider value at all, so there is no code path on which a pre-existing value is overwritten and later needs restoring."
    - "The adapter makes no claim it cannot execute: the unreachable stub-removal branch is gone from the source, the docstring sentence that recorded it as a mitigation is gone, and the matching 04-09 key-decision claim is withdrawn with a dated amendment rather than silently deleted."
    - "The three previously proven refusal paths are unchanged: an existing callable provider with no initializer, one whose initializer throws, and one whose initializer records nothing each still return no sink, append zero script elements, and leave the pre-existing global byte-identical with no options slot and no queue."
    - "The primary path is unchanged: on a scope carrying no provider value, the adapter still installs its stub, initializes it, confirms the recorded opt-out, and only then appends exactly one managed script."
    - "MEAS-07 holds on the new refusal path: with a truthy non-function pre-existing global, the HAOO journey still records every bounded local flag, the qualification path still works, no script is appended, and nothing is written to the console."
    - "Edge — MEAS-01 concurrency (spec-less probe row, resolved/explicit, carried not duplicated): the adapter is synchronous and single-shot per initialization, and the concurrency edge belongs to report generation, where it is already proven by the shipped exclusive-reservation and concurrent-loser cases carried in 04-10 and re-asserted in 04-11. No duplicate coverage is added here."
    - "Edge — MEAS-08 (spec-less probe row, deliberately left unresolved): surfaced as a flagged planner assumption in this plan's `## Flagged unresolved assumption` section rather than auto-resolved or dropped."
  prohibitions:
    - "No pre-existing value on `scope.plausible` may be read, replaced, wrapped, decorated, or deleted by this adapter on any refusal path, whatever its type."
    - "No provider script element may be appended and no event sink may be returned before the recorded automatic-capture opt-out check has passed."
    - "No claim may remain in the source, the docstring, or a plan summary that describes behaviour no executable path can reach."
    - "No stable identifier, fingerprint, visitor property, form value, or identity join may be introduced; the sink must keep forwarding exactly one bare event name with no parameter through which anything else could travel."
    - "No eleventh event name and no redirect-return event may be added; the closed allowlist stays at ten."
    - "No requirement checkbox or status may be promoted by this plan, and no open human gate recorded in `04-VERIFICATION.md` may be converted into an automated acceptance criterion."
    - "No `COVERAGE.md` table may be regenerated or extended; this plan changes no integrated or opted-out provider capability."
    - "No analytics origin may be hardcoded into any production `src/` module, and the approved-source contract from 04-08 must not be widened, re-typed, or bypassed."
  artifacts:
    - path: "src/measurement/plausible.ts"
      provides: "Complete adopted-versus-installed classification that refuses a non-callable pre-existing provider global before installing anything, with no unreachable cleanup branch and no unexecutable claim"
      exports: ["PlausibleInitOptions", "PlausibleGlobal", "PlausibleScope", "PlausibleAdapters", "createPlausibleEventSink"]
    - path: "src/test/measurement.test.ts"
      provides: "Adversarial coverage for a truthy non-function pre-existing provider global, at the adapter boundary and through the full journey facade"
    - path: ".planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md"
      provides: "Withdrawn stub-removal key-decision claim, recorded as a dated amendment"
  key_links:
    - from: "src/measurement/index.ts"
      to: "src/measurement/plausible.ts"
      via: "The facade calls the adapter after campaign cleanup and leaves `eventSink` undefined on every refusal, including the new non-callable-global refusal"
      pattern: "createPlausibleEventSink"
    - from: "src/measurement/plausible.ts"
      to: "the injected `PlausibleScope`"
      via: "The classification reads the scope once and assigns to it only when it carried no provider value"
      pattern: "scope.plausible"
---

<objective>
Close gap 2 of `04-VERIFICATION.md`, the one failed truth in this phase: complete the adopted-versus-installed classification in the Plausible adapter so a truthy non-function pre-existing global is refused instead of clobbered, and remove the refusal-cleanup branch that was recorded as a mitigation but can never execute.

Purpose: `adopted` is decided by a callability test alone, so a non-callable `window.plausible` — an object-shaped provider, or a value set by a tag manager — is classified as absent, overwritten by the installed stub, and never restored. The verifier probed it directly: the pre-existing object was replaced, a sink was returned, and one script was appended. That is a literal breach of the prohibition 04-09 declared. The companion claim that a refused initialization withdraws the installed stub has no executable evidence, because on the self-installed path the stub's own initializer can neither throw nor fail the recorded-opt-out check.
Output: a classification that decides before it assigns, adversarial coverage for the non-callable case at both the adapter boundary and the full journey, and a source and planning record that claims only what a path can reach.

This is a complete vertical fix for one gap: the classification change, the coverage that proves it, and the withdrawal of the unexecutable claim all ship in this plan. No layer of it is deferred.
</objective>

<execution_context>
@/home/paul/.claude/gsd-core/workflows/execute-plan.md
@/home/paul/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-CONTEXT.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-VERIFICATION.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-REVIEW.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md
@src/measurement/plausible.ts
@src/measurement/index.ts
@src/test/measurement.test.ts
</context>

## Artifacts this phase produces

Changed symbols:

- `resolveInitializedProvider(scope, options)` in `src/measurement/plausible.ts` — the classification now refuses a pre-existing non-callable provider value before any assignment to the scope, and the module-private `refuse` closure that carried the unreachable stub-removal branch is removed. Signature is unchanged: `(scope: PlausibleScope, options: PlausibleInitOptions) => PlausibleGlobal | null`.

New symbols:

- `nonFunctionGlobalScope()` in `src/test/measurement.test.ts` — a test fixture returning a `PlausibleScope` whose `plausible` is the verifier's probed truthy non-function value, together with the original reference so the caller can assert identity. Test-only; not exported from the suite.

Unchanged public surface:

- `createPlausibleEventSink`, `PlausibleInitOptions`, `PlausibleGlobal`, `PlausibleScope`, and `PlausibleAdapters` keep their current signatures and are not renamed. `installProviderStub`, `recordsOptOut`, `appendProviderScript`, `resolveScope`, and `resolveDocument` keep their names and signatures.

Behavior contracts added:

- Adapter-boundary case: a truthy non-function pre-existing provider global yields no sink, zero appended script elements, and the original value at its identical reference.
- Full-journey MEAS-07 case: the same scope keeps the HAOO journey and the bounded local context fully working with a silent console.

Documentation surfaces changed:

- The `resolveInitializedProvider` docstring in `src/measurement/plausible.ts` — the closing sentence describing withdrawal of a self-installed stub is removed and replaced by the rule the code implements.
- `04-09-SUMMARY.md` — the `key-decisions` entry asserting stub removal on refusal is withdrawn with a dated amendment note. The D1 `coverage` row is **not** touched by this plan; it belongs to plan 04-13.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Refuse a non-callable pre-existing provider global before anything is installed</name>
  <files>src/measurement/plausible.ts, src/test/measurement.test.ts</files>
  <read_first>
    - `src/measurement/plausible.ts` lines 85-102 — `installProviderStub`, which assigns the stub onto the scope, and its docstring explaining why the recorded opt-out is readable afterwards.
    - `src/measurement/plausible.ts` lines 120-164 — `resolveInitializedProvider`: its docstring, the callability test that decides `adopted`, the conditional stub installation, the absent-initializer early return, the `refuse` closure, and the try/catch around initialization and the recorded-opt-out check.
    - `src/measurement/plausible.ts` lines 184-224 — `createPlausibleEventSink`: the provider and script-source guards, the options object built with the domain and the automatic-capture opt-out, the null guard, the single script append, and the sink that forwards one bare event name.
    - `src/test/measurement.test.ts` lines 490-555 — `recordingScope`, `bareCallableScope`, `throwingInitScope`, and `silentInitScope`: the fixture shape the new fixture must match, and why each records forwarded calls so a refusal assertion is not vacuous.
    - `src/test/measurement.test.ts` lines 762-825 — the `fail-closed provider initialization` block: the three refusal cases and the confirmed-stub case, including the exact assertions about script count, global identity, and the absence of an options slot and a queue.
    - `src/test/measurement.test.ts` lines 826-900 — the `fail-closed provider initialization in the full journey` block, its `refusedRows` table typed against `bareCallableScope`'s return shape, and the original-identity case.
    - `04-VERIFICATION.md` gap 2 — the failed truth, the probed outcome, and the three `missing` items.
    - `04-REVIEW.md` IN-01 and IN-02 — the same two defects with the reviewer's reachability analysis.
  </read_first>
  <behavior>
    - "RED: with a scope whose `plausible` is the truthy non-function value `{ o: 'foreign' }`, the adapter is expected to return `undefined`, and fails today because it returns a function."
    - "RED: the same scope is expected to append zero script elements, and fails today because one is appended."
    - "RED: the same scope is expected to still hold the identical original object afterwards, and fails today because it holds the installed stub."
    - "GREEN: all three expectations hold once the classification refuses a non-callable pre-existing value before installing anything."
    - "GREEN: through the facade, the same scope keeps the whole journey working — three tracked actions each return true, each bounded local flag is recorded, zero script elements are appended, and nothing is written to the console."
    - "GREEN: the three existing refusal cases, the original-identity case, and the confirmed-stub primary-path case all still pass with unedited expectations."
  </behavior>
  <action>
    Write the failing coverage before changing the adapter, so the breach is demonstrated by the suite rather than asserted in prose.

    In `src/test/measurement.test.ts`, add a fixture named `nonFunctionGlobalScope` beside the three existing refusal fixtures. It returns a `PlausibleScope` whose `plausible` property is the verifier's exact probed value — a plain object with a single `o` property whose value is the string `foreign` — cast through `unknown` to satisfy the declared `PlausibleGlobal` type, together with a reference to that same object so a caller can assert identity rather than only structural equality. Keep the fixture's docstring honest about what it isolates: a truthy value that is not callable, which the type system forbids but an ambient page snippet can produce.

    Add one case to the `fail-closed provider initialization` block using that fixture. It must assert that `createPlausibleEventSink` returns `undefined`, that the injected document has zero script elements, that `scope.plausible` is still the identical original object by reference, and that the original object still has exactly its one original property — no options slot, no queue, and no callable behaviour attached.

    Add one case to the `fail-closed provider initialization in the full journey` block using the same fixture and the existing `configuredMeasurement` helper and `MemoryStorage`. Do not force it into the `refusedRows` table: that table is typed against the callable fixtures' return shape, which carries a `forwarded` array a non-callable global cannot have. Write it as its own case asserting that the three tracked actions each return true, that the matching bounded local flags are recorded, that zero script elements are appended, that the console stays silent through `silentConsole` and `expectSilent`, and that the scope still holds the identical original object.

    Run the measurement suite and confirm the new adapter-boundary case fails on all three of the sink, the script count, and the identity assertions, then record that observed failure as RED evidence.

    In `src/measurement/plausible.ts`, change `resolveInitializedProvider` so the classification is decided before anything is written to the scope. Read the existing value once. When it is defined but not callable, return null immediately — before the stub is installed — because there is no way to establish the opt-out through it and overwriting somebody else's global is not this adapter's to do. When it is callable, adopt it exactly as today. Only when the scope carries no provider value at all may the stub be installed. Refusing rather than restoring is the deliberate choice here: restoring would mean the value had already been replaced, and the declared truth forbids replacement rather than merely requiring repair.

    Update the `resolveInitializedProvider` docstring so it states the rule the code implements: an ambient provider global is untrusted input; a callable one is adopted, never replaced or wrapped, and only when its initializer records the opt-out; a non-callable one is refused outright and left untouched; and a stub is installed only onto a scope that carried no provider value. Remove the closing sentence that describes withdrawal of a self-installed stub — Task 2 removes the branch it refers to, and the two must not disagree even for one commit.

    Do not change `installProviderStub`, `recordsOptOut`, `appendProviderScript`, the options object built in `createPlausibleEventSink`, the ordering that keeps script insertion unreachable until the resolver returns non-null, or the sink's single-bare-argument contract. Do not widen, re-type, or bypass the approved-script-source contract delivered by 04-08.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**' src/test/measurement.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**' src/test/measurement.test.ts` passes and `npm run typecheck` exits 0.
    - With a scope whose `plausible` is a truthy non-function value, `createPlausibleEventSink` returns `undefined` — no sink.
    - With that same scope, the injected document contains zero script elements — no appended script.
    - With that same scope, `scope.plausible` is still strictly the original object reference and still carries exactly its original property set — the original global intact, neither replaced, wrapped, decorated, nor deleted.
    - Through the facade with that same scope, all three tracked actions return true, the matching bounded local flags read true, zero script elements are appended, and every console spy records zero calls.
    - The three existing refusal cases and the original-identity case pass with expectations byte-identical to their pre-task state, confirmed by `git diff src/test/measurement.test.ts` showing no change inside them.
    - The confirmed-stub primary-path case still passes: on an empty scope the adapter installs the stub, the recorded options equal the configured domain with automatic capture disabled, exactly one script element is appended, and a tracked event lands in the queue.
    - Non-vacuity: reverting only the classification change in `resolveInitializedProvider` makes the new adapter-boundary case fail on all three assertions; the executor must run this revert check and record the observed failure in the summary.
    - `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**' src/test/build-output.test.ts` passes, so no analytics origin entered a production module and the approved-source boundary is intact.
  </acceptance_criteria>
  <reversibility rating="reversible">One additional guard in a module-private classifier plus two test cases and a fixture. Revertible in one commit; it only narrows when a sink is created and installs nothing new.</reversibility>
  <done>A truthy non-function pre-existing provider global is refused before anything is installed, and the verifier's exact probe — no sink, no appended script, original value intact — is pinned by coverage that fails when the change is reverted.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the unreachable refusal-cleanup branch and withdraw the claim it backed</name>
  <files>src/measurement/plausible.ts, .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md</files>
  <read_first>
    - `src/measurement/plausible.ts` — `resolveInitializedProvider` as left by Task 1: the `refuse` closure, its conditional cleanup of a self-installed stub, and its two call sites in the recorded-opt-out check and the catch block.
    - `src/measurement/plausible.ts` lines 85-102 — `installProviderStub`, whose initializer assigns the received options to the recorded slot and can neither throw nor produce a value that fails the recorded-opt-out check. This is the reason the cleanup is unreachable.
    - `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md` lines 36-46 — the `key-decisions` list, in particular the third entry asserting that a stub installed by the call is removed from the scope when initialization is refused.
    - `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md` lines 27-36 — the `tech-stack.patterns` list, whose third entry makes the same self-installed-state-removal claim and must be corrected in the same amendment.
    - `.planning/REQUIREMENTS.md` lines 46-60 — the existing dated Phase 4 status note, whose amendment style this task's note must match.
    - `04-VERIFICATION.md` gap 2 `missing` item two — delete the unreachable branch and its key-decision claim, or make it reachable and cover it.
    - `04-REVIEW.md` IN-01 — the reachability proof.
  </read_first>
  <action>
    Remove the unreachable cleanup from `resolveInitializedProvider`. After Task 1 the only path that installs a stub is the path where the scope carried no provider value, and that stub's initializer assigns the options it was handed and cannot throw, so the recorded-opt-out check on that path cannot fail. The conditional cleanup inside the `refuse` closure is therefore dead in every reachable state. Delete that conditional statement. With it gone the closure does nothing but produce a null result, so collapse it as well and return null directly from the recorded-opt-out check and from the catch block, keeping both refusal paths and both of their explanatory comments intact.

    Choosing removal over reachability is deliberate and must be recorded as such in the summary. Making the branch reachable would require `installProviderStub` to return a stub that can legitimately fail its own initialization, which means deliberately weakening the one component in this module the project fully controls in order to exercise a cleanup for a state that component cannot reach. The honest outcome is fewer claims, not more machinery — the same standard this phase is applying to its prose.

    Add a short comment where the stub is installed recording why no cleanup is needed there: the stub is installed only onto a scope that carried no provider value, and its own initializer cannot fail the recorded-opt-out check, so a refusal after installation is unreachable and there is nothing to withdraw. State it as a reachability fact, not as a mitigation.

    In `04-09-SUMMARY.md`, withdraw the two claims that the removed branch backed. Rewrite the third `key-decisions` entry so it records what is now true — that a pre-existing provider global is never replaced on any refusal path, whatever its type, because the classification decides before it assigns — and rewrite the third `tech-stack.patterns` entry to match. Then append a dated amendment note at the end of the summary body, in the same style as the existing dated note in `.planning/REQUIREMENTS.md`, that states four facts: which two claims were withdrawn and why they had no executable path; that `04-VERIFICATION.md` recorded the defect as gap 2 with status failed; that plan 04-12 replaced them with the stronger and reachable guarantee covered by the new non-callable-global cases; and that the D1 `coverage` row in that same file is a separate correction owned by plan 04-13 and is deliberately untouched here. Do not edit the D1, D2, or D3 `coverage` rows, the `requirements-completed` list, the `actuals` block, or any other section of that summary.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**' &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; test "$(grep -v -E '^\s*(\*|//|/\*)' src/measurement/plausible.ts | grep -c 'delete scope.plausible')" = "0"</automated>
    <human-check>
      These items are human gates recorded by `04-VERIFICATION.md`. They are NOT acceptance criteria for this plan and must not be automated or claimed closed by it:
      1. Production privacy approval and live event uniqueness — approve the processor, create the exact ten dashboard goals, configure the approved script and site values, deploy, and perform each explicit action once; expect one name-only event per action, no automatic duplicate, and no form value, browser-context property, or visitor identifier on any event.
      2. On the deployed page, confirm no other snippet, tag manager, or extension defines `window.plausible` before the bundle runs; expect the global to be undefined until the bundle installs its own stub, so the adoption path is never taken in production. This is the gate that covers the adopted-path echo the verifier recorded as `behavior_unverified`, and this plan narrows the non-callable case only — it does not close that gate.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The full real-tree suite run with `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**'` passes, and `npm run typecheck` and `npm run lint` each exit 0.
    - Source assertion: `grep -v -E '^\s*(\*|//|/\*)' src/measurement/plausible.ts | grep -c 'delete scope.plausible'` returns `0`. The comment-line filter is applied because this module's prose is dense; the assertion targets executable source only.
    - Source assertion: `grep -c 'a refusal removes it again' src/measurement/plausible.ts` returns `0`. No comment filter is applied here on purpose — the target of this assertion is comment prose.
    - Source assertion: `grep -c 'refuse()' src/measurement/plausible.ts` returns `0`, confirming the collapsed closure left no orphan call site.
    - Behavior assertion: all three existing refusal paths still return no sink and append zero script elements, and the pre-existing global is still byte-identical in each — proven by the unedited existing cases plus the new non-callable case from Task 1.
    - Behavior assertion: the primary path still installs, initializes, confirms, and appends exactly one script, proven by the unedited confirmed-stub case.
    - `04-09-SUMMARY.md` no longer asserts in `key-decisions` or in `tech-stack.patterns` that a self-installed stub is removed on refusal, and carries a dated amendment note naming the withdrawal, the gap it answers, the replacement guarantee, and the explicit hand-off of the D1 coverage row to plan 04-13.
    - `04-09-SUMMARY.md` D1, D2, and D3 `coverage` rows, `requirements-completed`, and `actuals` are byte-identical to their pre-task state, confirmed by reading `git diff` on that file.
    - `node scripts/verify-phase4-coverage.mjs .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md` still exits 0 and `git diff` shows `COVERAGE.md` unchanged.
    - `.planning/REQUIREMENTS.md` is unchanged: no checkbox and no status row is touched by this plan.
    - The summary cites the real-tree file and test counts produced by the exclusion-flagged command, not the counts produced by a bare `npm test` run that also collects the stale `.claude/worktrees/` copy.
    - `git diff --stat` shows exactly three files changed across this plan: `src/measurement/plausible.ts`, `src/test/measurement.test.ts`, and `04-09-SUMMARY.md`.
  </acceptance_criteria>
  <reversibility rating="reversible">Removal of an unreachable statement plus a documentation withdrawal. Revertible in one commit; it changes no reachable behaviour and no deployment state.</reversibility>
  <done>The adapter contains no branch that cannot execute, and neither the source nor the 04-09 record claims a mitigation that has no executable path behind it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambient page scope -> analytics adapter | `window.plausible` may already be defined by another snippet, a tag manager, or a browser extension. Its value is untrusted input of arbitrary type, and reading or writing it crosses out of this application's control. |
| analytics adapter -> third-party page state | Any write to the shared global is a mutation of somebody else's state; a replacement is destructive and, once made, unobservable to its owner. |
| analytics adapter -> visitor privacy | A returned sink plus an appended script is the moment collection becomes possible; everything before it is the gate. |
| build configuration -> executable script origin | The approved-source contract from 04-08 decides which origin and path may become executable; this plan must not widen or bypass it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-49 | Tampering | pre-existing non-callable `scope.plausible` | high | mitigate | The classification refuses a defined non-callable value before any assignment, so third-party state is never destroyed. Pinned by an adapter-boundary case asserting reference identity and an unchanged property set. |
| T-04-50 | Spoofing | foreign callable global adopting the analytics sink | high | transfer | Deliberately **not** closed in code by this plan. The verifier recorded the echoing-foreign-global case as `behavior_unverified` with a live human gate: confirm on the deployed page that no other snippet, tag manager, or extension defines the provider global before the bundle runs. Transferred to that gate and restated verbatim in Task 2's `human-check`; plan 04-13 makes the source prose match this posture. |
| T-04-51 | Information Disclosure | event sink parameter surface | high | accept | Already mitigated and unchanged: the sink forwards exactly one bare event name and there is no parameter through which a form value, browser-context property, or visitor identifier could travel. Re-asserted by the unedited name-only sink cases. |
| T-04-52 | Repudiation | unexecutable mitigation claimed in source and in the plan record | high | mitigate | The unreachable cleanup branch is removed and both claims it backed are withdrawn from `04-09-SUMMARY.md` with a dated amendment, so no recorded mitigation lacks an executable path. |
| T-04-53 | Denial of Service | refusal degrading the visitor journey | medium | mitigate | The new refusal path carries a full-journey MEAS-07 case: three tracked actions still succeed, every bounded local flag is still recorded, and the console stays silent. |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed by this plan. Any later dependency addition requires the package-legitimacy gate plus a blocking human checkpoint for `[ASSUMED]` or `[SUS]` packages; that checkpoint is never auto-approvable. |

**ASVS level:** 1. **Blocking threshold:** high. Every `high` threat is dispositioned. T-04-50 is `transfer` rather than `mitigate` because `04-VERIFICATION.md` classified the adopted-path echo as behaviour external to this repository and routed it to a named live human gate; closing it in code here would exceed the recorded gap scope and would silently retire a gate the verifier deliberately left open. T-04-51 is `accept` because it is already mitigated by shipped coverage this plan re-asserts rather than weakens.
</threat_model>

## Flagged unresolved assumption (not auto-resolved)

The deterministic edge probe returned one **unclassified** row for **MEAS-08** with the generic prompt *"unclassified — review manually"*. Per the spec-less probe fallback rules it stays **`unresolved`**: it is not auto-resolved with a backstop marker and it is not dropped.

Reading for this plan: MEAS-08 is a vocabulary-and-honesty requirement. This plan's contribution to it is structural rather than linguistic — it removes a claim from the source and from the 04-09 record that no path could support. The linguistic edge itself, that a future label or comment could reintroduce an overstated claim, is carried in full by plan 04-13. Recorded here so the row is visible on every plan that declares MEAS-08, not so it is answered twice.

The three classified rows are carried, not duplicated: MEAS-01 concurrency is represented in `must_haves.truths` above by citation to the shipped report-generation cases carried in 04-10 and re-asserted in 04-11; the two MEAS-05 rows belong to the enquiry-summary surface this plan does not touch and are carried by plan 04-13.

## UI considerations carried, not re-implemented

`04-UI-SPEC.md` records 46 applicable considerations across surfaces E1–E7. This plan touches no UI surface: it changes an analytics adapter's classification, its test coverage, and a planning record. The five `verification: backstop` rows (E1, E2, E3, E4 and E7 `long-text`) are already carried in 04-10's `must_haves` and attached to the phase's MVP readability human gate; they are cited here and deliberately not duplicated. Surfaces E5 and E6 — the disclosure summary group and the visible collection notice — carry only `explicit` rows, all shipped and verified by plans 04-02 and 04-04, and no visitor-facing copy changes here.

## Human gates preserved

The five `human_verification` entries and both `behavior_unverified_items` in `04-VERIFICATION.md` remain open human gates. In particular, the second `behavior_unverified` item — that a configured sink can only exist when automatic capture has genuinely been disabled, on the adopted path — is **not** closed by this plan. This plan narrows the non-callable case only; the echoing-foreign-global case stays with its live gate, restated in Task 2's `human-check`. No requirement status is promoted.

<verification>
- `npm run build` succeeds, so the `dist` freshness contract in `src/test/build-output.test.ts` holds after a production source input changes mtime.
- `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**'` passes across the real tree, including the new non-callable-global adapter case and its full-journey MEAS-07 companion.
- `npm run typecheck` and `npm run lint` each exit 0.
- The three source assertions in Task 2 return their expected counts.
- `node scripts/verify-phase4-coverage.mjs .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md` exits 0 with `COVERAGE.md` unchanged.
- The revert check was run and its observed failure is recorded in the summary, so the new coverage is not vacuous.
- `git diff --stat` is confined to `src/measurement/plausible.ts`, `src/test/measurement.test.ts`, and `04-09-SUMMARY.md`.
</verification>

<success_criteria>
- Gap 2 of `04-VERIFICATION.md` — the phase's one failed truth — is closed: no pre-existing provider global of any type is replaced, wrapped, mutated, or deleted on a refusal path.
- The stub-removal claim is gone from the source, from the `key-decisions` list, and from the `patterns` list, and its withdrawal is recorded with a dated amendment rather than performed silently.
- The three previously proven refusal paths and the primary path are unchanged, proven by unedited cases.
- The adopted-path echo remains an open human gate, and no requirement status is promoted by this plan.
</success_criteria>

<output>
Create `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-12-SUMMARY.md` when done.
</output>
