---
phase: 04-report-and-enrich-the-haoo-funnel-truthfully
plan: 09
type: execute
wave: 2
depends_on: ["04-08"]
files_modified:
  - src/measurement/plausible.ts
  - src/test/measurement.test.ts
autonomous: true
gap_closure: true
requirements: [MEAS-01, MEAS-08]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Automatic pageview capture is disabled before any configured provider script or event sink becomes usable: `createPlausibleEventSink` appends no script element and returns `undefined` unless initialization has been confirmed to have recorded `autoCapturePageviews: false` for the configured domain."
    - "The verifier's first adversarial probe passes: importing `createPlausibleEventSink` with an existing callable provider that has no `init` function returns `undefined`, appends zero script elements, leaves the pre-existing global at the identical function reference, and adds no options or queue property to it."
    - "The verifier's second adversarial probe passes: an existing provider whose `init` throws returns `undefined`, appends zero script elements, and lets no exception escape into the caller."
    - "A third adversarial case is covered: an existing provider whose `init` is a silent no-op that records no options returns `undefined` and appends zero script elements, so a cosmetic initializer cannot satisfy the opt-out."
    - "Script insertion happens only after confirmed initialization, so the ordering defect that allowed a managed script to load with automatic capture unproven is structurally impossible rather than merely untested."
    - "MEAS-07 does not regress in any of the three fail-closed cases: a full `createMeasurement` initialize-and-track round trip still returns `true`, still updates the bounded local interaction flags, still writes nothing to the console, and still appends no script."
    - "The already-shipped preload contract is unchanged for the cooperative path: when this adapter installs the documented stub, initialization options land in `plausible.o`, `plausible.q` carries only real one-argument event calls, and all ten allowlisted names still forward exactly one bare argument."
    - "The pre-existing provider-failure isolation contracts still pass unchanged: an absent global, a throwing provider call, and a failed script load all leave the journey and the local context intact."
  prohibitions:
    - "No event sink may be returned, and no provider script may be appended, while the automatic-capture opt-out is unconfirmed."
    - "No pre-existing provider global may be replaced, wrapped, or mutated by this adapter when its initializer is absent, throwing, or non-recording."
    - "No initialization failure may be surfaced to the visitor, logged to the console, retried, or allowed to break the qualification journey or the bounded local context."
    - "No automatic pageview, outbound-link, download, form, hash-routing, or revenue capture may be enabled alongside the explicit ten-event seam."
    - "No provider call may carry event properties, form values, browser context, a stable visitor identifier, or more than the single allowlisted event-name argument."
    - "No aggregate occurrence may be inflated by a duplicate automatic pageview, because D-01's staged literal counts and D-04's counts-and-changes-only rule depend on one explicit action producing exactly one event."
    - "Canon referral: prototype pollution on the injected scope object is a canon security concern owned by `/gsd-secure-phase` and eslint; it is not minted as a bespoke prohibition here."
  artifacts:
    - path: "src/measurement/plausible.ts"
      provides: "Fail-closed provider initialization that confirms the recorded automatic-capture opt-out before any script insertion or sink return"
      exports: ["PlausibleGlobal", "PlausibleInitOptions", "PlausibleScope", "PlausibleAdapters", "createPlausibleEventSink"]
    - path: "src/test/measurement.test.ts"
      provides: "Absent-initializer, throwing-initializer, and non-recording-initializer adversarial cases plus their MEAS-07 journey regressions, and a `recordingScope` fixture that mirrors the documented preload stub"
  key_links:
    - from: "src/measurement/index.ts"
      to: "src/measurement/plausible.ts"
      via: "The unchanged facade calls the adapter after campaign cleanup and leaves `eventSink` undefined when the adapter refuses to initialize"
      pattern: "createPlausibleEventSink"
    - from: "src/test/fixtures/plausible-preload-contract.ts"
      to: "src/measurement/plausible.ts"
      via: "The independent vendor oracle still agrees with the adapter on the cooperative preload path"
      pattern: "plausible.o"
---

<objective>
Close verification gap 2 and review finding CR-02: make the automatic-capture opt-out a precondition of collection rather than a best-effort side call, so no provider script is inserted and no event sink exists until the opt-out is confirmed.

Purpose: MEAS-01 needs occurrence counts that are not inflated by automatic pageviews, and MEAS-08 needs the report to describe evidence the measurement layer actually controls. Today a pre-existing provider without a usable initializer, or one whose initializer throws, still yields a live sink and an appended script — so the phase's central privacy claim is unproven exactly where it matters most.
Output: fail-closed initialization ordering, three adversarial provider cases, and their MEAS-07 journey regressions.
</objective>

<execution_context>
@/home/paul/.claude/gsd-core/workflows/execute-plan.md
@/home/paul/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-CONTEXT.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-REVIEW.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-VERIFICATION.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-06-SUMMARY.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md
@src/measurement/plausible.ts
@src/measurement/index.ts
@src/test/measurement.test.ts
@src/test/fixtures/plausible-preload-contract.ts
</context>

## Artifacts this phase produces

New symbols:

- A private provider-resolution function in `src/measurement/plausible.ts` that returns a `PlausibleGlobal` only when initialization is confirmed and `null` otherwise, replacing the current unconditional `ensureProvider` return.

Changed symbols:

- `createPlausibleEventSink` in `src/measurement/plausible.ts` — now returns `undefined` on unconfirmed initialization and only appends the provider script after confirmation.
- `recordingScope()` in `src/test/measurement.test.ts` — its `init` now mirrors the documented preload stub by assigning the received options to the provider's options slot in addition to recording the call.

Behavior contracts added:

- Absent-initializer, throwing-initializer, and non-recording-initializer adversarial cases in `src/test/measurement.test.ts`.
- A MEAS-07 journey regression for each of those three cases.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Require a confirmed automatic-capture opt-out before any script insertion or sink return</name>
  <files>src/measurement/plausible.ts, src/test/measurement.test.ts</files>
  <read_first>
    - `src/measurement/plausible.ts` in full — `PlausibleInitOptions`, `PlausibleGlobal` including its `q`, `o`, and `init` members, `resolveScope`, `resolveDocument`, `alreadyAppended`, `appendProviderScript`, `ensureProvider`, and the current `createPlausibleEventSink` ordering at lines 121-155.
    - `src/measurement/index.ts` lines 300-325 — the initialization order, the campaign-cleanup-before-provider comment, and the `eventSink === undefined` guard that keeps an injected sink authoritative.
    - `src/test/measurement.test.ts` lines 478-500 — the `recordingScope()` fixture whose `init` currently records the call but never assigns the options slot.
    - `src/test/measurement.test.ts` lines 559-650 — the existing `name-only provider sink` cases, including the script-appended-once case, the initialization-before-forwarding case, the ten-name arity case, and the documented preload options and event-queue contract case.
    - `src/test/measurement.test.ts` lines 651-724 — the three existing `provider failure isolation` cases that must stay green unchanged.
    - `src/test/fixtures/plausible-preload-contract.ts` — the independent vendor oracle, whose stub assigns the options it receives; this is the documented behavior the confirmation check relies on.
    - `04-REVIEW.md` CR-02 and `04-VERIFICATION.md` gap 2 — the exact fail-open branches and the two `missing` items this task must satisfy.
  </read_first>
  <behavior>
    - "RED: a case supplying an existing callable provider with no `init` expects `undefined` and zero appended scripts, and fails today because the adapter returns a live sink and appends the script."
    - "RED: a case supplying an existing provider whose `init` throws expects `undefined` and zero appended scripts, and fails today for the same reason."
    - "GREEN: both cases return `undefined`, append zero scripts, and leave the pre-existing global at the identical reference with no options or queue property added."
    - "GREEN: a provider whose `init` runs without error but records no options also returns `undefined` and appends zero scripts."
    - "GREEN: the cooperative path is unchanged — the adapter installs the documented stub, the options slot holds the configured domain with automatic capture disabled, the queue holds only real one-argument event calls, and the script is appended exactly once."
    - "REGRESSION: the absent-global, throwing-call, and failed-script-load isolation cases still pass without edits to their expectations."
  </behavior>
  <action>
    Replace `ensureProvider` in `src/measurement/plausible.ts` with a resolution function that takes the scope and the exact `PlausibleInitOptions` this project sends and returns a `PlausibleGlobal` on success or `null` on any unconfirmed outcome. Its order of operations is the fix: first decide the provider, then initialize, then confirm, and only then let the caller append the script.

    Decide the provider as follows. When `scope.plausible` is already a function, adopt it without replacing or wrapping it, and record that this call did not install it; if that adopted function's `init` member is not a function, return `null` immediately and leave the existing global byte-identical — do not attach an `init`, an options slot, or a queue to somebody else's global. When `scope.plausible` is absent, install the documented preload stub exactly as it is installed today: the callable pushes its `arguments` onto `q`, and `init` assigns the received options to `o`.

    Initialize inside a try. If `init` throws, return `null`; if this call installed the stub, remove the property it added from the scope so a refused initialization leaves no provider behind. Then confirm the opt-out: read the provider's options slot and require it to be an object whose `autoCapturePageviews` is exactly `false` and whose `domain` equals the configured `providerScript.domain`. If the slot is absent or disagrees, treat the provider as unusable — return `null`, again removing an installed stub. Confirming the recorded options rather than merely a non-throwing call is what makes the truth "automatic pageview capture is disabled before any provider script or event sink becomes usable" provable instead of assumed, and it closes the silent no-op initializer that neither of the verifier's two probes would have caught. A pre-existing full provider implementation that does not expose the documented options slot is therefore treated as unconfirmable and yields no sink — an accepted, deliberate fail-closed outcome for this privacy posture.

    Rewrite `createPlausibleEventSink` so it keeps its existing early returns for a non-`plausible` provider, an empty `src`, an empty `domain`, a null scope, and a null document; then calls the resolution function; then returns `undefined` when it yields `null` — before `appendProviderScript` is reached. Only on a confirmed provider does it call `appendProviderScript(documentRef, providerScript.src)` and return the existing sink closure unchanged: one bare event-name argument, read fresh from `scope.plausible` at call time, wrapped in try/catch. Keep script de-duplication, the deferred script attribute, and every failure-isolation comment intact. Do not change `src/measurement/index.ts`, the five-member facade, provider resolution in `src/products/haoo.ts`, the ten-name allowlist, the deploy workflow, or any public build variable.

    In `src/test/measurement.test.ts`, update `recordingScope()` so its `init` assigns the received options to the provider's options slot in addition to pushing its `init` record — this makes the fixture mirror the documented vendor stub rather than a cooperative-but-unrealistic mock, and it is what keeps the existing initialization-order, ten-name arity, and facade-wiring cases green. Prove the two RED cases before editing production code.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/measurement.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit -- --run src/test/measurement.test.ts` passes and `npm run typecheck` exits 0.
    - Given a scope whose `plausible` is a plain callable with no `init`, `createPlausibleEventSink(CONFIGURED_MEASUREMENT, { documentRef, scope })` returns `undefined`, `documentRef.querySelectorAll('script')` has length 0, and `scope.plausible` is the same function reference with no options slot and no queue added.
    - Given a scope whose `plausible.init` throws, the call returns `undefined`, appends zero scripts, and does not propagate the exception.
    - Given a scope whose `plausible.init` returns without recording options, the call returns `undefined` and appends zero scripts.
    - Given an empty scope, the call returns a function, appends exactly one deferred script, the installed provider's options slot equals the configured domain with automatic capture disabled, and the queue is empty until a real event is forwarded.
    - Calling the adapter twice with the same document and scope still appends exactly one script.
    - All ten allowlisted event names still forward with an invocation arity of exactly 1.
    - The three pre-existing provider-failure isolation cases pass with their expectations unedited.
    - In `src/measurement/plausible.ts`, no code path reaches `appendProviderScript` before the resolution function has returned a non-null provider.
    - `git diff` shows no change to `src/measurement/index.ts`, `.github/workflows/deploy.yml`, or any `VITE_HAOO_*` value.
  </acceptance_criteria>
  <reversibility rating="reversible">A single adapter's ordering and guard conditions; revertible in one commit with no data, schema, or deployment consequence.</reversibility>
  <done>The adapter refuses to append a script or return a sink until it has confirmed the recorded automatic-capture opt-out, and both verifier probes plus the silent-no-op case return no sink and no script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the local journey stays fully functional in every refused-initialization case</name>
  <files>src/test/measurement.test.ts</files>
  <read_first>
    - `src/test/measurement.test.ts` lines 651-724 — the `provider failure isolation` block's `configuredMeasurement` helper, `silentConsole`, `expectSilent`, and `MemoryStorage` usage, which the new cases must reuse rather than reinvent.
    - `src/measurement/index.ts` lines 300-345 — `initialize()`, the `eventSink === undefined` guard, and `track()`'s return value and context-write ordering, which together define what "the journey still works" means here.
    - `src/measurement/plausible.ts` after Task 1 — the three refusal branches whose journey consequences this task pins.
    - `src/products/haoo.ts` `interactionEventFlags` — the mapping from `haoo_brochure_download` to `brochureDownloaded`, which is the observable local-context effect asserted below.
    - `04-VERIFICATION.md` gap 2 `missing` item two — the requirement that the absent-initializer and throwing-initializer tests assert no script and no sink while the local journey remains functional.
  </read_first>
  <behavior>
    - "For an existing callable with no `init`: `measurement.initialize()` then `measurement.track('haoo_brochure_download')` returns `true`, `readContext().flags.brochureDownloaded` is `true`, zero scripts are appended, and the console is silent."
    - "For an existing provider whose `init` throws: the same four assertions hold."
    - "For an existing provider whose `init` records nothing: the same four assertions hold."
    - "In all three cases a second and third tracked event still returns `true` and still updates the bounded local context, so refusal is not a one-shot degradation."
    - "In all three cases the pre-existing provider receives zero forwarded calls, proving no sink was silently wired."
  </behavior>
  <action>
    Add a describe block covering fail-closed provider initialization that builds each of the three refused-initialization scopes and runs them through the full facade using the existing `configuredMeasurement` helper pattern — a fresh `document.implementation.createHTMLDocument(...)`, a `MemoryStorage`, the fixed `TODAY` clock, the `PRODUCT_HREF` location, a stubbed `history.replaceState`, and `providerAdapters: { documentRef, scope }`. For each scope assert the MEAS-07 contract explicitly: `track('haoo_brochure_download')` returns `true`, `readContext().flags.brochureDownloaded` is `true`, a second tracked name such as `haoo_qualify_start` also returns `true` and sets `qualifyStarted`, `documentRef.querySelectorAll('script')` has length 0, the pre-existing provider recorded zero forwarded calls, and `expectSilent` holds for log, warn, error, and debug.

    Build the three scopes as distinct minimal fixtures rather than variations of `recordingScope()`, so each one isolates exactly one defect: a bare callable that records forwarded calls and has no `init` member at all; a callable whose `init` throws a plain `Error`; and a callable whose `init` returns without touching the options slot. Each fixture must record every forwarded call so the zero-forwarded-calls assertion is real rather than vacuous.

    Add one direct-adapter case asserting that a refused initialization leaves the scope's provider identity untouched: capture the pre-existing function reference before the call and assert `scope.plausible` is strictly the same reference afterwards, with no options slot and no queue property present. Do not modify the existing `provider failure isolation` cases, the documented preload contract case, or the facade-contract cases; this task only adds coverage.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes end to end, including the build step and every suite.
    - Three new fail-closed cases each assert: sink refused, zero scripts appended, `track()` returns `true`, the mapped local interaction flag becomes `true`, zero forwarded provider calls, and a silent console.
    - Each of the three cases covers a different defect — absent `init`, throwing `init`, and non-recording `init` — and none of them reuses `recordingScope()`.
    - A second tracked event in each case still returns `true` and still updates its mapped flag, proving refusal does not degrade later actions.
    - The strict-identity case proves the pre-existing provider function reference is unchanged and gained no options or queue property.
    - Reverting Task 1's production change makes at least the absent-`init` and throwing-`init` cases fail, so the new coverage is non-vacuous.
    - `npm run lint` exits 0 and `git diff` shows no change under `src/` outside `src/test/measurement.test.ts` for this task.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only additions; no production behavior, deployment, or data change.</reversibility>
  <done>Every refused-initialization path is pinned by a full-facade regression proving the HAOO journey and the bounded local context keep working with no analytics, no script, and no console noise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambient page globals -> measurement adapter | Another snippet may already have defined `window.plausible`; that global is untrusted input to this adapter. |
| measurement adapter -> managed provider script | Script insertion grants first-party execution and must not happen before the automatic-capture opt-out is confirmed. |
| explicit component actions -> measurement facade | Only closed event literals may cross; visitor context and form values must not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-33 | Tampering | `createPlausibleEventSink` initialization ordering | critical | mitigate | The provider script is appended only after the recorded opt-out is confirmed; every unconfirmed path returns `undefined` before insertion. |
| T-04-34 | Information Disclosure | automatic pageview capture with campaign parameters | high | mitigate | A provider that cannot record `autoCapturePageviews: false` yields no sink, so automatic capture can never begin with `utm_*` values still in the address bar. |
| T-04-35 | Spoofing | pre-existing `window.plausible` global | high | mitigate | A foreign callable is adopted only when it exposes a usable initializer and records the opt-out; otherwise it is left untouched and unused. |
| T-04-36 | Repudiation | aggregate occurrence counts | high | mitigate | Refusing collection when the opt-out is unproven prevents duplicate automatic pageviews from inflating the counts the report labels literally. |
| T-04-37 | Denial of Service | refused initialization | low | accept | Refusal disables analytics only; three full-facade regressions prove the journey, the bounded context, and the console stay unaffected (MEAS-07). |
| T-04-38 | Elevation of Privilege | stub installed onto a shared scope | medium | mitigate | A stub installed by this call is removed when initialization is refused, so no partially initialized provider is left on the page. |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation occurs in this plan; any later dependency addition requires the package-legitimacy gate plus a blocking human checkpoint for `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `npm run test:unit -- --run src/test/measurement.test.ts` passes with the three adversarial cases and the unedited pre-existing isolation cases.
- `npm test`, `npm run lint`, and `npm run typecheck` all pass.
- Manual re-run of the verifier's two probes against the built adapter returns no sink and appends no script in both.
- `git diff` confirms `src/measurement/index.ts`, `.github/workflows/deploy.yml`, and every `VITE_HAOO_*` value are untouched.
</verification>

<success_criteria>
- Verification gap 2 is closed with executable evidence for the absent-initializer, throwing-initializer, and non-recording-initializer paths.
- Automatic pageview capture is provably disabled before a provider script or event sink can exist, not merely attempted.
- MEAS-07 is protected by explicit regressions in every refusal path rather than assumed from the cooperative path.
- MEAS-01's count-integrity blocker and MEAS-08's evidence-integrity blocker no longer include an unproven-opt-out collection path.
</success_criteria>

<output>
Create `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md` when done.
</output>
