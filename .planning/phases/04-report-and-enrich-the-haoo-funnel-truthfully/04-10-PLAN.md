---
phase: 04-report-and-enrich-the-haoo-funnel-truthfully
plan: 10
type: execute
wave: 3
depends_on: ["04-09"]
files_modified:
  - src/reporting/generate.ts
  - src/test/haoo-report.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [MEAS-01, MEAS-05, MEAS-08]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The documented owner command creates its output directory on a first run regardless of host path separator: a Windows-style destination yields a `mkdirSync` call for the correct parent directory before the temporary sibling is reserved, and the existing POSIX behaviour is unchanged."
    - "A destination with no parent directory component still skips directory creation instead of attempting to create a bare drive root or an empty path."
    - "MEAS-05's requirement row and checklist entry reflect the verifier's `SATISFIED` finding, and MEAS-01 and MEAS-08 remain unchecked with an explicit dated note naming the exact human gates that still block them."
    - "No requirement is marked complete on the strength of these gap-closure plans alone: production privacy approval, live event uniqueness, and live report reconciliation are recorded as open human gates owned by re-verification, not by an executor."
    - "Edge — MEAS-01 concurrency: concurrent and interrupted report generations are already proven fail-closed by the exclusive-reservation, real-filesystem rename, partial-write, and concurrent-loser cases in `src/test/haoo-report.test.ts`; no duplicate coverage is added, and those cases must still pass."
    - "Edge — MEAS-05 boundary: the engagement summary's visit and recency thresholds and its 32-character campaign cap are already proven at each threshold and one step either side by the shipped cases in `src/test/qualify-form.test.tsx`; no duplicate coverage is added, and those cases must still pass."
    - "Edge — MEAS-05 precision: no rounding, tie-breaking, or overflow contract is required because the report renders only integer occurrence counts and the exact integer difference as `+N`, `−N`, or `No change`, and per D-04 it computes no percentage at all — already pinned by the shipped no-percent-sign and zero-one-many cases."
    - statement: "Held-out visual check at 320px and 200% zoom confirms the four period labels wrap without clipping, ellipsis, or overlap of the 44px targets."
      verification: backstop
    - statement: "Held-out visual check at 320px and 200% zoom confirms every stage clarifier and long stage label wraps fully inside the card without clipping or overlap."
      verification: backstop
    - statement: "Held-out visual check confirms the longest event label wraps inside its row header at 320px and 200% zoom without clipping and without forcing body-level horizontal scroll."
      verification: backstop
    - statement: "Held-out visual check at 320px and 200% zoom confirms the full caveat paragraph and the metadata line render without clipping, ellipsis, or overlap."
      verification: backstop
    - statement: "Held-out check renders the maximum-length summary (frequent visit band, all five flags true, all three normalized campaign values present) and confirms it stays one readable paragraph with no truncation and no field-splitting."
      verification: backstop
  prohibitions:
    - "No requirement checkbox or status may be marked complete for work these gap-closure plans do not actually prove, and no open human gate may be silently converted into an automated acceptance criterion."
    - "No report may calculate or display a percentage, a conversion rate, or any person-level progression claim, per D-04."
    - "No report may describe aggregate occurrences as people, sessions, cohorts, customers, confirmed delivery, or completed onboarding."
    - "No stage total or event-level breakdown may collapse separate observable actions into one another, per D-01 and D-02."
    - "No bounded period may be compared against a fabricated preceding period, and all time must never invent one, per D-03."
    - "No report generation failure may replace the prior report or leave an owned partial business-data artifact beside it."
    - "No credential value, and no analytics origin or query path, may appear in the generated report, in a diagnostic message, or in any browser bundle."
    - "No engagement summary may expose a raw storage record, a form answer, an opaque score, or a stable visitor identifier."
    - "Canon referral: path traversal and OS path handling are canon security concerns owned by `/gsd-secure-phase` and eslint; only the specific first-run directory-creation defect is fixed here."
  artifacts:
    - path: "src/reporting/generate.ts"
      provides: "Separator-agnostic output-directory extraction that creates the report directory on a first run on any host"
      exports: ["ReportFs", "generateHaooReport"]
    - path: "src/test/haoo-report.test.ts"
      provides: "Directory-extraction contract table covering POSIX, Windows-style, bare-filename, and drive-root destinations with recorded filesystem call order"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Truthful Phase 4 requirement status with an explicit open-human-gate note"
  key_links:
    - from: "scripts/generate-haoo-report.mjs"
      to: "src/reporting/generate.ts"
      via: "The CLI passes a platform-native resolved output path that the generator must parse on any host"
      pattern: "outputPath"
    - from: "src/reporting/generate.ts"
      to: "the injected `ReportFs` capability"
      via: "The extracted directory is created recursively before the temporary sibling is exclusively reserved"
      pattern: "mkdirSync"
---

<objective>
Close review warning WR-01 and make Phase 4's recorded requirement status truthful, without marking anything complete that these gap-closure plans do not prove.

Purpose: MEAS-01 depends on the owner actually being able to run the documented local report command; a POSIX-only path parser makes a first run on Windows skip directory creation and fail. MEAS-05 and MEAS-08 depend on the recorded status matching the verifier's findings rather than lagging behind them.
Output: a separator-agnostic directory extraction with a contract table, and a REQUIREMENTS.md record that names the exact human gates still holding MEAS-01 and MEAS-08 open.
</objective>

<execution_context>
@/home/paul/.claude/gsd-core/workflows/execute-plan.md
@/home/paul/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-CONTEXT.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-REVIEW.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-VERIFICATION.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-07-SUMMARY.md
@src/reporting/generate.ts
@src/test/haoo-report.test.ts
@scripts/generate-haoo-report.mjs
</context>

## Artifacts this phase produces

Changed symbols:

- `directoryOf(path)` in `src/reporting/generate.ts` — now separator-agnostic and guarded against returning a bare drive root.

Behavior contracts added:

- A directory-extraction contract table in `src/test/haoo-report.test.ts` covering POSIX, Windows-style, bare-filename, and drive-root destinations, asserting both the recorded `mkdirSync` argument and its ordering relative to `reserveTempSync`.

Documentation surfaces changed:

- `.planning/REQUIREMENTS.md` — MEAS-05 status and checklist entry; a dated Phase 4 open-human-gate note under Privacy-First Measurement.

## Flagged unresolved assumption (not auto-resolved)

The deterministic edge probe returned one **unclassified** row for **MEAS-08** with the generic prompt *"unclassified — review manually"*. Per the spec-less probe fallback rules this row is deliberately left **`unresolved`**: it is not auto-resolved with a backstop marker and it is not dropped. It is surfaced here for the developer's judgment.

Reading of that row for this phase: MEAS-08 is a *vocabulary and honesty* requirement rather than a data-shape one, so the shape taxonomy has no category for it. Its real edge is linguistic — a future label, caveat, or stage name could reintroduce a delivery, customer, cohort, or completed-onboarding claim. Plans 04-01 through 04-03 already pin the closed label map, the authored caveat block, the banned-vocabulary scan, and the zero-percent-sign contract, so the edge is *represented* by existing coverage. No new task is created for it. If the developer wants a stronger guarantee, the natural follow-up is a review-time human read of any new report copy, not another automated scan.

## UI considerations carried, not re-implemented

`04-UI-SPEC.md` `## UI Considerations` records 42 resolved (explicit) considerations across surfaces E1–E7. Those are already delivered and verified by plans 04-01 through 04-05; this gap-closure run touches no UI surface and does not re-litigate them. The resolved (backstop) considerations are carried into `must_haves.truths` above as structured backstop markers so they stay attached to the phase's human verification rather than being dropped.

**Discrepancy to note for the developer:** the `## UI Considerations` header states *"4 resolved (backstop)"* but the tables contain **five** backstop rows (E1, E2, E3, E4, and E7 `long-text`). All five are carried above. The header count in `04-UI-SPEC.md` is off by one and should be corrected the next time that document is edited; no plan here modifies the UI-SPEC.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make report output-directory creation work on a first run on any host</name>
  <files>src/reporting/generate.ts, src/test/haoo-report.test.ts</files>
  <read_first>
    - `src/reporting/generate.ts` lines 85-105 — `reportDay` and the current `directoryOf`, which searches for a forward slash only and returns the empty string unless the separator index is greater than zero.
    - `src/reporting/generate.ts` lines 235-275 — the write sequence: render, extract directory, conditional `mkdirSync`, `reserveTempSync`, `writeFileSync`, `renameSync`, and the owned-temp cleanup in the catch.
    - `src/reporting/generate.ts` lines 55-80 — the `ReportFs` capability interface and `GenerateHaooReportOptions`, which is how the test injects a recording filesystem.
    - `scripts/generate-haoo-report.mjs` lines 28-70 — `ROOT`, `OUTPUT_PATH` built with Node's platform-native `resolve`, and how the CLI injects real filesystem capabilities.
    - `src/test/haoo-report.test.ts` lines 315-365 — the existing fake `ReportFs` with its no-op `mkdirSync` and the `generateOptions` helper that accepts an output path.
    - `src/test/haoo-report.test.ts` lines 720-845 — the real-filesystem rename, partial-write, and concurrent-loser cases whose expectations must stay unedited.
    - `04-REVIEW.md` WR-01 — the exact defect, the failing scenario, and the suggested fixture path shape.
  </read_first>
  <behavior>
    - "RED: a case using the Windows-style destination `C:\\\\project\\\\.reports\\\\haoo-funnel-report.html` expects a recorded `mkdirSync` call for `C:\\\\project\\\\.reports` and fails today because no directory call is made at all."
    - "GREEN: the same case records exactly one `mkdirSync` call with the correct parent directory and `{ recursive: true }`."
    - "GREEN: the POSIX destination still records `mkdirSync` for its parent directory, unchanged from today."
    - "GREEN: a bare filename destination with no separator records no directory call."
    - "GREEN: a drive-root destination such as `C:\\\\haoo-funnel-report.html` records no directory call rather than attempting to create a bare drive designator."
    - "GREEN: in every case that does create a directory, the recorded call order places the directory creation strictly before the temporary-file reservation."
  </behavior>
  <action>
    In `src/reporting/generate.ts`, change `directoryOf` to take the last index of either a forward slash or a backslash, so a platform-native path produced by Node's `resolve` on Windows is parsed correctly. Keep the existing "no separator means no directory" behaviour by returning the empty string when no separator is present or when the separator is at index zero. Add one further guard: when the extracted directory is only a drive designator — a single ASCII letter followed by a colon — return the empty string as well, because asking the filesystem to recursively create a bare drive root is not a directory creation the report needs and would turn a working run into a caught generation failure. Do not import `node:path` or any Node module into this file: the module's whole design is that every filesystem effect arrives through the injected `ReportFs` capability, and the credential-and-provider-origin boundary case depends on this file staying free of host-specific seams. Leave the write sequence, the exclusive `reserveTempSync` reservation, the ownership flag, the rename, and the owned-temp cleanup in the catch exactly as they are.

    In `src/test/haoo-report.test.ts`, add a directory-extraction contract describe block driven by a table of destination paths and their expected directory outcome, covering at minimum: the canonical POSIX destination, the Windows-style destination `C:\\project\\.reports\\haoo-funnel-report.html`, a bare filename with no separator, a POSIX root-level destination, and the drive-root destination `C:\\haoo-funnel-report.html`. Drive an existing successful generation through a fake `ReportFs` that records every call as an ordered list of operation name and arguments, then assert both the exact `mkdirSync` argument pair and that its recorded index precedes the `reserveTempSync` index whenever a directory is expected, and that no `mkdirSync` entry exists at all when none is expected. Reuse the existing successful-response fetch fixtures and the `generateOptions` helper's output-path parameter rather than building a second response harness. Do not modify the existing real-filesystem rename, partial-write, or concurrent-loser cases; those already prove the D-03 four-period write path and the MEAS-01 concurrency edge and must keep passing untouched.
  </action>
  <verify>
    <automated>npm run test:unit -- --run src/test/haoo-report.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit -- --run src/test/haoo-report.test.ts` passes and `npm run typecheck` exits 0.
    - The Windows-style destination records exactly one `mkdirSync` call whose first argument is `C:\\project\\.reports` and whose second argument is an object with `recursive` set to `true`.
    - The canonical POSIX destination still records a `mkdirSync` call for its parent directory.
    - A bare filename destination and the drive-root destination `C:\\haoo-funnel-report.html` each record zero `mkdirSync` calls.
    - Wherever a directory is created, the recorded call order places `mkdirSync` strictly before `reserveTempSync`.
    - Reverting the `directoryOf` change makes the Windows-style row fail, so the new coverage is non-vacuous.
    - `src/reporting/generate.ts` still contains no Node module import and no analytics origin, query path, or credential variable name; the credential-and-provider-origin boundary case still passes.
    - The pre-existing 120 report cases still pass with unedited expectations.
  </acceptance_criteria>
  <reversibility rating="reversible">A two-line parsing change plus a test table; revertible in one commit with no data or deployment consequence.</reversibility>
  <done>The documented `npm run report:haoo` command creates its output directory on a first run on any host, and the behaviour is pinned by a destination-path contract table that also proves the creation-before-reservation ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Record truthful Phase 4 requirement status and the human gates that remain open</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 33-56 — the Privacy-First Measurement checklist, the current unchecked MEAS-01, MEAS-05, and MEAS-08 entries, and the existing dated MEAS-08 amendment note whose format this task must match.
    - `.planning/REQUIREMENTS.md` lines 112-132 — the requirement-to-phase status table and its existing status vocabulary of `Complete`, `Gaps Found`, and `Pending`.
    - `04-VERIFICATION.md` `## Requirements Coverage` — the verifier's findings: MEAS-01 blocked, MEAS-05 satisfied, MEAS-08 blocked.
    - `04-VERIFICATION.md` `## Human Verification Required After Automated Gaps Close` — the three human gates that must be recorded verbatim in substance and must never become automated criteria.
    - `04-VERIFICATION.md` `### Roadmap Success Criteria` — criterion 2 verified, criteria 1 and 3 status, which is the evidence for the MEAS-05 transition.
  </read_first>
  <action>
    Change the MEAS-05 checklist entry in the Privacy-First Measurement section from unchecked to checked, and change its row in the requirement-to-phase table from `Gaps Found` to `Complete`. That is the only status promotion this task makes, and its evidence is the verifier's `MEAS-05 ✓ SATISFIED` finding together with roadmap success criterion 2 recorded as verified. If `04-VERIFICATION.md` does not still record MEAS-05 as satisfied at execution time, leave the entry unchecked and record why instead — the recorded status must follow the artifact, never the other way round.

    Leave the MEAS-01 and MEAS-08 checklist entries unchecked and leave their table rows at `Gaps Found`. Re-verification owns that transition; a gap-closure executor must not pre-empt it. Add one dated note in the same style as the existing MEAS-08 amendment, immediately after the Privacy-First Measurement checklist, that records four facts: that plans 04-08 and 04-09 close the two code-level blockers named in `04-VERIFICATION.md` — the unapproved-script-origin path and the unproven automatic-capture opt-out; that MEAS-01 additionally requires production privacy approval, dashboard goal creation, and live event-uniqueness confirmation, all of which remain deferred human gates; that MEAS-08 additionally requires live report reconciliation against the raw provider dashboard; and that the MVP outcome and privacy readability judgment at 320px and 200% zoom with keyboard and screen-reader use is a human gate covering the report, the disclosure, and the maximum-context engagement summary. State plainly in the note that no box may be checked for MEAS-01 or MEAS-08 until `/gsd-verify-work 04` confirms the code-level closure and the named human gates are cleared.

    Do not change any requirement outside Phase 4, do not change the Coverage totals, do not renumber or reword any requirement text, and do not touch the existing MEAS-08 `redirect returns` amendment note.
  </action>
  <verify>
    <automated>grep -Fq '- [x] **MEAS-05**' .planning/REQUIREMENTS.md &amp;&amp; grep -Fq '- [ ] **MEAS-01**' .planning/REQUIREMENTS.md &amp;&amp; grep -Fq '- [ ] **MEAS-08**' .planning/REQUIREMENTS.md &amp;&amp; test "$(grep -c '^| MEAS-05 | Phase 4 | Complete |$' .planning/REQUIREMENTS.md)" = "1"</automated>
    <human-check>
      These three items are human gates recorded by `04-VERIFICATION.md`. They are NOT acceptance criteria for this plan and must not be automated:
      1. Production privacy approval and live event uniqueness — approve the processor, create the exact ten goals, configure the trusted script and site values, deploy, and perform each explicit action once; expect one name-only event per action, no automatic duplicate, and no form or context property.
      2. Live report reconciliation — run the documented command with the approved site and key, compare 7/30/90/all-time counts and dates against the raw dashboard, and reopen the HTML offline; expect exact site and range counts, literal evidence labels, no external request, and no credential exposure.
      3. MVP outcome and privacy readability judgment — review one maximum-context enquiry and the report and disclosure at 320px and 200% zoom with keyboard and screen-reader use; expect readable non-scoring prose, no identity or progression claim, and a usable self-contained presentation. This gate also covers the five carried backstop UI considerations in this plan's `must_haves`.
    </human-check>
  </verify>
  <acceptance_criteria>
    - MEAS-05 appears as a checked checklist entry and its table row reads `Complete`.
    - MEAS-01 and MEAS-08 appear as unchecked checklist entries and their table rows still read `Gaps Found`.
    - A dated Phase 4 note immediately follows the Privacy-First Measurement checklist and names all four recorded facts: the two code-level blockers closed by 04-08 and 04-09, the MEAS-01 production approval and live event-uniqueness gate, the MEAS-08 live reconciliation gate, and the MVP readability judgment gate.
    - The note states that no MEAS-01 or MEAS-08 box may be checked until re-verification confirms closure and the named human gates are cleared.
    - No requirement outside Phase 4 changed status, the Coverage totals still read 32 total, 32 mapped, 0 unmapped, and the existing MEAS-08 `redirect returns` amendment note is byte-identical.
    - `git diff .planning/REQUIREMENTS.md` shows changes confined to the MEAS-05 entry, the MEAS-05 table row, and the added note.
  </acceptance_criteria>
  <reversibility rating="reversible">A documentation status record; revertible in one commit and it changes no code or deployment state.</reversibility>
  <done>Phase 4's recorded requirement status matches the verifier's findings, and the three human gates plus the carried backstop UI considerations are written down as human gates rather than quietly converted into automated criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local report process -> host filesystem | The generator creates, reserves, writes, and renames files through injected capabilities on an untrusted host path shape. |
| planning record -> release decision | A requirement marked complete is read later as evidence that a gate was cleared; an untruthful record is a governance failure. |
| generated report artifact -> owner's browser | The report is opened locally and must remain self-contained with no external request and no credential content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-39 | Denial of Service | `directoryOf` on a non-POSIX host | medium | mitigate | Separator-agnostic extraction plus a drive-root guard, pinned by a destination-path contract table that asserts creation before reservation. |
| T-04-40 | Repudiation | requirement status record | high | mitigate | Only MEAS-05 is promoted, on recorded verifier evidence; MEAS-01 and MEAS-08 stay open with a dated note naming the exact remaining human gates. |
| T-04-41 | Tampering | report artifact replacement | high | accept | Already mitigated by plan 04-07's exclusive reservation and owned-temp cleanup; this plan changes nothing in that sequence and its cases must still pass. |
| T-04-42 | Information Disclosure | report generation diagnostics | high | mitigate | The generator keeps no Node import, no analytics origin, no query path, and no credential variable name; the existing boundary case is re-asserted. |
| T-04-43 | Elevation of Privilege | recursive directory creation | low | accept | Creation is limited to the parent of the caller-supplied destination and is refused for a bare drive root; path traversal generally is canon and referred to `/gsd-secure-phase`. |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installation occurs in this plan; any later dependency addition requires the package-legitimacy gate plus a blocking human checkpoint for `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `npm run test:unit -- --run src/test/haoo-report.test.ts` passes with the new destination-path contract table and the unedited real-filesystem cases.
- `npm test`, `npm run lint`, and `npm run typecheck` all pass.
- `node scripts/verify-phase4-coverage.mjs .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md` still exits 0.
- `.planning/REQUIREMENTS.md` shows MEAS-05 complete, MEAS-01 and MEAS-08 unchecked, and a dated note naming the open human gates.
- The three human gates listed in Task 2's `<human-check>` remain open and are not claimed by any automated criterion in this phase.
</verification>

<success_criteria>
- Review warning WR-01 is fixed, not deferred: a first run on a non-POSIX host creates its output directory, and the fix is pinned by a table that also asserts ordering.
- Phase 4's recorded requirement status is truthful and nothing is marked complete that these gap-closure plans do not prove.
- The three classified probe edges are represented by named existing coverage rather than duplicated, and the unclassified MEAS-08 row is surfaced as a flagged unresolved assumption.
- The five backstop UI considerations stay attached to the phase's human verification instead of being dropped.
</success_criteria>

<output>
Create `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-10-SUMMARY.md` when done.
</output>
