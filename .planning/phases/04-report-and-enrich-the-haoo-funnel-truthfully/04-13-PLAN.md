---
phase: 04-report-and-enrich-the-haoo-funnel-truthfully
plan: 13
type: execute
wave: 3
depends_on: ["04-12"]
files_modified:
  - src/measurement/plausible.ts
  - src/test/fixtures/plausible-preload-contract.ts
  - .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md
  - .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md
autonomous: true
gap_closure: true
requirements: [MEAS-01, MEAS-05, MEAS-08]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The adapter no longer claims the recorded-opt-out check demonstrates that automatic pageview capture is actually disabled. The source states what the check does: the opt-out this project sends is recorded and re-read, and whether the vendor script honours it is an external contract settled by the live human gate."
    - "The fail-closed value of the check is stated accurately and not weakened: re-reading the recorded slot still closes a silent no-op initializer that a throw-only check would accept, and it is still the gate that keeps script insertion and sink creation unreachable — the source says that much and no more."
    - "The preload contract fixture describes itself truthfully: it pins agreement with the documented preload shape only, and is not offered as evidence about the vendor script's runtime behaviour."
    - "The 04-09 D1 coverage row is a human-judgment row: it carries `human_judgment: true` with a rationale naming why no test in this tree can observe the vendor script, and it carries a manual-procedural verification entry pointing at the live dashboard gate, so `verify-work` routes it to a human instead of auto-passing it."
    - "The D1 row still records the narrow structural fact its passing tests do prove — that script insertion is unreachable until the resolver returns a non-null provider — rather than deleting real evidence along with the overstated inference."
    - "The two `behavior_unverified_items` recorded in `04-VERIFICATION.md` are discoverable by the owner who must clear them: the live dashboard watch for automatic-pageview duplication, and the deployed-page confirmation that no other snippet, tag manager, or extension defines the provider global before the bundle runs."
    - "The owner-facing setup record no longer asserts that application code disables automatic pageviews; it states that the code records and re-reads the opt-out and names the live confirmation that settles the rest."
    - "The MVP outcome and privacy readability judgment stays a human gate covering the generated report, the visitor-facing disclosure, and the maximum-context engagement summary — it is recorded, not converted into an automated criterion."
    - "No control-flow change is made to the measurement adapter by this plan: the three refusal paths, the non-callable refusal added by 04-12, the primary path, and the recorded-opt-out gate all behave exactly as they did before, proven by an unedited suite."
    - "Edge — MEAS-05 boundary (spec-less probe row, resolved/explicit, carried not duplicated): the engagement summary visit and recency thresholds and its 32-character campaign cap are already proven at each threshold and one step either side by the shipped cases in `src/test/qualify-form.test.tsx` and already carried in 04-10's must_haves. No duplicate coverage is added and those cases must still pass."
    - "Edge — MEAS-05 precision (spec-less probe row, resolved/explicit, carried not duplicated): no rounding, tie-breaking, or overflow contract is required, because the report renders integer occurrence counts and exact integer differences only and per D-04 computes no percentage at all — already pinned by the shipped no-percent-sign and zero-one-many cases carried in 04-10."
    - "Edge — MEAS-01 concurrency (spec-less probe row, resolved/explicit, carried not duplicated): proven by the shipped exclusive-reservation, rename, partial-write, and concurrent-loser cases carried in 04-10 and re-asserted in 04-11."
    - "Edge — MEAS-08 (spec-less probe row, deliberately left unresolved): surfaced in full as a flagged planner assumption in this plan's `## Flagged unresolved assumption` section rather than auto-resolved or dropped. This is the plan that carries it, since MEAS-08's real edge is linguistic and this plan is the one that changes truthfulness prose."
  prohibitions:
    - "No human gate recorded in `04-VERIFICATION.md` may be converted into an automated acceptance criterion, absorbed into a passing test reference, or marked cleared by this plan."
    - "No control-flow change may be made to `src/measurement/plausible.ts`: this plan edits comments and documentation only, and the compiled behaviour must be identical."
    - "No claim may be written that the repository can prove something external to it, and no fixture may be described as independent evidence of a third-party runtime it only transcribes."
    - "No requirement checkbox or status may be promoted by this plan; MEAS-01 and MEAS-08 stay unchecked and `Gaps Found`, and MEAS-05's existing status must not be widened by absorbing its readability human gate into an automated claim."
    - "No real verification evidence may be deleted while removing an overstated inference; the D1 row keeps the structural fact its passing tests do prove."
    - "No visitor-facing disclosure copy, no report label, and no event name may change; the closed ten-event allowlist and the authored disclosure strings stay exactly as shipped."
    - "No `COVERAGE.md` table may be regenerated or extended; this plan changes no integrated or opted-out provider capability."
  artifacts:
    - path: "src/measurement/plausible.ts"
      provides: "Comment prose that states what the recorded-opt-out check establishes and names the live human gate that settles the rest"
      exports: ["PlausibleInitOptions", "PlausibleGlobal", "PlausibleScope", "PlausibleAdapters", "createPlausibleEventSink"]
    - path: "src/test/fixtures/plausible-preload-contract.ts"
      provides: "A preload shape fixture that describes its own evidentiary scope truthfully"
      exports: ["VendorPlausibleScope", "installPlausibleVendorPreload"]
    - path: ".planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md"
      provides: "D1 recorded as a human-judgment coverage row with a manual-procedural reference to the live gate"
    - path: ".planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md"
      provides: "Owner-facing record of the two live confirmations that settle automatic-capture behaviour, plus the MVP readability judgment gate"
  key_links:
    - from: "src/measurement/plausible.ts"
      to: ".planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md"
      via: "The restated comment names a live human gate, and that gate exists as an owner-facing checklist item so the reference does not dangle"
      pattern: "live human gate"
    - from: ".planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md"
      to: "verify-work coverage classification"
      via: "`human_judgment: true` routes D1 to a human instead of auto-passing it on a structural test"
      pattern: "human_judgment"
---

<objective>
Close gap 3 of `04-VERIFICATION.md`: make the prose around the recorded automatic-capture opt-out match the evidence behind it, in the source, in the fixture that was cited as corroboration, in the 04-09 coverage record, and in the owner-facing setup checklist.

Purpose: the source asserts that requiring the recorded value settles whether automatic capture is disabled. It does not. On the primary path the project's own stub echoes back the very options object the caller built with a `false` literal, so the check cannot fail; on the adopted path a foreign global that assigns the options it receives is accepted while remaining free to auto-capture. The check is a genuinely useful fail-closed filter and stays exactly as it is — what has to change is the inference drawn from it, because the current wording invites a reader to treat the live-uniqueness human gate as already satisfied.
Output: source and fixture prose that claims only what is provable here, a D1 coverage row that routes to a human instead of auto-passing, and an owner checklist that carries the two live confirmations the verifier recorded as `behavior_unverified`.

This is a complete vertical fix for one gap. It is deliberately documentation-only: `04-VERIFICATION.md` classified gap 3 as a documentation-accuracy gap, not a control-flow gap, and routed the adopted-path echo to a live human gate. Changing control flow here would exceed the recorded scope and would silently retire a gate the verifier left open on purpose.
</objective>

<execution_context>
@/home/paul/.claude/gsd-core/workflows/execute-plan.md
@/home/paul/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-CONTEXT.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-VERIFICATION.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-REVIEW.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md
@.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md
@src/measurement/plausible.ts
@src/test/fixtures/plausible-preload-contract.ts
</context>

## Artifacts this phase produces

Changed symbols:

- None. This plan renames nothing, adds no symbol, removes no symbol, and changes no signature. `createPlausibleEventSink`, `PlausibleInitOptions`, `PlausibleGlobal`, `PlausibleScope`, `PlausibleAdapters`, `installProviderStub`, `recordsOptOut`, `resolveInitializedProvider`, `appendProviderScript`, `installPlausibleVendorPreload`, and `VendorPlausibleScope` all keep their current names, signatures, and bodies.

Documentation surfaces changed:

- The `recordsOptOut` docstring in `src/measurement/plausible.ts` — the sentence drawing an unsupported inference from the recorded value is replaced by an accurate statement of what the check establishes and what it defers to a live gate.
- The in-body comment above the resolver call in `createPlausibleEventSink` — restated to match, so the two do not disagree.
- The module docstring of `src/test/fixtures/plausible-preload-contract.ts` — its self-description as independent evidence about vendor behaviour is replaced by an accurate statement of its scope.
- `04-09-SUMMARY.md` — the D1 `coverage` row becomes a human-judgment row with a rationale and a manual-procedural verification entry, plus a dated amendment note.
- `04-USER-SETUP.md` — the automatic-capture confirmation item is restated, and the two live confirmations recorded as `behavior_unverified_items` plus the MVP readability judgment are added as owner-facing human gates.

Behavior contracts: unchanged. Every existing case in `src/test/measurement.test.ts`, `src/test/build-output.test.ts`, `src/test/haoo-report.test.ts`, and `src/test/qualify-form.test.tsx` must pass with expectations byte-identical to their pre-plan state.

<tasks>

<task type="tracer">
  <name>Task 1: State in the source what the recorded-opt-out check establishes and what it defers</name>
  <files>src/measurement/plausible.ts, src/test/fixtures/plausible-preload-contract.ts</files>
  <read_first>
    - `src/measurement/plausible.ts` lines 104-118 — the `recordsOptOut` docstring and body: the untrusted-input paragraph, the sentence drawing the inference the verifier rejected, the clause about closing a silent no-op initializer, and the structural check itself.
    - `src/measurement/plausible.ts` lines 85-102 — `installProviderStub`, whose initializer assigns the options it receives to the recorded slot. This is why the check cannot fail on the primary path and is the first half of the verifier's finding.
    - `src/measurement/plausible.ts` lines 120-164 — `resolveInitializedProvider` as left by plan 04-12, including its classification and the adopted path where a foreign global that assigns the options it receives is accepted. This is the second half of the finding.
    - `src/measurement/plausible.ts` lines 196-210 — the in-body comment above the resolver call in `createPlausibleEventSink` stating that collection is refused until the recorded opt-out is confirmed, and the options object built with the domain and the automatic-capture opt-out.
    - `src/test/fixtures/plausible-preload-contract.ts` — all 37 lines, in particular the module docstring's final sentence describing why the fixture imports no production types.
    - `04-VERIFICATION.md` gap 3 — the reason, both recorded artifact issues, and the two `missing` items that define this plan's scope.
    - `04-VERIFICATION.md` `behavior_unverified_items` — the two live gates whose framing must survive intact, including the `why_human` text this task's wording must stay consistent with.
    - `04-REVIEW.md` WR-01 — the reviewer's independent statement of the same finding. Note that its suggested control-flow change is deliberately not taken; see this plan's objective.
  </read_first>
  <action>
    Rewrite the `recordsOptOut` docstring so it describes the check accurately. Keep the paragraph explaining that the recorded slot is untrusted input and is inspected structurally rather than trusted to match its declared type — that is true and load-bearing. Keep the clause explaining that requiring the recorded value, rather than merely a non-throwing initializer call, is what closes a silent no-op initializer a throw-only check would accept — that is also true and is the check's real value. Replace the sentence that draws a stronger conclusion from the recorded value with an accurate one: the opt-out this project sends is recorded and re-read, and whether the vendor script honours it is an external contract settled by the live human gate. Use that phrasing, including the words `settled by the live human gate`, so the claim is greppable and so a reader is pointed at the gate rather than away from it.

    Add one short paragraph to the same docstring recording the two facts the verifier probed, so the limitation is written down where the check lives rather than only in a planning artifact: on the path where this module installs its own stub, the value re-read here is the object this module handed the stub, so the comparison confirms storage rather than vendor behaviour; and on the adopted path a foreign global that assigns the options it receives satisfies the check while remaining free to capture automatically, which is why the deployed page must be confirmed to carry no other provider global. Name `04-USER-SETUP.md` as the place those confirmations are recorded, so the reference resolves to an owner-facing checklist and not to a dangling phrase.

    Restate the in-body comment above the resolver call in `createPlausibleEventSink` so it agrees with the docstring: script insertion and sink creation stay unreachable until the recorded opt-out has been re-read for the configured domain, which is a fail-closed gate on this module's own behaviour, not a proof about the vendor script. Do not change the ordering, the options object, the null guard, the single script append, or the sink.

    Make no control-flow change in this file. Every executable line must be byte-identical to its state after plan 04-12: same classification, same refusal paths, same recorded-opt-out gate, same script insertion point, same sink. If the executor finds itself wanting to change behaviour here, stop and record it as a finding for re-verification instead — `04-VERIFICATION.md` classified this as a documentation-accuracy gap and routed the adopted-path echo to a live human gate, and closing that gate in code is not this plan's scope.

    In `src/test/fixtures/plausible-preload-contract.ts`, rewrite the module docstring's final sentence so the fixture describes its own evidentiary scope truthfully. It still explains that the fixture imports no production measurement type or helper, and that this keeps the shape it declares independent of the shape the adapter declares. It must then state plainly that this pins shape agreement only, not vendor behaviour — use that phrasing so the claim is greppable — and that the vendor script honouring the recorded slot is confirmed by the live gate recorded in `04-USER-SETUP.md`. Change nothing else in the file: the interfaces, the function, and its behaviour stay exactly as they are.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**' &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; grep -Fq 'settled by the live human gate' src/measurement/plausible.ts &amp;&amp; grep -Fq 'pins shape agreement only, not vendor behaviour' src/test/fixtures/plausible-preload-contract.ts &amp;&amp; test -z "$(git diff HEAD -- src/measurement/plausible.ts src/test/fixtures/plausible-preload-contract.ts | grep '^[+-]' | grep -v '^[+-][+-]' | grep -vE '^[+-][[:space:]]*(\*|//|/\*)' | grep -vE '^[+-][[:space:]]*$')"</automated>
  </verify>
  <acceptance_criteria>
    - The full real-tree suite run with `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**'` passes, and `npm run typecheck` and `npm run lint` each exit 0.
    - Source assertion: `grep -c 'provable instead of assumed' src/measurement/plausible.ts` returns `0`. No comment-line filter is applied on purpose — the target of this assertion is comment prose.
    - Source assertion: `grep -Fq 'settled by the live human gate' src/measurement/plausible.ts` succeeds.
    - Source assertion: `grep -Fq '04-USER-SETUP.md' src/measurement/plausible.ts` succeeds, so the named gate is locatable from the source.
    - Source assertion: `grep -c 'external contract oracle' src/test/fixtures/plausible-preload-contract.ts` returns `0`, and `grep -Fq 'pins shape agreement only, not vendor behaviour' src/test/fixtures/plausible-preload-contract.ts` succeeds.
    - Behavior assertion (machine): the comment-and-blank-line-only diff check in the automated block above exits 0 — `git diff HEAD` on the two files, reduced to its added and removed lines, leaves nothing once comment lines (` *`, `//`, `/*`) and blank lines are filtered out, so no executable line was added, removed, or reordered. Run it before committing this task, while the change is still in the working tree. This is the mechanical enforcement of threat T-04-56; the narrated inspection below supplements it and does not replace it.
    - Behavior assertion (narrated): `git diff src/measurement/plausible.ts` and `git diff src/test/fixtures/plausible-preload-contract.ts` contain only comment and docstring lines. No line of executable source is added, removed, or reordered in either file. The executor must state this explicitly in the summary after inspecting the diff.
    - Behavior assertion: every case in `src/test/measurement.test.ts` passes with expectations byte-identical to their pre-plan state — the three refusal paths still return no sink and append zero scripts, the non-callable refusal added by 04-12 still holds, and the primary path still installs, initializes, confirms, and appends exactly one script.
    - `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**' src/test/build-output.test.ts` passes, so no analytics origin, credential shape, or approved-source import entered a production module.
    - `node scripts/verify-phase4-coverage.mjs .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md` exits 0 and `git diff` shows `COVERAGE.md` unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">Comment and docstring text only, with an explicit criterion that no executable line changes. Revertible in one commit with no behavioural or deployment consequence.</reversibility>
  <done>The source states what the recorded-opt-out check establishes and names the live gate that settles the rest, the fixture describes its own scope truthfully, and the compiled behaviour is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Downgrade the 04-09 D1 coverage row to a human-judgment row without discarding its real evidence</name>
  <files>.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md` lines 47-80 — the `coverage` block: the D1 row with its description, its unit and other verification entries, and `human_judgment: false`; and the D2 and D3 rows, which are genuinely executable and must not be touched.
    - `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md` — the dated amendment note added by plan 04-12 withdrawing the stub-removal claims, whose style and placement this task's note must match and must not disturb.
    - `$HOME/.claude/gsd-core/templates/summary.md` `<coverage_guidance>` — the field semantics and the deterministic contract: a deliverable auto-passes only when `human_judgment` is false, `verification` is non-empty, and every status is `pass`; `rationale` is required whenever `human_judgment` is true; and `verification[].kind` is one of `unit`, `integration`, `e2e`, `automated_ui`, `manual_procedural`, or `other`.
    - `04-VERIFICATION.md` gap 3 `missing` item two — the instruction to downgrade this row from an executable claim to a human-judgment row.
    - `04-VERIFICATION.md` `behavior_unverified_items` entry one — the live dashboard test, its expected result, and its `why_human` text, which the rationale must stay faithful to.
  </read_first>
  <action>
    Edit only the D1 row of the `coverage` block in `04-09-SUMMARY.md`.

    Narrow its `description` so it records the structural fact its passing tests actually prove — that a provider script is appended and an event sink returned only after the resolver returns a non-null provider, and that the opt-out this project sends is recorded and re-read for the configured domain — and no longer asserts that automatic pageview capture is confirmed disabled. Keep both existing verification entries with their existing refs and their `pass` statuses: they are real evidence for the narrowed description and deleting them would trade one inaccuracy for another.

    Add a third verification entry of kind `manual_procedural` whose ref names the live gate from `04-VERIFICATION.md`: after production enablement, load the HAOO page once with the approved script and site values and watch the provider dashboard live view, expecting exactly one page-view occurrence for the visit, no additional automatic pageview, and no pageview carrying campaign parameter values. Give it status `unknown`, because it has not been performed.

    Set `human_judgment` to `true` and add the `rationale` the schema requires. The rationale must say that the assertion is about the vendor script honouring the recorded value, that the repository can only prove it wrote and re-read that value, and that no test in this tree can observe the real script's behaviour — the verifier's own reasoning, not a paraphrase that softens it.

    Do not edit D2 or D3: both are adversarial refusal probes with genuine unit evidence and they remain correctly classified. Do not edit `requirements-completed`, `actuals`, `provides`, `tech-stack`, `key-files`, or the `key-decisions` and `patterns` entries as left by plan 04-12.

    Append a dated amendment note at the end of the summary body, immediately after the note plan 04-12 added, recording four facts: that D1 was downgraded to a human-judgment row on the finding recorded as gap 3 of `04-VERIFICATION.md`; that the underlying control flow was not changed and the fail-closed gate still holds; which live gate now carries the claim; and that plan 04-13 also restated the matching source prose so the record and the code agree. Do not restate the gate as something a test could close.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-09-SUMMARY.md','utf8').split('---')[1];const d1=y.slice(y.indexOf('- id: D1'),y.indexOf('- id: D2'));if(!/human_judgment:\s*true/.test(d1))throw new Error('D1 not human_judgment true');if(!/rationale:/.test(d1))throw new Error('D1 missing rationale');if(!/manual_procedural/.test(d1))throw new Error('D1 missing manual_procedural entry');const d2=y.slice(y.indexOf('- id: D2'),y.indexOf('- id: D3'));if(!/human_judgment:\s*false/.test(d2))throw new Error('D2 was altered');console.log('D1 downgraded, D2 intact')"</automated>
    <human-check>
      These are human gates recorded by `04-VERIFICATION.md`. They are NOT acceptance criteria for this plan, must not be automated, and are not cleared by it:
      1. Production privacy approval and live event uniqueness — approve the processor, create the exact ten dashboard goals, configure the approved script and site values, deploy, and perform each explicit action once; expect one name-only event per action, no automatic duplicate, and no form value, browser-context property, or visitor identifier on any event.
      2. Automatic pageview capture disabled before the managed script loads — after production enablement, load the HAOO page once and watch the dashboard live view; expect exactly one page-view occurrence for the visit, no additional automatic pageview, and no pageview carrying campaign parameter values. This is the gate D1 now points at.
      3. No foreign provider global on the deployed page — confirm no other snippet, tag manager, or extension defines the provider global before the bundle runs, so the adoption path is never taken in production.
      4. Live report reconciliation — run the documented report command with the approved site and key, compare the 7/30/90/all-time counts and inclusive dates against the raw dashboard, and reopen the HTML with networking disabled.
      5. MVP outcome and privacy readability judgment — read one maximum-context enquiry summary and the generated report and page disclosure at a 320px viewport and 200% zoom with keyboard and screen-reader navigation. This gate also covers the five `verification: backstop` UI considerations carried in 04-10's must_haves.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The automated check above prints `D1 downgraded, D2 intact` and exits 0.
    - D1 carries `human_judgment: true`, a non-empty `rationale` naming that no test in this tree can observe the vendor script, and three verification entries: the two original ones with unchanged refs and `pass` statuses, plus one `manual_procedural` entry with status `unknown` referencing the live dashboard gate.
    - D1's `description` no longer asserts that automatic pageview capture is confirmed disabled, and does assert the narrower structural fact about script insertion being unreachable until the resolver returns non-null.
    - D2 and D3 are byte-identical to their pre-task state, confirmed by reading `git diff` on the file.
    - `requirements-completed`, `actuals`, `provides`, `tech-stack`, `key-files`, and the `key-decisions` and `patterns` entries as left by plan 04-12 are byte-identical.
    - A dated amendment note follows the note added by plan 04-12 and records all four required facts, and it describes the live gate as a human gate rather than as something a test could close.
    - `.planning/REQUIREMENTS.md` is unchanged: MEAS-01 and MEAS-08 stay unchecked and `Gaps Found`, and MEAS-05's existing entry is untouched.
    - `git diff --stat` for this task shows exactly one file changed.
  </acceptance_criteria>
  <reversibility rating="reversible">A classification change in a planning record. Revertible in one commit; it routes a deliverable to a human rather than away from one, so the failure mode of the change is a redundant prompt.</reversibility>
  <done>`verify-work` will route D1 to a human instead of auto-passing it on a structural test, and the row records both what is proven here and what is not.</done>
</task>

<task type="auto">
  <name>Task 3: Record the live confirmations and the readability judgment as owner-facing human gates</name>
  <files>.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md</files>
  <read_first>
    - `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md` lines 27-42 — the Dashboard Configuration checklist, in particular the item asserting that application code disables automatic pageviews, and the approved-script-variant item whose style the new items must match.
    - `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md` lines 59-77 — the Verification section, its command block, and its three expected results, one of which already states the one-event-no-duplicate expectation.
    - `04-VERIFICATION.md` `behavior_unverified_items` — both entries with their `test`, `expected`, and `why_human` text, which the new checklist items must reproduce in substance.
    - `04-VERIFICATION.md` `human_verification` — all five entries, in particular the MVP outcome and privacy readability judgment, whose scope explicitly covers the maximum-context enquiry summary that is MEAS-05's surface.
    - `.planning/REQUIREMENTS.md` lines 46-60 — the dated Phase 4 status note that already names these gates, so the checklist agrees with it rather than inventing a second, divergent list.
  </read_first>
  <action>
    Restate the Dashboard Configuration item that currently asserts the integration does not rely on automatic capture. Its note must say what is true: the application records the automatic-pageview opt-out in the provider options and re-reads it before any script is appended, which is a fail-closed gate on the application's own behaviour; whether the loaded vendor script honours it is confirmed live, by the item added below. Keep the item's location line and its unchecked state.

    Add two unchecked items to the Verification section, written in the same style as the existing checklist items, each naming an owner, a location, and an expected result. The first is the live event-uniqueness confirmation: after production enablement, load the HAOO page once with the approved script and site values and watch the provider dashboard live view, expecting exactly one page-view occurrence for the visit, no additional automatic pageview, and no pageview carrying campaign parameter values. The second is the foreign-global confirmation: on the deployed page, confirm that no other snippet, tag manager, or extension defines the provider global before the bundle runs, expecting it to be undefined until the bundle installs its own stub, so the adoption path is never taken in production. For the second item, record why it matters in one line: a foreign callable global that merely assigns the options it receives passes the application's recorded-opt-out check, so only inspection of the deployed page can establish that no such global exists.

    Add a third unchecked item recording the MVP outcome and privacy readability judgment as a human gate: read one maximum-context enquiry summary together with the generated report and the page disclosure at a 320px viewport and 200% zoom, with keyboard and screen-reader navigation, expecting readable non-scoring prose, no identity or stage-progression claim, four period labels and every stage clarifier wrapping without clipping or overlap, 44px targets intact, and no body-level horizontal scroll. Note in one line that this gate also covers the five held-out visual considerations carried in 04-10's must_haves, so they are not orphaned.

    Each of the three new items must be pinned independently, so write these three phrases verbatim into them: `exactly one page-view occurrence for the visit` in the live event-uniqueness item, `defines the provider global before the bundle runs` in the foreign-global item, and `maximum-context enquiry summary` in the readability-judgment item. Each is asserted separately by the automated check, so adding only some of the three fails.

    Write every one of these as a human gate. Do not attach a command that would be read as clearing one, do not mark any item complete, and do not change the file's `Status: Incomplete` line — production enablement is still deliberately deferred. Do not change the three environment-variable rows, the approved-origin and approved-path wording, the ten goal names, or the report-credential section.
  </action>
  <verify>
    <automated>test "$(grep -c '^- \[ \]' .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md)" -ge "11" &amp;&amp; grep -Fq 'exactly one page-view occurrence for the visit' .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md &amp;&amp; grep -Fq 'defines the provider global before the bundle runs' .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md &amp;&amp; grep -Fq 'maximum-context enquiry summary' .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md &amp;&amp; grep -Fq 'Status: Incomplete' .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md &amp;&amp; grep -Fq 'haoo_self_onboarding' .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md &amp;&amp; grep -Fq 'https://plausible.io' .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-USER-SETUP.md</automated>
  </verify>
  <acceptance_criteria>
    - The automated check above exits 0: the file carries at least eleven unchecked checklist items — eight before this task plus the three added here — each of the three new gates is matched independently by its own literal (`exactly one page-view occurrence for the visit`, `defines the provider global before the bundle runs`, `maximum-context enquiry summary`), so adding only one or two of them fails, and the file still declares itself incomplete and still carries the ten goal names and the approved origin unchanged.
    - The Dashboard Configuration item about automatic capture no longer asserts that application code disables automatic pageviews; it states that the opt-out is recorded and re-read and points at the live confirmation.
    - Three new unchecked items exist: live event uniqueness with the one-occurrence-no-duplicate-no-campaign-value expectation, deployed-page confirmation that no foreign provider global exists with its one-line reason, and the MVP readability judgment at 320px and 200% zoom with its stated expectations and its note covering the five held-out visual considerations.
    - No item in the file is marked complete, the `Status: Incomplete` line is unchanged, and no command in the file is presented as clearing a human gate.
    - The environment-variable table, the approved-origin and approved-path wording, the ten goal names, and the report-credential section are byte-identical to their pre-task state, confirmed by reading `git diff` on the file.
    - The new items agree in substance with the dated Phase 4 note in `.planning/REQUIREMENTS.md`; no divergent second list of gates is created, and `.planning/REQUIREMENTS.md` itself is unchanged.
    - `git diff --stat` for this task shows exactly one file changed.
  </acceptance_criteria>
  <reversibility rating="reversible">Owner-facing documentation only. Revertible in one commit; it adds gates rather than removing them, so the failure mode is extra manual confirmation.</reversibility>
  <done>Every live confirmation the verifier recorded as unverifiable in this repository is written down where the owner performing it will look, and the source comment's reference to a live human gate resolves to a real checklist item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository claim -> reader's belief | A source comment or a coverage row is read later as evidence that a control was verified. An overstated claim moves risk out of view without removing it. |
| coverage record -> verification routing | `verify-work` auto-passes a deliverable whose row says `human_judgment: false` with passing tests. A misclassified row silently skips the human who would have caught the gap. |
| ambient page scope -> analytics adapter | A foreign callable global that assigns the options it receives satisfies the application's recorded-opt-out check while remaining free to capture automatically. |
| owner checklist -> production enablement | The checklist is what the owner works from at enablement. A gate absent from it is a gate nobody performs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-54 | Repudiation | source and coverage claims about automatic-capture opt-out | high | mitigate | The inference is removed from the source, the fixture, and the D1 row, and each is replaced by an accurate statement plus a named live gate. Pinned by negative and positive source assertions and by a structural check on the D1 row. |
| T-04-55 | Spoofing | foreign callable global adopting the analytics sink | high | transfer | Deliberately not closed in code, per `04-VERIFICATION.md`, which classified gap 3 as documentation-accuracy and routed the adopted-path echo to a live gate. Transferred to the deployed-page confirmation now recorded in `04-USER-SETUP.md` and restated in Task 2's `human-check`. |
| T-04-56 | Tampering | documentation-only edit silently changing behaviour | high | mitigate | An explicit acceptance criterion requires the diff on both source files to contain comment lines only, with the executor stating that after inspecting it; the full unedited suite plus typecheck and lint back it. |
| T-04-57 | Information Disclosure | visitor-facing disclosure and report copy | medium | accept | Out of scope and prohibited from changing: no visitor-facing string, report label, or event name is touched, and the shipped disclosure and label coverage must still pass. |
| T-04-58 | Elevation of Privilege | a human gate quietly retired by a passing check | high | mitigate | Every gate is restated verbatim in substance in a `human-check` block and added to the owner checklist unchecked; a prohibition forbids converting any of them into an automated criterion, and no requirement status is promoted. |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed by this plan. Any later dependency addition requires the package-legitimacy gate plus a blocking human checkpoint for `[ASSUMED]` or `[SUS]` packages; that checkpoint is never auto-approvable. |

**ASVS level:** 1. **Blocking threshold:** high. Every `high` threat is dispositioned. T-04-55 is `transfer` rather than `mitigate` because the verifier deliberately left the adopted-path echo as a live gate; mitigating it in code here would exceed the recorded gap scope and silently retire a gate that was kept open on purpose.
</threat_model>

## Flagged unresolved assumption (not auto-resolved)

The deterministic edge probe returned one **unclassified** row for **MEAS-08** with the generic prompt *"unclassified — review manually"*. Per the spec-less probe fallback rules it stays **`unresolved`**: it is not auto-resolved with a backstop marker and it is not dropped. This is the plan in the gap-closure set that carries it in full, because MEAS-08's edge is linguistic and this is the plan that changes truthfulness prose.

Reading for the developer: MEAS-08 is a vocabulary-and-honesty requirement rather than a data-shape one, so the shape taxonomy has no category for it. Its real edge is that a future label, caveat, comment, or coverage row could reintroduce a claim the evidence does not support — which is exactly the defect recorded as gap 3, arriving in a source comment rather than in report copy. Plans 04-01 through 04-03 already pin the closed label map, the authored caveat block, the banned-vocabulary scan, and the zero-percent-sign contract for report copy; nothing equivalent exists for source comments and planning records, and no automated scan could reasonably supply one. This plan therefore treats it as it was treated in 04-10: represented by existing coverage where automation applies, and by a review-time human read where it does not. The natural follow-up, if the developer wants a stronger guarantee, is a review-time read of any new claim-bearing prose, not another automated scan. No new task is created for it.

The three classified rows are carried, not duplicated. MEAS-05 boundary and MEAS-05 precision are represented in `must_haves.truths` above by citation to the shipped `qualify-form.test.tsx` threshold and cap cases and the shipped no-percent-sign and zero-one-many report cases, both already carried in 04-10. MEAS-01 concurrency is represented by citation to the shipped report-generation cases carried in 04-10 and re-asserted in 04-11.

## Why MEAS-05 is declared by this plan

MEAS-05 is already recorded `Complete`, and this plan does not touch the engagement-summary code path. It is declared here for one specific reason, not reflexively: the MVP outcome and privacy readability judgment recorded in `04-VERIFICATION.md` explicitly covers the maximum-context enquiry summary, which is MEAS-05's surface, and Task 3 writes that gate into the owner-facing checklist so it is performed rather than assumed. This plan's obligation to MEAS-05 is therefore custodial — keep its human gate a human gate, and do not let its recorded `Complete` status be widened by absorbing that gate into an automated claim. Both are stated as prohibitions above.

## UI considerations carried, not re-implemented

`04-UI-SPEC.md` records 46 applicable considerations across surfaces E1–E7. This plan changes no visitor-facing copy and no rendered markup. Surfaces E5 (disclosure summary group) and E6 (visible collection notice) are the disclosure surfaces closest to this plan's subject, and every row on both is `explicit` and already shipped and verified by plans 04-02 and 04-04 — none of them is touched, and the E6 `populated` row's owner-approved sentence in particular must remain byte-identical. The five `verification: backstop` rows (E1, E2, E3, E4 and E7 `long-text`) are already carried in 04-10's `must_haves`; they are cited here and deliberately not duplicated, and Task 3 records the human gate that covers them so they are not orphaned.

## Human gates preserved

All five `human_verification` entries and both `behavior_unverified_items` from `04-VERIFICATION.md` remain open. This plan strengthens their standing rather than closing any: D1 is reclassified so it routes to a human, and three of the gates are written into the owner checklist unchecked. No requirement status is promoted, MEAS-01 and MEAS-08 stay unchecked and `Gaps Found`, and clearing any gate remains the business of `/gsd-verify-work 04` and the human who performs it.

<verification>
- `npm run build` succeeds, so the `dist` freshness contract in `src/test/build-output.test.ts` holds after a production source input changes mtime.
- `npx vitest run --exclude '**/.claude/worktrees/**' --exclude '**/node_modules/**'` passes across the real tree with every expectation unedited.
- `npm run typecheck` and `npm run lint` each exit 0.
- The negative and positive source assertions in Task 1 return their expected results, and the D1 structural check in Task 2 prints its success line.
- `node scripts/verify-phase4-coverage.mjs .planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/COVERAGE.md` exits 0 with `COVERAGE.md` unchanged.
- The diffs on `src/measurement/plausible.ts` and `src/test/fixtures/plausible-preload-contract.ts` contain comment lines only, stated explicitly in the summary after inspection.
- The summary cites the real-tree file and test counts produced by the exclusion-flagged command, not the counts produced by a bare `npm test` run that also collects the stale `.claude/worktrees/` copy.
- `git diff --stat` is confined to the four files in `files_modified`.
</verification>

<success_criteria>
- Gap 3 of `04-VERIFICATION.md` is closed: nothing in the source, the fixture, the 04-09 record, or the owner checklist claims the repository proves something it cannot.
- The fail-closed value of the recorded-opt-out check is stated accurately and is not weakened, and no control flow changes.
- D1 routes to a human, keeps the evidence it genuinely has, and points at the live gate that carries the rest.
- The two `behavior_unverified_items` and the MVP readability judgment are discoverable by the owner who must clear them, and every human gate in the phase is still open.
</success_criteria>

<output>
Create `.planning/phases/04-report-and-enrich-the-haoo-funnel-truthfully/04-13-SUMMARY.md` when done.
</output>
