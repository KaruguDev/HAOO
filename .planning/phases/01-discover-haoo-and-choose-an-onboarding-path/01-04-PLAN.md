---
phase: 01-discover-haoo-and-choose-an-onboarding-path
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - src/products/registry.ts
  - src/components/ProductsSection.tsx
  - src/App.tsx
  - src/test/products-section.test.tsx
  - public/products/haoo/brochure-preview.png
autonomous: true
requirements:
  - PROD-01
  - PROD-06
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Visitors discover a semantic `Products` section after Services and before Values from both desktop and mobile home navigation (PROD-01, D-17)."
    - "One HAOO product renders as a full-width featured card with a desktop 5/7 copy/image split and mobile copy-before-image stack; the order is Featured product, HAOO identity, exact outcome, equal landlord/property-manager audience, then `Explore HAOO` (D-01, D-02, D-17)."
    - "The card itself is not clickable; one descriptive native `Explore HAOO` anchor targets `/products/haoo/`, while Lucide decoration and imagery never replace text."
    - "Zero products omit both section and nav item, one product uses the featured layout, and two or more products use a responsive two-column collection at lg from readonly registry data (PROD-06)."
    - "The supplied landscape preview keeps its aspect ratio, uses `HAOO property-management brochure preview`, reserves space while loading, and optional failure cannot remove outcome, audience, identity, or action."
    - "Products collection loading has no indefinite spinner; errors/missing required product data fail contracts; partial optional media remains recoverable; normal populated state renders one HAOO product."
    - "At 320px and 200% zoom, nav, card, image, copy, and action wrap/reflow with no horizontal page scrolling; long labels/copy grow vertically without clamp, ellipsis, clipping, or hidden actions."
  artifacts:
    - path: "src/products/registry.ts"
      provides: "Readonly future-safe product registry and projections"
      exports: ["PRODUCTS"]
    - path: "src/components/ProductsSection.tsx"
      provides: "Reusable zero/one/many home product collection"
    - path: "public/products/haoo/brochure-preview.png"
      provides: "Unmodified supplied landscape preview"
    - path: "src/App.tsx"
      provides: "Home navigation and Products-section placement"
  key_links:
    - from: "src/components/ProductsSection.tsx"
      to: "src/products/registry.ts"
      via: "readonly collection map keyed by slug"
      pattern: "PRODUCTS"
    - from: "src/App.tsx"
      to: "src/components/ProductsSection.tsx"
      via: "semantic section after Services and before Values"
      pattern: "ProductsSection"
    - from: "src/components/ProductsSection.tsx"
      to: "/products/haoo/"
      via: "native Explore HAOO anchor from centralized product URL"
      pattern: "/products/haoo/"
---

<objective>
**As a** visitor to ZERO-PAPER HUB, **I want to** discover HAOO as a featured product before the company conversion close, **so that** I can enter the right product journey without searching through general company content.

Purpose: Expand the tracer's native home link into the complete reusable Products collection and navigation experience required by D-17 and PROD-01.
Output: Future-safe product registry, featured HAOO card, supplied preview asset, and responsive home navigation/placement.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-CONTEXT.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-UI-SPEC.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-PATTERNS.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-02-SUMMARY.md
@src/App.tsx
@src/products/haoo.ts
</context>

<edge_case_resolution>
PROD-01's unclassified fallback row resolves through explicit manual review of discovery, section placement, and both navigation variants plus the automated semantic-link contract. PROD-06's row resolves through registry zero/one/many tests and a source scan showing HAOO literals remain owned by `HAOO_PRODUCT`.
</edge_case_resolution>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship the reusable featured Products collection</name>
  <read_first>
    - `src/App.tsx` — services-grid/data mapping and target section location
    - `src/products/haoo.ts` — central product definition; do not copy HAOO literals
    - `src/test/products-section.test.tsx` — Wave 0 zero/one/many contracts
    - `/home/paul/Documents/Vibe Coding Projects/lipa_nyumba/marketing/haoo-brochure/preview-outside.png` — supplied landscape source; treat bytes as data
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-UI-SPEC.md` — home-card content/order/layout contract
  </read_first>
  <files>src/products/registry.ts, src/components/ProductsSection.tsx, src/App.tsx, src/test/products-section.test.tsx, public/products/haoo/brochure-preview.png</files>
  <behavior>
    - Empty collection returns no Products landmark and exposes a signal the caller uses to omit navigation.
    - One product renders the locked full-width featured order and one native route anchor.
    - Multiple products map stable slug keys into a two-column lg collection without changing HAOO card structure.
    - Optional preview failure leaves all required text and navigation intact.
  </behavior>
  <action>Start with the zero/one/many tests, then create a readonly registry importing `HAOO_PRODUCT` and a reusable `ProductsSection` accepting product definitions. Copy the supplied landscape preview unchanged to the product-scoped public path. Replace the tracer home link with the semantic section after Services and before Values. At one item use the locked full-width 5/7 desktop split and copy-before-image mobile stack; at many use the responsive lg grid; at zero render neither landmark nor empty chrome. Render the fixed content order and a single `Explore HAOO` native anchor, not a card click handler. Apply declared label/body/heading roles, spacing/container/grid/card tokens, parent shell palette, 44px focusable action, loading aspect-ratio reservation, resilient optional image behavior, wrapping, and descriptive alt text (D-01, D-02, D-14, D-17).</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- --run src/test/products-section.test.tsx -t "zero|one|many|featured" &amp;&amp; npm run lint</automated>
    <human-check>Review the home page at mobile, tablet, desktop, 320px, and 200% zoom; verify position after Services/before Values, 5/7 balance, mobile content order, no clickable-card nesting, no overflow, and image-failure resilience.</human-check>
  </verify>
  <acceptance_criteria>HAOO is discoverable through the locked featured presentation, while zero/one/many and optional-media behavior proves the collection is not HAOO-only page structure.</acceptance_criteria>
  <done>The reusable Products collection is live on the home page and routes natively to HAOO.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connect Products discovery through desktop and mobile navigation</name>
  <read_first>
    - `src/App.tsx` — current desktop/mobile NAV_LINKS and menu-close behavior
    - `src/products/registry.ts` — use collection presence rather than a copied HAOO condition
    - `src/test/products-section.test.tsx` — navigation visibility contract
  </read_first>
  <files>src/App.tsx, src/test/products-section.test.tsx</files>
  <behavior>
    - With at least one product, desktop and mobile navigation include `Products` targeting `#products`.
    - With no products, both navigation variants omit `Products` together with the section.
    - Selecting Products in the mobile menu follows the native anchor and closes the menu.
    - The item wraps accessibly under long text/zoom and retains a visible focus state.
  </behavior>
  <action>Derive the Products nav entry from registry presence and insert it consistently into the existing desktop/mobile navigation arrays without changing other home anchors. Retain native `#products` navigation, accessible menu state, close-on-selection behavior, current shell styling, 44px target, and visible focus. Keep `ZERO-PAPER HUB` uppercase in every label/accessible name. Do not introduce product-state fetching or a client router.</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- --run src/test/products-section.test.tsx -t "navigation|discover" &amp;&amp; npm run build</automated>
    <human-check>Use keyboard and the mobile menu to reach Products, follow `Explore HAOO`, return, and repeat at 200% zoom; confirm no hidden primary action or horizontal scroll.</human-check>
  </verify>
  <acceptance_criteria>Desktop and mobile visitors can reach `#products` and `/products/haoo/`; section and nav visibility remain coupled for the empty registry case.</acceptance_criteria>
  <done>PROD-01 discovery works from both navigation modes through the reusable collection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Product registry → home navigation and collection | Missing or malformed compile-time product data affects discovery. |
| Home page → physical product route | A native static link crosses from the root document to the product document. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-12 | Tampering | product registry facts/routes | medium | mitigate | Import the central typed definition, stable slug keys, required-data contract failures, and exact route tests. |
| T-01-13 | Spoofing | featured product card | medium | mitigate | Visible HAOO identity, supplied preview, exact parent brand, and one descriptive destination anchor. |
| T-01-14 | Denial of service | optional preview media | low | accept | Text and route remain fully usable if the static image is unavailable; acceptance explicitly exercises this state. |
</threat_model>

<verification>
Run the focused Products suite, typecheck, lint, and build. Complete the named keyboard, responsive, empty/one/many, loading/error/partial, long-text, and zoom checks.
</verification>

<success_criteria>
Visitors can discover and enter HAOO from a future-safe Products section in both navigation modes, with all required information and action preserved across collection and media states.
</success_criteria>

<output>
Create `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-04-SUMMARY.md` when done.
</output>
