---
phase: 01-discover-haoo-and-choose-an-onboarding-path
plan: 09
type: execute
wave: 8
gap_closure: true
depends_on:
  - 01-08
files_modified:
  - products/haoo/index.html
  - src/test/build-output.test.ts
autonomous: false
requirements:
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04
  - ONBD-05
must_haves:
  truths:
    # --- Gap-derived (01-VERIFICATION.md human_verification[5], PROHIB-ONBD-02) ---
    - "The JavaScript-readiness scope call is made by the developer at a recorded decision point, not silently by the planner or the executor: either the built HAOO document gains a no-script onboarding fallback, or PROHIB-ONBD-02 is narrowed to match ONBD-05's enumerated conditions with the reason written down (ONBD-05)."
    - "Whichever branch is chosen, ONBD-01 through ONBD-04 keep exactly the destinations that ship today — `tel:+254702188044`, `mailto:info@haoo.online`, the fixed WhatsApp URL, and `https://manage.haoo.online/` — with no destination added, removed, or altered (ONBD-01, ONBD-02, ONBD-03, ONBD-04)."
    - "If the fallback branch is taken, every href inside the no-script block is asserted equal to the value derived from `HAOO_PRODUCT.contacts` and `HAOO_PRODUCT.brochure`, in both the source document and the built document, so the second copy cannot drift from the single source of truth (ONBD-05, D-11)."
    - "If the fallback branch is taken, the no-script block discloses that its destinations leave ZERO-PAPER HUB, keeps the assisted invitation as a friendly consultation rather than a demo-only or commitment framing, and writes the brand in uppercase (D-12, D-14, D-15)."
    - "The no-script block renders only when scripting is unavailable, so with JavaScript enabled the page contains exactly one set of onboarding links and no duplicated accessible name (ONBD-05)."
    # --- Probe-derived, resolved explicit (ONBD-02 empty + encoding, lifted per specless-probe §C) ---
    - "Empty: the WhatsApp starter text is a non-empty compile-time constant with no runtime input path — there is no empty, single-element, or null input case, because nothing is ever passed in. `new URL(whatsappHref).searchParams.get('text')` returns a non-empty string equal to `HAOO_PRODUCT.contacts.whatsappStarterText` in every render and in any no-script copy (ONBD-02)."
    - "Encoding: equality for the starter text is code-point equality after URL decoding. The encoded value round-trips to a code-point-identical string; no NFC or NFD normalization, byte-length comparison, or grapheme-cluster segmentation is applied anywhere in the path (ONBD-02)."
    # --- Backstop (jsdom cannot disable scripting) ---
    - statement: "With JavaScript disabled in a real browser, opening the built HAOO document presents at least one assisted destination and the self-onboarding destination as working links."
      verification: backstop
  artifacts:
    - path: "products/haoo/index.html"
      provides: "The physical HAOO document, which the fallback branch extends with a no-script onboarding block"
    - path: "src/test/build-output.test.ts"
      provides: "Assertions binding the no-script destinations to the centralized product data in both source and built HTML"
  key_links:
    - from: "products/haoo/index.html"
      to: "src/products/haoo.ts"
      via: "href equality asserted against HAOO_PRODUCT contacts and brochure"
      pattern: "manage.haoo.online"
    - from: "src/test/build-output.test.ts"
      to: "dist/products/haoo/index.html"
      via: "the built document carrying the same no-script destinations as the source"
      pattern: "noscript"
  prohibitions:
    - id: "PROHIB-ONBD-02"
      requirement_id: "ONBD-05"
      category: "resilience"
      status: "unresolved"
      verification: null
      statement: "MUST NOT make assisted or self-onboarding conditional on tracking, browser storage, form delivery, JavaScript readiness, or PDF support"
      reason: "flagged-unverified with directly observed contrary evidence on the JavaScript clause — the built document ships an empty root with no fallback; this plan's decision checkpoint resolves the scope call and the outcome is recorded, not assumed"
    - id: "PROHIB-ONBD-03"
      requirement_id: "ONBD-05"
      category: "transparency"
      status: "unresolved"
      verification: null
      statement: "MUST NOT frame a demo as the only next step or imply the visitor has already committed to onboarding"
      reason: "flagged-unverified — recalled from 01-02-PLAN; a compressed no-script block is where consultation framing is most likely to be lost"
    - id: "PROHIB-ONBD-04"
      requirement_id: "ONBD-02"
      category: "privacy"
      status: "unresolved"
      verification: null
      statement: "MUST NOT put visitor, page, analytics, or campaign data in the WhatsApp starter URL"
      reason: "flagged-unverified — recalled from 01-02-PLAN; a second hand-written copy of the WhatsApp URL is a new place for context to be appended"
    - id: "PROHIB-ONBD-01"
      requirement_id: "ONBD-04"
      category: "transparency"
      status: "unresolved"
      verification: null
      statement: "MUST NOT obscure that onboarding destinations leave ZERO-PAPER HUB"
      reason: "flagged-unverified — recalled from 01-02-PLAN; the disclosure copy must survive into any fallback block"
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
---

<objective>
**As a** landlord or property manager whose browser has JavaScript disabled, blocked, or still loading, **I want to** reach HAOO by phone, WhatsApp, email, or the self-onboarding platform, **so that** my browser configuration does not decide whether I can start with HAOO — or, if the developer scopes that out of v1, **so that** the decision is written down rather than left as an unexplained empty page.

Purpose: Resolve the one prohibition in `01-VERIFICATION.md` carrying directly observed contrary evidence. `dist/products/haoo/index.html` ships an empty root element with no fallback, so with JavaScript disabled no onboarding link renders at all. ONBD-05 as written in `REQUIREMENTS.md` does not enumerate JavaScript, so the requirement is met and only the phase's self-imposed prohibition is breached — which makes this a scope call that belongs to the developer.
Output: A recorded decision, and whichever of the two outcomes the developer selects.

**Gap-closure plan.** Plans 01-01 through 01-05 are executed and must not be rewritten.
</objective>

<execution_context>
@/home/paul/.claude/gsd-core/workflows/execute-plan.md
@/home/paul/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-CONTEXT.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VERIFICATION.md
@products/haoo/index.html
@src/products/haoo.ts
</context>

<gap_source>
From `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VERIFICATION.md`, `human_verification[5]`:

> PROHIBITION REVIEW (PROHIB-ONBD-02, judgment-tier, status unresolved) — "MUST NOT make assisted or self-onboarding conditional on tracking, browser storage, form delivery, JavaScript readiness, or PDF support." Tracking, storage, form delivery, and PDF support are honored and enforced by tests. JavaScript readiness is NOT: `dist/products/haoo/index.html` ships an empty `<div id="root">` with no `<noscript>` fallback, so with JavaScript disabled no onboarding link renders at all. ONBD-05 as written in REQUIREMENTS.md does not enumerate JavaScript, so the requirement is met and only the plan's self-imposed prohibition is breached. Why human: the scope call (tighten the code, or narrow the prohibition to match ONBD-05) belongs to the developer.

Related fact already in the codebase, and part of why this is a genuine judgment call rather than an obvious defect: the original brochure is already reachable without scripting. `products/haoo/index.html:10` declares `<link rel="alternate" type="application/pdf" href="/products/haoo/HAOO-Marketing-Brochure.pdf" title="HAOO Marketing Brochure (PDF)" />`, recorded in STATE.md as a deliberate progressive-enhancement decision. What is missing without scripting is the four onboarding destinations, not the product information.
</gap_source>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide the PROHIB-ONBD-02 JavaScript-readiness scope call</name>
  <decision>Resolve PROHIB-ONBD-02's JavaScript-readiness clause: add a no-script onboarding fallback to the HAOO document, or narrow the prohibition to match ONBD-05 as written</decision>
  <context>
    Phase 1 declared, in its own plans, that onboarding must not be conditional on "tracking, browser storage, form delivery, JavaScript readiness, or PDF support". Four of those five clauses are honored and enforced by passing tests. The fifth is not: the built HAOO document is an empty React root, so a visitor with JavaScript disabled or blocked sees nothing — no phone number, no WhatsApp link, no email, no self-onboarding link.

    This is not a requirement failure. ONBD-05 in `REQUIREMENTS.md` enumerates "analytics, browser storage, PDF embedding, or form-provider availability" and does not mention JavaScript. The verifier marked ONBD-05 satisfied. What is breached is the stricter prohibition the phase wrote for itself, which is why the verifier routed it here instead of recording it as a gap.

    The brochure PDF is already reachable without scripting via the `rel="alternate"` link in the document head, so the product information is not lost with JavaScript off — only the four onboarding destinations are.

    Cost of reversal, either way, is one static HTML block plus its assertions. What makes this worth stopping for is not the code, it is that the answer changes what the phase record says about a contract the accessibility work in Phase 5 will inherit.
  </context>
  <options>
    <option id="noscript-fallback">
      <name>Add a minimal no-script onboarding block to the HAOO document</name>
      <pros>Honors PROHIB-ONBD-02 exactly as the phase wrote it, and honors the spirit of ONBD-05 rather than only its enumerated list. Makes the four destinations reachable in a JS-disabled browser, in a browser where the bundle fails to load, and in the window before hydration. Costs one static block in a file the phase already owns; no dependency, no build step, no runtime code. The brochure is already reachable this way, so this makes the document consistent with a decision already taken.</pros>
      <cons>Creates a second, hand-written copy of the four destination URLs alongside `src/products/haoo.ts`, which is a real drift risk — mitigated, not eliminated, by asserting exact equality against the centralized data in both the source and built HTML. The block cannot be generated from the TypeScript product definition without adding a build plugin, which is out of scope for this phase.</cons>
    </option>
    <option id="narrow-prohibition">
      <name>Narrow PROHIB-ONBD-02 to ONBD-05's enumerated conditions and record JavaScript readiness as out of scope for v1</name>
      <pros>Removes the duplicate source of truth entirely. ONBD-05 as written is already satisfied and independently verified, so nothing the milestone actually committed to is left undone. Phase 5 already owns the accessibility audit (QUAL-02, QUAL-03), which is a more natural home for a no-script contract applied across the whole funnel rather than to one document. Keeps the phase honest by narrowing a self-imposed clause deliberately instead of leaving it silently breached.</pros>
      <cons>The built HAOO document keeps shipping an empty body for a JS-disabled or JS-failed visitor, and the phase record will say the phase chose that. If Phase 5 later decides a no-script path is required, the fallback gets built then anyway, and the interim releases ship without it.</cons>
    </option>
  </options>
  <resume-signal>Select: `noscript-fallback` or `narrow-prohibition`</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement the selected JavaScript-readiness outcome</name>
  <reversibility rating="costly">Undo touches one static block and its assertions, but the choice is recorded against a contract Phase 5's accessibility audit inherits — reversing it means re-opening a closed verification finding across every release shipped in between. Not rated one-way: there is no migration and no published contract broken, only a documented position to restate.</reversibility>
  <read_first>
    - `products/haoo/index.html` — the file being modified under the fallback branch; the `rel="alternate"` PDF link at line 10 is the existing no-script precedent, and the empty `<div id="root">` at line 23 is the observed defect
    - `src/products/haoo.ts` — `WHATSAPP_STARTER_TEXT`, `PHONE_NUMBER`, and the derived `contacts` block; the single source of truth every destination must equal
    - `src/components/OnboardingChoices.tsx` — the shipped assisted invitation, the `These contact links leave the ZERO-PAPER HUB product page.` disclosure, and the `Opens ... outside ZERO-PAPER HUB.` disclosure; the fallback copy must preserve both disclosures and the consultation framing
    - `src/test/build-output.test.ts` — the source-and-built HTML assertion style at lines 74-122, which the new assertions follow
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-CONTEXT.md` — D-09, D-10, D-11, D-12, D-14, D-15
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VERIFICATION.md` — the `human_verification` entry this task closes
  </read_first>
  <files>products/haoo/index.html, src/test/build-output.test.ts</files>
  <action>Execute only the branch the checkpoint selected, and record the selection and its rationale verbatim in the plan summary either way.

If `noscript-fallback` was selected: add a `noscript` element inside the body of `products/haoo/index.html`, placed immediately before the root div so it is the first content a non-scripting browser encounters. Inside it put a short identity line naming HAOO as a ZERO-PAPER HUB product with the brand in uppercase, the assisted invitation phrased as a friendly consultation matching the shipped `assistedInvitation` framing, then five native anchors and two disclosure lines: the WhatsApp URL exactly as `HAOO_PRODUCT.contacts.whatsappHref` produces it including its single fixed encoded starter text and nothing else appended, `tel:+254702188044`, `mailto:info@haoo.online`, `https://manage.haoo.online/`, and `/products/haoo/HAOO-Marketing-Brochure.pdf`; then the disclosure that the contact links leave the ZERO-PAPER HUB product page and the disclosure that the self-onboarding link opens `manage.haoo.online` outside ZERO-PAPER HUB. Order WhatsApp first, then phone and email, then self-onboarding, matching D-09, D-10, and D-12. Do not restate the product story, the capabilities, the journey, or the market claim — this block exists to preserve destinations, not to become a second product page. Add no inline styles, no script, no form, and no tracking parameter of any kind. Then add assertions to `src/test/build-output.test.ts` that both the source document and the built document contain a `noscript` element; that each of the five hrefs appears inside it; that each equals the corresponding value derived from `HAOO_PRODUCT` rather than a literal retyped in the test, importing the product definition the way the content test already does; that decoding the WhatsApp URL's text parameter yields a string code-point-identical to `HAOO_PRODUCT.contacts.whatsappStarterText`; and that the block contains neither a query parameter beyond that single text parameter nor any additional key.

Add one further assertion in that same test file, scoped to the block rather than to the document. Extract the substring lying strictly between the opening and closing no-script tags, from the source document and from the built document, and fail the test when either extraction yields nothing, so a renamed, removed, or unbalanced block cannot pass vacuously. Then assert the extracted substring matches none of the five forbidden patterns enumerated in this task's acceptance criteria — an embedded script element, an inline event-handler attribute, a form element, an inline style attribute, and a campaign-tracking query prefix. Every one of those five checks must be evaluated against the extracted substring and never against the whole file: the document legitimately carries a module script tag outside the block, so a whole-file search would always match and would reduce the check to a judgment about scoping rather than a decidable assertion.

If `narrow-prohibition` was selected: change no source file. Instead record the narrowing in this plan's summary and in the phase record: state that PROHIB-ONBD-02's JavaScript-readiness clause is withdrawn for v1 because ONBD-05 does not enumerate it, name Phase 5 QUAL-02 and QUAL-03 as the owner of any future no-script contract, and state plainly that the built HAOO document ships an empty body for a visitor without scripting. Add no test and modify no HTML. The acceptance criteria for this branch are documentary, and the phase verifier must be able to read the decision and its reason without inferring either.</action>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
    <human-check>Under the fallback branch: open the built HAOO document with JavaScript disabled in a real browser and confirm the WhatsApp, phone, email, self-onboarding, and brochure links are present and activate correctly, and that with JavaScript enabled the page shows exactly one set of onboarding links. Under the narrowing branch: confirm the phase record states the withdrawal, its reason, and its Phase 5 owner.</human-check>
  </verify>
  <acceptance_criteria>
    **Both branches**
    - The selected option id and the developer's stated reason appear verbatim in `01-09-SUMMARY.md`.
    - `npm test` exits 0, and `npm run typecheck` and `npm run lint` each exit 0.
    - `grep -c "manage.haoo.online" src/products/haoo.ts` is unchanged from HEAD — the centralized destinations are not edited by this plan.

    **`noscript-fallback` branch only**
    - `products/haoo/index.html` contains a `noscript` element, and `dist/products/haoo/index.html` contains it too after `npm run build`.
    - The new assertions in `src/test/build-output.test.ts` derive every expected href from `HAOO_PRODUCT` rather than from a retyped literal, verified by reading the test source.
    - `npx vitest run src/test/build-output.test.ts` exits 0, and it fails if the self-onboarding href inside the no-script block is changed to any other value.
    - Decoding the WhatsApp URL's `text` parameter yields exactly `HAOO_PRODUCT.contacts.whatsappStarterText`, and the URL carries no second query parameter.
    - `src/test/build-output.test.ts` extracts the substring strictly between `<noscript>` and `</noscript>` from both `products/haoo/index.html` and `dist/products/haoo/index.html`, fails when either extraction yields nothing, and asserts that extracted substring matches none of `/<script\b/i`, `/\son[a-z]+\s*=/i`, `/<form\b/i`, `/\sstyle\s*=/i`, and `/utm_/i`.
    - `npx vitest run src/test/build-output.test.ts` exits 0 with that assertion present, and exits non-zero when an inline event-handler attribute is added to any anchor inside the no-script block — both directions run before the commit.
    - `grep -c "<noscript" products/haoo/index.html` returns `1`, so the extraction targets exactly one block.

    **`narrow-prohibition` branch only**
    - `git diff --stat` for this task shows zero changed lines under `src/`, `products/`, and `public/`.
    - `01-09-SUMMARY.md` states the withdrawal, the ONBD-05 reason, the Phase 5 QUAL-02 / QUAL-03 owner, and the shipped consequence that the document has an empty body without scripting.
  </acceptance_criteria>
  <done>The JavaScript-readiness clause is resolved by an explicit developer decision, and the outcome — a working no-script onboarding path or a written, owned narrowing — is in the phase record rather than left as an open contradiction.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**Branch-dependent. Under `noscript-fallback`:**

**Changed files**
- `products/haoo/index.html` — gains a `noscript` element inside `<body>`, before `<div id="root">`, containing: an uppercase-brand identity line, a consultation-framed assisted invitation, five native anchors (`https://wa.me/254702188044?text=...`, `tel:+254702188044`, `mailto:info@haoo.online`, `https://manage.haoo.online/`, `/products/haoo/HAOO-Marketing-Brochure.pdf`), and the two shipped disclosure lines
- `src/test/build-output.test.ts` — new assertions in the existing `Phase 1 static build contracts` describe block binding every no-script href to `HAOO_PRODUCT`, in both source and built HTML, plus the WhatsApp starter-text decode round-trip, the single-query-parameter assertion, and a region-scoped markup assertion over the substring extracted from between the no-script tags (non-empty-extraction guard, then five forbidden-pattern checks)

**Under `narrow-prohibition`:** no source, test, or asset artifact. The artifact is the recorded decision in `01-09-SUMMARY.md` and the phase record.

**Under either branch:** no new npm scripts, no new dependencies, no new components, no new routes, and no change to `src/products/haoo.ts`.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; blocking threshold `high`.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser scripting availability → onboarding reachability | Whether a visitor can contact HAOO currently depends on a client-side capability the visitor may not control. |
| Hand-written HTML destinations → centralized `ProductDefinition` contacts | A second copy of four external URLs is a place where the published destination can silently diverge from the reviewed one. |
| Visitor context → the WhatsApp starter URL | A duplicated URL is a new opportunity for page, campaign, or visitor data to be appended to an outbound link. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-25 | Denial of service | onboarding reachability without scripting | high | mitigate (fallback branch) / accept (narrowing branch) | The fallback branch makes all four destinations reachable with scripting unavailable. The narrowing branch accepts the exposure explicitly, records ONBD-05 as the reason, and names Phase 5 QUAL-02 / QUAL-03 as the owner — accepted in the record, never silently. |
| T-01-26 | Spoofing | duplicated destination URLs in static HTML | high | mitigate | Every no-script href is asserted equal to the value derived from `HAOO_PRODUCT`, in both the source and the built document, so a typo or a substituted destination fails the suite instead of shipping. |
| T-01-27 | Information disclosure | the WhatsApp starter URL in the no-script block | medium | mitigate | The URL carries exactly one fixed compile-time `text` parameter; the contract asserts a code-point-identical decode and rejects any second query parameter, which is what enforces `PROHIB-ONBD-04`. |
| T-01-28 | Spoofing | outbound destination transparency | medium | mitigate | Both shipped disclosures — that contact links leave the ZERO-PAPER HUB product page, and that self-onboarding opens `manage.haoo.online` outside ZERO-PAPER HUB — are carried into the fallback block, enforcing `PROHIB-ONBD-01`. |
| T-01-29 | Elevation of privilege | static markup injected into the product document | medium | mitigate | The block is plain anchors and text only. Enforced by a region-scoped assertion in `src/test/build-output.test.ts` over the substring extracted from between the no-script tags in both the source and the built document — never a whole-file grep, which the document's own module script tag would always match — with a non-empty-extraction guard so a removed block cannot pass vacuously. |
| T-01-SC | Tampering | package-manager installs | — | not applicable | This plan installs nothing, so the RESEARCH.md Package Legitimacy Audit gate does not fire and no legitimacy checkpoint is required. |

**Schema gate:** not applicable — no ORM or schema directory exists in this project.
</threat_model>

<verification>
Run `npm test`, `npm run typecheck`, and `npm run lint`. Under the fallback branch also run `npm run build` and read `dist/products/haoo/index.html` directly to confirm the no-script block survived bundling with its hrefs intact. Carry the backstop truth — real-browser behavior with scripting disabled — into end-of-phase UAT alongside the three named, Phase 5-owned visual items enumerated in `01-06-PLAN.md` under `<deferred_human_verification>`: `DEFER-HV-PDF-EMBED`, `DEFER-HV-REFLOW`, and `DEFER-HV-CARD-SPLIT`.
</verification>

<success_criteria>
PROHIB-ONBD-02 no longer sits in the phase record as an unresolved contradiction: either the built HAOO document reaches all four onboarding destinations without scripting and a test binds them to the centralized product data, or the clause is narrowed with the reason, the owner, and the accepted consequence written down — chosen by the developer at a blocking checkpoint, not assumed.
</success_criteria>

<output>
Create `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-09-SUMMARY.md` when done.
</output>
