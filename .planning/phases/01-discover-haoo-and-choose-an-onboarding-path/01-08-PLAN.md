---
phase: 01-discover-haoo-and-choose-an-onboarding-path
plan: 08
type: execute
wave: 7
gap_closure: true
depends_on:
  - 01-07
files_modified:
  - src/test/build-output.test.ts
  - package.json
  - .planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md
autonomous: true
requirements:
  - QUAL-04
  - PROD-02
  - PROD-04
  - PROD-05
must_haves:
  truths:
    # --- Gap-derived (01-VERIFICATION.md coincidental_reliance_items[1]) ---
    - "The build is a declared, executed precondition of the `npm test` contract: `npm test` produces the `dist/` tree it then asserts against, so a green suite can no longer depend on a build that happened at some earlier, unspecified time (QUAL-04)."
    - "A `dist/` tree that is absent, or older than any file the build consumes, fails `src/test/build-output.test.ts` with a message naming the offending input and the command that fixes it — it never yields a silent pass against a stale artifact (QUAL-04, PROD-02, PROD-05)."
    - "A fast inner-loop script `test:unit` runs the suite without rebuilding, so the freshness guard is the thing that catches staleness rather than a rebuild being silently required of every focused run (QUAL-04)."
    - "The freshness guard reads only build inputs that Vite actually consumes; editing a file under `src/test/` does not mark `dist/` stale, because test files are not build inputs (QUAL-04)."
    - "The redefinition of `npm test` is recorded where the phase's validation contract lives: `01-VALIDATION.md` states that `npm test` now performs the production build before the runner, names `npm run test:unit` as the sub-30-second inner loop, and reconciles the `01-RESEARCH.md` Validation Architecture line that component tests stay independent of `dist/` — so no phase record still describes `npm test` as the runner alone (QUAL-04)."
    # --- Probe-derived, resolved explicit (PROD-04 concurrency, lifted per specless-probe §C) ---
    - "Concurrency: `npm test` run twice in succession, and `npm run build` followed by `npm run test:unit`, produce identical pass results; the freshness guard reads mtimes only and holds no state, so it does not depend on test file execution order or on tests running in parallel. The original PROD-04 reading of this probe — repeated, interrupted, and concurrent activation of the brochure Open and Download controls — is inapplicable to a static marketing site, since both are native anchors with no shared mutable state, and is already asserted by a shipped test."
    # --- Backstop (a clean-checkout run is Phase 5's gate, not this plan's) ---
    - statement: "`npm test` succeeds on a freshly cloned checkout after `npm ci`, with no pre-existing `dist/`."
      verification: backstop
  artifacts:
    - path: "src/test/build-output.test.ts"
      provides: "A build-artifact freshness guard that fails on an absent or stale dist tree"
    - path: "package.json"
      provides: "A `test` script that builds before asserting, and a `test:unit` script for the fast inner loop"
    - path: ".planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md"
      provides: "The phase validation contract updated to the new `npm test` semantics, with `test:unit` named as the inner loop"
  key_links:
    - from: "package.json"
      to: "dist/products/haoo/index.html"
      via: "the test script running the build that emits the artifact the suite asserts against"
      pattern: "npm run build"
    - from: "src/test/build-output.test.ts"
      to: "src/"
      via: "mtime comparison of build outputs against build inputs"
      pattern: "mtimeMs"
  prohibitions:
    - id: "PROHIB-QUAL-01"
      requirement_id: "QUAL-04"
      category: "integrity"
      status: "unresolved"
      verification: null
      statement: "MUST NOT make the suite appear green by weakening, skipping, or deleting a dist-dependent assertion instead of producing the artifact it asserts against"
      reason: "flagged-unverified — descriptor-less by design; the whole point of this plan is that a green suite must mean the artifact was built, and an autonomous run has no human to distinguish a real fix from a removed assertion"
    - id: "PROHIB-QUAL-02"
      requirement_id: "QUAL-04"
      category: "integrity"
      status: "unresolved"
      verification: null
      statement: "MUST NOT soften the freshness guard with a time tolerance, a skip on missing dist, or an environment flag that disables it"
      reason: "flagged-unverified — any of the three restores the exact silent-pass path the verifier found"
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
---

<objective>
**As a** developer trusting this phase's green test suite, **I want to** know that a passing run actually built the artifact it asserted against, **so that** a stale `dist/` can never hide a broken route, a missing asset, or wrong metadata behind 44 green checks.

Purpose: Close `coincidental_reliance_items[1]` from `01-VERIFICATION.md` — `src/test/build-output.test.ts` asserts against a gitignored `dist/` that `"test": "vitest run"` never produces, so suite green depends on an undeclared prior build and passes against a stale artifact.
Output: A build-artifact freshness guard and a `test` script that makes the build an executed precondition, plus a `test:unit` script for the fast inner loop.

**Gap-closure plan.** Plans 01-01 through 01-05 are executed and must not be rewritten.
</objective>

<execution_context>
@/home/paul/.claude/gsd-core/workflows/execute-plan.md
@/home/paul/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VERIFICATION.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md
@src/test/build-output.test.ts
@package.json
</context>

<gap_source>
From `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VERIFICATION.md`, `coincidental_reliance_items[1]`, reason `undeclared-precondition`, against ROADMAP SC-1 / SC-3 and QUAL-04:

> `src/test/build-output.test.ts` asserts against `dist/`, which is gitignored and which `"test": "vitest run"` never produces. Suite green depends on a build having been run at some earlier, unspecified time — and passes against a stale `dist/`. This verifier ran `rm -rf dist && npm run build` immediately before `npm test`, so the evidence recorded here is sound; the suite's own guarantee is not. Make the build an explicit precondition of `npm test` and add a staleness guard.

**Scope boundary against CR-01 and Phase 5 — read this before starting.**

This item overlaps CR-01, which `01-VERIFICATION.md` records under `deferred:` as addressed by Phase 5 Success Criterion 3 (QUAL-05: "build, typecheck, lint, automated contract/component tests, and required deployed checks pass"). The two are not the same thing, and this plan takes only the half it can own:

| Concern | Owner | This plan |
|---|---|---|
| The suite can pass against a **stale** `dist/` — a green run that proves nothing | This plan | **Closes it.** The freshness guard is the deliverable. |
| The build is not a **declared** precondition of the `npm test` contract | This plan | **Closes it.** The `test` script executes the build. |
| `npm test` green on a **clean checkout** with no prior state, as a release gate | Phase 5 / QUAL-05 | **Not claimed.** Recorded here as a `backstop` truth, not as a closed criterion. Phase 5 still owns proving it. |
| CR-02 — `npm run test:phase1:red` inverts its exit code and can never pass post-GREEN | Phase 5 / QUAL-05 | **Not touched.** `scripts/assert-phase1-red.mjs` and the `test:phase1:red` script are left exactly as they are. |

Making the build execute inside `npm test` does incidentally satisfy CR-01's symptom. That is stated plainly rather than claimed as a Phase 5 deliverable: Phase 5 retains the clean-checkout release gate and CR-02, and this plan pre-empts neither.

**Locked-decision coverage:** none of `01-CONTEXT.md` D-01 through D-17 governs this plan. Those seventeen decisions cover the product story, the brochure experience, onboarding emphasis, and the brand relationship; none of them speaks to the test harness or the npm script contract. This plan therefore cites no `D-NN`, and that absence is deliberate rather than an omission. It must not add, remove, or reinterpret any of them — the acceptance criteria constrain the diff to `src/test/build-output.test.ts` and the `scripts` block of `package.json`.
</gap_source>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a build-artifact freshness guard to the build contract suite</name>
  <read_first>
    - `src/test/build-output.test.ts` — the file being modified; `ROOT`, `DIST`, `SOURCE_HTML`, `BUILT_HTML`, `BUILT_PDF` at lines 6-11, the `listFiles` recursive walker at lines 50-59, and the `existsSync`-tolerant `readText` and `sha256` helpers at 40-48 which are exactly what let an absent `dist/` degrade quietly
    - `vite.config.ts` — `rollupOptions.input` names `index.html` and `products/haoo/index.html`, which fixes the set of build inputs and outputs
    - `.gitignore` — `dist` is ignored, which is why the artifact can be absent or arbitrarily old
    - `package.json` — the current `test` script, unchanged by this task
    - `vitest.config.ts` — `environment: 'jsdom'`, `globals: false`; the guard uses explicit imports like the rest of the file
  </read_first>
  <files>src/test/build-output.test.ts</files>
  <behavior>
    - With no `dist/` at all, the guard fails and its message names the missing output path and instructs the developer to run the build.
    - With a `dist/` older than any build input, the guard fails and its message names the newest offending input file and its timestamp alongside the oldest output.
    - Immediately after `npm run build`, the guard passes.
    - Touching a file under `src/test/` does not make the guard fail, because test files are not build inputs.
    - Touching `src/products/haoo.ts`, `public/products/haoo/HAOO-Marketing-Brochure.pdf`, `vite.config.ts`, `index.html`, or `products/haoo/index.html` does make the guard fail until the next build.
  </behavior>
  <action>Add a new describe block to `src/test/build-output.test.ts` named for the artifact freshness contract, placed before the existing `Phase 1 static build contracts` block so it reports first and a stale-artifact failure is the first thing a developer reads. Define `BUILD_INPUTS` as the recursive file list of `src/` with every path under `src/test/` excluded, plus the recursive file list of `public/`, plus the four fixed paths `index.html`, `products/haoo/index.html`, `vite.config.ts`, and `package.json`. Define `BUILD_OUTPUTS` as the two fixed paths `dist/index.html` and `dist/products/haoo/index.html` plus the recursive file list of `dist/assets`. Reuse the existing `listFiles` walker rather than writing a second one. Add `newestInput()` returning the input path with the greatest `statSync(...).mtimeMs` and `oldestOutput()` returning the output path with the least `mtimeMs`. Write three assertions: that `BUILD_OUTPUTS` is non-empty and every entry exists, failing with a message that names the first missing path and states that the build must be run; that `BUILD_INPUTS` is non-empty, so a path typo cannot yield a vacuous pass; and that the oldest output `mtimeMs` is greater than or equal to the newest input `mtimeMs`, failing with a message naming both files and both timestamps. Compare `mtimeMs` in milliseconds and use greater-than-or-equal so an output written in the same millisecond as its input is not treated as stale. Do not add a tolerance window, do not skip when `dist/` is absent, and do not gate the guard behind an environment variable — each of those restores the silent-pass path this plan exists to close, and each is forbidden by `PROHIB-QUAL-02`. Change no existing assertion, helper, or constant in the file beyond adding the new block and any shared helper it needs. Demonstrate RED before GREEN by running the guard against a removed `dist/` first.</action>
  <verify>
    <automated>rm -rf dist &amp;&amp; ! npx vitest run src/test/build-output.test.ts &amp;&amp; npm run build &amp;&amp; npx vitest run src/test/build-output.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
    <human-check>Read the failure output from the removed-dist run and confirm it names the missing artifact and the command to fix it, rather than reporting a generic assertion failure.</human-check>
  </verify>
  <acceptance_criteria>
    - `rm -rf dist && npx vitest run src/test/build-output.test.ts` exits non-zero, and the failure message contains the string `dist/products/haoo/index.html`.
    - `npm run build && npx vitest run src/test/build-output.test.ts` exits 0.
    - `touch src/products/haoo.ts && npx vitest run src/test/build-output.test.ts` exits non-zero; a following `npm run build` makes it exit 0 again.
    - `touch src/test/setup.ts && npx vitest run src/test/build-output.test.ts` exits 0 — test files are not build inputs.
    - `grep -n "tolerance\|process.env" src/test/build-output.test.ts` returns no match inside the new block.
    - Every assertion present in the file at HEAD is still present and unmodified.
  </acceptance_criteria>
  <done>A stale or absent `dist/` fails the suite loudly with an actionable message, so suite green now carries the artifact guarantee it previously only appeared to carry.</done>
</task>

<task type="auto">
  <name>Task 2: Make the build an executed precondition of the npm test contract</name>
  <read_first>
    - `package.json` — the file being modified; the `scripts` block, specifically `build`, `test`, `test:phase1:red`, and `typecheck`
    - `scripts/assert-phase1-red.mjs` — read only to confirm it is NOT touched; its inverted exit code is CR-02, deferred to Phase 5
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md` — the second file being modified; the Test Infrastructure table (quick-run and full-suite rows), the Sampling Rate bullets, and the Automated Command column of the Per-Task Verification Map all name `npm test` under its old semantics
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-RESEARCH.md` — the Validation Architecture line stating that build-output tests must run after `npm run build` while component tests stay independent of `dist/` so quick feedback stays under 30 seconds; this is the statement `test:unit` preserves and the validation record must reconcile
    - `.github/workflows/deploy.yml` — confirm whether any job invokes `npm test`, so the added build step is understood before it lands in CI
  </read_first>
  <files>package.json, .planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md</files>
  <action>Change the `test` script from running the test runner alone to running the production build first and the test runner second, chained so a build failure fails the script. Add a sibling script named `test:unit` that runs the test runner alone, for the fast inner loop where a rebuild is not wanted; the freshness guard from Task 1 is what protects that loop from asserting against a stale artifact. Leave `build`, `dev`, `lint`, `preview`, `typecheck`, and `test:phase1:red` exactly as they are, and do not open or modify `scripts/assert-phase1-red.mjs` — that script's inverted exit code is CR-02 and belongs to Phase 5. Do not add, remove, or change any dependency or devDependency, and do not change the package name, version, or type fields. After the change, confirm by running both scripts that `npm test` emits a fresh `dist/` before asserting, and that `npm run test:unit` on a freshly built tree passes while the same command after removing `dist/` fails on the freshness guard rather than on a confusing downstream assertion.

Then update the phase validation record so no document still describes `npm test` under its old semantics — the command keeps its name and gains a stronger contract, and that has to be written down where the phase's validation contract lives rather than left for a reader to infer from `package.json`. In `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md`: change the Test Infrastructure quick-run row to invoke `npm run test:unit` with the single test file, change the full-suite row to `npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm test`, and add one row or note line directly under that table stating the new contract in full — that `npm test` runs the production build before the runner and therefore supersedes the Wave 0 checklist line recording it as the runner alone, that `npm run test:unit` is the inner loop, and that the `01-RESEARCH.md` Validation Architecture requirement is preserved because component and contract suites other than `src/test/build-output.test.ts` remain independent of `dist/` and the inner loop stays under thirty seconds. Rewrite the Sampling Rate bullets to use `npm run test:unit` for the per-task affected-file loop and `npm test` for the per-wave gate. In the Automated Command column of the Per-Task Verification Map apply one mechanical rule with no exceptions: every row whose command names `src/test/build-output.test.ts` uses the build-inclusive `npm test`, and every other row uses `npm run test:unit`; remove the now-redundant `npm run build &amp;&amp;` prefix wherever it appears, since `npm test` performs that build itself. Leave the Wave 0 Requirements checklist, the Manual-Only Verifications table, the Validation Sign-Off checklist, the frontmatter, and every Requirement, Threat Ref, Secure Behavior, and Status cell exactly as they are — this is a command-semantics correction, not a re-scoping of the validation strategy.</action>
  <verify>
    <automated>rm -rf dist &amp;&amp; npm test &amp;&amp; npm run test:unit &amp;&amp; rm -rf dist &amp;&amp; ! npm run test:unit &amp;&amp; npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
    <human-check>Confirm the deploy workflow still succeeds in whatever it invokes, and that no CI job now runs the build twice in a way that meaningfully slows the pipeline.</human-check>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts.test` invokes `npm run build` before the test runner, chained with `&&`.
    - `package.json` `scripts["test:unit"]` exists and runs the test runner alone.
    - `package.json` `scripts["test:phase1:red"]` is byte-identical to its value at HEAD, and `git diff --stat scripts/` shows no change.
    - `rm -rf dist && npm test` exits 0 and leaves `dist/products/haoo/index.html` on disk.
    - `rm -rf dist && npm run test:unit` exits non-zero on the freshness guard.
    - `git diff package.json` touches only the `scripts` block; no dependency line changes.
    - `grep -c "npm run build && npm test" .planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md` returns `0` — the redundant prefix is gone everywhere.
    - In `01-VALIDATION.md`, every Per-Task Verification Map row whose Automated Command names `src/test/build-output.test.ts` invokes `npm test`, and every row that does not names `npm run test:unit`; `grep -c "npm run test:unit" .planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md` returns at least `10`.
    - `01-VALIDATION.md` contains a note directly under the Test Infrastructure table stating all three facts: that `npm test` runs the production build before the runner, that `npm run test:unit` is the inner loop, and that the `01-RESEARCH.md` Validation Architecture requirement holds because suites other than `src/test/build-output.test.ts` remain independent of `dist/`.
    - `git diff --stat` for this task lists exactly two files, `package.json` and `01-VALIDATION.md`, and `git diff .planning/.../01-VALIDATION.md` shows no changed line in the frontmatter, the Wave 0 Requirements checklist, the Manual-Only Verifications table, or the Validation Sign-Off checklist.
  </acceptance_criteria>
  <done>The build is an executed part of the `npm test` contract, and the fast inner loop remains available without giving up the staleness protection.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**New npm scripts**
- `test:unit` — runs `vitest run` alone, without a rebuild

**Changed npm scripts**
- `test` — now runs the production build before `vitest run`

**New symbols in `src/test/build-output.test.ts`**
- `BUILD_INPUTS` — const array of every file Vite consumes: `src/` excluding `src/test/`, `public/`, plus `index.html`, `products/haoo/index.html`, `vite.config.ts`, `package.json`
- `BUILD_OUTPUTS` — const array of `dist/index.html`, `dist/products/haoo/index.html`, and every file under `dist/assets`
- `newestInput(): { path: string; mtimeMs: number }`
- `oldestOutput(): { path: string; mtimeMs: number }`
- `describe('Phase 1 build artifact freshness')` — three cases: outputs exist, inputs were actually scanned, outputs are not older than inputs

**Changed planning records**
- `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md` — Test Infrastructure quick-run and full-suite rows, a new note stating the `npm test` contract and the `test:unit` inner loop and the `01-RESEARCH.md` reconciliation, the Sampling Rate bullets, and the Automated Command column of the Per-Task Verification Map

**Explicitly unchanged**
- `scripts/assert-phase1-red.mjs` and the `test:phase1:red` script (CR-02, Phase 5 / QUAL-05)
- Every assertion, helper, and constant already present in `src/test/build-output.test.ts`
- All dependencies and devDependencies
- `01-VALIDATION.md` frontmatter, Wave 0 Requirements checklist, Manual-Only Verifications table, and Validation Sign-Off checklist

**No new files, no new dependencies, no new components, no new routes.**
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; blocking threshold `high`.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Committed source → gitignored `dist/` artifact | Nothing in version control proves the artifact matches the source it claims to represent; the build is the only link and it was previously implicit. |
| Test suite result → release decision | A green suite is treated as evidence for QUAL-04 and the deployed-route criteria; if green can be reached without a build, that evidence is counterfeit. |
| Local developer state → CI state | A developer's stale `dist/` and CI's fresh one produce different meanings for the same command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-21 | Repudiation | `src/test/build-output.test.ts` green result | high | mitigate | The freshness guard makes an absent or stale `dist/` a loud failure naming the offending input, so a green run can no longer be produced without building. |
| T-01-22 | Tampering | metadata, asset, and PDF-checksum assertions | high | mitigate | Those assertions previously ran against whatever `dist/` happened to be on disk; with the build executed inside `npm test`, they now run against the artifact the current source produces. |
| T-01-23 | Denial of service | developer inner loop | low | accept | Building on every `npm test` costs roughly two seconds per run; `test:unit` is provided for the fast loop, and the freshness guard still protects it. Accepted rather than mitigated further. |
| T-01-24 | Tampering | the freshness guard itself | medium | mitigate | `PROHIB-QUAL-02` forbids a tolerance window, a skip-on-missing branch, and an environment-variable escape hatch; the acceptance criteria grep for two of the three. |
| T-01-SC | Tampering | package-manager installs | — | not applicable | This plan changes the `scripts` block only. No dependency is added, removed, or upgraded, so the RESEARCH.md Package Legitimacy Audit gate does not fire. |

**Schema gate:** not applicable — no ORM or schema directory exists in this project.
</threat_model>

<verification>
Run `rm -rf dist && npm test` and confirm it exits 0 having produced the artifact; run `rm -rf dist && npm run test:unit` and confirm it fails on the freshness guard rather than on a downstream assertion; run `npm run typecheck`, `npm run lint`, and `npm run build`. Confirm `git diff` for this plan touches only `src/test/build-output.test.ts`, the `scripts` block of `package.json`, and the command-semantics rows of `01-VALIDATION.md`. Re-read `01-VALIDATION.md` and confirm no line still describes `npm test` as the runner alone and that the `test:unit` inner loop is named. Carry the clean-checkout backstop truth into end-of-phase UAT and leave it attributed to Phase 5 / QUAL-05.
</verification>

<success_criteria>
`npm test` builds what it asserts against; a stale or absent `dist/` fails loudly with an actionable message; the fast inner loop survives as `test:unit` and is recorded as such in `01-VALIDATION.md`, which no longer describes `npm test` under its superseded semantics; and CR-02 plus the clean-checkout release gate remain untouched and attributed to Phase 5.
</success_criteria>

<output>
Create `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-08-SUMMARY.md` when done.
</output>
