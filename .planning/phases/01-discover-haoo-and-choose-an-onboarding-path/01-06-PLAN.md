---
phase: 01-discover-haoo-and-choose-an-onboarding-path
plan: 06
type: execute
wave: 5
gap_closure: true
depends_on:
  - 01-03
  - 01-04
  - 01-05
files_modified:
  - src/components/OnboardingChoices.tsx
  - src/components/ProductsSection.tsx
  - src/test/focus-contrast.test.ts
  - src/test/products-section.test.tsx
autonomous: true
requirements:
  - PROD-01
  - PROD-03
  - ONBD-04
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    # --- Gap-derived (01-VERIFICATION.md gaps[0] and human_verification[0]) ---
    - "Every focus indicator declared in a Phase 1 product source contrasts at 3:1 or better against the surface it sits on, including the dark-panel `Start with HAOO` self-onboarding action, which uses a white ring on the #18275F panel at 14.05:1 (D-09, D-11, D-13, ONBD-04, PROD-03)."
    - "No focus utility string pairs a ring colour with a ring-offset colour equal to that ring colour or to the panel background it renders on; `src/test/focus-contrast.test.ts` fails the suite when it does (PROD-03)."
    - "Activating the home `Products` navigation entry at md+ lands the `Products` heading below the fixed home header, because `section#products` reserves 96px of scroll margin below md and 128px at md and above (D-17, PROD-01)."
    - "The focus fix changes only ring and offset colour tokens: every existing accessible name, href, placement, and rendered string in `src/test/haoo-page.test.tsx` and `src/test/products-section.test.tsx` continues to pass unchanged (D-09, D-11)."
    # --- Probe-derived, resolved explicit (QUAL-06 boundary + precision, lifted per specless-probe §C) ---
    - "Boundary: the focus-contrast gate fails a computed ratio of 2.99:1, fails 2.999:1, and passes exactly 3.0:1 and 3.01:1; the comparison is `ratio >= 3` with no epsilon and no rounding before the comparison."
    - "Precision: WCAG relative luminance and contrast are computed in IEEE-754 doubles straight from the sRGB formula with no intermediate rounding, and any ratio printed in a failure message is rounded for display only, never fed back into the assertion."
    # --- Backstop (human_verification carry-forward; abstains to human_needed when unconfirmed) ---
    - statement: "Tabbing to `Start with HAOO` in the opening (navy hero) and closing (navy) onboarding panels shows a clearly visible focus indicator in a real browser."
      verification: backstop
    - statement: "Clicking `Products` in the home primary navigation at >=768px leaves the `Products` heading fully visible below the fixed header in a real browser, in both the scrolled and unscrolled header states."
      verification: backstop
  artifacts:
    - path: "src/test/focus-contrast.test.ts"
      provides: "Executable WCAG focus-indicator contrast contract over every Phase 1 product source"
    - path: "src/components/OnboardingChoices.tsx"
      provides: "Dark-surface focus ring that clears WCAG 2.2 SC 1.4.11 on the navy onboarding panels"
    - path: "src/components/ProductsSection.tsx"
      provides: "Products anchor target that reserves fixed-header space"
  key_links:
    - from: "src/test/focus-contrast.test.ts"
      to: "src/components/OnboardingChoices.tsx"
      via: "source scan of focus utility strings with a computed WCAG ratio"
      pattern: "focus-visible:ring-offset-"
    - from: "src/components/ProductsSection.tsx"
      to: "src/App.tsx"
      via: "the fixed home header the #products anchor target must clear"
      pattern: "scroll-mt-24"
  prohibitions:
    - id: "PROHIB-A11Y-01"
      requirement_id: "PROD-03"
      category: "accessibility"
      status: "unresolved"
      verification: null
      statement: "MUST NOT satisfy the focus-contrast contract by weakening it, narrowing its source list, removing a ring utility, or suppressing a visible focus indicator; the only permitted route to green is a compliant colour pairing"
      reason: "flagged-unverified — descriptor-less by design; an autonomous run has no human to wire a check that distinguishes a real fix from a suppressed indicator"
    - id: "PROHIB-ONBD-01"
      requirement_id: "ONBD-04"
      category: "transparency"
      status: "unresolved"
      verification: null
      statement: "MUST NOT obscure that the self-onboarding action leaves ZERO-PAPER HUB while restyling its focus treatment"
      reason: "flagged-unverified — recalled from 01-02-PLAN; this plan edits the component that carries the disclosure copy"
---

<objective>
**As a** landlord or property manager navigating by keyboard, **I want to** see where my focus is when I reach `Start with HAOO` on the navy onboarding panels and land on the `Products` heading when I follow the home navigation, **so that** I can choose an onboarding path without losing my place.

Purpose: Close the one failed must-have from `01-VERIFICATION.md` — a 2.22:1 focus indicator on half of the phase's headline assisted-vs-self choice — and close the statically-reproduced CR-03 anchor overlap on the discovery entry point, with executable gates rather than manual observation.
Output: A corrected dark-surface focus ring, a reusable WCAG focus-contrast contract test, a scroll-margin fix on the Products anchor target, and a component assertion for it.

**Gap-closure plan.** Plans 01-01 through 01-05 are executed and must not be rewritten. This plan only adds to and corrects what they shipped.
</objective>

<execution_context>
@/home/paul/.claude/gsd-core/workflows/execute-plan.md
@/home/paul/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-CONTEXT.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-UI-SPEC.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VERIFICATION.md
@src/components/OnboardingChoices.tsx
@src/components/ProductsSection.tsx
</context>

<gap_source>
From `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VERIFICATION.md`:

- `gaps[0]` (status: failed) — "Color follows the locked HAOO palette and all combinations meet WCAG AA". `src/components/OnboardingChoices.tsx:13` sets `focus-visible:ring-[#4054C6]` together with `focus-visible:ring-offset-[#18275F]` — the panel's own background — measuring 2.22:1, below the 3:1 that WCAG 2.2 SC 1.4.11 requires. Applied at line 49 whenever `onDark` is true, i.e. the opening and closing placements of `Start with HAOO`.
- `human_verification[0]` (CR-03) — `#products` carries only `md:py-16` (64px) of top padding against a fixed home header measuring up to 120px at md+, with no `scroll-mt` on the section and no `scroll-padding-top` on `html`.

Contract being restored: `01-UI-SPEC.md` line 174 ("focus uses a 2px accent ring with a 2px light offset") and line 255 ("Focus indicators remain visible on light, navy, and image-adjacent surfaces"). Line 255 is the governing clause on a dark surface: the *light* element of the pair must be the ring, not the offset, so the accent-ring/light-offset pattern inverts to light-ring/panel-offset on navy. This is an adaptation of the spec to the surface it names, not a deviation from it.
</gap_source>

<probe_assumptions>
The spec-less edge probe over the 13 phase requirement IDs reported `applicable: 15, resolved: 0, unresolved: 15`. Accounting for the whole gap-closure plan set is recorded here once, so no item is silently dropped. **15 surfaced = 5 authored into `must_haves.truths` + 10 surfaced as flagged assumptions.**

**Authored into `must_haves.truths` (5, classified rows with a defensible acceptance criterion):**

| Requirement | Probe | Owning plan | Disposition |
|---|---|---|---|
| QUAL-06 | boundary | 01-06 | Resolved explicit. As a *brochure-copy* probe this is a classifier false positive already recorded in 01-03 (fidelity is string equality, not arithmetic — there is no min/max or threshold in a phone number or a claim). This gap set introduces a genuine numeric threshold, the 3:1 WCAG ratio, so the boundary is authored there instead of being dismissed. |
| QUAL-06 | precision | 01-06 | Resolved explicit. Same false-positive reading for brochure copy — no rounding or tie-breaking exists in exact string comparison. Real precision now exists in the floating-point luminance computation, so it is authored against that. |
| PROD-04 | concurrency | 01-08 | Resolved explicit. As a *brochure-link* probe this is plainly inapplicable to a static marketing site: `Open brochure` and `Download brochure` are native anchors with no shared mutable state, and independence under repeated, interrupted and concurrent activation is already asserted by a shipped test. The applicable reading in this gap set is build/test ordering, authored in 01-08. |
| ONBD-02 | empty | 01-09 | Resolved explicit against the WhatsApp starter text, which is a non-empty compile-time constant with no runtime input path. |
| ONBD-02 | encoding | 01-09 | Resolved explicit against the URL-encoded starter text round-trip. |

**Flagged assumptions (10, `unclassified` rows — these stay `unresolved`; per the fallback protocol an unclassified row is never auto-resolved with a backstop marker):**

PROD-01, PROD-02, PROD-03, PROD-05, PROD-06, ONBD-01, ONBD-03, ONBD-04, ONBD-05, QUAL-04 each returned `unclassified — review manually`. The assumption this plan set carries: **each of these ten requirements is exercised by an existing passing contract test at HEAD (44/44 green as re-run by the verifier), so no additional edge behavior is invented for them here.** They remain flagged for the phase verifier and are not treated as closed by this plan set. The gap set touches four of them (PROD-01, PROD-03, PROD-06, ONBD-04) only to repair defects the verifier found — it does not extend their behavior.
</probe_assumptions>

<deferred_human_verification>
**Explicit written deferral — three visual human-verification items from `01-VERIFICATION.md` that no gap plan (01-06 through 01-09) closes.**

`01-VERIFICATION.md` records seven `human_verification` entries. This gap-closure plan set disposes of four of them: `[0]` (CR-03 anchor overlap) and `[1]` (focus indicator on navy) are closed by this plan's two tasks and carried forward as this plan's two backstop truths; `[5]` (PROHIB-ONBD-02 JavaScript readiness) is resolved by the blocking decision checkpoint in `01-09`; `[6]` (the judgment-tier prohibition review batch) is carried as `must_haves.prohibitions` entries across `01-06`, `01-07`, and `01-09`.

The remaining three are **layout-engine items no jsdom test and no plan in this gap set can settle**. They are enumerated here so `/gsd-execute-phase` and the phase re-verifier see named, owned, phase-attributed items rather than an unenumerated aside. None is dropped, and none is claimed as closed by Phase 1.

| ID | `01-VERIFICATION.md` item | What must be observed | Deferred to | Why not closable here |
|---|---|---|---|---|
| `DEFER-HV-PDF-EMBED` | `human_verification[2]` | Opening `/products/haoo/` at >=1024px embeds the brochure in the inline `object[type="application/pdf"]`, or shows the exact branded `Brochure preview unavailable` panel; below 1024px the compact preview image shows with an obvious `Open brochure` action and no tall inline viewer; `Open` and `Download` stay visible in every state. | **Phase 5**, Success Criterion 2 (QUAL-03 — zoom support and an HTML equivalent for brochure information) with the responsive split under Success Criterion 1 (QUAL-01). | The `lg:hidden` / `hidden lg:block` split is a CSS media-query behavior and real PDF embedding is a browser plugin behavior; jsdom renders both branches and never embeds a PDF. No plan in this gap set touches `BrochurePanel.tsx`. |
| `DEFER-HV-REFLOW` | `human_verification[3]` | The home Products section and the HAOO page at 320px width and at 200% browser zoom: navigation, the featured card, imagery, copy, footer, and every action wrap and reflow with no horizontal page scrolling and no clipped or hidden action. | **Phase 5**, Success Criterion 1 (QUAL-01 — supported widths without horizontal overflow or hidden primary actions) and Success Criterion 2 (QUAL-03 — zoom support). | jsdom has no layout engine, so the existing `scrollWidth <= clientWidth` assertion is not real evidence. This plan's `scroll-mt` change adds scroll margin only and does not alter any wrapping or overflow behavior. |
| `DEFER-HV-CARD-SPLIT` | `human_verification[4]` | The home featured HAOO card at lg+ shows the 5/7 copy/image split in visual balance; on mobile it stacks copy before the image. | **Phase 5**, Success Criterion 1 (QUAL-01). | Grid balance and stacking order are visual properties with no jsdom-observable proxy. This plan edits only the `<section>` className of `ProductsSection.tsx` and is forbidden by its own acceptance criteria from touching the featured card. |

**Ownership statement.** Phase 1 does not claim any of these three. They stay open in `01-VERIFICATION.md` with Phase 5 (QUAL-01 / QUAL-03) named as owner, and the phase re-verifier must continue to report them as human-needed rather than absorbing them into a pass.
</deferred_human_verification>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a keyboard user can see focus on the navy onboarding panels" — one path, gated</name>
  <precondition>`node_modules/vitest/package.json` exists and reports version 3.2.4 — the test harness was installed by executed plan 01-01, and a `--gaps-only` run does not re-run it.</precondition>
  <read_first>
    - `src/components/OnboardingChoices.tsx` — the file being modified; `focusLight` (line 11), `focusDark` (line 12), and the `onDark` application at line 48
    - `src/components/ProductHeader.tsx` — `focusClasses` (line 15), a light-surface focus string the new contract must also pass
    - `src/components/BrochurePanel.tsx` — `focusClasses` (line 22), same
    - `src/components/ProductsSection.tsx` — `focusClasses` (line 5), the one focus string using a named Tailwind colour rather than an arbitrary hex
    - `src/pages/ProductPage.tsx` — the skip link at line 36 and `footerLinkClasses` at line 31, which use the `focus:` prefix rather than `focus-visible:`
    - `src/test/build-output.test.ts` — the `PRODUCT_SOURCES` array (lines 29-38) is the source list the new test mirrors
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-UI-SPEC.md` — lines 105, 110, 174, 255: accent reservation and the visible-on-navy clause
  </read_first>
  <files>src/test/focus-contrast.test.ts, src/components/OnboardingChoices.tsx</files>
  <behavior>
    - RED first: `src/test/focus-contrast.test.ts` fails at HEAD, reporting `src/components/OnboardingChoices.tsx` with a computed ratio of about 2.21 for the `#4054C6` on `#18275F` pairing.
    - The same test passes for `focusLight` in `OnboardingChoices.tsx`, `focusClasses` in `ProductHeader.tsx`, `focusClasses` in `BrochurePanel.tsx`, `focusClasses` in `ProductsSection.tsx`, and both `focus:`-prefixed strings in `ProductPage.tsx`, because each resolves against the Tailwind default offset colour `#ffffff`.
    - GREEN: after the colour change the whole file scan passes, and the `Start with HAOO` pairing reports about 14.05.
    - A pairing whose ring colour equals its offset colour fails regardless of the computed ratio.
    - A ring or offset colour token the resolver does not recognize fails the test with the unrecognized token in the message; it is never skipped.
  </behavior>
  <action>Create `src/test/focus-contrast.test.ts` and write it to fail against the current source before changing any component. Give it: `MIN_FOCUS_CONTRAST = 3`; `DEFAULT_RING_OFFSET = '#ffffff'` (the Tailwind `--tw-ring-offset-color` default that applies when a string declares `ring-offset-2` with no colour); a `RING_COLOR_TOKENS` record mapping the named Tailwind colours actually in use, `white` to `#ffffff` and `blue-700` to `#1d4ed8`; and a `FOCUS_SOURCES` array listing `src/pages/ProductPage.tsx`, `src/components/ProductHeader.tsx`, `src/components/OnboardingChoices.tsx`, `src/components/BrochurePanel.tsx`, and `src/components/ProductsSection.tsx`. Export or define `srgbLuminance(hex)` implementing the WCAG 2.x relative-luminance formula in doubles with no intermediate rounding, `contrastRatio(foreground, background)` returning `(lighter + 0.05) / (darker + 0.05)`, `resolveRingColor(token)` returning a lowercase six-digit hex for either an arbitrary bracket value or a `RING_COLOR_TOKENS` key and throwing on anything else, and `extractFocusPairs(source, file)` which finds every string literal or template literal containing a ring-colour utility with either the `focus:` or the `focus-visible:` prefix, pairs it with the offset colour declared in that same literal, and falls back to `DEFAULT_RING_OFFSET` when the literal declares an uncoloured offset. Assert for every extracted pair that the resolved ring colour differs from the resolved offset colour and that `contrastRatio` of the two is greater than or equal to `MIN_FOCUS_CONTRAST`, comparing the unrounded double and rounding only for the failure message. Assert that at least one pair was extracted, so an extraction regression cannot pass the file vacuously. Add a pure-function boundary case asserting the gate rejects a synthetic pair computing to 2.99 and accepts one computing to exactly 3.0. Then make it green by changing only the ring colour token in the `focusDark` string in `src/components/OnboardingChoices.tsx` from the accent value to `focus-visible:ring-white`, keeping `focus-visible:outline-none`, `focus-visible:ring-2`, `focus-visible:ring-offset-2`, and `focus-visible:ring-offset-[#18275F]` exactly as they are. Change nothing else in the component: no markup, no accessible name, no href, no `onDark` branch condition, no `focusLight` string. The self-onboarding action must remain structurally equal to the assisted action with outlined treatment and the WhatsApp action must remain the sole filled accent, per D-09 and D-10, and the visible disclosure that the destination leaves ZERO-PAPER HUB must survive untouched.</action>
  <verify>
    <automated>npx vitest run src/test/focus-contrast.test.ts &amp;&amp; npx vitest run src/test/haoo-page.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
    <human-check>Tab to `Start with HAOO` in the opening navy hero panel and again in the closing navy panel; confirm a clearly visible focus indicator against the navy, distinguishable from the unfocused white button border.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/components/OnboardingChoices.tsx` contains the substring `focus-visible:ring-white` and no longer contains `focus-visible:ring-[#4054C6] focus-visible:ring-offset-2 focus-visible:ring-offset-[#18275F]`.
    - `src/components/OnboardingChoices.tsx` still contains `focus-visible:ring-offset-[#18275F]`.
    - `npx vitest run src/test/focus-contrast.test.ts` exits 0.
    - `npx vitest run src/test/haoo-page.test.tsx` exits 0 with the same test count as at HEAD and with no assertion in that file edited.
    - The commit history for this task shows a failing-test commit before the implementation commit, and the failing run named `src/components/OnboardingChoices.tsx` with a ratio below 3.
    - `npm run typecheck` and `npm run lint` both exit 0.
  </acceptance_criteria>
  <done>A keyboard user reaches `Start with HAOO` on both navy placements and sees a 14.05:1 focus indicator, and the suite fails if any future edit reintroduces a sub-3:1 focus pairing anywhere in the product surface.</done>
</task>

<task type="auto">
  <name>Task 2: Reserve fixed-header space above the Products anchor target</name>
  <read_first>
    - `src/components/ProductsSection.tsx` — the file being modified; the `<section>` element at lines 84-88 carrying `id={PRODUCTS_SECTION_ID}` and `className="bg-white py-12 md:py-16"`
    - `src/App.tsx` — line 212, the fixed home header: `py-3 md:py-5` unscrolled and `py-2.5 md:py-3` scrolled, wrapping a logo at `h-14 sm:h-16 md:h-20`
    - `src/index.css` — `html { scroll-behavior: smooth; }`, which the scroll-margin fix must keep working
    - `src/test/products-section.test.tsx` — existing navigation and landmark assertions this task extends without editing
    - `src/pages/ProductPage.tsx` — lines 86, 106, 162, 170 use `scroll-mt-4`; this is the established convention being applied to the home anchor
  </read_first>
  <files>src/components/ProductsSection.tsx, src/test/products-section.test.tsx</files>
  <action>Add `scroll-mt-24 md:scroll-mt-32` to the className of the `<section>` element that carries `id={PRODUCTS_SECTION_ID}` in `src/components/ProductsSection.tsx`, keeping `bg-white py-12 md:py-16` unchanged. These two values are chosen against the measured header: below md the fixed header is at most 24px of padding plus a 56px logo, 80px total, cleared by 96px; at sm it is 24px plus a 64px logo, 88px, still cleared by 96px; at md and above it is at most 40px of padding plus an 80px logo, 120px total in the unscrolled state and 104px scrolled, both cleared by 128px. Use `scroll-margin-top` on the target rather than `scroll-padding-top` on `html`, because `html` is shared with the product document whose own header is not fixed and whose sections already declare `scroll-mt-4`; a global rule would push those anchors by an offset they do not need. Then add one test to `src/test/products-section.test.tsx` inside the existing `Phase 1 Products discovery navigation contracts` describe block, named so it states the contract rather than the class, which renders the home page, reads the `section` element whose id is the products section id, and asserts its className contains both `scroll-mt-24` and `md:scroll-mt-32`. Do not edit any existing assertion in that file. Keep the featured HAOO card, its single native `Explore HAOO` action, and the section/nav pairing exactly as shipped, per D-17.</action>
  <verify>
    <automated>npx vitest run src/test/products-section.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
    <human-check>At >=768px load the home page, scroll down, then activate the `Products` navigation entry; confirm the `Products` heading is visible below the fixed header and not covered by it, in both the scrolled and unscrolled header states.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/components/ProductsSection.tsx` contains the substring `scroll-mt-24 md:scroll-mt-32` on the element that also carries `id={PRODUCTS_SECTION_ID}`.
    - `npx vitest run src/test/products-section.test.tsx` exits 0 and reports exactly one more test than at HEAD.
    - The new test fails if `scroll-mt-24` is removed from the section className.
    - `grep -n "scroll-padding" src/index.css` returns no match — the fix stays scoped to the anchor target.
    - `npm run build` exits 0 and `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Following the home `Products` navigation entry lands the visitor on the `Products` heading rather than behind the fixed header, and the reserved offset is asserted rather than left to observation.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New and changed symbols introduced by this plan:

**New files**
- `src/test/focus-contrast.test.ts`

**New symbols in `src/test/focus-contrast.test.ts`**
- `MIN_FOCUS_CONTRAST` — const, numeric `3`
- `DEFAULT_RING_OFFSET` — const, `#ffffff`
- `RING_COLOR_TOKENS` — const record mapping `white` and `blue-700` to hex
- `FOCUS_SOURCES` — const array of the five product source paths scanned
- `srgbLuminance(hex: string): number` — WCAG relative luminance
- `contrastRatio(foreground: string, background: string): number`
- `resolveRingColor(token: string): string` — throws on an unrecognized token
- `extractFocusPairs(source: string, file: string)` — returns `{ file, ringColor, offsetColor, raw }` entries
- `describe('Phase 1 focus indicator contrast contracts')` with per-source cases and one pure-function boundary case

**Changed symbols**
- `focusDark` in `src/components/OnboardingChoices.tsx` — ring colour token changes to `focus-visible:ring-white`; offset colour `#18275F` retained
- `<section id={PRODUCTS_SECTION_ID}>` className in `src/components/ProductsSection.tsx` — gains `scroll-mt-24 md:scroll-mt-32`
- `src/test/products-section.test.tsx` — one added test asserting the reserved scroll margin on the Products anchor target

**No new npm scripts, no new dependencies, no new public assets, no new components.**
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1; blocking threshold `high`.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Keyboard / assistive-technology user → rendered focus indicator | The only signal telling a non-pointer user which control is active crosses here; if it is imperceptible the control is effectively unreachable. |
| Tailwind class strings in source → compiled CSS custom properties | Focus colour is expressed as untyped utility text; nothing in the type system prevents a pairing that computes to an invisible indicator. |
| Fixed home header → anchor scroll position | The header overlays the document; the anchor target must reserve space the header will occupy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-12 | Denial of service | `OnboardingChoices.tsx` dark-surface focus ring | high | mitigate | Replace the ring colour so the indicator measures 14.05:1 on `#18275F`, and gate every product-source focus pairing at 3:1 in `src/test/focus-contrast.test.ts` so the regression cannot return silently. |
| T-01-13 | Tampering | focus utility class strings across the five product sources | medium | mitigate | The contract scans source text rather than trusting review, fails on an unrecognized colour token instead of skipping it, and asserts a non-zero extraction count so a broken extractor cannot pass vacuously. |
| T-01-14 | Denial of service | `#products` anchor target under the fixed home header | medium | mitigate | Reserve 96px/128px of scroll margin on the target element and assert it in `src/test/products-section.test.tsx`. |
| T-01-15 | Repudiation | the focus-contrast gate itself | medium | mitigate | `PROHIB-A11Y-01` records that weakening the gate, shrinking its source list, or removing an indicator are not permitted routes to green; the gate is descriptor-less and stays flagged for human review. |
| T-01-SC | Tampering | package-manager installs | — | not applicable | This plan installs nothing. No `npm`, `pip`, or `cargo` install task exists, so the RESEARCH.md Package Legitimacy Audit gate does not fire and no legitimacy checkpoint is required. |

**Schema gate:** not applicable. This project has no `prisma/`, `drizzle/`, `supabase/`, `src/collections/`, or TypeORM entity directory, so no `[BLOCKING]` schema-push task is required.
</threat_model>

<verification>
Run `npx vitest run src/test/focus-contrast.test.ts`, then the full `npx vitest run`, then `npm run typecheck`, `npm run lint`, and `npm run build`. The suite must report the HEAD count plus the two tests this plan adds, with zero failures and zero edits to existing assertions. Carry the two backstop truths into end-of-phase UAT. Carry the three named items in this plan's `<deferred_human_verification>` section — `DEFER-HV-PDF-EMBED`, `DEFER-HV-REFLOW`, `DEFER-HV-CARD-SPLIT` — forward as open, Phase 5-owned human-verification items; they are enumerated and attributed there, not closed here, and the re-verifier must keep reporting them as human-needed.
</verification>

<success_criteria>
The failed WCAG must-have from `01-VERIFICATION.md` is closed with a measured 14.05:1 indicator and a reusable contract that fails the suite on any future sub-3:1 focus pairing; the CR-03 anchor overlap is closed deterministically on the discovery entry point; and every previously passing assertion still passes untouched.
</success_criteria>

<output>
Create `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-06-SUMMARY.md` when done.
</output>
