---
phase: 01-discover-haoo-and-choose-an-onboarding-path
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - scripts/assert-phase1-red.mjs
  - src/test/setup.ts
  - src/test/products-section.test.tsx
  - src/test/haoo-page.test.tsx
  - src/test/haoo-content.test.ts
  - src/test/build-output.test.ts
  - src/products/types.ts
  - src/products/haoo.ts
  - src/components/ProductsSection.tsx
  - src/pages/ProductPage.tsx
autonomous: false
requirements:
  - QUAL-04
  - QUAL-06
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Wave 0 supplies runnable component, content-contract, and built-artifact tests before implementation-dependent work begins."
    - "The test stack is exact-pinned to the Vite-5-compatible versions approved in research and does not change the application runtime stack."
    - "The physical route, static metadata, PDF checksum, centralized facts, responsive semantic content, Products collection states, and unconditional native links each have a named automated contract."
    - "QUAL-04 direct-route evidence and QUAL-06 exact-string/source-fidelity evidence cannot be silently dropped during implementation."
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest jsdom configuration"
    - path: "src/test/setup.ts"
      provides: "Explicit Testing Library cleanup"
    - path: "src/test/products-section.test.tsx"
      provides: "PROD-01/PROD-06 Products collection contracts"
    - path: "src/test/haoo-page.test.tsx"
      provides: "Semantic page, brochure, onboarding, and UI-contract tests"
    - path: "src/test/haoo-content.test.ts"
      provides: "Exact fact, URL-encoding, brand, and claim contracts"
    - path: "src/test/build-output.test.ts"
      provides: "Nested route, metadata, asset, and checksum contracts"
    - path: "scripts/assert-phase1-red.mjs"
      provides: "Expected-red discriminator that rejects collection, import, syntax, and configuration failures"
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "npm test invokes Vitest with the project configuration"
      pattern: "vitest run"
    - from: "src/test/build-output.test.ts"
      to: "dist/products/haoo/index.html"
      via: "post-build filesystem assertions"
      pattern: "dist/products/haoo/index.html"
---

<objective>
**As a** HAOO prospect, **I want to** rely on a product journey whose route, facts, actions, and fallbacks are continuously checked, **so that** I can choose an onboarding path without encountering silent regressions.

Purpose: Establish the Wave 0 validation contract required by `01-VALIDATION.md` before any implementation-dependent tests execute. This tooling exception precedes the implementation tracer because the package-legitimacy checkpoint must occur before installation.
Output: Exact-pinned Vitest/Testing Library setup and fail-first contract suites for the complete Phase 1 surface.

Scope rationale: This Wave 0 plan intentionally crosses 13 small files because the four contract suites, their compile-safe stubs, package wiring, and expected-red discriminator form one indivisible validation gate. Splitting them would leave either uncollectable tests or an undiscriminated red command between plans, recreating the false-positive condition this wave exists to prevent. The expanded 24,000-token estimate accounts for the wiring and evidence capture; production behavior remains in Plans 02–05.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-RESEARCH.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md
@.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-UI-SPEC.md
@package.json
@vite.config.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the two package-legitimacy exceptions</name>
  <what-built>A researched exact-pin proposal for the Phase 1 test harness; no packages have been installed yet.</what-built>
  <how-to-verify>Open the official npm registry pages for `vitest@3.2.4` and `@testing-library/react@16.3.2`, follow their repository links, and compare package identity/version/scripts to the automated `npm view` output. Also confirm the two already-OK exact pins.</how-to-verify>
  <resume-signal>Reply `approved` to permit installation of exactly the four listed development packages, or identify the rejected package and reason.</resume-signal>
  <read_first>
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-RESEARCH.md` — Package Legitimacy Audit and exact selected releases
    - `package.json` — existing Vite 5 and React 18 boundary
  </read_first>
  <action>Before any install, verify the official npm registry pages and source repositories for `vitest` and `@testing-library/react`, the two names marked SUS by the automated name-level gate. Confirm that `vitest@3.2.4` and `@testing-library/react@16.3.2` are the intended stable, Vite-5-compatible packages. Also confirm the already-OK exact pins `jsdom@26.1.0` and `@testing-library/dom@10.4.1`. Reject any package substitution, Vite upgrade, postinstall addition, shadcn initialization, `components.json`, component library, third-party UI registry, or extra icon system.</action>
  <verify>
    <automated>npm view vitest@3.2.4 name version repository scripts --json &amp;&amp; npm view @testing-library/react@16.3.2 name version repository scripts --json &amp;&amp; npm view jsdom@26.1.0 name version repository scripts --json &amp;&amp; npm view @testing-library/dom@10.4.1 name version repository scripts --json</automated>
    <human-check>Approve only after the npm pages identify the official Vitest and Testing Library projects and the exact releases match the research audit.</human-check>
  </verify>
  <acceptance_criteria>The human explicitly approves both SUS-name exceptions and all four exact releases before Task 2 begins.</acceptance_criteria>
  <done>Package legitimacy is confirmed without changing the locked runtime stack.</done>
</task>

<task type="auto">
  <name>Task 2: Install and configure the exact Wave 0 test stack</name>
  <read_first>
    - `package.json` — preserve existing scripts and dependencies
    - `vite.config.ts` — preserve Vite 5/React configuration
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md` — required commands and setup path
  </read_first>
  <files>package.json, package-lock.json, vitest.config.ts, src/test/setup.ts</files>
  <action>Install only `vitest@3.2.4`, `jsdom@26.1.0`, `@testing-library/react@16.3.2`, and `@testing-library/dom@10.4.1` as exact development dependencies. Add `test` as `vitest run`. Configure jsdom with `setupFiles: ['./src/test/setup.ts']`; in setup, import Vitest `afterEach` and Testing Library `cleanup` explicitly, without enabling globals. Preserve React 18, Vite 5, TypeScript strictness, Tailwind, Lucide, and every existing script/dependency. Let npm update the lockfile and inspect the diff for unrelated dependency movement.</action>
  <verify>
    <automated>npm test -- --run --passWithNoTests &amp;&amp; npm run typecheck &amp;&amp; npm ls vitest jsdom @testing-library/react @testing-library/dom</automated>
  </verify>
  <acceptance_criteria>The test command exits successfully, the four packages resolve to the exact approved versions, setup performs explicit cleanup, and no runtime framework or UI dependency changes.</acceptance_criteria>
  <done>A Vite-5-compatible test harness is runnable through the project-standard npm command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the Phase 1 contract suites before production implementation</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — all 13 Phase 1 behavior contracts
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-VALIDATION.md` — exact file-to-requirement map
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-UI-SPEC.md` — semantic roles, copy, responsive states, and accessibility contracts
    - `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-RESEARCH.md` — canonical ledger and PDF checksum
  </read_first>
  <files>scripts/assert-phase1-red.mjs, src/test/products-section.test.tsx, src/test/haoo-page.test.tsx, src/test/haoo-content.test.ts, src/test/build-output.test.ts, src/products/types.ts, src/products/haoo.ts, src/components/ProductsSection.tsx, src/pages/ProductPage.tsx, package.json</files>
  <behavior>
    - Products tests cover zero products (section/nav omitted), one product (featured layout), and many products (collection rendering), with `Explore HAOO` linking to `/products/haoo/`.
    - Page tests cover one h1, sequential semantic sections, six capability groups, four ordered journey steps, three onboarding placements, all exact native hrefs, mobile/desktop brochure treatments, fallback recovery copy, and independent Open/Download controls.
    - Content tests cover the exact brochure ledger, uppercase `ZERO-PAPER HUB`, required non-empty WhatsApp starter datum, digits-only number, deterministic URL encoding, and decoded equality including punctuation.
    - Build tests cover the physical nested HTML, canonical/social metadata, built asset references, public PDF path, and SHA-256 `38d5ad8e7497c65c4fa2d374e7ed5e8d81ab79f3b25d1e0daa73321d45b9e7a6`.
  </behavior>
  <action>First create the smallest compile-safe production contract surfaces required for test collection: shared product types, a deliberately incomplete typed `HAOO_PRODUCT`, and minimal `ProductsSection`/`ProductPage` components that render explicit placeholder landmarks but no required Phase 1 behavior. These are TDD stubs only; do not implement acceptance behavior in Wave 0. Then write named, semantic contract tests matching `01-VALIDATION.md`. Query UI by role and accessible name, never Tailwind selectors. Add `scripts/assert-phase1-red.mjs` and an npm `test:phase1:red` script that runs the four suites, requires non-zero Vitest status, requires the expected named assertion failures from all four files, and exits non-zero if output contains collection/config/syntax/module-resolution signatures such as `Failed to resolve import`, `Cannot find module`, `SyntaxError`, `Unhandled Error`, or `No test files found`. Preserve the expected-red output as evidence. Include the complete UI contract: fixed section order, visible copy, interaction states, native navigation, 44px targets, one h1, skip link, mobile menu accessibility, no horizontal clipping contracts, and media fallback semantics. Do not weaken expectations into snapshots or pending cases.</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run test:phase1:red</automated>
  </verify>
  <acceptance_criteria>All four required suites and the compile-safe TDD stubs exist, every Phase 1 requirement has a named contract location, the discriminator confirms named behavior assertions fail in every suite, and collection/config/syntax/module-resolution failures cannot satisfy expected-red verification.</acceptance_criteria>
  <done>The Phase 1 tests fail first and define the complete implementation target without visual-selector coupling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry → development toolchain | Third-party packages enter the lockfile and execute during tests. |
| canonical brochure/planning facts → tests | Untrusted source markup is reduced to inert expected strings, never executed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | npm development dependencies | high | mitigate | Blocking legitimacy checkpoint, exact pins, registry/source verification, scripts inspection, and lockfile diff review before install. |
| T-01-01 | Tampering | contract tests | medium | mitigate | Derive exact expectations from the canonical ledger and locked UI/requirement artifacts; require fail-first evidence. |
| T-01-02 | Denial of service | test command | low | accept | Test-only tooling may fail locally; explicit npm commands expose the failure and do not affect the static runtime artifact. |
</threat_model>

<verification>
Run `npm run typecheck`, confirm exact dependency versions with `npm ls`, and preserve the expected-red output for the four implementation contract suites. No production task begins until this Wave 0 plan is complete.
</verification>

<success_criteria>
The approved exact-pinned harness runs, all required contract files exist, and their failures identify missing Phase 1 behavior rather than broken test infrastructure.
</success_criteria>

<output>
Create `.planning/phases/01-discover-haoo-and-choose-an-onboarding-path/01-01-SUMMARY.md` when done.
</output>
