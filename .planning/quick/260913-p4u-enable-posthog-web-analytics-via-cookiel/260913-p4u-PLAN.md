---
phase: quick-260913-p4u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/measurement/posthog-lockdown.ts
  - src/measurement/posthog.ts
  - src/measurement/index.ts
  - src/test/fixtures/posthog-capture-contract.ts
  - src/test/measurement.test.ts
  - src/products/haoo.ts
  - src/products/types.ts
  - src/test/measurement-page.test.tsx
  - e2e/recovery.e2e.ts
  - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md
  - scripts/verify-phase4-coverage.mjs
  - src/test/haoo-report.test.ts
autonomous: true
requirements: [MEAS-02, MEAS-03, MEAS-04, MEAS-06, MEAS-07]
user_setup:
  - service: posthog
    why: "Events sent with cookieless_mode 'always' are dropped at PostHog ingestion unless the project's server-side hash mode is on. Without this step the deployed page reads as a dead funnel (Web Analytics AND the haoo_* report counts go to zero)."
    dashboard_config:
      - task: "BEFORE the next deploy: enable \"Cookieless server hash mode\" (project 589225, US region). Leave \"Discard client IP data\" as currently configured."
        location: "PostHog -> Project settings -> Web analytics"
      - task: "BEFORE the next deploy: read the rewritten measurement disclosure copy in src/products/haoo.ts (signalBoundary, campaignDescription, processorNote, neverCollected) and approve or amend it."
        location: "Repository, src/products/haoo.ts, disclosure block"
      - task: "AFTER deploy: open https://www.haoo.online/ in a real, non-headless browser with ad/tracker blockers off, wait a few minutes, confirm PostHog Web Analytics shows the visit (1 pageview on path /, host www.haoo.online) and Activity shows a $pageview whose $current_url has no query string or fragment. A Playwright or headless run reads 0 by design (posthog-js drops navigator.webdriver traffic) and is not a failure."
        location: "PostHog -> Web analytics, and PostHog -> Activity -> Events"

estimate:
  tokens: 90000
  raw_tokens: 180000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "On a configured build, a page load emits exactly one $pageview and page exit emits one $pageleave, alongside the unchanged ten haoo_* events"
    - "Every delivered payload carries distinct_id '$posthog_cookieless', $cookieless_mode true and $process_person_profile false; a payload lacking any of these exact values is dropped, so no per-browser identifier can leave the page"
    - "Delivered payloads carry only allowlisted Web Analytics properties; $current_url has no query string or fragment; $referrer is an origin or '$direct'; no $ip, $device_id, click identifier, search keyword, title, $initial_* , $set/$set_once/$unset or feature-flag data is delivered"
    - "Accepted normalized utm_source / utm_medium / utm_campaign values reach PostHog on every delivered event; raw SDK-read campaign or query-string values never do"
    - "The provider writes no cookie, no sessionStorage key and no localStorage key beyond the Phase 3 context record"
    - "If any locked key (including cookieless_mode, capture_pageview, capture_pageleave, save_referrer) resolves differently, the adapter returns no sink and signals posthog:unconfirmed-lockdown-readback"
    - "The rendered disclosure truthfully describes page address, referring site, browser/device details, approximate IP-derived location, a daily server-side anonymous code, no cookies or browser storage by PostHog, campaign values, and no form answers"
    - "Owner report numbers are unchanged: the HogQL still filters on HAOO_REPORT_EVENTS only and src/reporting is untouched"
    - "npm test, lint, typecheck, verify:coverage, verify:disjoint and test:phase1:contracts all pass"
  artifacts:
    - path: "src/measurement/posthog-lockdown.ts"
      provides: "Cookieless lockdown config, property allowlist reducer (reduceCapture), exact readback (lockdownHolds)"
      contains: "cookieless_mode: 'always'"
    - path: "src/measurement/posthog.ts"
      provides: "Adapter passing normalized campaign into the lockdown and into the re-entry memo"
      contains: "sameCampaign"
    - path: "src/measurement/index.ts"
      provides: "Facade handing readCampaign output to the adapter"
      contains: "createPostHogEventSink(config, adapters.providerAdapters, campaign)"
    - path: "src/test/fixtures/posthog-capture-contract.ts"
      provides: "Cookieless-shaped vendor payload, corrected defaults, automatic-capture simulation"
      contains: "simulateAutomaticCapture"
    - path: "src/products/haoo.ts"
      provides: "Truthful visitor disclosure copy"
      contains: "changes every day"
    - path: ".planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md"
      provides: "Re-decided capability rows and the owner-performed cookieless setting in the Operational boundary"
      contains: "Cookieless server hash mode"
    - path: "scripts/verify-phase4-coverage.mjs"
      provides: "Audit pinning the new INTEGRATE decisions and the cookieless boundary sentence"
      contains: "Cookieless"
  key_links:
    - from: "src/measurement/index.ts"
      to: "src/measurement/posthog.ts"
      via: "readCampaign result passed as third argument"
      pattern: "createPostHogEventSink\\(config, adapters\\.providerAdapters, campaign\\)"
    - from: "src/measurement/posthog.ts"
      to: "src/measurement/posthog-lockdown.ts"
      via: "POSTHOG_LOCKDOWN called with campaign; lockdownHolds identity readback of before_send"
      pattern: "POSTHOG_LOCKDOWN\\("
    - from: "src/measurement/posthog-lockdown.ts"
      to: "posthog-js before_send"
      via: "before_send closure delegating to reduceCapture"
      pattern: "reduceCapture\\(result, allowedEvents"
    - from: "scripts/verify-phase4-coverage.mjs"
      to: "COVERAGE.md"
      via: "REQUIRED_TABLES decisions and boundaryChecks regex"
      pattern: "Cookieless\\\\s\\+server"
    - from: "src/products/haoo.ts"
      to: "src/components/MeasurementDisclosure.tsx"
      via: "disclosure.processorNote / signalBoundary / campaignDescription / neverCollected rendered from product data"
      pattern: "disclosure\\.processorNote"
---

<objective>
Make PostHog Web Analytics and Product Analytics work for https://www.haoo.online/ by moving the measurement lockdown to PostHog cookieless mode, enabling automatic $pageview/$pageleave, and replacing the bare-name property reducer with a strict Web Analytics property allowlist. Update the disclosure copy so it matches the new payload, and re-align tests, fixture, coverage matrix and verifier.

Purpose: Web Analytics is empty today because the lockdown turns off pageview capture and strips every property except three transport keys. The owner decided on 2026-09-13 to reverse that part of Phase 04.1 D-03, while keeping everything else locked: no browser storage, no person profiles, no autocapture and no replay.

Output: updated lockdown/adapter/facade, cookieless-shaped test fixture, rewritten unit and page tests, rewritten disclosure copy, re-decided COVERAGE.md rows with a pinned owner-setting sentence, all project gates green. No deploy, no push.
</objective>

<owner_decisions>
Locked by the owner on 2026-09-13 (task description). Do not revisit.
- OD-1: Web Analytics AND Product Analytics must work for https://www.haoo.online/. This deliberately reverses the "bare event name" half of Phase 04.1 D-03.
- OD-2: Visitor identity uses PostHog cookieless mode, `cookieless_mode: 'always'`. Nothing is persisted in the browser and there is no consent banner; PostHog hashes visitors server-side per day.
- OD-3: Process is a GSD quick task. No deploy, no push. `config/approved-analytics-hosts.ts` is not modified (ingestion host unchanged).
- OD-4: Everything else stays off: autocapture, rageclick, dead clicks, session recording, surveys, heatmaps, exceptions, performance, flags, toolbar, external dependency loading, site apps, identify/alias/group.
- OD-5: Owner report semantics stay unchanged unless trivially required.

Planner decisions (discretion areas, verified against node_modules/posthog-js/dist/module.js at 1.425.1). The executor implements these exactly:
- PD-1 persistence: keep `persistence: 'memory'` and `disable_persistence: true`, and read both back. The SDK already forces persistence disabled under `'always'`: `Fl(){if("always"===this.config.cookieless_mode)return!0`. It builds no session manager (`var m=this.El();m||(this.sessionManager=new il(this)`), registers `distinct_id` as the `$posthog_cookieless` sentinel with `$device_id:null`, and stamps `$cookieless_mode: true` on every event (`this.El()&&(u[Gr]=!0)`). The two keys stay as defence in depth. Because the session manager is never built, `$session_id` and `$window_id` are absent at this version; PostHog derives sessions server-side.
- PD-2 pageview: `capture_pageview: true`, not `'history_change'`. For a boolean `true` the SDK's `uv()` returns `{}`, so only the initial load is captured (`this.config.capture_pageview&&setTimeout(...this.El()&&this.ou(),1)`). The page is single-route and hash links must not add pageviews. The initial `$pageview` fires 1 ms after `init`, which the facade calls only after `readCampaign` has cleaned the address bar. `capture_pageleave: true` as a literal: `uu()` sends `$pageleave` on unload, which gives Web Analytics session duration and bounce rate.
- PD-3 referrer: `save_referrer: true` is REQUIRED. The base property builder does not emit `$referrer`/`$referring_domain`; they reach events only via `this.config.save_referrer&&this.sessionPersistence.update_referrer_info()`. Disabled persistence still keeps registered props in memory (`save(){if(!this.Ca)...`, `register_once` writes `this.props`). The reducer reduces `$referrer` to its URL origin, or keeps the literal `$direct`.
- PD-4 campaign: `save_campaign_params` stays `false`. It would read raw `utm_*` plus click identifiers straight from the address bar and bypass MEAS-06 normalization, and by capture time the facade has already removed `utm_*` anyway. Instead, the facade passes its normalized `readCampaign()` record through the adapter into `POSTHOG_LOCKDOWN`. The reducer re-validates each of `utm_source`, `utm_medium`, `utm_campaign` against its own lowercase `[a-z0-9-]`, 1..32 rule and writes it onto every delivered event. Any `utm_*` or click identifier the SDK supplies is never copied.
- PD-5 URLs: `$current_url` becomes origin plus pathname (query string and fragment removed); a value that is not a parseable http(s) URL is omitted. `$referrer` becomes the origin, or the literal `$direct`, else it is omitted. Other query-string content never leaves.
- PD-6 events: `haoo_page_view` stays (owner report and funnels). `$pageview`/`$pageleave` are admitted by a lockdown-owned constant `SDK_PAGE_EVENTS`. They are NOT added to `HAOO_MEASUREMENT_EVENTS`, so the sink type, `HAOO_REPORT_EVENTS` and the report HogQL (`WHERE event IN (${eventNameLiterals()})` in src/reporting/generate.ts) are unchanged and no report number moves (OD-5).
- PD-7 fail-closed transport: `TRANSPORT_REQUIRED_PROPERTIES` becomes `token`, `distinct_id`, `$process_person_profile`, `$cookieless_mode` (the vendor's own minimal set is `["token","distinct_id","$cookieless_mode"]`). A payload is dropped unless `distinct_id === '$posthog_cookieless'`, `$cookieless_mode === true` and `$process_person_profile === false`. This guarantees no per-browser identifier leaves even if cookieless mode silently lapses.
- PD-8 allowlist (copied only when an own property with a string, finite number or boolean value, in this order): `$current_url`, `$host`, `$pathname`, `$referrer`, `$referring_domain`, `$session_id`, `$window_id`, `$pageview_id`, `$prev_pageview_id`, `$prev_pageview_pathname`, `$prev_pageview_duration`, `$browser`, `$os`, `$device_type`, `$raw_user_agent`, `$browser_language`, `$browser_language_prefix`, `$screen_height`, `$screen_width`, `$viewport_height`, `$viewport_width`, `$timezone`, `$timezone_offset`, `$lib`, `$lib_version`, `$insert_id`, `$time`. `$raw_user_agent` and `$host` must survive, because the server hash is derived from team id, daily salt, IP, user agent and hostname (COVERAGE.md `cookieless_mode` row). Everything else is dropped by construction, never by denylist.
</owner_decisions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@src/measurement/posthog-lockdown.ts
@src/measurement/posthog.ts
@src/test/fixtures/posthog-capture-contract.ts

Interfaces the executor works against (current state):
- `POSTHOG_LOCKDOWN(apiHost: string, token: string, allowedEvents: readonly string[])` returns the config object with `satisfies Partial<PostHogConfig>` (not `as const`).
- `stripToBareName(result: CaptureResult | null, allowedEvents: readonly string[]): CaptureResult | null`.
- `lockdownHolds(resolved: unknown, expected: { apiHost; token; beforeSend }): boolean`, a key-by-key literal readback plus identity for `before_send`.
- `createPostHogEventSink<EventName>(config: Pick<ProductMeasurement<EventName>, 'provider' | 'providerConfig' | 'events'>, adapters: PostHogAdapters = {})` returns a one-argument sink or `undefined`. The `established` WeakMap holds `{ token, apiHost, events, sink }` per client.
- In `createMeasurement().initialize()` (src/measurement/index.ts), `campaign = readCampaign(adapters)` runs before `createPostHogEventSink(config, adapters.providerAdapters)`.
- Fixture: `createPostHogVendorClient(overrides)`, `createLoadedOncePostHogVendorClient(overrides)`, `VENDOR_DOCUMENTED_DEFAULTS`, `VACUOUS_BY_VENDOR_AGREEMENT`, `assembleVendorPayload(event, token, ordinal)`.

Static source boundary that applies to the three measurement files and to src/products/haoo.ts, including comments and copy. Read `PRODUCT_SOURCE_BOUNDARY` in src/test/build-output.test.ts (around lines 116-190) before writing any doc comment or copy. Pitfalls this change is likely to hit:
- lowercase `analytics` directly followed by a period, e.g. at the end of a sentence;
- the word for a random unique identifier (case-insensitive) in the three measurement files;
- camelCase `sessionId`/`deviceId`/`visitorId`/`userId`;
- naming the browser beacon API or the fetch call when describing how `$pageleave` is sent (say "sent on page unload");
- `window.location`, `document.referrer`, `navigator.userAgent`, or naming either web storage API in the measurement files;
- a `track(` or `eventSink(` call text followed by a comma.

URL parsing in the reducer uses `new URL(value)` on the property value, never an ambient browser global.
</context>

<source_audit>
| Source item | Covered by |
|---|---|
| GOAL: Web Analytics + Product Analytics work (OD-1) | Task 1 (config, reducer, readback), Task 3 (full gates) |
| Outcome 1: lockdown cookieless, pageview/pageleave, allowlist, UTM, readback, event list | Task 1 (PD-1..PD-8) |
| Outcome 2: posthog.ts / index.ts minimal changes, fail-closed + idempotency preserved | Task 1 |
| Outcome 3: disclosure copy truthful | Task 2 |
| Outcome 4: tests + verifier scripts + coverage doc, all gates | Tasks 1, 2, 3 |
| Outcome 5: owner setting + post-deploy check recorded | frontmatter user_setup, Task 3 (Operational boundary sentence + SUMMARY) |
| Outcome 6: reporting HogQL unchanged | PD-6, Task 3 git-diff gate on src/reporting |
| REQ MEAS-02 / MEAS-03 / MEAS-06 / MEAS-07 | Task 1 (+ Task 2 page-level proofs) |
| REQ MEAS-04 | Task 2 |
| Constraint: no host change, no deploy/push | Task 3 git-diff gate on config/, no push step anywhere |
</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer, one cookieless $pageview end-to-end (facade to adapter to lockdown to reducer to wire), then expand the unit contract</name>
  <files>src/measurement/posthog-lockdown.ts, src/measurement/posthog.ts, src/measurement/index.ts, src/test/fixtures/posthog-capture-contract.ts, src/test/measurement.test.ts</files>
  <read_first>
    - src/measurement/posthog-lockdown.ts (whole file, already in context)
    - src/measurement/posthog.ts (whole file, already in context)
    - src/measurement/index.ts lines 260-340 (initialize, readCampaign call order)
    - src/test/fixtures/posthog-capture-contract.ts (whole file)
    - src/test/measurement.test.ts lines 1-140 (imports, constants), 630-700 (LOCKED_CONFIGURATION, hostileValue, vendorPayload), 822-1060 (sink describe, droppedRows, transport-key cases, defaults completeness, vacuous set), 1477-1640 (repeat initialization), 2060-2388 (campaign-before-init case, tracer describe, date-gated defaults)
    - src/test/build-output.test.ts lines 116-190 (source boundary patterns for these files)
    - SDK anchors, via grep -o with about 200 chars of context on node_modules/posthog-js/dist/module.js: `capture_pageview:!t||"2025-05-24">t||"history_change"`, `Fl(){if("always"===`, `var m=this.El();m||(this.sessionManager`, `this.El()&&(u[Gr]=!0)`, `uu(){return!0===this.config.capture_pageleave`, `this.config.save_referrer&&this.sessionPersistence.update_referrer_info()`
  </read_first>
  <behavior>
    - Tracer (write first, must fail first): with the jsdom address set to a URL that has an unrelated query parameter (`ref=private-note`) and a `#products` fragment, and the facade's injected location carrying that URL plus `utm_source=Partner&utm_medium=email`, `initialize()` followed by `client.simulateAutomaticCapture('$pageview')` delivers exactly one `$pageview`. Its properties start with the four transport keys in order (distinct_id `$posthog_cookieless`, `$cookieless_mode` true, `$process_person_profile` false). `$current_url` equals jsdom origin plus pathname. `utm_source` is `partner`, `utm_medium` is `email`, and there is no `utm_campaign`. `$referrer` is the fixture referrer's origin and `$referring_domain` is present. None of `$ip`, `$device_id`, `gclid`, `utm_term`, `title`, `ph_keyword`, `$initial_person_info` is present, the envelope has no `$set_once`, and no property value contains `private-note` or `#`.
    - `$pageleave` is admitted, and so is each of the ten haoo_* names. `$autocapture`, `$rageclick`, `$web_vitals`, `$exception`, `$feature_flag_called`, `$identify` and case/whitespace/NFKC variants of allowed names are dropped.
    - Dropped: null input; absent or empty properties; any missing transport key; distinct_id other than the sentinel; `$cookieless_mode` absent or false; `$process_person_profile` true.
    - `$current_url` that is not a parseable http(s) URL, or is not a string, is omitted and the event still delivers. `$referrer` of `$direct` is kept verbatim. An allowlisted key whose value is an object or array is omitted.
    - Campaign values that are uppercase, longer than 32 characters, contain a space, or are non-strings are not written. A `utm_source` supplied by the SDK payload is never copied; only the facade's normalized value appears.
    - Thirty unknown provider properties reduce to transport keys plus only allowlisted keys, and the emitted properties object is a fresh object, not the received one.
    - The readback refuses (no sink, the lockdown refusal reason) when `cookieless_mode`, `capture_pageview`, `capture_pageleave` or `save_referrer` resolves wrong. This comes for free from the derived hostile table over LOCKED_KEYS, which must still pass.
    - Re-entry: a second `createPostHogEventSink` for the same client with identical token, host, events AND campaign returns the established sink without a second init. The same call with a different campaign refuses with `posthog:refused-client-reconfiguration`.
  </behavior>
  <action>
RED first. In src/test/measurement.test.ts add a new describe titled `cookieless web analytics tracer`, placed immediately before the existing `PostHog tracer: one event end-to-end` describe, containing the tracer case from `<behavior>`. Set and restore the jsdom address with `window.history.replaceState` inside the case (restore in a finally block or afterEach). It needs the fixture method `simulateAutomaticCapture`, so add a throwing stub of that method to the fixture client first. Run `npx vitest run src/test/measurement.test.ts -t "cookieless web analytics tracer"` and confirm it fails for the expected reason. Then implement.

Fixture (src/test/fixtures/posthog-capture-contract.ts). It must still import nothing from src/.
- (a) In `VENDOR_DOCUMENTED_DEFAULTS`, add `cookieless_mode: undefined`, with a comment that the key is absent from the vendor defaults object. Correct `capture_pageview` from `true` to `'history_change'`, with a comment quoting the default-builder expression and noting that `'unset'` sorts above the date literal. Keep `capture_pageleave: 'if_capture_pageview'` and `save_referrer: true`.
- (b) Append `'save_referrer'` to `VACUOUS_BY_VENDOR_AGREEMENT`, with a comment that the vendor default is already `true`, so the readback cannot prove the adapter supplied it and the hostile table still covers it.
- (c) Rewrite `assembleVendorPayload` to model the 1.425.1 cookieless payload.
  - Transport keys: `token`, `distinct_id: '$posthog_cookieless'`, `$process_person_profile: false`, `$cookieless_mode: true`.
  - Ambient enrichment, which the reducer should keep in reduced form: `$current_url` taken from `globalThis.location?.href` when it is a string, otherwise `https://www.haoo.online/?ref=fixture#products`; `$host` and `$pathname` derived from that URL; `$referrer: 'https://search.example/results?q=private+words'`; `$referring_domain: 'search.example'`; `$raw_user_agent`, `$browser`, `$os`, `$device_type`, `$timezone`, `$lib: 'web'`, `$lib_version`, `$insert_id`, `$time`.
  - Enrichment the reducer must strip: `$ip`, `$device_id: null`, `gclid`, `utm_term`, `utm_source: 'raw-sdk-value'`, `ph_keyword`, `$search_engine`, `title`, `$initial_person_info`, and a `$set_once` object on the envelope.
- (d) Add `simulateAutomaticCapture(event: '$pageview' | '$pageleave'): void` to `InstalledVendorPostHogClient`, and forward it in `createLoadedOncePostHogVendorClient`.
  - Before `init`, it is a no-op.
  - `$pageview` runs only when the merged `capture_pageview` is truthy.
  - `$pageleave` runs only when the merged `capture_pageleave` is literal `true`, or is `'if_capture_pageview'` with a truthy `capture_pageview`. This mirrors the SDK's `uu()`.
  - Otherwise it runs exactly the `capture` path: record into capturedEvents, run the merged `before_send`, record non-null results into deliveredPayloads.
  - Document it as a transcription of the SDK's automatic channels, not evidence of vendor behaviour.

src/measurement/posthog-lockdown.ts (implements OD-1, OD-2, OD-4, PD-1..PD-8):
- (a) Signature becomes `POSTHOG_LOCKDOWN(apiHost, token, allowedEvents, campaign: Readonly<Record<string, string>> = {})`. Take a frozen shallow copy of `campaign` once, inside the factory.
- (b) Config changes:
  - add `cookieless_mode: 'always'`;
  - `capture_pageview: true`;
  - `capture_pageleave: true`;
  - `save_referrer: true`;
  - `before_send` becomes a closure calling `reduceCapture(result, allowedEvents, snapshot)`.
  - Every other key and value stays byte-identical, including `defaults: 'unset'`, `person_profiles: 'never'`, `persistence: 'memory'`, `disable_persistence: true`, `save_campaign_params: false`, `custom_campaign_params: []`, all disable/advanced flags, and `satisfies Partial<PostHogConfig>`.
- (c) Export these constants:
  - `SDK_PAGE_EVENTS` (`$pageview`, `$pageleave`, as const);
  - `COOKIELESS_DISTINCT_ID` (`$posthog_cookieless`);
  - `TRANSPORT_REQUIRED_PROPERTIES` extended to the four keys of PD-7, in that order;
  - `WEB_ANALYTICS_PROPERTIES` (the PD-8 list, in order, as const);
  - `CAMPAIGN_PROPERTIES` (`utm_source`, `utm_medium`, `utm_campaign`, as const).
- (d) Replace `stripToBareName` with `reduceCapture(result, allowedEvents, campaign = {})`, whose doc comment names it the successor of the withdrawn bare-name reducer, per the repository's named-successor convention. Algorithm, in order:
  1. Reject a non-object.
  2. Match the event by plain `===` against `allowedEvents` or `SDK_PAGE_EVENTS`, with no normalization.
  3. Require a properties object.
  4. Require every transport key as an own property, else return null.
  5. Enforce the PD-7 exact values, else return null.
  6. Build a fresh literal: transport keys first, in order, then each `WEB_ANALYTICS_PROPERTIES` key that is an own property with a string, finite-number or boolean value. `$current_url` and `$referrer` are transformed per PD-5 using `new URL` inside try/catch and an http/https protocol check; an untransformable value is omitted.
  7. For each `CAMPAIGN_PROPERTIES` key whose campaign value is an own string matching lowercase letters, digits and hyphens, length 1 to 32, write it.
  8. Return `{ ...result, properties }` with `$set`, `$set_once` and `$unset` deleted, as today.
- (e) In `lockdownHolds`, add literal assertions `merged.cookieless_mode === 'always'`, `merged.capture_pageview === true`, `merged.capture_pageleave === true` (with the existing never-the-coupled-string comment adjusted) and `merged.save_referrer === true`. Every other assertion stays; the `before_send` identity check is unchanged.
- (f) Rewrite every doc-comment statement that becomes false: the `capture_pageview`/`capture_pageleave` bullet, the `save_referrer` half of the campaign bullet, the `before_send` "bare name" bullet, the persistence bullet (add the PD-1 forcing fact), and the locked-value count in the `PostHogLockdownExpectation.beforeSend` comment (count LOCKED_KEYS). Add a `cookieless_mode` bullet citing OD-2 and the owner-performed server setting. Cite the owner decision date 2026-09-13 as reversing the bare-name half of Phase 04.1 D-03.

src/measurement/posthog.ts:
- Add a third parameter `campaign: Readonly<Record<string, string>> = {}` to `createPostHogEventSink` and pass it as the fourth argument to `POSTHOG_LOCKDOWN`.
- Add a `campaign` copy to the `established` record type and entry.
- Add a module-private `sameCampaign(left, right)`: same own key set, each value `===`. AND it into the re-entry comparison; a mismatch takes the existing `reconfiguration` refusal.
- Refusal vocabulary, gate order, the WeakMap and the one-argument sink stay unchanged.
- Update doc comments that claim the wire carries only a bare name. The sink still passes only the event name; the SDK enriches the event automatically, and the lockdown reducer decides what survives.

src/measurement/index.ts: in `initialize`, change the call to `createPostHogEventSink(config, adapters.providerAdapters, campaign)`. Extend the adjacent comment: normalization and address-bar cleanup complete before init, so the SDK's first automatic `$pageview` sees a clean address bar, and the normalized record is the only campaign data PostHog receives (PD-4). Nothing else in the facade changes; `track`, context and clearContext stay byte-identical.

src/test/measurement.test.ts, expansion after the tracer is green:
- Update imports to `reduceCapture` and the new constants.
- Change the `vendorPayload` default properties to the cookieless transport shape.
- Rename the `name-only provider sink` describe to `allowlisted provider sink`.
- In both `droppedRows` tables, replace the `$pageview` rows with `$autocapture` and add the PD-7 value rows.
- Convert the three-transport-key assertions into: transport keys first, in order, and every remaining key a member of `WEB_ANALYTICS_PROPERTIES` or `CAMPAIGN_PROPERTIES`.
- Add the `<behavior>` cases not already covered.
- In the existing `PostHog tracer: one event end-to-end` spot checks, expect `capture_pageview` true, `capture_pageleave` true, `cookieless_mode` `'always'`, `save_referrer` true.
- In `normalizes and clears campaign parameters before the provider is initialized`, add that the delivered payload carries `utm_source: 'partner'`.
- Add the two campaign re-entry cases to `repeat initialization against an already-loaded client`.
- Update the `disable_capture_url_hashes` reason in `ACKNOWLEDGED_UNLOCKED_DATE_GATED_KEYS`: the reducer removes query strings and fragments from `$current_url` itself.
- Leave `hostileValue`, LOCKED_KEYS derivation and the vacuous-set derivation logic untouched. They must pass against the corrected fixture.
  </action>
  <verify>
    <automated>cd "/home/paul/Documents/Vibe Coding Projects/HAOO" && npx vitest run src/test/measurement.test.ts && npm run typecheck && grep -c "merged.cookieless_mode === 'always'" src/measurement/posthog-lockdown.ts && grep -c "createPostHogEventSink(config, adapters.providerAdapters, campaign)" src/measurement/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - The tracer case was observed failing before implementation, and the SUMMARY records the failing reason.
    - `npx vitest run src/test/measurement.test.ts` exits 0 and `npm run typecheck` exits 0.
    - `grep -c "cookieless_mode: 'always'" src/measurement/posthog-lockdown.ts` is at least 1; `grep -c "merged.save_referrer === true" src/measurement/posthog-lockdown.ts` equals 1; `grep -c "merged.capture_pageleave === true" src/measurement/posthog-lockdown.ts` equals 1.
    - `grep -rn "stripToBareName(" src --include=*.ts --include=*.tsx | grep -v "^\s*\*"` returns no call sites, because the reducer was renamed and every caller migrated.
    - `git diff --stat -- src/reporting config` is empty.
  </acceptance_criteria>
  <done>A cookieless `$pageview` travels from the facade through the adapter, lockdown readback and allowlist reducer to the fixture wire. It arrives with sentinel transport keys, reduced URLs and normalized campaign values. All measurement unit tests pass against the corrected vendor-defaults fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Truthful disclosure copy and page-level journey proofs</name>
  <files>src/products/haoo.ts, src/products/types.ts, src/test/measurement-page.test.tsx, e2e/recovery.e2e.ts</files>
  <read_first>
    - src/products/haoo.ts lines 250-340 (disclosure block and its provenance comments)
    - src/test/measurement-page.test.tsx lines 740-800 (intro byte pin), 930-1200 (disclosure structure tests), 1380-1610 (FORBIDDEN_PROPERTY_PATTERNS, runConfiguredJourney, payloadShape, wire tests, campaign and storage cases)
    - src/test/build-output.test.ts lines 116-190 (haoo.ts boundary: ALWAYS_FORBIDDEN + NETWORK_FORBIDDEN + FORM_MARKUP_FORBIDDEN apply to copy strings too)
    - e2e/recovery.e2e.ts lines 1180-1320
  </read_first>
  <behavior>
    - The rendered disclosure contains the new signalBoundary, campaignDescription, processorNote and all seven neverCollected items, and those bytes are pinned by hand-typed expectations in measurement-page.test.tsx beside the existing intro pin.
    - The full configured journey still delivers exactly the ten haoo_* names from visitor actions. After the journey, `simulateAutomaticCapture('$pageview')` and `simulateAutomaticCapture('$pageleave')` deliver exactly one of each.
    - Every delivered payload's keys are the four transport keys plus members of `WEB_ANALYTICS_PROPERTIES` or `CAMPAIGN_PROPERTIES`, and no key matches a forbidden pattern: geoip, exactly `$ip`, device_id, exactly `$device`, feature flag, `$set` prefix, `initial_`, click identifiers (gclid, gclsrc, dclid, gbraid, wbraid, fbclid, msclkid, twclid, li_fat_id, mc_cid, igshid, ttclid), `utm_content`/`utm_term`, keyword or search_engine, exactly `title`, scroll or content-percentage.
    - A journey started at `/products/haoo/?utm_source=partner&utm_medium=email&utm_campaign=launch&ref=news` delivers `utm_source`, `utm_medium` and `utm_campaign` equal to `partner`, `email` and `launch`. No delivered property value contains `ref=news`, `?` or `#`. The address bar reads `?ref=news` at provider init. A plain journey delivers no utm key.
    - The provider still writes no cookie, no sessionStorage key, and no localStorage key other than the Phase 3 context record.
  </behavior>
  <action>
Disclosure copy in src/products/haoo.ts, per OD-1 and OD-2 and MEAS-04. Before editing, grep src/, e2e/ and scripts/ for each string being replaced, to find any other byte pin, and update each pin found. The `intro`, `signalLines`, `browserFacts`, `browserBoundary`, controller copy and summary copy stay unchanged; the intro remains true and is pinned at measurement-page.test.tsx around line 761. Replace exactly these fields with these exact strings:
- `signalBoundary`: "Each of these signals, and each page visit and page exit, is sent with: this page's address without anything after a ? or #; the name of the site that sent you here; the browser, operating system, device type, language, time zone and screen size your browser reports; and any accepted campaign values. No form answers or contact details are attached."
- `campaignDescription`: "On one page load, we may read utm_source, utm_medium, and utm_campaign. Accepted values are lowercased, limited to short letters, numbers, and hyphens, kept only for this page lifetime, sent with this page's signals to PostHog, and removed from the address bar after being read."
- `processorNote`: "Page signals are processed by PostHog in the United States. PostHog uses no cookies and saves nothing in this browser. It groups visits using an anonymous code calculated on its own servers from your IP address and browser details, and that code changes every day. PostHog may also work out your approximate location, such as your country or city, from your IP address. Page signals are never linked to your form answers."
- `neverCollected`, in this order:
  1. "Name, email address, phone number, or organization."
  2. "Message text."
  3. "Role, county, timeframe, or exact portfolio values."
  4. "Cookies, identifiers saved in this browser, or cross-site identifiers."
  5. "Raw click history."
  6. "Anything in the page address after a ? or #, apart from accepted campaign values."
  7. "Any form answer attached to an analytics event."

The fingerprints wording is dropped because the daily server-side code is derived from IP and browser details. Raw click history stays true because autocapture, rageclick, dead clicks, heatmaps and replay stay off.

Rewrite the provenance doc comment above `processorNote`. It must say the wording was replaced on 2026-09-13 under the owner's decision (quick task 260913-p4u) to enable cookieless PostHog Web Analytics, that the executor drafted it under that decision, and that it is owed an owner read before the next deploy. Do not label the new strings owner-approved byte-exact. Add a one-line note of the same provenance above `signalBoundary` and `campaignDescription`. If src/products/types.ts carries a doc comment stating the payload is a bare name, correct that comment only; types stay unchanged. Check every new string against the haoo.ts boundary patterns before running tests.

src/test/measurement-page.test.tsx:
- Add hand-typed byte pins for the four replaced fields beside the intro pin; the single hand-typed home of approved bytes stays this file.
- Rewrite `FORBIDDEN_PROPERTY_PATTERNS` and its doc comment to the `<behavior>` list; the allowed set is now a named allowlist, not a count.
- Rename and rewrite `carries exactly the three transport properties on every payload` to the allowlist-membership assertion, importing `TRANSPORT_REQUIRED_PROPERTIES`, `WEB_ANALYTICS_PROPERTIES` and `CAMPAIGN_PROPERTIES`.
- Rename `carries no geo-IP, session, device, URL, referrer, campaign or feature-flag property` to a title naming what is still forbidden.
- In `drops any name the SDK itself would emit`, remove `$pageview` and `$pageleave` from the list, add `$exception`, `$feature_flag_called` and `$identify`, and give the payload the full cookieless transport shape, so the drop is proven to be caused by the name.
- Add a case for the automatic `$pageview`/`$pageleave` delivery.
- Rewrite `emits an identical payload shape when campaign parameters are on the address bar` to the campaign `<behavior>`, keeping the `searchAtProviderResolution` assertion.
- Update the MEAS-03 comment in the storage case to cite cookieless mode forcing persistence off; the assertions there stay.
- Keep the ten-names case, retitled so it no longer says bare names.

e2e/recovery.e2e.ts: two prose sites (the block comment near line 1214 and the evidence `note` string near line 1309) state that `capture_pageview` is false. Rewrite both. Since 2026-09-13 `capture_pageview` is true, so a configured build issues a `$pageview` on load in a real browser. Under Playwright, posthog-js drops the traffic because `navigator.webdriver` is true, which tracer.e2e.ts asserts. So a blocked-request count of 0 is still the expected reading, and the bundle reading is what distinguishes configured from unconfigured. Change no assertion or selector.
  </action>
  <verify>
    <automated>cd "/home/paul/Documents/Vibe Coding Projects/HAOO" && npx vitest run src/test/measurement-page.test.tsx src/test/haoo-page.test.tsx src/test/haoo-content.test.ts src/test/product-shell-reuse.test.tsx && npm run typecheck && npm run lint && test "$(grep -c 'current page load' src/products/haoo.ts)" = "0" && test "$(grep -c 'changes every day' src/products/haoo.ts)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - The four listed vitest files pass; `npm run typecheck` and `npm run lint` exit 0.
    - `grep -c "changes every day" src/products/haoo.ts` equals 1 and `grep -c "changes every day" src/test/measurement-page.test.tsx` is at least 1, so the pin exists.
    - `grep -c "current page load" src/products/haoo.ts` equals 0.
    - `grep -c "simulateAutomaticCapture" src/test/measurement-page.test.tsx` is at least 2.
    - `grep -c "capture_pageview is false" e2e/recovery.e2e.ts` equals 0.
  </acceptance_criteria>
  <done>The visitor-facing disclosure describes exactly what the cookieless payload carries and its bytes are pinned. The page-level journey proves the allowlist, campaign delivery, query-string removal and storage silence on the rendered product page. The e2e prose no longer claims pageview capture is off.</done>
</task>

<task type="auto">
  <name>Task 3: Re-decide the coverage matrix, pin the owner-performed cookieless setting, run every project gate</name>
  <files>.planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md, scripts/verify-phase4-coverage.mjs, src/test/haoo-report.test.ts</files>
  <read_first>
    - .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md lines 1-60 and 98-end (blockquote, SDK table, Operational boundary)
    - scripts/verify-phase4-coverage.mjs (whole file; REQUIRED_TABLES and boundaryChecks)
    - src/test/haoo-report.test.ts lines 1676-1730 (audit test and its row/boundary count comment)
    - package.json scripts (gate list)
  </read_first>
  <action>
COVERAGE.md, PostHog browser SDK table. Capability cell text stays byte-identical, because the verifier keys on it; only decision and reason cells change.
- `automatic $pageview (capture_pageview)` becomes INTEGRATE. Reason: owner decision 2026-09-13 (quick task 260913-p4u); Web Analytics counts `$pageview`; locked to literal `true`, which at 1.425.1 captures only the initial load (the `'history_change'` default would also count history changes); fires 1 ms after `init`, which runs after `readCampaign` cleans the address bar; `haoo_page_view` stays for the owner report.
- `automatic $pageleave (capture_pageleave)` becomes INTEGRATE. Reason: session duration and bounce rate; set and read back as literal `true`, never the coupled string.
- `save_referrer` becomes INTEGRATE. Reason: the SDK attaches `$referrer`/`$referring_domain` only through this option; held in memory under disabled persistence; `before_send` reduces `$referrer` to its origin or `$direct`.
- `cookieless_mode` becomes INTEGRATE. Reason: owner decision 2026-09-13 reverses the earlier opt-out; `'always'` sends the `$posthog_cookieless` sentinel instead of a per-browser identifier, forces persistence off and builds no client session; PostHog derives `hash(team_id, daily_salt, ip_address, user_agent, hostname)` server-side; events are dropped at ingestion unless the owner-performed setting in the Operational boundary is on.
- `save_campaign_params` stays OPT-OUT. New reason: it would read raw `utm_*` and click identifiers from the address bar and bypass MEAS-06 normalization; instead `before_send` writes the normalized `readCampaign` values onto each event.
- `$geoip_disable` as an event property stays OPT-OUT. New reason: approximate IP-derived location is now disclosed to visitors and used by Web Analytics; any suppression remains the owner-performed "Discard client IP data" setting.
- `before_send payload reduction` stays INTEGRATE. New reason: the sole property chokepoint; admits the ten HAOO names plus `$pageview`/`$pageleave`, requires the four cookieless transport keys with exact values, copies a fixed Web Analytics property allowlist into a fresh literal, reduces URLs, writes normalized campaign values, and returns `null` for anything else.
- `persistence: 'memory' with disable_persistence: true` stays INTEGRATE. Append to its reason that cookieless `'always'` also forces persistence off.
- `capture(name) with a bare name and no property argument` stays INTEGRATE. New reason: the facade still passes only a name; the SDK enriches the event and `before_send` decides which properties survive.

Add one sentence to the top blockquote: on 2026-09-13 the owner reversed the bare-name half of D-03 in favour of cookieless Web Analytics; the remaining subtractions stand.

In `## Operational boundary`, before the next `##` heading:
- Change "ingestion acceptance of the three-property payload" to "ingestion acceptance of the reduced cookieless payload".
- Add this paragraph verbatim: "\"Cookieless server hash mode\" is an owner-performed project setting (PostHog, Project settings > Web analytics). The browser SDK runs with `cookieless_mode: 'always'`, and events sent that way are dropped at ingestion while it is off, so the setting must be enabled before a build carrying this configuration is deployed. Whether it is on is an owner observation, not a fact this tree can establish."
- Do NOT touch any copy of this file under ../ZERO-PAPERHUB. `.planning/` is excluded from the disjointness audit (EXCLUDED_PREFIXES in scripts/verify-tree-disjointness.mjs).

scripts/verify-phase4-coverage.mjs:
- In REQUIRED_TABLES, change the expected decision to INTEGRATE for the four rows re-decided above. Put a successor comment beside them, in the file's existing style, recording that they were re-decided on 2026-09-13 by owner decision, in the same commit as the COVERAGE.md rows they pin, and that each predecessor was OPT-OUT.
- Append a boundaryChecks entry with description `cookieless server hash mode is an owner-performed project setting and events are dropped at ingestion while it is off`. Its pattern spans the whole claim with bounded whitespace, in the style of the neighbouring entries: case-insensitive, the quoted setting name followed by "is an owner-performed project setting", then at most 400 characters, then "dropped at ingestion while it is off".
- Mutation-probe the new check once. Copy COVERAGE.md to the scratchpad, delete the words "while it is off" from the copy, run `node scripts/verify-phase4-coverage.mjs <copy>` and confirm exit 1 naming the new description. Delete the copy, and record the probe in the SUMMARY.

src/test/haoo-report.test.ts: update only the doc comment that counts operational-boundary assertions (eleven becomes twelve); the assertions stay unchanged.

Full gate run, in this order, fixing any failure at its root cause. Never widen a build-output source-boundary pattern or loosen a verifier to get green:
1. `npm run verify:coverage`
2. `npm run lint`
3. `npm run typecheck`
4. `npm test` (builds, then vitest)
5. `npm run verify:disjoint`
6. `npm run test:phase1:contracts`

Then confirm `git diff --stat -- src/reporting config` is empty (OD-3, OD-5, PD-6). In the SUMMARY, copy the three frontmatter user_setup items verbatim under an "Owner steps" heading, stating the setting must be on BEFORE deploy. Do not push or deploy.
  </action>
  <verify>
    <automated>cd "/home/paul/Documents/Vibe Coding Projects/HAOO" && npm run verify:coverage && npm run lint && npm run typecheck && npm test && npm run verify:disjoint && npm run test:phase1:contracts && test -z "$(git diff --stat -- src/reporting config)"</automated>
  </verify>
  <acceptance_criteria>
    - All six gate commands exit 0, and `git diff --stat -- src/reporting config` prints nothing.
    - `grep -c "Cookieless server hash mode" .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md` is at least 2 (row reason plus boundary paragraph).
    - `grep -E "^\| \`cookieless_mode\` \| INTEGRATE \|" .planning/phases/04.1-migrate-measurement-from-plausible-to-posthog/COVERAGE.md` matches one line.
    - `grep -c "\['\`save_referrer\`', 'INTEGRATE'\]" scripts/verify-phase4-coverage.mjs` equals 1.
    - The SUMMARY records the mutation-probe exit code 1 and the Owner steps section.
  </acceptance_criteria>
  <done>The coverage matrix states the new decisions with reasons, the verifier enforces them and the owner-performed cookieless setting, and every project gate is green. Reporting and the approved host list are untouched, and nothing is pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page (visitor browser) to PostHog US ingestion | The SDK assembles event properties from ambient browser state (address bar, referrer, user agent); only `before_send` output crosses |
| address bar / referrer to measurement code | Untrusted query strings, fragments and referrer URLs can carry form answers, emails or tokens from other sites |
| posthog-js merged config to adapter | Resolved config is untrusted and re-read key by key (`lockdownHolds`) |
| product data to rendered disclosure | Copy is a legal-facing statement of what crosses the first boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-p4u-01 | Information disclosure | `reduceCapture` URL keys | high | mitigate | `$current_url` reduced to origin+pathname, `$referrer` to origin or `$direct`, unparseable omitted (PD-5); unit rows plus the page journey asserting no delivered value contains `ref=news`, `?` or `#` |
| T-p4u-02 | Information disclosure | transport identity | high | mitigate | readback asserts `cookieless_mode === 'always'`; reducer drops any payload whose `distinct_id` is not `$posthog_cookieless`, or whose `$cookieless_mode`/`$process_person_profile` are not exactly true/false (PD-7) |
| T-p4u-03 | Information disclosure | future SDK properties (click ids, keywords, geo, flags, person props) | high | mitigate | fixed allowlist copied into a fresh literal, never a denylist (PD-8); `$set`/`$set_once`/`$unset` deleted; fixture payload carries each forbidden key so a regression is visible |
| T-p4u-04 | Repudiation (untrue disclosure) | `src/products/haoo.ts` disclosure | high | mitigate | copy rewritten to the exact payload, bytes pinned in measurement-page.test.tsx, provenance comment marks it owner-review-before-deploy, user_setup item 2 |
| T-p4u-05 | Denial of service (silent dead funnel) | PostHog project setting | medium | transfer | owner enables "Cookieless server hash mode" before deploy (user_setup item 1); sentence pinned in COVERAGE.md Operational boundary by a new verifier check; post-deploy real-browser check (user_setup item 3) |
| T-p4u-06 | Tampering | campaign values reaching PostHog | medium | mitigate | `save_campaign_params: false`; only `readCampaign` output passed in; reducer re-validates lowercase `[a-z0-9-]` 1..32 and never copies SDK-supplied `utm_*` (PD-4) |
| T-p4u-07 | Elevation of privilege | person processing via identify/alias/group | low | accept | unchanged: adapter seam exposes only `init`/`capture`, `person_profiles: 'never'`, cookieless mode ignores consent and person calls |
| T-p4u-08 | Tampering | re-entry memo under a different campaign | low | mitigate | `sameCampaign` in the re-entry gate; mismatch refuses as `reconfiguration` (unit case) |
| T-p4u-SC | Tampering | npm installs | low | accept | no package is installed or upgraded; posthog-js stays pinned at 1.425.1 |
</threat_model>

<verification>
- `npm run verify:coverage`, `npm run lint`, `npm run typecheck`, `npm test`, `npm run verify:disjoint`, `npm run test:phase1:contracts` all exit 0.
- `git diff --stat -- src/reporting config` is empty; no `git push`, no deploy.
- Lockdown readback includes the four new literal assertions, and the derived hostile table passes with no hand-added rows.
- The tracer case in measurement.test.ts was observed RED before GREEN.
</verification>

<success_criteria>
- A configured build sends `$pageview` on load and `$pageleave` on exit, each carrying the cookieless sentinel transport keys, reduced page and referrer addresses, browser/device/screen/locale properties, and normalized campaign values. Nothing else is sent.
- No cookie, sessionStorage key or new localStorage key is written by the provider; no per-browser identifier can be delivered.
- The disclosure describes that payload in plain words, and the owner has a recorded pre-deploy review item.
- The coverage matrix and verifier record and enforce the re-decisions and the owner-performed "Cookieless server hash mode" setting.
- Owner report numbers are unchanged.
</success_criteria>

<output>
Create `.planning/quick/260913-p4u-enable-posthog-web-analytics-via-cookiel/260913-p4u-SUMMARY.md` when done. It must include:
- the tracer RED reason;
- the mutation-probe result;
- the gate outputs (counts lines);
- the "Owner steps" section copying the three user_setup items, stating that "Cookieless server hash mode" must be enabled BEFORE deploy and that a Playwright/headless reading of 0 events is expected;
- a note that the disclosure strings were drafted under OD-1/OD-2 and await the owner's read.
</output>
