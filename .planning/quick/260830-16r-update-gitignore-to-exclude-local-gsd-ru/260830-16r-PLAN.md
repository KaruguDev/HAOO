---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - QUICK-260830-16R

must_haves:
  truths:
    - Local GSD runtime state under `.gsd/` no longer appears as untracked repository content.
    - Newly generated files under `.planning/research/.cache/` no longer appear as untracked repository content.
    - Real planning and research documents outside the cache directory remain visible to Git.
    - Cache files already tracked by Git remain tracked and are not deleted or untracked by this task.
  artifacts:
    - path: .gitignore
      provides: Narrow ignore rules for local GSD runtime state and generated research cache files.
  key_links:
    - from: .gitignore
      to: .gsd/
      via: repository-root directory ignore rule
    - from: .gitignore
      to: .planning/research/.cache/
      via: cache-directory-specific ignore rule
---

<objective>
Update the repository ignore policy so local GSD runtime state and newly generated planning research cache files stay out of Git status without hiding real planning documents or altering cache files that are already tracked.

Purpose: Keep local workflow artifacts from creating repository noise while preserving the project's committed planning history.
Output: Two narrowly scoped entries appended to `.gitignore`.
</objective>

<execution_context>
@/home/paul/.codex/gsd-core/workflows/execute-plan.md
@/home/paul/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ignore local GSD runtime state and generated research cache files</name>
  <files>.gitignore</files>
  <action>Preserve every existing `.gitignore` entry and append a clearly labeled local-GSD section containing exactly the directory rules `.gsd/` and `.planning/research/.cache/`. Keep the research ignore scoped to the `.cache` child directory; do not add a broader `.planning/` or `.planning/research/` rule because those paths contain real project documents. Do not delete, move, run `git rm`, or otherwise alter the cache JSON files that are already tracked: ignore rules affect untracked files only, and the existing tracked cache files must remain in the index.</action>
  <verify>
    <automated>git diff --check -- .gitignore &amp;&amp; test "$(grep -Fxc '.gsd/' .gitignore)" -eq 1 &amp;&amp; test "$(grep -Fxc '.planning/research/.cache/' .gitignore)" -eq 1 &amp;&amp; git check-ignore -q .gsd/runtime-state.json &amp;&amp; git check-ignore -q .planning/research/.cache/generated.json &amp;&amp; ! git check-ignore -q .planning/research/RESEARCH.md &amp;&amp; test -n "$(git ls-files '.planning/research/.cache/**')" &amp;&amp; test -z "$(git diff --name-only --diff-filter=D -- '.planning/research/.cache/**')"</automated>
  </verify>
  <done>The two requested directories are ignored exactly once; existing ignore rules are intact; a representative planning research document is not ignored; and all previously tracked cache files remain tracked with no cache deletions in the diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local generated state to Git index | Runtime and cache outputs should remain local, while authored planning documents must stay reviewable and committable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QUICK-01 | Tampering | `.gitignore` planning paths | medium | mitigate | Use cache-child-specific rules and verify that `.planning/research/RESEARCH.md` is not ignored. |
| T-QUICK-02 | Repudiation | Already tracked cache JSON files | low | mitigate | Leave the index untouched and verify tracked cache entries still exist with no deletion diff. |
</threat_model>

<verification>
Run the task's automated command, then inspect `git status --short -- .gsd .planning/research/.cache .gitignore`: `.gitignore` should be the only newly reported target, while previously tracked cache files remain unchanged.
</verification>

<success_criteria>
- `.gitignore` retains all prior entries and adds only `.gsd/` and `.planning/research/.cache/` for this task.
- Git ignores representative untracked files beneath both requested directories.
- Git does not ignore authored research documents outside `.planning/research/.cache/`.
- Existing tracked cache JSON files remain in the index and no cache file is removed.
</success_criteria>

<output>
Create `.planning/quick/260830-16r-update-gitignore-to-exclude-local-gsd-ru/260830-16r-SUMMARY.md` when done.
</output>
